<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Haida Consulting — Client Portal</title>
<script src="https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2"></script>
<style>
:root{
  --ink:#644a3c;--ink2:#5e4f46;--gold:#7e7b67;--gold-light:#fff2e6;--gold-mid:#dbc9ba;
  --surface:#fff2e6;--surface2:#fddcd3;--border:#dbc9ba;--muted:#7e7b67;--white:#FFFFFF;
  --danger:#8B1A1A;--danger-light:#FBEAEA;--success:#1A5C38;--success-light:#E8F5EE;
  --warn:#5e4f46;--warn-light:#fddcd3;--tag-bg:#f6c2ba;--tag-text:#644a3c;
}
*,*::before,*::after{box-sizing:border-box;margin:0;padding:0}
html{font-size:15px}
body{font-family:'Georgia',serif;background:var(--surface);color:var(--ink);min-height:100vh}

/* LOGIN */
#login-screen{min-height:100vh;display:flex;align-items:center;justify-content:center;background:var(--ink)}
.login-card{width:420px;background:var(--white);border-radius:2px;overflow:hidden}
.login-header{background:var(--ink);padding:40px 40px 32px;border-bottom:3px solid var(--gold)}
.login-header .wordmark{font-family:'Arial',sans-serif;font-size:11px;letter-spacing:3px;color:var(--gold);text-transform:uppercase;margin-bottom:8px}
.login-header h1{font-size:22px;font-weight:normal;color:var(--white);line-height:1.3}
.login-body{padding:36px 40px 40px}
.field{margin-bottom:20px}
.field label{display:block;font-family:'Arial',sans-serif;font-size:11px;letter-spacing:1.5px;text-transform:uppercase;color:var(--muted);margin-bottom:7px}
.field input{width:100%;padding:11px 14px;border:1px solid var(--border);border-radius:2px;font-family:'Arial',sans-serif;font-size:14px;color:var(--ink);background:var(--white);outline:none;transition:border-color 0.15s}
.field input:focus{border-color:var(--gold)}
.btn-primary{width:100%;padding:13px;background:var(--ink);color:var(--white);border:none;border-radius:2px;font-family:'Arial',sans-serif;font-size:13px;letter-spacing:1px;text-transform:uppercase;cursor:pointer;transition:background 0.15s;margin-top:8px}
.btn-primary:hover{background:#1a1a2e}
.login-error{font-family:'Arial',sans-serif;font-size:12px;color:var(--danger);margin-top:10px;min-height:18px}

/* APP SHELL */
#app{display:none;height:100vh;overflow:hidden;flex-direction:column}
.topbar{background:var(--ink);padding:0 24px;height:52px;display:flex;align-items:center;justify-content:space-between;flex-shrink:0}
.topbar .brand{font-family:'Arial',sans-serif;font-size:11px;letter-spacing:3px;text-transform:uppercase;color:var(--gold)}
.topbar .right{display:flex;align-items:center;gap:16px}
#topbar-user{font-family:'Arial',sans-serif;font-size:12px;color:rgba(255,255,255,.6)}
.btn-logout{font-family:'Arial',sans-serif;font-size:11px;letter-spacing:1px;text-transform:uppercase;color:var(--gold);background:none;border:1px solid rgba(184,145,42,.4);padding:5px 12px;border-radius:2px;cursor:pointer}
.main-area{display:flex;flex:1;overflow:hidden}
#admin-nav{display:none;flex-direction:column;width:200px;background:var(--ink2);padding:20px 0;flex-shrink:0;overflow-y:auto}
.tab-btn{font-family:'Arial',sans-serif;font-size:12px;letter-spacing:.5px;text-transform:uppercase;color:rgba(255,255,255,.55);background:none;border:none;padding:11px 20px;text-align:left;cursor:pointer;transition:all .15s;border-left:2px solid transparent}
.tab-btn:hover{color:rgba(255,255,255,.85);background:rgba(255,255,255,.04)}
.tab-btn.active{color:var(--gold);border-left-color:var(--gold);background:rgba(184,145,42,.08)}
.tab-section{font-family:'Arial',sans-serif;font-size:9px;letter-spacing:2px;text-transform:uppercase;color:rgba(255,255,255,.25);padding:16px 20px 6px;margin-top:8px}
.content-area{flex:1;overflow-y:auto;padding:28px 32px}
.page{display:none}.page.active{display:block}
.page-title{font-size:22px;font-weight:normal;color:var(--ink);margin-bottom:4px}
.page-sub{font-family:'Arial',sans-serif;font-size:12px;color:var(--muted);margin-bottom:24px}

/* CARDS */
.card{background:var(--white);border:1px solid var(--border);border-radius:2px;padding:24px;margin-bottom:20px}
.card-title{font-size:16px;font-weight:normal;color:var(--ink);margin-bottom:16px;padding-bottom:12px;border-bottom:1px solid var(--border)}
.section-label{font-family:'Arial',sans-serif;font-size:10px;letter-spacing:2px;text-transform:uppercase;color:var(--gold);margin:20px 0 12px}

/* FORMS */
.form-grid{display:grid;grid-template-columns:1fr 1fr;gap:14px}
.form-grid.full .form-field{grid-column:1/-1}
.form-field{display:flex;flex-direction:column;gap:6px}
.form-field label{font-family:'Arial',sans-serif;font-size:10px;letter-spacing:1.5px;text-transform:uppercase;color:var(--muted)}
.form-field input,.form-field select,.form-field textarea{padding:9px 12px;border:1px solid var(--border);border-radius:2px;font-family:'Arial',sans-serif;font-size:13px;color:var(--ink);background:var(--white);outline:none;transition:border-color .15s;resize:vertical}
.form-field input:focus,.form-field select:focus,.form-field textarea:focus{border-color:var(--gold)}
.score-row{display:flex;align-items:center;gap:12px}
.score-row input[type=range]{flex:1;accent-color:var(--gold)}
.score-val{font-family:'Arial',sans-serif;font-size:18px;font-weight:bold;color:var(--gold);min-width:24px}

/* BUTTONS */
.btn-save{background:var(--ink);color:var(--white);border:none;padding:10px 22px;border-radius:2px;font-family:'Arial',sans-serif;font-size:12px;letter-spacing:1px;text-transform:uppercase;cursor:pointer}
.btn-save:hover{background:#1a1a2e}
.btn-cancel{background:none;color:var(--muted);border:1px solid var(--border);padding:10px 18px;border-radius:2px;font-family:'Arial',sans-serif;font-size:12px;cursor:pointer;margin-left:8px}
.btn-add{background:var(--gold);color:var(--white);border:none;padding:8px 16px;border-radius:2px;font-family:'Arial',sans-serif;font-size:11px;letter-spacing:.5px;cursor:pointer}
.btn-add:hover{background:var(--gold-mid)}
.btn-sm{background:none;border:1px solid var(--border);padding:4px 10px;border-radius:2px;font-family:'Arial',sans-serif;font-size:11px;cursor:pointer;color:var(--ink2)}
.btn-sm:hover{border-color:var(--gold);color:var(--gold)}
.btn-danger{background:none;border:1px solid var(--danger);padding:4px 10px;border-radius:2px;font-family:'Arial',sans-serif;font-size:11px;cursor:pointer;color:var(--danger)}

/* TABLES */
.data-table{width:100%;border-collapse:collapse;font-family:'Arial',sans-serif;font-size:13px}
.data-table th{text-align:left;font-size:10px;letter-spacing:1.5px;text-transform:uppercase;color:var(--muted);padding:10px 12px;border-bottom:2px solid var(--border);font-weight:normal}
.data-table td{padding:11px 12px;border-bottom:1px solid var(--border);vertical-align:middle}
.data-table tr:last-child td{border-bottom:none}
.data-table tr:hover td{background:var(--surface)}

/* BADGES */
.badge{display:inline-block;font-family:'Arial',sans-serif;font-size:10px;letter-spacing:.5px;padding:3px 8px;border-radius:2px;text-transform:uppercase;font-weight:500}
.badge-green{background:var(--success-light);color:var(--success)}
.badge-gold{background:var(--gold-light);color:var(--warn)}
.badge-gray{background:var(--surface2);color:var(--muted)}
.badge-red{background:var(--danger-light);color:var(--danger)}
.badge-blue{background:var(--tag-bg);color:var(--tag-text)}

/* INNER TABS */
.inner-tabs{display:flex;gap:0;border-bottom:1px solid var(--border);margin-bottom:20px;overflow-x:auto}
.inner-tab{font-family:'Arial',sans-serif;font-size:11px;letter-spacing:.5px;text-transform:uppercase;color:var(--muted);background:none;border:none;border-bottom:2px solid transparent;padding:10px 16px;cursor:pointer;white-space:nowrap;transition:all .15s}
.inner-tab:hover{color:var(--ink)}
.inner-tab.active{color:var(--gold);border-bottom-color:var(--gold)}

/* REPORT CARDS (client view) */
.report-card{background:var(--white);border:1px solid var(--border);border-radius:2px;margin-bottom:12px;overflow:hidden}
.report-header{padding:16px 20px;cursor:pointer;display:flex;justify-content:space-between;align-items:center}
.report-header:hover{background:var(--surface)}
.report-title{font-size:15px;font-weight:normal;color:var(--ink);margin-bottom:3px}
.report-meta{font-family:'Arial',sans-serif;font-size:12px;color:var(--muted)}
.report-body{display:none;padding:0 20px 20px;border-top:1px solid var(--border)}
.report-body.open{display:block}
.expand-icon{font-size:20px;color:var(--muted);transition:transform .2s}
.expand-icon.open{transform:rotate(90deg)}
.kpi-grid{display:grid;grid-template-columns:repeat(auto-fill,minmax(100px,1fr));gap:10px;margin:16px 0}
.kpi-box{background:var(--surface2);border-radius:2px;padding:12px;text-align:center}
.kpi-score{font-size:24px;font-weight:bold;color:var(--gold)}
.kpi-label{font-family:'Arial',sans-serif;font-size:10px;text-transform:uppercase;color:var(--muted);margin-top:4px;letter-spacing:.5px}
.detail-section{margin-top:16px}
.detail-section h4{font-family:'Arial',sans-serif;font-size:10px;letter-spacing:1.5px;text-transform:uppercase;color:var(--gold);margin-bottom:6px}
.detail-text{font-size:14px;line-height:1.7;color:var(--ink2)}
.action-items{padding-left:18px}
.action-items li{font-family:'Arial',sans-serif;font-size:13px;color:var(--ink2);margin-bottom:4px}

/* FEEDBACK */
.error-msg{font-family:'Arial',sans-serif;font-size:12px;color:var(--danger);margin-top:8px;padding:8px 12px;background:var(--danger-light);border-radius:2px}
.success-msg{font-family:'Arial',sans-serif;font-size:12px;color:var(--success);margin-top:8px;padding:8px 12px;background:var(--success-light);border-radius:2px}

/* MODAL */
.modal-overlay{position:fixed;inset:0;background:rgba(0,0,0,.5);display:none;align-items:center;justify-content:center;z-index:100}
.modal-overlay.open{display:flex}
.modal{background:var(--white);border-radius:2px;width:580px;max-width:95vw;max-height:90vh;overflow-y:auto}
.modal-lg{width:760px}
.modal-header{padding:20px 24px;border-bottom:1px solid var(--border);display:flex;justify-content:space-between;align-items:center}
.modal-title{font-size:16px;font-weight:normal;color:var(--ink)}
.modal-close{background:none;border:none;font-size:22px;color:var(--muted);cursor:pointer;line-height:1}
.modal-body{padding:24px}

/* STATS ROW */
.stats-row{display:grid;grid-template-columns:repeat(auto-fit,minmax(140px,1fr));gap:14px;margin-bottom:24px}
.stat-box{background:var(--white);border:1px solid var(--border);border-radius:2px;padding:16px;text-align:center}
.stat-num{font-size:28px;font-weight:bold;color:var(--gold)}
.stat-label{font-family:'Arial',sans-serif;font-size:10px;text-transform:uppercase;color:var(--muted);letter-spacing:.5px;margin-top:4px}

/* CLIENT PORTAL SPECIFIC */
.welcome-bar{background:var(--ink);color:var(--white);padding:24px 28px;border-radius:2px;margin-bottom:24px}
.welcome-bar h2{font-size:20px;font-weight:normal;margin-bottom:4px}
.welcome-bar p{font-family:'Arial',sans-serif;font-size:12px;opacity:.6}
.portal-tabs{display:flex;gap:0;border-bottom:2px solid var(--border);margin-bottom:24px}
.ptab{font-family:'Arial',sans-serif;font-size:12px;text-transform:uppercase;letter-spacing:.5px;color:var(--muted);background:none;border:none;border-bottom:2px solid transparent;margin-bottom:-2px;padding:10px 18px;cursor:pointer}
.ptab.active{color:var(--gold);border-bottom-color:var(--gold)}

/* INFECTION CONTROL */
.ic-section-btn.active{background:var(--ink)!important;color:white!important}
.ic-staff-row-flagged{background:#FBEAEA!important}
.ic-staff-row-overdue{background:#FBF0DC!important}
.ic-result-clear{color:var(--success);font-weight:600}
.ic-result-flagged{color:var(--danger);font-weight:600}
.ic-result-pending{color:var(--warn);font-weight:600}
.ic-result-pass{color:var(--success);font-weight:600}
.ic-result-fail{color:var(--danger);font-weight:600}
.ic-report-card{background:var(--white);border:1px solid var(--border);border-radius:2px;margin-bottom:16px;overflow:hidden}
.ic-report-header{padding:16px 20px;background:var(--ink);color:white}
.ic-report-header h3{font-size:16px;font-weight:normal;margin-bottom:4px}
.ic-report-header p{font-family:Arial;font-size:12px;opacity:.7}
.ic-report-section{padding:16px 20px;border-bottom:1px solid var(--border)}
.ic-report-section h4{font-family:Arial;font-size:10px;letter-spacing:1.5px;text-transform:uppercase;color:var(--gold);margin-bottom:10px}
.ic-score-grid{display:grid;grid-template-columns:repeat(auto-fill,minmax(120px,1fr));gap:10px;margin-bottom:10px}
.ic-score-box{background:var(--surface2);border-radius:2px;padding:10px;text-align:center}
.ic-score-num{font-size:22px;font-weight:bold;color:var(--gold)}
.ic-score-lbl{font-family:Arial;font-size:10px;color:var(--muted);margin-top:3px;text-transform:uppercase;letter-spacing:.3px}

/* COMPLIANCE MODULE */
.comp-tier-header{font-family:'Arial',sans-serif;font-size:10px;letter-spacing:2px;text-transform:uppercase;color:var(--gold);margin:22px 0 10px;padding-bottom:6px;border-bottom:1px solid var(--border)}
.comp-card{background:var(--white);border:1px solid var(--border);border-radius:2px;margin-bottom:8px;overflow:hidden}
.comp-card-header{padding:14px 16px;display:flex;align-items:center;gap:12px;cursor:pointer}
.comp-card-header:hover{background:var(--surface)}
.comp-abbr-tag{background:var(--surface2);border:1px solid var(--border);border-radius:2px;font-family:'Arial',sans-serif;font-size:10px;letter-spacing:.5px;padding:3px 8px;color:var(--ink2);flex-shrink:0}
.comp-card-names{flex:1;min-width:0}
.comp-card-name{font-size:14px;color:var(--ink);line-height:1.3}
.comp-card-abbr{font-family:'Arial',sans-serif;font-size:11px;color:var(--muted);margin-top:1px}
.comp-niche-pills{display:flex;gap:4px;flex-wrap:wrap;margin-top:4px}
.comp-niche-pill{font-family:'Arial',sans-serif;font-size:9px;letter-spacing:.3px;padding:2px 7px;border-radius:10px;text-transform:uppercase;font-weight:600}
.cn-all{background:#e8f5ee;color:#1A5C38}
.cn-food{background:#e8f0fe;color:#3c5aab}
.cn-hospitality{background:#fff2e6;color:#7A4F00}
.cn-employer{background:#f5e8fe;color:#5a2d8c}
.cn-data{background:#e8f8fe;color:#0a5c7a}
.cn-fct{background:#fef5e8;color:#7a5a0a}
.cn-fintech{background:#fee8e8;color:#8B1A1A}
.comp-card-right{display:flex;align-items:center;gap:10px;flex-shrink:0}
.comp-expand{font-size:11px;color:var(--muted);transition:transform .2s}
.comp-card.open .comp-expand{transform:rotate(180deg)}
.comp-body{display:none;padding:0 16px 16px;border-top:1px solid var(--border)}
.comp-card.open .comp-body{display:block}
.comp-info-grid{display:grid;grid-template-columns:1fr 1fr;gap:10px;margin:14px 0}
.comp-info-box{background:var(--surface);border-radius:2px;padding:10px 12px}
.comp-info-box label{display:block;font-family:'Arial',sans-serif;font-size:9px;letter-spacing:1.5px;text-transform:uppercase;color:var(--gold);margin-bottom:4px}
.comp-info-box .cval{font-family:'Arial',sans-serif;font-size:12px;color:var(--ink2);line-height:1.5}
.comp-penalty{background:var(--danger-light);border-left:3px solid var(--danger);border-radius:2px;padding:8px 12px;font-family:'Arial',sans-serif;font-size:12px;color:var(--danger);margin-bottom:12px;line-height:1.5}
.comp-docs-section h5{font-family:'Arial',sans-serif;font-size:9px;letter-spacing:1.5px;text-transform:uppercase;color:var(--gold);margin-bottom:6px}
.comp-doc-list{list-style:none;padding:0}
.comp-doc-list li{font-family:'Arial',sans-serif;font-size:12px;color:var(--ink2);padding:2px 0;display:flex;gap:7px;line-height:1.4}
.comp-doc-list li::before{content:'○';color:var(--gold);font-size:9px;margin-top:3px;flex-shrink:0}
.comp-contact{font-family:'Arial',sans-serif;font-size:11px;color:var(--muted);margin:10px 0}
.comp-status-row{display:flex;gap:8px;align-items:center;flex-wrap:wrap;margin-top:12px;padding-top:12px;border-top:1px solid var(--border)}
.comp-saved-note{font-family:'Arial',sans-serif;font-size:11px;color:var(--success);margin-top:6px;display:none}
.status-badge-comp{display:inline-block;font-family:'Arial',sans-serif;font-size:10px;letter-spacing:.5px;padding:3px 9px;border-radius:2px;text-transform:uppercase;font-weight:500;flex-shrink:0}
.sbc-not-started{background:var(--danger-light);color:var(--danger)}
.sbc-in-progress{background:var(--gold-light);color:var(--warn)}
.sbc-done{background:var(--success-light);color:var(--success)}
.sbc-na{background:var(--surface2);color:var(--muted)}
.comp-due-flag{font-family:'Arial',sans-serif;font-size:10px;padding:2px 8px;border-radius:2px}
.comp-due-soon{background:#FBF0DC;color:#7A4F00}
.comp-overdue{background:var(--danger-light);color:var(--danger)}


.inv-status-paid{color:var(--success);font-weight:500}
.inv-status-partial{color:var(--warn);font-weight:500}
.inv-status-unpaid{color:var(--danger);font-weight:500}

/* COMPLAINT */
.complaint-open{border-left:3px solid var(--danger)}
.complaint-resolved{border-left:3px solid var(--success)}
.complaint-escalated{border-left:3px solid var(--warn)}
.strike-dot{display:inline-block;width:10px;height:10px;border-radius:50%;background:var(--danger);margin-right:3px}
.strike-empty{display:inline-block;width:10px;height:10px;border-radius:50%;background:var(--border);margin-right:3px}

/* PRIORITY TAG */
.priority-high{color:var(--danger);font-weight:500}
.priority-medium{color:var(--warn);font-weight:500}
.priority-low{color:var(--success);font-weight:500}
.pending-badge{background:#7A4F00;color:white;font-family:Arial;font-size:10px;font-weight:500;padding:2px 8px;border-radius:2px;letter-spacing:.5px;text-transform:uppercase}
.pending-row{background:#FBF0DC!important}
.approval-card{border:1.5px solid #dbc9ba;border-left:4px solid #7A4F00;border-radius:2px;padding:16px 18px;margin-bottom:12px;background:white}
@media print{#modal-invoice-pdf .modal{box-shadow:none;border:none;max-width:100%;width:100%}.modal-overlay{background:white;position:relative}.modal-header{display:none!important}}
/* INFECTION CONTROL */
.ic-section-btn.active{background:var(--ink)!important;color:white!important}
.ic-staff-row-flagged{background:#FBEAEA!important}
.ic-staff-row-overdue{background:#FBF0DC!important}
.ic-result-clear{color:var(--success);font-weight:600}
.ic-result-flagged{color:var(--danger);font-weight:600}
.ic-result-pending{color:var(--warn);font-weight:600}
.ic-result-pass{color:var(--success);font-weight:600}
.ic-result-fail{color:var(--danger);font-weight:600}
.ic-report-card{background:var(--white);border:1px solid var(--border);border-radius:2px;margin-bottom:16px;overflow:hidden}
.ic-report-header{padding:16px 20px;background:var(--ink);color:white}
.ic-report-header h3{font-size:16px;font-weight:normal;margin-bottom:4px}
.ic-report-header p{font-family:Arial;font-size:12px;opacity:.7}
.ic-report-section{padding:16px 20px;border-bottom:1px solid var(--border)}
.ic-report-section h4{font-family:Arial;font-size:10px;letter-spacing:1.5px;text-transform:uppercase;color:var(--gold);margin-bottom:10px}
.ic-score-grid{display:grid;grid-template-columns:repeat(auto-fill,minmax(120px,1fr));gap:10px;margin-bottom:10px}
.ic-score-box{background:var(--surface2);border-radius:2px;padding:10px;text-align:center}
.ic-score-num{font-size:22px;font-weight:bold;color:var(--gold)}
.ic-score-lbl{font-family:Arial;font-size:10px;color:var(--muted);margin-top:3px;text-transform:uppercase;letter-spacing:.3px}

/* COMPLIANCE MODULE */
.comp-tier-header{font-family:'Arial',sans-serif;font-size:10px;letter-spacing:2px;text-transform:uppercase;color:var(--gold);margin:22px 0 10px;padding-bottom:6px;border-bottom:1px solid var(--border)}
.comp-card{background:var(--white);border:1px solid var(--border);border-radius:2px;margin-bottom:8px;overflow:hidden}
.comp-card-header{padding:14px 16px;display:flex;align-items:center;gap:12px;cursor:pointer}
.comp-card-header:hover{background:var(--surface)}
.comp-abbr-tag{background:var(--surface2);border:1px solid var(--border);border-radius:2px;font-family:'Arial',sans-serif;font-size:10px;letter-spacing:.5px;padding:3px 8px;color:var(--ink2);flex-shrink:0}
.comp-card-names{flex:1;min-width:0}
.comp-card-name{font-size:14px;color:var(--ink);line-height:1.3}
.comp-card-abbr{font-family:'Arial',sans-serif;font-size:11px;color:var(--muted);margin-top:1px}
.comp-niche-pills{display:flex;gap:4px;flex-wrap:wrap;margin-top:4px}
.comp-niche-pill{font-family:'Arial',sans-serif;font-size:9px;letter-spacing:.3px;padding:2px 7px;border-radius:10px;text-transform:uppercase;font-weight:600}
.cn-all{background:#e8f5ee;color:#1A5C38}
.cn-food{background:#e8f0fe;color:#3c5aab}
.cn-hospitality{background:#fff2e6;color:#7A4F00}
.cn-employer{background:#f5e8fe;color:#5a2d8c}
.cn-data{background:#e8f8fe;color:#0a5c7a}
.cn-fct{background:#fef5e8;color:#7a5a0a}
.cn-fintech{background:#fee8e8;color:#8B1A1A}
.comp-card-right{display:flex;align-items:center;gap:10px;flex-shrink:0}
.comp-expand{font-size:11px;color:var(--muted);transition:transform .2s}
.comp-card.open .comp-expand{transform:rotate(180deg)}
.comp-body{display:none;padding:0 16px 16px;border-top:1px solid var(--border)}
.comp-card.open .comp-body{display:block}
.comp-info-grid{display:grid;grid-template-columns:1fr 1fr;gap:10px;margin:14px 0}
.comp-info-box{background:var(--surface);border-radius:2px;padding:10px 12px}
.comp-info-box label{display:block;font-family:'Arial',sans-serif;font-size:9px;letter-spacing:1.5px;text-transform:uppercase;color:var(--gold);margin-bottom:4px}
.comp-info-box .cval{font-family:'Arial',sans-serif;font-size:12px;color:var(--ink2);line-height:1.5}
.comp-penalty{background:var(--danger-light);border-left:3px solid var(--danger);border-radius:2px;padding:8px 12px;font-family:'Arial',sans-serif;font-size:12px;color:var(--danger);margin-bottom:12px;line-height:1.5}
.comp-docs-section h5{font-family:'Arial',sans-serif;font-size:9px;letter-spacing:1.5px;text-transform:uppercase;color:var(--gold);margin-bottom:6px}
.comp-doc-list{list-style:none;padding:0}
.comp-doc-list li{font-family:'Arial',sans-serif;font-size:12px;color:var(--ink2);padding:2px 0;display:flex;gap:7px;line-height:1.4}
.comp-doc-list li::before{content:'○';color:var(--gold);font-size:9px;margin-top:3px;flex-shrink:0}
.comp-contact{font-family:'Arial',sans-serif;font-size:11px;color:var(--muted);margin:10px 0}
.comp-status-row{display:flex;gap:8px;align-items:center;flex-wrap:wrap;margin-top:12px;padding-top:12px;border-top:1px solid var(--border)}
.comp-saved-note{font-family:'Arial',sans-serif;font-size:11px;color:var(--success);margin-top:6px;display:none}
.status-badge-comp{display:inline-block;font-family:'Arial',sans-serif;font-size:10px;letter-spacing:.5px;padding:3px 9px;border-radius:2px;text-transform:uppercase;font-weight:500;flex-shrink:0}
.sbc-not-started{background:var(--danger-light);color:var(--danger)}
.sbc-in-progress{background:var(--gold-light);color:var(--warn)}
.sbc-done{background:var(--success-light);color:var(--success)}
.sbc-na{background:var(--surface2);color:var(--muted)}
.comp-due-flag{font-family:'Arial',sans-serif;font-size:10px;padding:2px 8px;border-radius:2px}
.comp-due-soon{background:#FBF0DC;color:#7A4F00}
.comp-overdue{background:var(--danger-light);color:var(--danger)}


.inv-status-paid{color:var(--success);font-weight:500}
.inv-status-partial{color:var(--warn);font-weight:500}
.inv-status-unpaid{color:var(--danger);font-weight:500}

/* COMPLAINT */
.complaint-open{border-left:3px solid var(--danger)}
.complaint-resolved{border-left:3px solid var(--success)}
.complaint-escalated{border-left:3px solid var(--warn)}
.strike-dot{display:inline-block;width:10px;height:10px;border-radius:50%;background:var(--danger);margin-right:3px}
.strike-empty{display:inline-block;width:10px;height:10px;border-radius:50%;background:var(--border);margin-right:3px}

/* PRIORITY TAG */
.priority-high{color:var(--danger);font-weight:500}
.priority-medium{color:var(--warn);font-weight:500}
.priority-low{color:var(--success);font-weight:500}
.pending-badge{background:#7A4F00;color:white;font-family:Arial;font-size:10px;font-weight:500;padding:2px 8px;border-radius:2px;letter-spacing:.5px;text-transform:uppercase}
.pending-row{background:#FBF0DC!important}
.approval-card{border:1.5px solid #dbc9ba;border-left:4px solid #7A4F00;border-radius:2px;padding:16px 18px;margin-bottom:12px;background:white}
@media print{#modal-invoice-pdf .modal{box-shadow:none;border:none;max-width:100%;width:100%}.modal-overlay{background:white;position:relative}.modal-header{display:none!important}}
</style>

<script>
// ══ SERVICE WORKER + PUSH NOTIFICATIONS ══
const VAPID_PUBLIC_KEY = 'BOkkUrUZLR2lHhiyOkNGIy2lx7r8QeMAqTbqFRQ_5NYxM6bClbq4nIkc09TEdq6NYEtBQRxodAeVQaqRUl3pP3Y';

function urlBase64ToUint8Array(base64String){
  const padding='='.repeat((4-base64String.length%4)%4);
  const base64=(base64String+padding).replace(/-/g,'+').replace(/_/g,'/');
  const raw=window.atob(base64);
  return new Uint8Array([...raw].map(c=>c.charCodeAt(0)));
}

async function registerPushNotifications(){
  if(!('serviceWorker' in navigator)||!('PushManager' in window)){
    console.log('Push not supported');return false;
  }
  try{
    const reg=await navigator.serviceWorker.register('/sw.js');
    const permission=await Notification.requestPermission();
    if(permission!=='granted'){console.log('Permission denied');return false;}
    const sub=await reg.pushManager.subscribe({
      userVisibleOnly:true,
      applicationServerKey:urlBase64ToUint8Array(VAPID_PUBLIC_KEY)
    });
    // Save subscription to Supabase so server can push to it
    const subStr=JSON.stringify(sub);
    // Store in localStorage for this device
    localStorage.setItem('haida_push_sub',subStr);
    console.log('Push subscription active');
    return true;
  }catch(e){console.log('Push registration failed:',e);return false;}
}

async function sendPushToSelf(title,body){
  // Since we can't run a server here, use the Notification API directly
  // This works when the portal tab is open OR via SW when registered
  if(!('Notification' in window))return;
  if(Notification.permission==='granted'){
    if(navigator.serviceWorker&&navigator.serviceWorker.controller){
      navigator.serviceWorker.controller.postMessage({type:'SHOW_NOTIF',title,body});
    } else {
      new Notification(title,{body,icon:'/icon.png',badge:'/icon.png',tag:'haida-request',renotify:true});
    }
  }
}

// Auto-register SW when owner logs in
async function initPushForOwner(){
  if(!('serviceWorker' in navigator))return;
  try{
    // Register a minimal inline service worker via blob
    const swCode=`
self.addEventListener('push',function(e){
  const data=e.data?e.data.json():{};
  e.waitUntil(self.registration.showNotification(data.title||'Haida Portal',{
    body:data.body||'New notification',
    icon:'https://haidataskmachine.netlify.app/favicon.ico',
    badge:'https://haidataskmachine.netlify.app/favicon.ico',
    tag:'haida-request',
    renotify:true,
    requireInteraction:true
  }));
});
self.addEventListener('message',function(e){
  if(e.data&&e.data.type==='SHOW_NOTIF'){
    self.registration.showNotification(e.data.title,{
      body:e.data.body,
      tag:'haida-request',
      renotify:true
    });
  }
});
self.addEventListener('notificationclick',function(e){
  e.notification.close();
  e.waitUntil(clients.openWindow('/'));
});`;
    const blob=new Blob([swCode],{type:'application/javascript'});
    const swUrl=URL.createObjectURL(blob);
    const reg=await navigator.serviceWorker.register(swUrl,{scope:'/'}).catch(()=>null);
    if(!reg){
      // Fallback: just request notification permission
      await Notification.requestPermission();
      return;
    }
    const perm=await Notification.requestPermission();
    if(perm==='granted'){
      console.log('Notifications enabled for Haida Portal');
    }
  }catch(e){
    // Fallback to basic notifications
    Notification.requestPermission();
  }
}
</script>
</head>
<body>

<!-- LOGIN -->
<div id="login-screen">
  <div class="login-card">
    <div class="login-header">
      <div class="wordmark">Haida Yammar Consulting</div>
      <h1>Client Portal</h1>
    </div>
    <div class="login-body">
      <form onsubmit="handleLogin();return false;" autocomplete="on">
      <div class="field"><label>Username</label><input type="text" id="login-user" placeholder="Enter your username" autocomplete="username"></div>
      <div class="field"><label>Access Code</label><input type="password" id="login-pass" placeholder="Enter your access code" autocomplete="current-password"></div>
      <button type="submit" class="btn-primary">Sign In</button>
      </form>
      <div class="login-error" id="login-error"></div>
    </div>
  </div>
</div>

<!-- APP -->
<div id="app">
  <div class="topbar">
    <div class="brand">Haida Consulting</div>
    <div class="right">
      <span id="topbar-user"></span>
      <span id="notif-badge" style="display:none;background:#8B1A1A;color:white;border-radius:10px;padding:2px 8px;font-family:Arial;font-size:11px;font-weight:500;cursor:pointer" onclick="showAdminTabByName('approvals');markNotificationsRead()">
        🔔 <span id="notif-count">0</span> new
      </span>
      <button id="push-enable-btn" style="display:none;background:none;border:1px solid rgba(184,145,42,.4);color:var(--gold);font-family:Arial;font-size:11px;padding:4px 10px;border-radius:2px;cursor:pointer" onclick="initPushForOwner().then(()=>{this.style.display='none';this.nextElementSibling.style.display='inline'})">🔔 Enable alerts</button>
      <span id="push-active-indicator" style="display:none;font-family:Arial;font-size:11px;color:var(--gold)">🔔 Alerts on</span>
      <button class="btn-logout" onclick="logout()">Sign Out</button>
    </div>
  </div>
  <div class="main-area">

    <!-- ADMIN NAV -->
    <nav id="admin-nav">
      <div class="tab-section">Clients</div>
      <button class="tab-btn active" onclick="showAdminTab('clients',this)">All Clients</button>
      <button class="tab-btn" onclick="showAdminTab('reports',this)">Add Report</button>
      <div class="tab-section" id="finance-section" style="display:none!important">Finance</div>
      <button class="tab-btn" id="invoices-tab-btn" style="display:none!important" onclick="showAdminTab('invoices',this)">Invoices</button>
      <div class="tab-section">Operations</div>
      <button class="tab-btn" onclick="showAdminTab('complaints',this)">Complaints</button>
      <button class="tab-btn" onclick="showAdminTab('eventplanner',this)">🎪 Event Planner</button>
      <button class="tab-btn" onclick="showAdminTab('actions',this)">Action Tracker</button>
      <button class="tab-btn" onclick="showAdminTab('staff',this)">Client Staff</button>
      <button class="tab-btn" onclick="showAdminTab('contracts',this)">Contracts</button>
      <button class="tab-btn" onclick="showAdminTab('documents',this)">Documents</button>
      <button class="tab-btn" id="compliance-tab-btn" onclick="showAdminTab('compliance',this)">🗂 Compliance</button>
      <div class="tab-section" id="admin-section" style="display:none">Admin</div>
      <button class="tab-btn" id="onboarding-tab-btn" style="display:none" onclick="showAdminTab('onboarding',this)">Onboarding</button>
      <button class="tab-btn" id="approvals-tab-btn" style="display:none" onclick="showAdminTab('approvals',this)">
        Approvals <span id="approvals-badge" style="background:#8B1A1A;color:white;border-radius:10px;padding:1px 7px;font-size:10px;margin-left:4px;display:none">0</span>
      </button>
      <button class="tab-btn" id="expenditure-tab-btn" style="display:none" onclick="showAdminTab('expenditure',this)">Expenditure</button>
    </nav>

    <div class="content-area">

      <!-- CLIENTS TAB -->
      <div class="page active" id="page-clients">
        <div class="page-title">Clients</div>
        <div class="page-sub">Manage all active and onboarding clients</div>
        <div style="display:flex;justify-content:flex-end;margin-bottom:16px;">
          <button class="btn-add" onclick="gateAddClient()">+ Add Client</button>
        </div>
        <div class="card" style="padding:0;overflow:hidden">
          <table class="data-table">
            <thead><tr>
              <th>Client</th><th>Niche</th><th>Package</th><th>Status</th><th>Reports</th><th>Contract</th><th>Actions</th>
            </tr></thead>
            <tbody id="clients-tbody"><tr><td colspan="7" style="text-align:center;padding:24px;color:var(--muted);font-family:Arial">Loading…</td></tr></tbody>
          </table>
        </div>
      </div>

      <!-- REPORTS TAB -->
      <div class="page" id="page-reports">
        <div class="page-title">Add Report</div>
        <div class="page-sub">Submit a report for a client — fields adapt to the client's niche automatically</div>
        <div class="card">
          <div style="margin-bottom:12px;padding:10px 14px;background:var(--surface2);border-radius:2px;font-family:Arial;font-size:12px;color:var(--muted)" id="niche-report-banner">
            Select a client to see niche-specific report fields
          </div>
          <div class="inner-tabs">
            <button class="inner-tab active" onclick="switchReportType('audit',this)">Audit</button>
            <button class="inner-tab" onclick="switchReportType('supervision',this)">Supervision</button>
            <button class="inner-tab" onclick="switchReportType('site_visit',this)">Site Visit</button>
            <button class="inner-tab" onclick="switchReportType('cx_mystery',this)">CX Mystery</button>
            <button class="inner-tab" onclick="switchReportType('staff_training',this)">Staff Training</button>
            <button class="inner-tab" onclick="switchReportType('brand_review',this)">Brand Review</button>
            <button class="inner-tab" onclick="switchReportType('performance',this)">Performance</button>
            <button class="inner-tab" onclick="switchReportType('kpi',this)">KPI Review</button>
            <button class="inner-tab" style="background:var(--gold-light);color:var(--warn);font-weight:600" onclick="generateFullReport()">📄 Full Report</button>
          </div>
          <div id="report-form">
            <div class="form-grid">
              <div class="form-field"><label>Client</label><select id="rf-client"><option value="">Select client…</option></select></div>
              <div class="form-field"><label>Date</label><input type="date" id="rf-date"></div>
              <div class="form-field"><label>Report Title</label><input type="text" id="rf-title" placeholder="e.g. Monthly Audit — June 2026"></div>
              <div class="form-field"><label>Location / Site</label><input type="text" id="rf-location" placeholder="e.g. Guzape Mall, Abuja"></div>
            </div>
            <div class="section-label" id="dynamic-sections"></div>
            <div id="type-fields"></div>
            <div class="section-label">Overall Summary</div>
            <div class="form-grid">
              <div class="form-field" style="grid-column:1/-1"><label>Summary / Key Findings</label><textarea id="rf-summary" rows="4" placeholder="Describe the overall findings…"></textarea></div>
              <div class="form-field" style="grid-column:1/-1"><label>Action Items (one per line)</label><textarea id="rf-actions" rows="4" placeholder="List action items, one per line…"></textarea></div>
              <div class="form-field" style="grid-column:1/-1"><label>Recommendations</label><textarea id="rf-recs" rows="3" placeholder="Recommendations for improvement…"></textarea></div>
            </div>
            <div style="margin-top:16px">
              <button class="btn-save" onclick="saveReport()">Save Report</button>
              <button class="btn-cancel" onclick="clearReportForm()">Clear</button>
            </div>
            <div id="form-feedback"></div>
          </div>
        </div>
      </div>

      <!-- INVOICES TAB -->
      <div class="page" id="page-invoices">
        <div class="page-title">Invoices</div>
        <div class="page-sub">Create, track, and manage all client invoices</div>
        <div style="display:flex;justify-content:flex-end;margin-bottom:16px;">
          <button class="btn-add" onclick="openAddInvoice()">+ Create Invoice</button>
        </div>
        <div class="stats-row" id="invoice-stats"></div>
        <div class="card" style="padding:0;overflow:hidden">
          <table class="data-table">
            <thead><tr><th>Invoice</th><th>Client</th><th>Subtotal</th><th>VAT</th><th>Total</th><th>Paid</th><th>Balance</th><th>Due</th><th>Status</th><th>Actions</th></tr></thead>
            <tbody id="invoices-tbody"><tr><td colspan="8" style="text-align:center;padding:24px;color:var(--muted);font-family:Arial">Loading…</td></tr></tbody>
          </table>
        </div>
      </div>

      <!-- COMPLAINTS TAB -->
      <div class="page" id="page-complaints">
        <div class="page-title">Complaints</div>
        <div class="page-sub">Log, track, and resolve client or customer complaints — 3-strike escalation model</div>
        <div style="display:flex;justify-content:flex-end;margin-bottom:16px;">
          <button class="btn-add" onclick="openModal('modal-add-complaint')">+ Log Complaint</button>
        </div>
        <div class="stats-row" id="complaint-stats"></div>
        <div id="complaints-list"></div>
      </div>

      <!-- ACTION TRACKER -->
      <div class="page" id="page-actions">
        <div class="page-title">Action Tracker</div>
        <div class="page-sub">All outstanding actions across every client — nothing falls through</div>
        <div style="display:flex;justify-content:flex-end;margin-bottom:16px;">
          <button class="btn-add" onclick="openModal('modal-add-action')">+ Add Action</button>
        </div>
        <div class="card" style="padding:0;overflow:hidden">
          <table class="data-table">
            <thead><tr><th>Action</th><th>Client</th><th>Owner</th><th>Due</th><th>Priority</th><th>Status</th><th>Actions</th></tr></thead>
            <tbody id="actions-tbody"><tr><td colspan="7" style="text-align:center;padding:24px;color:var(--muted);font-family:Arial">Loading…</td></tr></tbody>
          </table>
        </div>
      </div>

      <!-- CLIENT STAFF -->
      <div class="page" id="page-staff">
        <div class="page-title">Client Staff Records</div>
        <div class="page-sub">Staff members employed by clients — flags, performance notes, training records</div>
        <div style="display:flex;justify-content:space-between;align-items:center;margin-bottom:16px;flex-wrap:wrap;gap:10px">
          <div style="display:flex;gap:8px;align-items:center;flex-wrap:wrap">
            <select id="staff-client-filter" onchange="filterStaffByClient()" style="padding:7px 12px;border:1px solid var(--border);border-radius:2px;font-family:Arial;font-size:13px;color:var(--ink);min-width:200px">
              <option value="">All Clients</option>
            </select>
            <select id="staff-status-filter" onchange="filterStaffByClient()" style="padding:7px 12px;border:1px solid var(--border);border-radius:2px;font-family:Arial;font-size:13px;color:var(--ink)">
              <option value="">All Statuses</option>
              <option value="Active">Active</option>
              <option value="Probation">Probation</option>
              <option value="Flagged">Flagged</option>
              <option value="Terminated">Terminated</option>
            </select>
          </div>
          <button class="btn-add" onclick="openModal('modal-add-staff')">+ Add Staff Member</button>
        </div>
        <div class="card" style="padding:0;overflow:hidden">
          <table class="data-table">
            <thead><tr><th>Name</th><th>Client</th><th>Role</th><th>Status</th><th>Flags</th><th>Last Trained</th><th>Actions</th></tr></thead>
            <tbody id="staff-tbody"><tr><td colspan="7" style="text-align:center;padding:24px;color:var(--muted);font-family:Arial">Loading…</td></tr></tbody>
          </table>
        </div>
      </div>

      <!-- CONTRACTS -->
      <div class="page" id="page-contracts">
        <div class="page-title">Contracts</div>
        <div class="page-sub">Contract dates, renewal alerts, and retainer tracking per client</div>
        <div style="display:flex;justify-content:flex-end;margin-bottom:16px;">
          <button class="btn-add" onclick="openModal('modal-add-contract')">+ Add Contract</button>
        </div>
        <div class="card" style="padding:0;overflow:hidden">
          <table class="data-table">
            <thead><tr><th>Client</th><th>Type</th><th>Value</th><th>Start</th><th>End / Renewal</th><th>Days Left</th><th>Status</th><th>Actions</th></tr></thead>
            <tbody id="contracts-tbody"><tr><td colspan="8" style="text-align:center;padding:24px;color:var(--muted);font-family:Arial">Loading…</td></tr></tbody>
          </table>
        </div>
      </div>

      <!-- DOCUMENTS -->
      <div class="page" id="page-documents">
        <div class="page-title">Documents</div>
        <div class="page-sub">SOPs, handbooks, policies, and other documents shared with clients</div>
        <div style="display:flex;justify-content:flex-end;margin-bottom:16px;">
          <button class="btn-add" onclick="openModal('modal-add-doc')">+ Add Document</button>
        </div>
        <div class="card" style="padding:0;overflow:hidden">
          <table class="data-table">
            <thead><tr><th>Document</th><th>Client</th><th>Type</th><th>Date Added</th><th>Link / Notes</th><th>Actions</th></tr></thead>
            <tbody id="docs-tbody"><tr><td colspan="6" style="text-align:center;padding:24px;color:var(--muted);font-family:Arial">Loading…</td></tr></tbody>
          </table>
        </div>
      </div>

      <!-- COMPLIANCE PAGE -->
      <div class="page" id="page-compliance">
        <div class="page-title">Compliance & Infection Control</div>
        <div class="page-sub">Regulatory bodies by jurisdiction · Staff health testing · Facility hygiene · Client-visible reports</div>

        <!-- Inner tabs -->
        <div class="inner-tabs" style="margin-bottom:20px">
          <button class="inner-tab active" id="ctab-btn-bodies" onclick="switchCompTab('bodies',this)">🗂 Regulatory Bodies</button>
          <button class="inner-tab" id="ctab-btn-infection" onclick="switchCompTab('infection',this)">🧪 Infection Control</button>
          <button class="inner-tab" id="ctab-btn-risktraining" onclick="switchCompTab('risktraining',this)">📋 Risk & Training</button>
        </div>

        <!-- ── TAB: REGULATORY BODIES ── -->
        <div id="ctab-bodies">
          <div style="display:flex;gap:10px;flex-wrap:wrap;align-items:center;margin-bottom:18px">
            <input type="text" id="comp-search" placeholder="Search body, niche, keyword…" style="padding:8px 12px;border:1px solid var(--border);border-radius:2px;font-family:Arial;font-size:13px;min-width:200px;outline:none;color:var(--ink)">
            <select id="comp-country" style="padding:8px 12px;border:1px solid var(--border);border-radius:2px;font-family:Arial;font-size:13px;color:var(--ink)">
              <option value="all">All Jurisdictions</option>
              <option value="Nigeria">🇳🇬 Nigeria</option>
              <option value="United Kingdom">🇬🇧 United Kingdom</option>
              <option value="UAE / Dubai">🇦🇪 UAE / Dubai</option>
              <option value="Saudi Arabia">🇸🇦 Saudi Arabia</option>
            </select>
            <select id="comp-niche" style="padding:8px 12px;border:1px solid var(--border);border-radius:2px;font-family:Arial;font-size:13px;color:var(--ink)">
              <option value="all">All Niches</option>
              <option value="all-biz">All Businesses</option>
              <option value="food">Food & Beverage</option>
              <option value="hospitality">Hospitality / Café</option>
              <option value="employer">Employer / Staff</option>
              <option value="data">Data / Digital</option>
              <option value="fct">FCT / Abuja Specific</option>
              <option value="aesthetics">Aesthetics / Wellness</option>
              <option value="retail">Retail</option>
              <option value="education">Education</option>
            </select>
            <select id="comp-status-filter" style="padding:8px 12px;border:1px solid var(--border);border-radius:2px;font-family:Arial;font-size:13px;color:var(--ink)">
              <option value="all">All Statuses</option>
              <option value="Not Started">Not Started</option>
              <option value="In Progress">In Progress</option>
              <option value="Done">Done</option>
              <option value="N/A">N/A</option>
            </select>
            <select id="comp-client-filter" style="padding:8px 12px;border:1px solid var(--border);border-radius:2px;font-family:Arial;font-size:13px;color:var(--ink)">
              <option value="all">All / Haida Internal</option>
            </select>
            <button class="btn-add" onclick="openModal('modal-add-compliance-body')" style="margin-left:auto">+ Add Body</button>
          </div>
          <div class="stats-row" id="comp-stats-row" style="margin-bottom:20px"></div>
          <div id="comp-body-list"></div>
        </div>

        <!-- ── TAB: INFECTION CONTROL ── -->
        <div id="ctab-infection" style="display:none">

          <!-- IC inner nav -->
          <div style="display:flex;gap:8px;flex-wrap:wrap;margin-bottom:20px;align-items:center">
            <select id="ic-client-sel" style="padding:8px 12px;border:1px solid var(--border);border-radius:2px;font-family:Arial;font-size:13px;color:var(--ink);min-width:200px">
              <option value="">Select client / location…</option>
            </select>
            <div style="display:flex;gap:0;border:1px solid var(--border);border-radius:2px;overflow:hidden;font-family:Arial;font-size:12px">
              <button class="ic-section-btn active" data-sec="staff" onclick="switchICSection('staff',this)" style="padding:7px 14px;background:var(--ink);color:white;border:none;cursor:pointer;text-transform:uppercase;letter-spacing:.5px">Staff Testing</button>
              <button class="ic-section-btn" data-sec="facility" onclick="switchICSection('facility',this)" style="padding:7px 14px;background:var(--white);color:var(--ink);border:none;cursor:pointer;text-transform:uppercase;letter-spacing:.5px;border-left:1px solid var(--border)">Facility Hygiene</button>
              <button class="ic-section-btn" data-sec="food" onclick="switchICSection('food',this)" style="padding:7px 14px;background:var(--white);color:var(--ink);border:none;cursor:pointer;text-transform:uppercase;letter-spacing:.5px;border-left:1px solid var(--border)">Food Safety</button>
              <button class="ic-section-btn" data-sec="reports" onclick="switchICSection('reports',this)" style="padding:7px 14px;background:var(--white);color:var(--ink);border:none;cursor:pointer;text-transform:uppercase;letter-spacing:.5px;border-left:1px solid var(--border)">IC Reports</button>
            </div>
            <button class="btn-add" id="ic-add-btn" onclick="icAddAction()" style="margin-left:auto">+ Log Entry</button>
            <button onclick="generateICStaffReport()" style="padding:7px 14px;background:var(--ink);color:white;border:none;border-radius:2px;font-family:Arial;font-size:12px;font-weight:700;cursor:pointer;letter-spacing:.5px;margin-left:8px">📄 Generate Report</button>
          </div>

          <div class="stats-row" id="ic-stats-row" style="margin-bottom:20px"></div>

          <!-- STAFF TESTING SECTION -->
          <div id="ic-sec-staff">
            <div style="background:var(--white);border:1px solid var(--border);border-radius:2px;padding:16px 20px;margin-bottom:16px">
              <div style="font-family:Arial;font-size:11px;letter-spacing:1.5px;text-transform:uppercase;color:var(--gold);margin-bottom:10px">Required Tests Per Staff Member</div>
              <div style="display:grid;grid-template-columns:repeat(auto-fill,minmax(200px,1fr));gap:10px;font-family:Arial;font-size:12px">
                <div style="background:var(--surface);padding:10px;border-radius:2px"><strong>Hepatitis B & C</strong><br><span style="color:var(--muted)">Annual · All niches</span></div>
                <div style="background:var(--surface);padding:10px;border-radius:2px"><strong>HIV (ELISA/Rapid)</strong><br><span style="color:var(--muted)">Annual · Aesthetics, clinical, food</span></div>
                <div style="background:var(--surface);padding:10px;border-radius:2px"><strong>Tuberculosis (TB)</strong><br><span style="color:var(--muted)">Annual · All niches</span></div>
                <div style="background:var(--surface);padding:10px;border-radius:2px"><strong>Typhoid / Stool Culture</strong><br><span style="color:var(--muted)">6-monthly · Food, hospitality</span></div>
                <div style="background:var(--surface);padding:10px;border-radius:2px"><strong>MRSA Screening</strong><br><span style="color:var(--muted)">Annual · Aesthetics, clinical</span></div>
                <div style="background:var(--surface);padding:10px;border-radius:2px"><strong>Skin / Scalp Infection Screen</strong><br><span style="color:var(--muted)">Annual · Hair, beauty, aesthetics</span></div>
                <div style="background:var(--surface);padding:10px;border-radius:2px"><strong>Full Blood Count (FBC)</strong><br><span style="color:var(--muted)">Annual · All niches</span></div>
                <div style="background:var(--surface);padding:10px;border-radius:2px"><strong>Covid / Respiratory Screen</strong><br><span style="color:var(--muted)">As needed · All</span></div>
              </div>
            </div>
            <div style="display:flex;justify-content:flex-end;margin-bottom:10px">
              <span style="font-family:Arial;font-size:11px;color:var(--muted);margin-right:12px;align-self:center">Filter:</span>
              <select id="ic-staff-filter" onchange="renderICStaffTable()" style="padding:6px 10px;border:1px solid var(--border);border-radius:2px;font-family:Arial;font-size:12px;color:var(--ink)">
                <option value="all">All Staff</option>
                <option value="Overdue">Overdue</option>
                <option value="Due Soon">Due Soon</option>
                <option value="Clear">Clear</option>
                <option value="Flagged">Flagged</option>
              </select>
            </div>
            <div id="ic-staff-table"></div>
          </div>

          <!-- FACILITY HYGIENE SECTION -->
          <div id="ic-sec-facility" style="display:none">
            <div style="background:var(--white);border:1px solid var(--border);border-radius:2px;padding:16px 20px;margin-bottom:16px">
              <div style="font-family:Arial;font-size:11px;letter-spacing:1.5px;text-transform:uppercase;color:var(--gold);margin-bottom:10px">Hygiene Checklist Framework</div>
              <div style="display:grid;grid-template-columns:repeat(auto-fill,minmax(220px,1fr));gap:10px;font-family:Arial;font-size:12px">
                <div style="background:var(--surface);padding:10px;border-radius:2px"><strong>Surface Disinfection Log</strong><br><span style="color:var(--muted)">Daily · All premises</span></div>
                <div style="background:var(--surface);padding:10px;border-radius:2px"><strong>Kitchen / Prep Area Deep Clean</strong><br><span style="color:var(--muted)">Weekly · Food & hospitality</span></div>
                <div style="background:var(--surface);padding:10px;border-radius:2px"><strong>Equipment Sterilisation Record</strong><br><span style="color:var(--muted)">Per use · Aesthetics, clinical, hair</span></div>
                <div style="background:var(--surface);padding:10px;border-radius:2px"><strong>Pest Control Inspection</strong><br><span style="color:var(--muted)">Monthly / Quarterly · All</span></div>
                <div style="background:var(--surface);padding:10px;border-radius:2px"><strong>Waste & Sharps Disposal Log</strong><br><span style="color:var(--muted)">Per use / Daily · Aesthetics, clinical</span></div>
                <div style="background:var(--surface);padding:10px;border-radius:2px"><strong>Water Quality Test</strong><br><span style="color:var(--muted)">6-monthly · Food, school, wellness</span></div>
                <div style="background:var(--surface);padding:10px;border-radius:2px"><strong>PPE Stock Check</strong><br><span style="color:var(--muted)">Weekly · All client-facing staff</span></div>
                <div style="background:var(--surface);padding:10px;border-radius:2px"><strong>Toilet / Handwashing Audit</strong><br><span style="color:var(--muted)">Daily · All public-facing premises</span></div>
                <div style="background:var(--surface);padding:10px;border-radius:2px"><strong>Cross-contamination Risk Review</strong><br><span style="color:var(--muted)">Monthly · Food, aesthetics</span></div>
                <div style="background:var(--surface);padding:10px;border-radius:2px"><strong>Tableware & Utensil Sanitisation</strong><br><span style="color:var(--muted)">Per use + daily · Food, hospitality</span></div>
              </div>
            </div>
            <div id="ic-facility-table"></div>
          </div>

          <!-- FOOD SAFETY SECTION -->
          <div id="ic-sec-food" style="display:none">
            <div style="background:var(--white);border:1px solid var(--border);border-radius:2px;padding:16px 20px;margin-bottom:16px">
              <div style="font-family:Arial;font-size:11px;letter-spacing:1.5px;text-transform:uppercase;color:var(--gold);margin-bottom:10px">Food Safety Standards — Eat Off The Floor Level</div>
              <div style="display:grid;grid-template-columns:repeat(auto-fill,minmax(220px,1fr));gap:10px;font-family:Arial;font-size:12px">
                <div style="background:var(--surface);padding:10px;border-radius:2px"><strong>Temp Log — Cold Storage</strong><br><span style="color:var(--muted)">Twice daily · ≤4°C</span></div>
                <div style="background:var(--surface);padding:10px;border-radius:2px"><strong>Temp Log — Hot Holding</strong><br><span style="color:var(--muted)">Every 2hrs · ≥63°C</span></div>
                <div style="background:var(--surface);padding:10px;border-radius:2px"><strong>Delivery Acceptance Check</strong><br><span style="color:var(--muted)">Per delivery · Temp + visual</span></div>
                <div style="background:var(--surface);padding:10px;border-radius:2px"><strong>Date Labelling Audit</strong><br><span style="color:var(--muted)">Daily · All stored food</span></div>
                <div style="background:var(--surface);padding:10px;border-radius:2px"><strong>Allergen Record</strong><br><span style="color:var(--muted)">Per menu change · 14 allergens</span></div>
                <div style="background:var(--surface);padding:10px;border-radius:2px"><strong>Cooking Temp Probe Log</strong><br><span style="color:var(--muted)">Per batch · ≥75°C core temp</span></div>
                <div style="background:var(--surface);padding:10px;border-radius:2px"><strong>Plate & Cutlery Sanitisation</strong><br><span style="color:var(--muted)">Per service · Eat-off-the-floor standard</span></div>
                <div style="background:var(--surface);padding:10px;border-radius:2px"><strong>Hand Hygiene Compliance</strong><br><span style="color:var(--muted)">Supervisor check · Every shift</span></div>
              </div>
            </div>
            <div id="ic-food-table"></div>
          </div>

          <!-- IC REPORTS SECTION -->
          <div id="ic-sec-reports" style="display:none">
            <div id="ic-reports-list"></div>
          </div>

        </div>
      </div>

        <!-- ── TAB: RISK & TRAINING ── -->
        <div id="ctab-risktraining" style="display:none">

          <div style="display:flex;gap:8px;flex-wrap:wrap;margin-bottom:20px;align-items:center">
            <select id="rt-client-sel" style="padding:8px 12px;border:1px solid var(--border);border-radius:2px;font-family:Arial;font-size:13px;color:var(--ink);min-width:200px" onchange="loadRTData()">
              <option value="">Select client / location…</option>
            </select>
            <div style="display:flex;gap:0;border:1px solid var(--border);border-radius:2px;overflow:hidden;font-family:Arial;font-size:12px">
              <button class="rt-section-btn active" onclick="switchRTSection('risk',this)" style="padding:7px 14px;background:var(--ink);color:white;border:none;cursor:pointer;text-transform:uppercase;letter-spacing:.5px">Risk Assessments</button>
              <button class="rt-section-btn" onclick="switchRTSection('training',this)" style="padding:7px 14px;background:var(--white);color:var(--ink);border:none;cursor:pointer;text-transform:uppercase;letter-spacing:.5px;border-left:1px solid var(--border)">Training Records</button>
              <button class="rt-section-btn" onclick="switchRTSection('checks',this)" style="padding:7px 14px;background:var(--white);color:var(--ink);border:none;cursor:pointer;text-transform:uppercase;letter-spacing:.5px;border-left:1px solid var(--border)">Checks & Balances</button>
            </div>
            <button class="btn-add" id="rt-add-btn" onclick="rtAddAction()" style="margin-left:auto">+ Add Risk Assessment</button>
          </div>

          <div class="stats-row" id="rt-stats-row" style="margin-bottom:20px"></div>

          <!-- RISK ASSESSMENTS -->
          <div id="rt-sec-risk">
            <div style="background:var(--white);border:1px solid var(--border);border-radius:2px;padding:14px 18px;margin-bottom:14px;font-family:Arial;font-size:12px;color:var(--ink2);line-height:1.7">
              <strong style="color:var(--gold);font-size:11px;letter-spacing:1px;text-transform:uppercase">Risk Assessment Categories</strong><br>
              Fire &amp; Emergency · Manual Handling · Chemical/COSHH · Food Safety · Infection Control · Lone Working · Electrical Safety · Slip/Trip/Fall · Equipment Operation · Mental Health &amp; Wellbeing · Customer-Facing Risk · Data Security
            </div>
            <div id="rt-risk-table"></div>
          </div>

          <!-- TRAINING RECORDS -->
          <div id="rt-sec-training" style="display:none">
            <div style="background:var(--white);border:1px solid var(--border);border-radius:2px;padding:14px 18px;margin-bottom:14px;font-family:Arial;font-size:12px;color:var(--ink2);line-height:1.7">
              <strong style="color:var(--gold);font-size:11px;letter-spacing:1px;text-transform:uppercase">Training Types</strong><br>
              Food Hygiene (Level 1/2/3) · Infection Control · Fire Safety · Manual Handling · First Aid · Data Protection · Allergen Awareness · Customer Service · Safeguarding · Health &amp; Safety Induction · Equipment Operation · Brand &amp; SOP Training
            </div>
            <div id="rt-training-table"></div>
          </div>

          <!-- CHECKS & BALANCES -->
          <div id="rt-sec-checks" style="display:none">
            <div style="background:var(--white);border:1px solid var(--border);border-radius:2px;padding:14px 18px;margin-bottom:14px;font-family:Arial;font-size:12px;color:var(--ink2);line-height:1.7">
              <strong style="color:var(--gold);font-size:11px;letter-spacing:1px;text-transform:uppercase">Checks & Balances Framework</strong><br>
              Daily Opening Check · Daily Closing Check · Weekly Management Review · Monthly Operations Audit · Quarterly Compliance Review · Annual Full Audit · Cash Reconciliation · Stock Count · Equipment Inspection · SOP Adherence Check
            </div>
            <div id="rt-checks-table"></div>
          </div>

        </div>

      <!-- RISK & TRAINING MODALS -->
      <div class="modal-overlay" id="modal-rt-risk">
        <div class="modal" style="width:680px">
          <div class="modal-header"><div class="modal-title" id="rt-risk-modal-title">Add Risk Assessment</div><button class="modal-close" onclick="closeModal('modal-rt-risk')">×</button></div>
          <div class="modal-body">
            <input type="hidden" id="rt-risk-edit-id">
            <div class="form-grid">
              <div class="form-field"><label>Client / Location</label><select id="rt-r-client"><option value="">Select…</option></select></div>
              <div class="form-field"><label>Risk Category</label>
                <select id="rt-r-category">
                  <option>Fire & Emergency</option><option>Manual Handling</option><option>Chemical / COSHH</option>
                  <option>Food Safety</option><option>Infection Control</option><option>Lone Working</option>
                  <option>Electrical Safety</option><option>Slip / Trip / Fall</option><option>Equipment Operation</option>
                  <option>Mental Health & Wellbeing</option><option>Customer-Facing Risk</option><option>Data Security</option><option>Other</option>
                </select>
              </div>
              <div class="form-field" style="grid-column:1/-1"><label>Risk Description</label><textarea id="rt-r-desc" rows="2" placeholder="What is the hazard? Who might be harmed?"></textarea></div>
              <div class="form-field"><label>Likelihood (1–5)</label><input type="number" id="rt-r-likelihood" min="1" max="5" placeholder="1=rare, 5=certain"></div>
              <div class="form-field"><label>Severity (1–5)</label><input type="number" id="rt-r-severity" min="1" max="5" placeholder="1=minor, 5=fatal"></div>
              <div class="form-field" style="grid-column:1/-1"><label>Control Measures in Place</label><textarea id="rt-r-controls" rows="3" placeholder="What is already done to control the risk?"></textarea></div>
              <div class="form-field" style="grid-column:1/-1"><label>Further Action Required</label><textarea id="rt-r-action" rows="2" placeholder="What else needs to be done? By whom? By when?"></textarea></div>
              <div class="form-field"><label>Assessed By</label><input type="text" id="rt-r-by" placeholder="Name"></div>
              <div class="form-field"><label>Date Assessed</label><input type="date" id="rt-r-date"></div>
              <div class="form-field"><label>Review Date</label><input type="date" id="rt-r-review"></div>
              <div class="form-field"><label>Status</label>
                <select id="rt-r-status"><option>Open</option><option>Controls Applied</option><option>Closed / Accepted</option><option>Escalated</option></select>
              </div>
              <div class="form-field"><label>Visible to Client?</label>
                <select id="rt-r-visible"><option value="yes">Yes</option><option value="no">No — Internal only</option></select>
              </div>
            </div>
            <div style="margin-top:16px;display:flex;gap:8px">
              <button class="btn-save" onclick="saveRiskAssessment()">Save</button>
              <button class="btn-cancel" onclick="closeModal('modal-rt-risk')">Cancel</button>
            </div>
            <div id="rt-risk-feedback"></div>
          </div>
        </div>
      </div>

      <div class="modal-overlay" id="modal-rt-training">
        <div class="modal" style="width:640px">
          <div class="modal-header"><div class="modal-title">Log Training Record</div><button class="modal-close" onclick="closeModal('modal-rt-training')">×</button></div>
          <div class="modal-body">
            <input type="hidden" id="rt-tr-edit-id">
            <div class="form-grid">
              <div class="form-field"><label>Client / Location</label><select id="rt-t-client"><option value="">Select…</option></select></div>
              <div class="form-field"><label>Staff Member</label><input type="text" id="rt-t-staff" placeholder="Name of staff trained"></div>
              <div class="form-field"><label>Role</label><input type="text" id="rt-t-role" placeholder="e.g. Baker, Therapist"></div>
              <div class="form-field"><label>Training Type</label>
                <select id="rt-t-type">
                  <option>Food Hygiene Level 1</option><option>Food Hygiene Level 2</option><option>Food Hygiene Level 3</option>
                  <option>Infection Control</option><option>Fire Safety</option><option>Manual Handling</option>
                  <option>First Aid</option><option>Data Protection / GDPR</option><option>Allergen Awareness</option>
                  <option>Customer Service</option><option>Safeguarding</option><option>Health & Safety Induction</option>
                  <option>Equipment Operation</option><option>Brand & SOP Training</option><option>Other</option>
                </select>
              </div>
              <div class="form-field"><label>Date Completed</label><input type="date" id="rt-t-date"></div>
              <div class="form-field"><label>Next Due / Renewal</label><input type="date" id="rt-t-next"></div>
              <div class="form-field"><label>Trainer / Provider</label><input type="text" id="rt-t-provider" placeholder="e.g. In-house, Highfield, British Red Cross"></div>
              <div class="form-field"><label>Certificate Issued?</label>
                <select id="rt-t-cert"><option value="yes">Yes</option><option value="no">No</option><option value="pending">Pending</option></select>
              </div>
              <div class="form-field" style="grid-column:1/-1"><label>Notes</label><textarea id="rt-t-notes" rows="2" placeholder="Any notes, score, pass/fail details…"></textarea></div>
              <div class="form-field"><label>Visible to Client?</label>
                <select id="rt-t-visible"><option value="yes">Yes</option><option value="no">No — Internal only</option></select>
              </div>
            </div>
            <div style="margin-top:16px;display:flex;gap:8px">
              <button class="btn-save" onclick="saveTrainingRecord()">Save</button>
              <button class="btn-cancel" onclick="closeModal('modal-rt-training')">Cancel</button>
            </div>
            <div id="rt-training-feedback"></div>
          </div>
        </div>
      </div>

      <div class="modal-overlay" id="modal-rt-check">
        <div class="modal" style="width:640px">
          <div class="modal-header"><div class="modal-title">Log Check & Balance</div><button class="modal-close" onclick="closeModal('modal-rt-check')">×</button></div>
          <div class="modal-body">
            <input type="hidden" id="rt-ch-edit-id">
            <div class="form-grid">
              <div class="form-field"><label>Client / Location</label><select id="rt-c-client"><option value="">Select…</option></select></div>
              <div class="form-field"><label>Check Type</label>
                <select id="rt-c-type">
                  <option>Daily Opening Check</option><option>Daily Closing Check</option><option>Weekly Management Review</option>
                  <option>Monthly Operations Audit</option><option>Quarterly Compliance Review</option><option>Annual Full Audit</option>
                  <option>Cash Reconciliation</option><option>Stock Count</option><option>Equipment Inspection</option>
                  <option>SOP Adherence Check</option><option>Other</option>
                </select>
              </div>
              <div class="form-field"><label>Date</label><input type="date" id="rt-c-date"></div>
              <div class="form-field"><label>Conducted By</label><input type="text" id="rt-c-by" placeholder="Name"></div>
              <div class="form-field"><label>Overall Result</label>
                <select id="rt-c-result"><option>Pass</option><option>Pass with Issues</option><option>Fail — Action Required</option></select>
              </div>
              <div class="form-field"><label>Score (optional, /10)</label><input type="number" id="rt-c-score" min="0" max="10"></div>
              <div class="form-field" style="grid-column:1/-1"><label>Findings</label><textarea id="rt-c-findings" rows="3" placeholder="What was checked and what was found…"></textarea></div>
              <div class="form-field" style="grid-column:1/-1"><label>Actions Required</label><textarea id="rt-c-actions" rows="2" placeholder="What needs fixing, by whom, by when…"></textarea></div>
              <div class="form-field"><label>Next Check Due</label><input type="date" id="rt-c-next"></div>
              <div class="form-field"><label>Visible to Client?</label>
                <select id="rt-c-visible"><option value="yes">Yes</option><option value="no">No — Internal only</option></select>
              </div>
            </div>
            <div style="margin-top:16px;display:flex;gap:8px">
              <button class="btn-save" onclick="saveCheckRecord()">Save</button>
              <button class="btn-cancel" onclick="closeModal('modal-rt-check')">Cancel</button>
            </div>
            <div id="rt-check-feedback"></div>
          </div>
        </div>
      </div>

      <!-- Log Staff Test -->
      <div class="modal-overlay" id="modal-ic-staff">
        <div class="modal" style="width:640px">
          <div class="modal-header">
            <div class="modal-title">Log Staff Health Test</div>
            <button class="modal-close" onclick="closeModal('modal-ic-staff')">×</button>
          </div>
          <div class="modal-body">
            <input type="hidden" id="ic-staff-edit-id">
            <div class="form-grid">
              <div class="form-field"><label>Client / Location</label><select id="ic-s-client"><option value="">Select…</option></select></div>
              <div class="form-field"><label>Staff Name</label><input type="text" id="ic-s-name" placeholder="Full name"></div>
              <div class="form-field"><label>Role</label><input type="text" id="ic-s-role" placeholder="e.g. Baker, Therapist, Waitstaff"></div>
              <div class="form-field"><label>Test Type</label>
                <select id="ic-s-test">
                  <option>Hepatitis B & C</option>
                  <option>HIV (ELISA/Rapid)</option>
                  <option>Tuberculosis (TB)</option>
                  <option>Typhoid / Stool Culture</option>
                  <option>MRSA Screening</option>
                  <option>Skin / Scalp Infection Screen</option>
                  <option>Full Blood Count (FBC)</option>
                  <option>Covid / Respiratory Screen</option>
                  <option>Other</option>
                </select>
              </div>
              <div class="form-field"><label>Date Tested</label><input type="date" id="ic-s-date"></div>
              <div class="form-field"><label>Next Due Date</label><input type="date" id="ic-s-next"></div>
              <div class="form-field"><label>Result</label>
                <select id="ic-s-result">
                  <option value="Clear">✓ Clear</option>
                  <option value="Flagged">⚠ Flagged — Follow Up Required</option>
                  <option value="Pending">⏳ Pending Lab Result</option>
                  <option value="Exempt">Exempt / Not Applicable</option>
                </select>
              </div>
              <div class="form-field"><label>Lab / Tested By</label><input type="text" id="ic-s-lab" placeholder="e.g. Maitama General Hospital, Wuse Lab"></div>
              <div class="form-field" style="grid-column:1/-1"><label>Notes / Action Required</label><textarea id="ic-s-notes" rows="2" placeholder="Any follow-up action, restrictions, or notes…"></textarea></div>
              <div class="form-field" style="grid-column:1/-1">
                <label>Visible to Client?</label>
                <select id="ic-s-visible">
                  <option value="yes">Yes — show in client report</option>
                  <option value="no">No — internal only</option>
                </select>
              </div>
            </div>
            <div style="margin-top:16px;display:flex;gap:8px">
              <button class="btn-save" onclick="saveICStaffTest()">Save Test Record</button>
              <button class="btn-cancel" onclick="closeModal('modal-ic-staff')">Cancel</button>
            </div>
            <div id="ic-staff-feedback"></div>
          </div>
        </div>
      </div>

      <!-- Log Facility / Food Check -->
      <div class="modal-overlay" id="modal-ic-facility">
        <div class="modal" style="width:640px">
          <div class="modal-header">
            <div class="modal-title" id="ic-facility-modal-title">Log Facility / Food Safety Check</div>
            <button class="modal-close" onclick="closeModal('modal-ic-facility')">×</button>
          </div>
          <div class="modal-body">
            <input type="hidden" id="ic-fac-edit-id">
            <input type="hidden" id="ic-fac-type">
            <div class="form-grid">
              <div class="form-field"><label>Client / Location</label><select id="ic-f-client"><option value="">Select…</option></select></div>
              <div class="form-field"><label>Check Type</label>
                <select id="ic-f-check">
                  <option>Surface Disinfection Log</option>
                  <option>Kitchen / Prep Area Deep Clean</option>
                  <option>Equipment Sterilisation Record</option>
                  <option>Pest Control Inspection</option>
                  <option>Waste & Sharps Disposal Log</option>
                  <option>Water Quality Test</option>
                  <option>PPE Stock Check</option>
                  <option>Toilet / Handwashing Audit</option>
                  <option>Cross-contamination Risk Review</option>
                  <option>Tableware & Utensil Sanitisation</option>
                  <option>Temp Log — Cold Storage</option>
                  <option>Temp Log — Hot Holding</option>
                  <option>Delivery Acceptance Check</option>
                  <option>Date Labelling Audit</option>
                  <option>Allergen Record</option>
                  <option>Cooking Temp Probe Log</option>
                  <option>Plate & Cutlery Sanitisation</option>
                  <option>Hand Hygiene Compliance</option>
                  <option>Other</option>
                </select>
              </div>
              <div class="form-field"><label>Date of Check</label><input type="date" id="ic-f-date"></div>
              <div class="form-field"><label>Checked By</label><input type="text" id="ic-f-by" placeholder="Name of inspector / supervisor"></div>
              <div class="form-field"><label>Result / Status</label>
                <select id="ic-f-result">
                  <option value="Pass">✓ Pass</option>
                  <option value="Pass with Notes">Pass with Notes</option>
                  <option value="Fail — Action Required">✗ Fail — Action Required</option>
                  <option value="Pending">Pending</option>
                </select>
              </div>
              <div class="form-field"><label>Score (optional, /10)</label><input type="number" id="ic-f-score" min="0" max="10" placeholder="e.g. 9"></div>
              <div class="form-field" style="grid-column:1/-1"><label>Detailed Findings</label><textarea id="ic-f-findings" rows="3" placeholder="What was checked, what was found, any temperature readings, measurements…"></textarea></div>
              <div class="form-field" style="grid-column:1/-1"><label>Action Required (if any)</label><textarea id="ic-f-action" rows="2" placeholder="What needs to be corrected and by when…"></textarea></div>
              <div class="form-field"><label>Next Check Due</label><input type="date" id="ic-f-next"></div>
              <div class="form-field"><label>Visible to Client?</label>
                <select id="ic-f-visible">
                  <option value="yes">Yes — show in client report</option>
                  <option value="no">No — internal only</option>
                </select>
              </div>
            </div>
            <div style="margin-top:16px;display:flex;gap:8px">
              <button class="btn-save" onclick="saveICFacilityCheck()">Save Check</button>
              <button class="btn-cancel" onclick="closeModal('modal-ic-facility')">Cancel</button>
            </div>
            <div id="ic-facility-feedback"></div>
          </div>
        </div>
      </div>

      <!-- COMPLIANCE MODALS -->
      <!-- Add/Edit Compliance Body -->
      <div class="modal-overlay" id="modal-add-compliance-body">
        <div class="modal" style="width:720px">
          <div class="modal-header">
            <div class="modal-title" id="comp-modal-title">Add Regulatory Body</div>
            <button class="modal-close" onclick="closeModal('modal-add-compliance-body')">×</button>
          </div>
          <div class="modal-body">
            <input type="hidden" id="comp-edit-id">
            <div class="form-grid">
              <div class="form-field"><label>Country / Jurisdiction</label>
                <select id="comp-country-field">
                  <option value="Nigeria">🇳🇬 Nigeria</option>
                  <option value="United Kingdom">🇬🇧 United Kingdom</option>
                  <option value="United States">🇺🇸 United States</option>
                  <option value="UAE / Dubai">🇦🇪 UAE / Dubai</option>
                  <option value="Saudi Arabia">🇸🇦 Saudi Arabia</option>
                  <option value="Other">Other</option>
                </select>
              </div>
              <div class="form-field"><label>Tier / Category</label>
                <input type="text" id="comp-tier-field" placeholder="e.g. Federal — Taxation, FCT / Abuja — Local Govt">
              </div>
              <div class="form-field" style="grid-column:1/-1"><label>Full Body Name</label>
                <input type="text" id="comp-name-field" placeholder="e.g. Federal Inland Revenue Service">
              </div>
              <div class="form-field"><label>Abbreviation</label>
                <input type="text" id="comp-abbr-field" placeholder="e.g. FIRS / VAT">
              </div>
              <div class="form-field"><label>Applicable Niches (comma-separated)</label>
                <input type="text" id="comp-niches-field" placeholder="e.g. all-biz, food, hospitality, employer, data, fct">
              </div>
              <div class="form-field" style="grid-column:1/-1"><label>What It Does / Why It Matters</label>
                <textarea id="comp-what-field" rows="2" placeholder="Brief description of the body and what compliance means…"></textarea>
              </div>
              <div class="form-field"><label>Estimated Cost</label>
                <input type="text" id="comp-cost-field" placeholder="e.g. ₦20,000–₦100,000">
              </div>
              <div class="form-field"><label>Frequency</label>
                <input type="text" id="comp-freq-field" placeholder="e.g. Annual, Monthly, One-time">
              </div>
              <div class="form-field" style="grid-column:1/-1"><label>Deadline / When to File</label>
                <input type="text" id="comp-deadline-field" placeholder="e.g. Within 18 months of incorporation, then annually by June 30">
              </div>
              <div class="form-field" style="grid-column:1/-1"><label>Penalty for Non-Compliance</label>
                <textarea id="comp-penalty-field" rows="2" placeholder="Fines, closure, imprisonment…"></textarea>
              </div>
              <div class="form-field" style="grid-column:1/-1"><label>Documents Required (one per line)</label>
                <textarea id="comp-docs-field" rows="4" placeholder="CAC Certificate&#10;TIN&#10;Director's ID"></textarea>
              </div>
              <div class="form-field" style="grid-column:1/-1"><label>How to Renew / Update</label>
                <textarea id="comp-renewal-field" rows="2" placeholder="Annual. File by June 30. Update if directors change…"></textarea>
              </div>
              <div class="form-field" style="grid-column:1/-1"><label>Contact / Website</label>
                <input type="text" id="comp-contact-field" placeholder="e.g. firs.gov.ng | FIRS HQ, Abuja">
              </div>
            </div>
            <div style="margin-top:16px;display:flex;gap:8px">
              <button class="btn-save" onclick="saveComplianceBody()">Save Body</button>
              <button class="btn-cancel" onclick="closeModal('modal-add-compliance-body')">Cancel</button>
            </div>
            <div id="comp-body-feedback"></div>
          </div>
        </div>
      </div>

      <!-- Update Status / Notes per client modal -->
      <div class="modal-overlay" id="modal-comp-status">
        <div class="modal" style="width:560px">
          <div class="modal-header">
            <div class="modal-title" id="comp-status-modal-title">Update Compliance Status</div>
            <button class="modal-close" onclick="closeModal('modal-comp-status')">×</button>
          </div>
          <div class="modal-body">
            <input type="hidden" id="comp-status-body-id">
            <input type="hidden" id="comp-status-client-id">
            <div class="form-grid">
              <div class="form-field"><label>Status</label>
                <select id="comp-status-val">
                  <option value="Not Started">Not Started</option>
                  <option value="In Progress">In Progress</option>
                  <option value="Done">Done</option>
                  <option value="N/A">N/A (Contract Staff / Not Applicable)</option>
                </select>
              </div>
              <div class="form-field"><label>Renewal / Next Due Date</label>
                <input type="date" id="comp-status-due">
              </div>
              <div class="form-field" style="grid-column:1/-1"><label>Contact Name / Agent</label>
                <input type="text" id="comp-status-contact" placeholder="e.g. Musa at AMAC — 0812 000 0000">
              </div>
              <div class="form-field" style="grid-column:1/-1"><label>Notes / Reference Number</label>
                <textarea id="comp-status-notes" rows="3" placeholder="Certificate number, file reference, date submitted…"></textarea>
              </div>
            </div>
            <div style="margin-top:16px;display:flex;gap:8px">
              <button class="btn-save" onclick="saveComplianceStatus()">Save</button>
              <button class="btn-cancel" onclick="closeModal('modal-comp-status')">Cancel</button>
            </div>
            <div id="comp-status-feedback"></div>
          </div>
        </div>
      </div>

      <div class="page" id="page-compliance">
        <div class="page-title">Compliance & Infection Control</div>
        <div class="page-sub">Regulatory bodies by jurisdiction · Staff health testing · Facility hygiene · Client-visible reports</div>

        <!-- Inner tabs -->
        <div class="inner-tabs" style="margin-bottom:20px">
          <button class="inner-tab active" id="ctab-btn-bodies" onclick="switchCompTab('bodies',this)">🗂 Regulatory Bodies</button>
          <button class="inner-tab" id="ctab-btn-infection" onclick="switchCompTab('infection',this)">🧪 Infection Control</button>
          <button class="inner-tab" id="ctab-btn-risktraining" onclick="switchCompTab('risktraining',this)">📋 Risk & Training</button>
        </div>

        <!-- ── TAB: REGULATORY BODIES — MASTER LIBRARY ── -->
        <div id="ctab-bodies">
          <div style="background:var(--surface);border:1px solid var(--border);border-radius:2px;padding:10px 16px;margin-bottom:16px;font-family:Arial;font-size:12px;color:var(--muted)">
            📚 <strong style="color:var(--ink)">Master Library</strong> — Reference only. Edit body details here. To assign bodies to a client and track compliance, go to the client's profile → Compliance tab.
          </div>
          <div style="display:flex;gap:10px;flex-wrap:wrap;align-items:center;margin-bottom:18px">
            <input type="text" id="comp-search" placeholder="Search body, niche, keyword…" oninput="renderComplianceBodies()" style="padding:8px 12px;border:1px solid var(--border);border-radius:2px;font-family:Arial;font-size:13px;min-width:200px;outline:none;color:var(--ink)">
            <select id="comp-country" onchange="renderComplianceBodies()" style="padding:8px 12px;border:1px solid var(--border);border-radius:2px;font-family:Arial;font-size:13px;color:var(--ink)">
              <option value="all">All Jurisdictions</option>
              <option value="Nigeria">🇳🇬 Nigeria</option>
              <option value="United Kingdom">🇬🇧 United Kingdom</option>
              <option value="UAE / Dubai">🇦🇪 UAE / Dubai</option>
              <option value="Saudi Arabia">🇸🇦 Saudi Arabia</option>
            </select>
            <select id="comp-niche" onchange="renderComplianceBodies()" style="padding:8px 12px;border:1px solid var(--border);border-radius:2px;font-family:Arial;font-size:13px;color:var(--ink)">
              <option value="all">All Niches</option>
              <option value="all-biz">All Businesses</option>
              <option value="food">Food & Beverage</option>
              <option value="hospitality">Hospitality / Café</option>
              <option value="employer">Employer / Staff</option>
              <option value="data">Data / Digital</option>
              <option value="fct">FCT / Abuja Specific</option>
              <option value="aesthetics">Aesthetics / Wellness</option>
              <option value="retail">Retail</option>
              <option value="education">Education</option>
            </select>
            <button class="btn-add" onclick="openModal('modal-add-compliance-body')" style="margin-left:auto">+ Add Body</button>
          </div>
          <div id="comp-stats-row" style="margin-bottom:16px;font-family:Arial;font-size:12px;color:var(--muted)"></div>
          <div id="comp-body-list"></div>
        </div>

        <!-- ── TAB: INFECTION CONTROL ── -->
        <div id="ctab-infection" style="display:none">

          <!-- IC inner nav -->
          <div style="display:flex;gap:8px;flex-wrap:wrap;margin-bottom:20px;align-items:center">
            <select id="ic-client-sel" style="padding:8px 12px;border:1px solid var(--border);border-radius:2px;font-family:Arial;font-size:13px;color:var(--ink);min-width:200px">
              <option value="">Select client / location…</option>
            </select>
            <div style="display:flex;gap:0;border:1px solid var(--border);border-radius:2px;overflow:hidden;font-family:Arial;font-size:12px">
              <button class="ic-section-btn active" data-sec="staff" onclick="switchICSection('staff',this)" style="padding:7px 14px;background:var(--ink);color:white;border:none;cursor:pointer;text-transform:uppercase;letter-spacing:.5px">Staff Testing</button>
              <button class="ic-section-btn" data-sec="facility" onclick="switchICSection('facility',this)" style="padding:7px 14px;background:var(--white);color:var(--ink);border:none;cursor:pointer;text-transform:uppercase;letter-spacing:.5px;border-left:1px solid var(--border)">Facility Hygiene</button>
              <button class="ic-section-btn" data-sec="food" onclick="switchICSection('food',this)" style="padding:7px 14px;background:var(--white);color:var(--ink);border:none;cursor:pointer;text-transform:uppercase;letter-spacing:.5px;border-left:1px solid var(--border)">Food Safety</button>
              <button class="ic-section-btn" data-sec="reports" onclick="switchICSection('reports',this)" style="padding:7px 14px;background:var(--white);color:var(--ink);border:none;cursor:pointer;text-transform:uppercase;letter-spacing:.5px;border-left:1px solid var(--border)">IC Reports</button>
            </div>
            <button class="btn-add" id="ic-add-btn" onclick="icAddAction()" style="margin-left:auto">+ Log Entry</button>
          </div>

          <div class="stats-row" id="ic-stats-row" style="margin-bottom:20px"></div>

          <!-- STAFF TESTING SECTION -->
          <div id="ic-sec-staff">
            <div style="background:var(--white);border:1px solid var(--border);border-radius:2px;padding:16px 20px;margin-bottom:16px">
              <div style="font-family:Arial;font-size:11px;letter-spacing:1.5px;text-transform:uppercase;color:var(--gold);margin-bottom:10px">Required Tests Per Staff Member</div>
              <div style="display:grid;grid-template-columns:repeat(auto-fill,minmax(200px,1fr));gap:10px;font-family:Arial;font-size:12px">
                <div style="background:var(--surface);padding:10px;border-radius:2px"><strong>Hepatitis B & C</strong><br><span style="color:var(--muted)">Annual · All niches</span></div>
                <div style="background:var(--surface);padding:10px;border-radius:2px"><strong>HIV (ELISA/Rapid)</strong><br><span style="color:var(--muted)">Annual · Aesthetics, clinical, food</span></div>
                <div style="background:var(--surface);padding:10px;border-radius:2px"><strong>Tuberculosis (TB)</strong><br><span style="color:var(--muted)">Annual · All niches</span></div>
                <div style="background:var(--surface);padding:10px;border-radius:2px"><strong>Typhoid / Stool Culture</strong><br><span style="color:var(--muted)">6-monthly · Food, hospitality</span></div>
                <div style="background:var(--surface);padding:10px;border-radius:2px"><strong>MRSA Screening</strong><br><span style="color:var(--muted)">Annual · Aesthetics, clinical</span></div>
                <div style="background:var(--surface);padding:10px;border-radius:2px"><strong>Skin / Scalp Infection Screen</strong><br><span style="color:var(--muted)">Annual · Hair, beauty, aesthetics</span></div>
                <div style="background:var(--surface);padding:10px;border-radius:2px"><strong>Full Blood Count (FBC)</strong><br><span style="color:var(--muted)">Annual · All niches</span></div>
                <div style="background:var(--surface);padding:10px;border-radius:2px"><strong>Covid / Respiratory Screen</strong><br><span style="color:var(--muted)">As needed · All</span></div>
              </div>
            </div>
            <div style="display:flex;justify-content:flex-end;margin-bottom:10px">
              <span style="font-family:Arial;font-size:11px;color:var(--muted);margin-right:12px;align-self:center">Filter:</span>
              <select id="ic-staff-filter" onchange="renderICStaffTable()" style="padding:6px 10px;border:1px solid var(--border);border-radius:2px;font-family:Arial;font-size:12px;color:var(--ink)">
                <option value="all">All Staff</option>
                <option value="Overdue">Overdue</option>
                <option value="Due Soon">Due Soon</option>
                <option value="Clear">Clear</option>
                <option value="Flagged">Flagged</option>
              </select>
            </div>
            <div id="ic-staff-table"></div>
          </div>

          <!-- FACILITY HYGIENE SECTION -->
          <div id="ic-sec-facility" style="display:none">
            <div style="background:var(--white);border:1px solid var(--border);border-radius:2px;padding:16px 20px;margin-bottom:16px">
              <div style="font-family:Arial;font-size:11px;letter-spacing:1.5px;text-transform:uppercase;color:var(--gold);margin-bottom:10px">Hygiene Checklist Framework</div>
              <div style="display:grid;grid-template-columns:repeat(auto-fill,minmax(220px,1fr));gap:10px;font-family:Arial;font-size:12px">
                <div style="background:var(--surface);padding:10px;border-radius:2px"><strong>Surface Disinfection Log</strong><br><span style="color:var(--muted)">Daily · All premises</span></div>
                <div style="background:var(--surface);padding:10px;border-radius:2px"><strong>Kitchen / Prep Area Deep Clean</strong><br><span style="color:var(--muted)">Weekly · Food & hospitality</span></div>
                <div style="background:var(--surface);padding:10px;border-radius:2px"><strong>Equipment Sterilisation Record</strong><br><span style="color:var(--muted)">Per use · Aesthetics, clinical, hair</span></div>
                <div style="background:var(--surface);padding:10px;border-radius:2px"><strong>Pest Control Inspection</strong><br><span style="color:var(--muted)">Monthly / Quarterly · All</span></div>
                <div style="background:var(--surface);padding:10px;border-radius:2px"><strong>Waste & Sharps Disposal Log</strong><br><span style="color:var(--muted)">Per use / Daily · Aesthetics, clinical</span></div>
                <div style="background:var(--surface);padding:10px;border-radius:2px"><strong>Water Quality Test</strong><br><span style="color:var(--muted)">6-monthly · Food, school, wellness</span></div>
                <div style="background:var(--surface);padding:10px;border-radius:2px"><strong>PPE Stock Check</strong><br><span style="color:var(--muted)">Weekly · All client-facing staff</span></div>
                <div style="background:var(--surface);padding:10px;border-radius:2px"><strong>Toilet / Handwashing Audit</strong><br><span style="color:var(--muted)">Daily · All public-facing premises</span></div>
                <div style="background:var(--surface);padding:10px;border-radius:2px"><strong>Cross-contamination Risk Review</strong><br><span style="color:var(--muted)">Monthly · Food, aesthetics</span></div>
                <div style="background:var(--surface);padding:10px;border-radius:2px"><strong>Tableware & Utensil Sanitisation</strong><br><span style="color:var(--muted)">Per use + daily · Food, hospitality</span></div>
              </div>
            </div>
            <div id="ic-facility-table"></div>
          </div>

          <!-- FOOD SAFETY SECTION -->
          <div id="ic-sec-food" style="display:none">
            <div style="background:var(--white);border:1px solid var(--border);border-radius:2px;padding:16px 20px;margin-bottom:16px">
              <div style="font-family:Arial;font-size:11px;letter-spacing:1.5px;text-transform:uppercase;color:var(--gold);margin-bottom:10px">Food Safety Standards — Eat Off The Floor Level</div>
              <div style="display:grid;grid-template-columns:repeat(auto-fill,minmax(220px,1fr));gap:10px;font-family:Arial;font-size:12px">
                <div style="background:var(--surface);padding:10px;border-radius:2px"><strong>Temp Log — Cold Storage</strong><br><span style="color:var(--muted)">Twice daily · ≤4°C</span></div>
                <div style="background:var(--surface);padding:10px;border-radius:2px"><strong>Temp Log — Hot Holding</strong><br><span style="color:var(--muted)">Every 2hrs · ≥63°C</span></div>
                <div style="background:var(--surface);padding:10px;border-radius:2px"><strong>Delivery Acceptance Check</strong><br><span style="color:var(--muted)">Per delivery · Temp + visual</span></div>
                <div style="background:var(--surface);padding:10px;border-radius:2px"><strong>Date Labelling Audit</strong><br><span style="color:var(--muted)">Daily · All stored food</span></div>
                <div style="background:var(--surface);padding:10px;border-radius:2px"><strong>Allergen Record</strong><br><span style="color:var(--muted)">Per menu change · 14 allergens</span></div>
                <div style="background:var(--surface);padding:10px;border-radius:2px"><strong>Cooking Temp Probe Log</strong><br><span style="color:var(--muted)">Per batch · ≥75°C core temp</span></div>
                <div style="background:var(--surface);padding:10px;border-radius:2px"><strong>Plate & Cutlery Sanitisation</strong><br><span style="color:var(--muted)">Per service · Eat-off-the-floor standard</span></div>
                <div style="background:var(--surface);padding:10px;border-radius:2px"><strong>Hand Hygiene Compliance</strong><br><span style="color:var(--muted)">Supervisor check · Every shift</span></div>
              </div>
            </div>
            <div id="ic-food-table"></div>
          </div>

          <!-- IC REPORTS SECTION -->
          <div id="ic-sec-reports" style="display:none">
            <div id="ic-reports-list"></div>
          </div>

        </div>
      </div>

        <!-- ── TAB: RISK & TRAINING ── -->
        <div id="ctab-risktraining" style="display:none">

          <div style="display:flex;gap:8px;flex-wrap:wrap;margin-bottom:20px;align-items:center">
            <select id="rt-client-sel" style="padding:8px 12px;border:1px solid var(--border);border-radius:2px;font-family:Arial;font-size:13px;color:var(--ink);min-width:200px" onchange="loadRTData()">
              <option value="">Select client / location…</option>
            </select>
            <div style="display:flex;gap:0;border:1px solid var(--border);border-radius:2px;overflow:hidden;font-family:Arial;font-size:12px">
              <button class="rt-section-btn active" onclick="switchRTSection('risk',this)" style="padding:7px 14px;background:var(--ink);color:white;border:none;cursor:pointer;text-transform:uppercase;letter-spacing:.5px">Risk Assessments</button>
              <button class="rt-section-btn" onclick="switchRTSection('training',this)" style="padding:7px 14px;background:var(--white);color:var(--ink);border:none;cursor:pointer;text-transform:uppercase;letter-spacing:.5px;border-left:1px solid var(--border)">Training Records</button>
              <button class="rt-section-btn" onclick="switchRTSection('checks',this)" style="padding:7px 14px;background:var(--white);color:var(--ink);border:none;cursor:pointer;text-transform:uppercase;letter-spacing:.5px;border-left:1px solid var(--border)">Checks & Balances</button>
            </div>
            <button class="btn-add" id="rt-add-btn" onclick="rtAddAction()" style="margin-left:auto">+ Add Risk Assessment</button>
          </div>

          <div class="stats-row" id="rt-stats-row" style="margin-bottom:20px"></div>

          <!-- RISK ASSESSMENTS -->
          <div id="rt-sec-risk">
            <div style="background:var(--white);border:1px solid var(--border);border-radius:2px;padding:14px 18px;margin-bottom:14px;font-family:Arial;font-size:12px;color:var(--ink2);line-height:1.7">
              <strong style="color:var(--gold);font-size:11px;letter-spacing:1px;text-transform:uppercase">Risk Assessment Categories</strong><br>
              Fire &amp; Emergency · Manual Handling · Chemical/COSHH · Food Safety · Infection Control · Lone Working · Electrical Safety · Slip/Trip/Fall · Equipment Operation · Mental Health &amp; Wellbeing · Customer-Facing Risk · Data Security
            </div>
            <div id="rt-risk-table"></div>
          </div>

          <!-- TRAINING RECORDS -->
          <div id="rt-sec-training" style="display:none">
            <div style="background:var(--white);border:1px solid var(--border);border-radius:2px;padding:14px 18px;margin-bottom:14px;font-family:Arial;font-size:12px;color:var(--ink2);line-height:1.7">
              <strong style="color:var(--gold);font-size:11px;letter-spacing:1px;text-transform:uppercase">Training Types</strong><br>
              Food Hygiene (Level 1/2/3) · Infection Control · Fire Safety · Manual Handling · First Aid · Data Protection · Allergen Awareness · Customer Service · Safeguarding · Health &amp; Safety Induction · Equipment Operation · Brand &amp; SOP Training
            </div>
            <div id="rt-training-table"></div>
          </div>

          <!-- CHECKS & BALANCES -->
          <div id="rt-sec-checks" style="display:none">
            <div style="background:var(--white);border:1px solid var(--border);border-radius:2px;padding:14px 18px;margin-bottom:14px;font-family:Arial;font-size:12px;color:var(--ink2);line-height:1.7">
              <strong style="color:var(--gold);font-size:11px;letter-spacing:1px;text-transform:uppercase">Checks & Balances Framework</strong><br>
              Daily Opening Check · Daily Closing Check · Weekly Management Review · Monthly Operations Audit · Quarterly Compliance Review · Annual Full Audit · Cash Reconciliation · Stock Count · Equipment Inspection · SOP Adherence Check
            </div>
            <div id="rt-checks-table"></div>
          </div>

        </div>

      <!-- RISK & TRAINING MODALS -->
      <div class="modal-overlay" id="modal-rt-risk">
        <div class="modal" style="width:680px">
          <div class="modal-header"><div class="modal-title" id="rt-risk-modal-title">Add Risk Assessment</div><button class="modal-close" onclick="closeModal('modal-rt-risk')">×</button></div>
          <div class="modal-body">
            <input type="hidden" id="rt-risk-edit-id">
            <div class="form-grid">
              <div class="form-field"><label>Client / Location</label><select id="rt-r-client"><option value="">Select…</option></select></div>
              <div class="form-field"><label>Risk Category</label>
                <select id="rt-r-category">
                  <option>Fire & Emergency</option><option>Manual Handling</option><option>Chemical / COSHH</option>
                  <option>Food Safety</option><option>Infection Control</option><option>Lone Working</option>
                  <option>Electrical Safety</option><option>Slip / Trip / Fall</option><option>Equipment Operation</option>
                  <option>Mental Health & Wellbeing</option><option>Customer-Facing Risk</option><option>Data Security</option><option>Other</option>
                </select>
              </div>
              <div class="form-field" style="grid-column:1/-1"><label>Risk Description</label><textarea id="rt-r-desc" rows="2" placeholder="What is the hazard? Who might be harmed?"></textarea></div>
              <div class="form-field"><label>Likelihood (1–5)</label><input type="number" id="rt-r-likelihood" min="1" max="5" placeholder="1=rare, 5=certain"></div>
              <div class="form-field"><label>Severity (1–5)</label><input type="number" id="rt-r-severity" min="1" max="5" placeholder="1=minor, 5=fatal"></div>
              <div class="form-field" style="grid-column:1/-1"><label>Control Measures in Place</label><textarea id="rt-r-controls" rows="3" placeholder="What is already done to control the risk?"></textarea></div>
              <div class="form-field" style="grid-column:1/-1"><label>Further Action Required</label><textarea id="rt-r-action" rows="2" placeholder="What else needs to be done? By whom? By when?"></textarea></div>
              <div class="form-field"><label>Assessed By</label><input type="text" id="rt-r-by" placeholder="Name"></div>
              <div class="form-field"><label>Date Assessed</label><input type="date" id="rt-r-date"></div>
              <div class="form-field"><label>Review Date</label><input type="date" id="rt-r-review"></div>
              <div class="form-field"><label>Status</label>
                <select id="rt-r-status"><option>Open</option><option>Controls Applied</option><option>Closed / Accepted</option><option>Escalated</option></select>
              </div>
              <div class="form-field"><label>Visible to Client?</label>
                <select id="rt-r-visible"><option value="yes">Yes</option><option value="no">No — Internal only</option></select>
              </div>
            </div>
            <div style="margin-top:16px;display:flex;gap:8px">
              <button class="btn-save" onclick="saveRiskAssessment()">Save</button>
              <button class="btn-cancel" onclick="closeModal('modal-rt-risk')">Cancel</button>
            </div>
            <div id="rt-risk-feedback"></div>
          </div>
        </div>
      </div>

      <div class="modal-overlay" id="modal-rt-training">
        <div class="modal" style="width:640px">
          <div class="modal-header"><div class="modal-title">Log Training Record</div><button class="modal-close" onclick="closeModal('modal-rt-training')">×</button></div>
          <div class="modal-body">
            <input type="hidden" id="rt-tr-edit-id">
            <div class="form-grid">
              <div class="form-field"><label>Client / Location</label><select id="rt-t-client"><option value="">Select…</option></select></div>
              <div class="form-field"><label>Staff Member</label><input type="text" id="rt-t-staff" placeholder="Name of staff trained"></div>
              <div class="form-field"><label>Role</label><input type="text" id="rt-t-role" placeholder="e.g. Baker, Therapist"></div>
              <div class="form-field"><label>Training Type</label>
                <select id="rt-t-type">
                  <option>Food Hygiene Level 1</option><option>Food Hygiene Level 2</option><option>Food Hygiene Level 3</option>
                  <option>Infection Control</option><option>Fire Safety</option><option>Manual Handling</option>
                  <option>First Aid</option><option>Data Protection / GDPR</option><option>Allergen Awareness</option>
                  <option>Customer Service</option><option>Safeguarding</option><option>Health & Safety Induction</option>
                  <option>Equipment Operation</option><option>Brand & SOP Training</option><option>Other</option>
                </select>
              </div>
              <div class="form-field"><label>Date Completed</label><input type="date" id="rt-t-date"></div>
              <div class="form-field"><label>Next Due / Renewal</label><input type="date" id="rt-t-next"></div>
              <div class="form-field"><label>Trainer / Provider</label><input type="text" id="rt-t-provider" placeholder="e.g. In-house, Highfield, British Red Cross"></div>
              <div class="form-field"><label>Certificate Issued?</label>
                <select id="rt-t-cert"><option value="yes">Yes</option><option value="no">No</option><option value="pending">Pending</option></select>
              </div>
              <div class="form-field" style="grid-column:1/-1"><label>Notes</label><textarea id="rt-t-notes" rows="2" placeholder="Any notes, score, pass/fail details…"></textarea></div>
              <div class="form-field"><label>Visible to Client?</label>
                <select id="rt-t-visible"><option value="yes">Yes</option><option value="no">No — Internal only</option></select>
              </div>
            </div>
            <div style="margin-top:16px;display:flex;gap:8px">
              <button class="btn-save" onclick="saveTrainingRecord()">Save</button>
              <button class="btn-cancel" onclick="closeModal('modal-rt-training')">Cancel</button>
            </div>
            <div id="rt-training-feedback"></div>
          </div>
        </div>
      </div>

      <div class="modal-overlay" id="modal-rt-check">
        <div class="modal" style="width:640px">
          <div class="modal-header"><div class="modal-title">Log Check & Balance</div><button class="modal-close" onclick="closeModal('modal-rt-check')">×</button></div>
          <div class="modal-body">
            <input type="hidden" id="rt-ch-edit-id">
            <div class="form-grid">
              <div class="form-field"><label>Client / Location</label><select id="rt-c-client"><option value="">Select…</option></select></div>
              <div class="form-field"><label>Check Type</label>
                <select id="rt-c-type">
                  <option>Daily Opening Check</option><option>Daily Closing Check</option><option>Weekly Management Review</option>
                  <option>Monthly Operations Audit</option><option>Quarterly Compliance Review</option><option>Annual Full Audit</option>
                  <option>Cash Reconciliation</option><option>Stock Count</option><option>Equipment Inspection</option>
                  <option>SOP Adherence Check</option><option>Other</option>
                </select>
              </div>
              <div class="form-field"><label>Date</label><input type="date" id="rt-c-date"></div>
              <div class="form-field"><label>Conducted By</label><input type="text" id="rt-c-by" placeholder="Name"></div>
              <div class="form-field"><label>Overall Result</label>
                <select id="rt-c-result"><option>Pass</option><option>Pass with Issues</option><option>Fail — Action Required</option></select>
              </div>
              <div class="form-field"><label>Score (optional, /10)</label><input type="number" id="rt-c-score" min="0" max="10"></div>
              <div class="form-field" style="grid-column:1/-1"><label>Findings</label><textarea id="rt-c-findings" rows="3" placeholder="What was checked and what was found…"></textarea></div>
              <div class="form-field" style="grid-column:1/-1"><label>Actions Required</label><textarea id="rt-c-actions" rows="2" placeholder="What needs fixing, by whom, by when…"></textarea></div>
              <div class="form-field"><label>Next Check Due</label><input type="date" id="rt-c-next"></div>
              <div class="form-field"><label>Visible to Client?</label>
                <select id="rt-c-visible"><option value="yes">Yes</option><option value="no">No — Internal only</option></select>
              </div>
            </div>
            <div style="margin-top:16px;display:flex;gap:8px">
              <button class="btn-save" onclick="saveCheckRecord()">Save</button>
              <button class="btn-cancel" onclick="closeModal('modal-rt-check')">Cancel</button>
            </div>
            <div id="rt-check-feedback"></div>
          </div>
        </div>
      </div>

      <!-- Log Staff Test -->
      <div class="modal-overlay" id="modal-ic-staff">
        <div class="modal" style="width:640px">
          <div class="modal-header">
            <div class="modal-title">Log Staff Health Test</div>
            <button class="modal-close" onclick="closeModal('modal-ic-staff')">×</button>
          </div>
          <div class="modal-body">
            <input type="hidden" id="ic-staff-edit-id">
            <div class="form-grid">
              <div class="form-field"><label>Client / Location</label><select id="ic-s-client"><option value="">Select…</option></select></div>
              <div class="form-field"><label>Staff Name</label><input type="text" id="ic-s-name" placeholder="Full name"></div>
              <div class="form-field"><label>Role</label><input type="text" id="ic-s-role" placeholder="e.g. Baker, Therapist, Waitstaff"></div>
              <div class="form-field"><label>Test Type</label>
                <select id="ic-s-test">
                  <option>Hepatitis B & C</option>
                  <option>HIV (ELISA/Rapid)</option>
                  <option>Tuberculosis (TB)</option>
                  <option>Typhoid / Stool Culture</option>
                  <option>MRSA Screening</option>
                  <option>Skin / Scalp Infection Screen</option>
                  <option>Full Blood Count (FBC)</option>
                  <option>Covid / Respiratory Screen</option>
                  <option>Other</option>
                </select>
              </div>
              <div class="form-field"><label>Date Tested</label><input type="date" id="ic-s-date"></div>
              <div class="form-field"><label>Next Due Date</label><input type="date" id="ic-s-next"></div>
              <div class="form-field"><label>Result</label>
                <select id="ic-s-result">
                  <option value="Clear">✓ Clear</option>
                  <option value="Flagged">⚠ Flagged — Follow Up Required</option>
                  <option value="Pending">⏳ Pending Lab Result</option>
                  <option value="Exempt">Exempt / Not Applicable</option>
                </select>
              </div>
              <div class="form-field"><label>Lab / Tested By</label><input type="text" id="ic-s-lab" placeholder="e.g. Maitama General Hospital, Wuse Lab"></div>
              <div class="form-field" style="grid-column:1/-1"><label>Notes / Action Required</label><textarea id="ic-s-notes" rows="2" placeholder="Any follow-up action, restrictions, or notes…"></textarea></div>
              <div class="form-field" style="grid-column:1/-1">
                <label>Visible to Client?</label>
                <select id="ic-s-visible">
                  <option value="yes">Yes — show in client report</option>
                  <option value="no">No — internal only</option>
                </select>
              </div>
            </div>
            <div style="margin-top:16px;display:flex;gap:8px">
              <button class="btn-save" onclick="saveICStaffTest()">Save Test Record</button>
              <button class="btn-cancel" onclick="closeModal('modal-ic-staff')">Cancel</button>
            </div>
            <div id="ic-staff-feedback"></div>
          </div>
        </div>
      </div>

      <!-- Log Facility / Food Check -->
      <div class="modal-overlay" id="modal-ic-facility">
        <div class="modal" style="width:640px">
          <div class="modal-header">
            <div class="modal-title" id="ic-facility-modal-title">Log Facility / Food Safety Check</div>
            <button class="modal-close" onclick="closeModal('modal-ic-facility')">×</button>
          </div>
          <div class="modal-body">
            <input type="hidden" id="ic-fac-edit-id">
            <input type="hidden" id="ic-fac-type">
            <div class="form-grid">
              <div class="form-field"><label>Client / Location</label><select id="ic-f-client"><option value="">Select…</option></select></div>
              <div class="form-field"><label>Check Type</label>
                <select id="ic-f-check">
                  <option>Surface Disinfection Log</option>
                  <option>Kitchen / Prep Area Deep Clean</option>
                  <option>Equipment Sterilisation Record</option>
                  <option>Pest Control Inspection</option>
                  <option>Waste & Sharps Disposal Log</option>
                  <option>Water Quality Test</option>
                  <option>PPE Stock Check</option>
                  <option>Toilet / Handwashing Audit</option>
                  <option>Cross-contamination Risk Review</option>
                  <option>Tableware & Utensil Sanitisation</option>
                  <option>Temp Log — Cold Storage</option>
                  <option>Temp Log — Hot Holding</option>
                  <option>Delivery Acceptance Check</option>
                  <option>Date Labelling Audit</option>
                  <option>Allergen Record</option>
                  <option>Cooking Temp Probe Log</option>
                  <option>Plate & Cutlery Sanitisation</option>
                  <option>Hand Hygiene Compliance</option>
                  <option>Other</option>
                </select>
              </div>
              <div class="form-field"><label>Date of Check</label><input type="date" id="ic-f-date"></div>
              <div class="form-field"><label>Checked By</label><input type="text" id="ic-f-by" placeholder="Name of inspector / supervisor"></div>
              <div class="form-field"><label>Result / Status</label>
                <select id="ic-f-result">
                  <option value="Pass">✓ Pass</option>
                  <option value="Pass with Notes">Pass with Notes</option>
                  <option value="Fail — Action Required">✗ Fail — Action Required</option>
                  <option value="Pending">Pending</option>
                </select>
              </div>
              <div class="form-field"><label>Score (optional, /10)</label><input type="number" id="ic-f-score" min="0" max="10" placeholder="e.g. 9"></div>
              <div class="form-field" style="grid-column:1/-1"><label>Detailed Findings</label><textarea id="ic-f-findings" rows="3" placeholder="What was checked, what was found, any temperature readings, measurements…"></textarea></div>
              <div class="form-field" style="grid-column:1/-1"><label>Action Required (if any)</label><textarea id="ic-f-action" rows="2" placeholder="What needs to be corrected and by when…"></textarea></div>
              <div class="form-field"><label>Next Check Due</label><input type="date" id="ic-f-next"></div>
              <div class="form-field"><label>Visible to Client?</label>
                <select id="ic-f-visible">
                  <option value="yes">Yes — show in client report</option>
                  <option value="no">No — internal only</option>
                </select>
              </div>
            </div>
            <div style="margin-top:16px;display:flex;gap:8px">
              <button class="btn-save" onclick="saveICFacilityCheck()">Save Check</button>
              <button class="btn-cancel" onclick="closeModal('modal-ic-facility')">Cancel</button>
            </div>
            <div id="ic-facility-feedback"></div>
          </div>
        </div>
      </div>

      <!-- COMPLIANCE MODALS -->
      <!-- Add/Edit Compliance Body -->
      <div class="modal-overlay" id="modal-add-compliance-body">
        <div class="modal" style="width:720px">
          <div class="modal-header">
            <div class="modal-title" id="comp-modal-title">Add Regulatory Body</div>
            <button class="modal-close" onclick="closeModal('modal-add-compliance-body')">×</button>
          </div>
          <div class="modal-body">
            <input type="hidden" id="comp-edit-id">
            <div class="form-grid">
              <div class="form-field"><label>Country / Jurisdiction</label>
                <select id="comp-country-field">
                  <option value="Nigeria">🇳🇬 Nigeria</option>
                  <option value="United Kingdom">🇬🇧 United Kingdom</option>
                  <option value="United States">🇺🇸 United States</option>
                  <option value="UAE / Dubai">🇦🇪 UAE / Dubai</option>
                  <option value="Saudi Arabia">🇸🇦 Saudi Arabia</option>
                  <option value="Other">Other</option>
                </select>
              </div>
              <div class="form-field"><label>Tier / Category</label>
                <input type="text" id="comp-tier-field" placeholder="e.g. Federal — Taxation, FCT / Abuja — Local Govt">
              </div>
              <div class="form-field" style="grid-column:1/-1"><label>Full Body Name</label>
                <input type="text" id="comp-name-field" placeholder="e.g. Federal Inland Revenue Service">
              </div>
              <div class="form-field"><label>Abbreviation</label>
                <input type="text" id="comp-abbr-field" placeholder="e.g. FIRS / VAT">
              </div>
              <div class="form-field"><label>Applicable Niches (comma-separated)</label>
                <input type="text" id="comp-niches-field" placeholder="e.g. all-biz, food, hospitality, employer, data, fct">
              </div>
              <div class="form-field" style="grid-column:1/-1"><label>What It Does / Why It Matters</label>
                <textarea id="comp-what-field" rows="2" placeholder="Brief description of the body and what compliance means…"></textarea>
              </div>
              <div class="form-field"><label>Estimated Cost</label>
                <input type="text" id="comp-cost-field" placeholder="e.g. ₦20,000–₦100,000">
              </div>
              <div class="form-field"><label>Frequency</label>
                <input type="text" id="comp-freq-field" placeholder="e.g. Annual, Monthly, One-time">
              </div>
              <div class="form-field" style="grid-column:1/-1"><label>Deadline / When to File</label>
                <input type="text" id="comp-deadline-field" placeholder="e.g. Within 18 months of incorporation, then annually by June 30">
              </div>
              <div class="form-field" style="grid-column:1/-1"><label>Penalty for Non-Compliance</label>
                <textarea id="comp-penalty-field" rows="2" placeholder="Fines, closure, imprisonment…"></textarea>
              </div>
              <div class="form-field" style="grid-column:1/-1"><label>Documents Required (one per line)</label>
                <textarea id="comp-docs-field" rows="4" placeholder="CAC Certificate&#10;TIN&#10;Director's ID"></textarea>
              </div>
              <div class="form-field" style="grid-column:1/-1"><label>How to Renew / Update</label>
                <textarea id="comp-renewal-field" rows="2" placeholder="Annual. File by June 30. Update if directors change…"></textarea>
              </div>
              <div class="form-field" style="grid-column:1/-1"><label>Contact / Website</label>
                <input type="text" id="comp-contact-field" placeholder="e.g. firs.gov.ng | FIRS HQ, Abuja">
              </div>
            </div>
            <div style="margin-top:16px;display:flex;gap:8px">
              <button class="btn-save" onclick="saveComplianceBody()">Save Body</button>
              <button class="btn-cancel" onclick="closeModal('modal-add-compliance-body')">Cancel</button>
            </div>
            <div id="comp-body-feedback"></div>
          </div>
        </div>
      </div>

      <!-- CLIENT VIEW PAGE -->
      <div class="page" id="page-client-view">
        <div style="display:flex;align-items:center;gap:12px;margin-bottom:20px;flex-wrap:wrap">
          <button class="btn-sm" onclick="showAdminTabByName('clients')" style="font-size:12px">← All Clients</button>
          <div>
            <div style="font-size:22px;color:var(--ink)" id="client-view-name"></div>
            <div style="font-family:Arial;font-size:12px;color:var(--muted);display:flex;gap:10px;margin-top:4px">
              <span id="client-view-niche"></span>
              <span>·</span>
              <span id="client-view-package"></span>
              <span>·</span>
              <span id="client-view-status"></span>
            </div>
          </div>
          <div style="margin-left:auto;display:flex;gap:8px">
            <button class="btn-add" onclick="goAddReport(_currentViewClientId)">+ Add Report</button>
            <button class="btn-sm" id="cv-reset-btn" onclick="resetClientAccess(_currentViewClientId,'')">🔑 Reset Access</button>
          </div>
        </div>
        <div class="inner-tabs" style="margin-bottom:20px">
          <button class="inner-tab cv-tab active" id="cvtab-cv-reports" onclick="loadClientViewTab('cv-reports')">Reports</button>
          <button class="inner-tab cv-tab" id="cvtab-cv-complaints" onclick="loadClientViewTab('cv-complaints')">Complaints</button>
          <button class="inner-tab cv-tab" id="cvtab-cv-actions" onclick="loadClientViewTab('cv-actions')">Actions</button>
          <button class="inner-tab cv-tab" id="cvtab-cv-staff" onclick="loadClientViewTab('cv-staff')">Staff</button>
          <button class="inner-tab cv-tab" id="cvtab-cv-docs" onclick="loadClientViewTab('cv-docs')">Documents</button>
          <button class="inner-tab cv-tab" id="cvtab-cv-kpis" onclick="loadClientViewTab('cv-kpis')" style="color:var(--gold);font-weight:600">📊 KPIs</button>
          <button class="inner-tab cv-tab" id="cvtab-cv-compliance" onclick="loadClientViewTab('cv-compliance')" style="color:var(--ink2);font-weight:600">🗂 Compliance</button>
        </div>
        <div id="cv-reports-panel" class="cv-tab-content"><div id="cv-reports-list"></div></div>
        <div id="cv-complaints-panel" class="cv-tab-content" style="display:none"><div id="cv-complaints-list"></div></div>
        <div id="cv-actions-panel" class="cv-tab-content" style="display:none"><div id="cv-actions-list"></div></div>
        <div id="cv-staff-panel" class="cv-tab-content" style="display:none"><div id="cv-staff-list"></div></div>
        <div id="cv-docs-panel" class="cv-tab-content" style="display:none"><div id="cv-docs-list"></div></div>
        <div id="cv-kpis-panel" class="cv-tab-content" style="display:none"><div id="cv-kpis-list"></div></div>
        <div id="cv-compliance-panel" class="cv-tab-content" style="display:none"><div id="cv-compliance-list"></div></div>
      </div>

      <!-- EVENT PLANNER PAGE -->
      <div class="page" id="page-eventplanner">
        <div class="page-title">Event Planner</div>
        <div class="page-sub">Manage events Haida is planning or coordinating — inventory, production, vendors, timeline</div>
        <div style="display:flex;justify-content:flex-end;margin-bottom:16px">
          <button class="btn-add" onclick="openModal('modal-add-event')">+ New Event</button>
        </div>
        <div id="events-list"><p style="font-family:Arial;font-size:13px;color:var(--muted);padding:24px 0">Loading…</p></div>
      </div>

      <!-- ONBOARDING PAGE -->
      <div class="page" id="page-onboarding">
        <div class="page-title">Client Onboarding</div>
        <div class="page-sub">Track every step from first contact to first report — fill this in live on a call</div>
        <div style="display:flex;justify-content:flex-end;margin-bottom:16px;">
          <div class="form-field" style="min-width:260px"><select id="onb-client-select" onchange="loadOnboarding()"><option value="">Select a client…</option></select></div>
        </div>
        <div id="onboarding-body"></div>
      </div>

      <!-- APPROVALS PAGE -->
      <div class="page" id="page-approvals">
        <div class="page-title">Pending Approvals</div>
        <div class="page-sub">Review and approve or reject edits made by your admin</div>
        <div id="approvals-list"><p style="font-family:Arial;font-size:13px;color:var(--muted)">No pending approvals.</p></div>
      </div>

      <!-- EXPENDITURE PAGE -->
      <div class="page" id="page-expenditure">
        <div class="page-title">Expenditure Tracker</div>
        <div class="page-sub">Log all business spending — salaries, software, fuel, client expenses, and more</div>
        <div style="display:flex;justify-content:space-between;align-items:center;margin-bottom:16px;flex-wrap:wrap;gap:10px">
          <div style="display:flex;gap:8px;align-items:center">
            <select id="exp-month-filter" onchange="loadExpenditure()" style="font-family:Arial;font-size:13px;padding:7px 12px;border:1px solid var(--border);border-radius:2px;background:white">
              <option value="">All months</option>
            </select>
          </div>
          <button class="btn-add" onclick="openModal('modal-add-expenditure')">+ Log Expense</button>
        </div>
        <div class="stats-row" id="exp-stats"></div>
        <div class="card" style="padding:0;overflow:hidden;margin-bottom:16px">
          <table class="data-table">
            <thead><tr><th>Item</th><th>Category</th><th>Vendor</th><th>Date</th><th>Amount</th><th>Client</th><th>Notes</th><th>Actions</th></tr></thead>
            <tbody id="exp-tbody"><tr><td colspan="8" style="text-align:center;padding:24px;color:var(--muted);font-family:Arial">Loading…</td></tr></tbody>
          </table>
        </div>
      </div>

      <!-- CLIENT PORTAL VIEW -->
      <div class="page" id="page-portal">
        <div class="welcome-bar">
          <h2 id="portal-welcome">Welcome</h2>
          <p id="portal-package"></p>
        </div>
        <div style="background:#fff2e6;border:2px solid #644a3c;border-radius:6px;padding:16px 20px;margin-bottom:20px;display:flex;justify-content:space-between;align-items:center;flex-wrap:wrap;gap:12px">
          <div>
            <div style="font-family:Arial;font-size:14px;font-weight:600;color:#644a3c;margin-bottom:3px">Need something from Haida Consulting?</div>
            <div style="font-family:Arial;font-size:12px;color:#7e7b67">Submit a request and it goes directly to our team</div>
          </div>
          <button onclick="switchPortalTab('portal-requests-tab',document.querySelector('.ptab:last-child'))" style="background:#644a3c;color:white;border:none;padding:10px 20px;border-radius:4px;font-family:Arial;font-size:13px;font-weight:600;cursor:pointer">+ New Request</button>
        </div>
        <div class="portal-tabs">
          <button class="ptab active" onclick="switchPortalTab('portal-reports-tab',this)">Reports</button>
          <button class="ptab" onclick="switchPortalTab('portal-docs-tab',this)">Documents</button>
          <button class="ptab" onclick="switchPortalTab('portal-actions-tab',this)">Action Items</button>
          <button class="ptab" onclick="switchPortalTab('portal-staff-tab',this)">Our Staff</button>
          <button class="ptab" onclick="switchPortalTab('portal-events-tab',this);loadPortalEvents(currentUser.clientId)">📅 Events</button>
          <button class="ptab" onclick="switchPortalTab('portal-requests-tab',this)" style="font-weight:600;color:var(--brand)">+ New Request</button>
        </div>
        <div id="portal-reports-tab" class="portal-tab-content"></div>
        <div id="portal-invoices-tab" class="portal-tab-content" style="display:none"></div>
        <div id="portal-complaints-tab" class="portal-tab-content" style="display:none"></div>
        <div id="portal-docs-tab" class="portal-tab-content" style="display:none"></div>
        <div id="portal-actions-tab" class="portal-tab-content" style="display:none"></div>
        <div id="portal-staff-tab" class="portal-tab-content" style="display:none"></div>
        <div id="portal-events-tab" class="portal-tab-content" style="display:none"><div id="portal-events-list"></div></div>

        <div id="portal-requests-tab" class="portal-tab-content" style="display:none">
          <div class="card">
            <div class="card-title">Submit a Request</div>
            <div class="form-grid">
              <div class="form-field" style="grid-column:1/-1">
                <label>Task Title</label>
                <input type="text" id="req-title" placeholder="e.g. Update staff handbook with new shift times">
              </div>
              <div class="form-field" style="grid-column:1/-1">
                <label>Description</label>
                <textarea id="req-desc" rows="3" placeholder="Describe what you need in as much detail as possible…"></textarea>
              </div>
              <div class="form-field">
                <label>Urgency</label>
                <select id="req-urgency">
                  <option value="Low">Low — no rush</option>
                  <option value="Medium" selected>Medium — within the week</option>
                  <option value="High">High — urgent, needed soon</option>
                </select>
              </div>
              <div class="form-field">
                <label>Service Area</label>
                <select id="req-service">
                  <option>Operations & SOPs</option>
                  <option>Staff Training</option>
                  <option>Brand & Marketing</option>
                  <option>Audit & Review</option>
                  <option>Documents & Handbooks</option>
                  <option>CX & Customer Service</option>
                  <option>Reporting</option>
                  <option>Other</option>
                </select>
              </div>
              <div class="form-field">
                <label>Deadline (if any)</label>
                <input type="date" id="req-deadline">
              </div>
              <div class="form-field">
                <label>Additional Notes</label>
                <input type="text" id="req-notes" placeholder="Anything else we should know…">
              </div>
            </div>
            <div style="margin-top:16px;display:flex;gap:8px;align-items:center">
              <button style="background:#644a3c;color:white;border:none;padding:14px 28px;border-radius:4px;font-family:Arial;font-size:14px;font-weight:600;cursor:pointer;letter-spacing:.5px;width:100%;margin-top:4px" onclick="submitClientRequest()">🚀 Submit Request to Haida Consulting</button>
              <span id="req-feedback" style="font-family:Arial;font-size:12px"></span>
            </div>
          </div>
          <div class="card-title" style="margin-top:8px;font-size:15px;padding-bottom:8px;border-bottom:1px solid var(--border)">Previous Requests</div>
          <div id="portal-requests-list" style="margin-top:12px"></div>
        </div>
      </div>

    </div>
  </div>
</div>

<!-- ══ MODALS ══ -->

<!-- CV Assign Bodies Modal -->
<div class="modal-overlay" id="modal-cv-assign">
  <div class="modal" style="width:680px;max-height:90vh;display:flex;flex-direction:column">
    <div class="modal-header">
      <div class="modal-title" id="cv-assign-modal-title">Assign Compliance Bodies</div>
      <button class="modal-close" onclick="closeModal('modal-cv-assign')">×</button>
    </div>
    <div class="modal-body" style="flex:1;overflow-y:auto">
      <input type="hidden" id="cv-assign-client-id">
      <p style="font-family:Arial;font-size:12px;color:var(--muted);margin-bottom:12px">Tick all bodies that apply to this client. Only selected bodies appear on their compliance tracker.</p>
      <div style="display:flex;gap:8px;margin-bottom:12px;flex-wrap:wrap">
        <input type="text" placeholder="Search…" oninput="document.querySelectorAll('.cv-assign-row').forEach(r=>r.style.display=r.dataset.name.includes(this.value.toLowerCase())?'':'none')" style="padding:6px 10px;border:1px solid var(--border);border-radius:2px;font-family:Arial;font-size:12px;flex:1;color:var(--ink)">
        <select onchange="document.querySelectorAll('.cv-assign-row').forEach(r=>r.style.display=this.value==='all'||r.dataset.country===this.value?'':'none')" style="padding:6px 10px;border:1px solid var(--border);border-radius:2px;font-family:Arial;font-size:12px;color:var(--ink)">
          <option value="all">All Countries</option>
          <option value="Nigeria">🇳🇬 Nigeria</option>
          <option value="United Kingdom">🇬🇧 United Kingdom</option>
          <option value="UAE / Dubai">🇦🇪 UAE / Dubai</option>
          <option value="Saudi Arabia">🇸🇦 Saudi Arabia</option>
        </select>
        <button onclick="document.querySelectorAll('.cv-assign-cb').forEach(c=>c.checked=true)" style="padding:6px 12px;background:var(--surface2);border:1px solid var(--border);border-radius:2px;font-family:Arial;font-size:11px;cursor:pointer;color:var(--ink)">Select All</button>
        <button onclick="document.querySelectorAll('.cv-assign-cb').forEach(c=>c.checked=false)" style="padding:6px 12px;background:var(--surface2);border:1px solid var(--border);border-radius:2px;font-family:Arial;font-size:11px;cursor:pointer;color:var(--ink)">Clear All</button>
      </div>
      <div id="cv-assign-body-list"></div>
    </div>
    <div style="padding:16px 20px;border-top:1px solid var(--border);display:flex;gap:8px;align-items:center">
      <button class="btn-save" onclick="saveCVAssignedBodies()">Save</button>
      <button class="btn-cancel" onclick="closeModal('modal-cv-assign')">Cancel</button>
      <span id="cv-assign-count" style="font-family:Arial;font-size:12px;color:var(--muted);margin-left:8px"></span>
      <div id="cv-assign-feedback"></div>
    </div>
  </div>
</div>

<!-- CV Update Status Modal -->
<div class="modal-overlay" id="modal-cv-status">
  <div class="modal" style="width:540px">
    <div class="modal-header">
      <div class="modal-title" id="cv-status-modal-title">Update Status</div>
      <button class="modal-close" onclick="closeModal('modal-cv-status')">×</button>
    </div>
    <div class="modal-body">
      <input type="hidden" id="cv-status-body-id">
      <input type="hidden" id="cv-status-client-id">
      <input type="hidden" id="cv-status-freq">
      <div class="form-grid">
        <div class="form-field"><label>Status</label>
          <select id="cv-status-val">
            <option>Not Started</option><option>In Progress</option><option>Done</option><option>N/A</option>
          </select>
        </div>
        <div class="form-field"><label>Date Done / Submitted</label><input type="date" id="cv-status-done-date"></div>
        <div class="form-field"><label>Next Renewal Due</label><input type="date" id="cv-status-next-due"></div>
        <div class="form-field"><label>Certificate / Reference No.</label><input type="text" id="cv-status-ref" placeholder="e.g. CAC/RC/0012345"></div>
        <div class="form-field" style="grid-column:1/-1"><label>Contact / Agent Used</label><input type="text" id="cv-status-contact" placeholder="e.g. Musa at AMAC — 0812 000 0000"></div>
        <div class="form-field" style="grid-column:1/-1"><label>Notes</label><textarea id="cv-status-notes" rows="3" placeholder="Details, cost paid, observations…"></textarea></div>
      </div>
      <div style="margin-top:16px;display:flex;gap:8px">
        <button class="btn-save" onclick="saveCVStatus()">Save</button>
        <button class="btn-cancel" onclick="closeModal('modal-cv-status')">Cancel</button>
      </div>
      <div id="cv-status-feedback"></div>
    </div>
  </div>
</div>

<!-- Add Client -->
<div class="modal-overlay" id="modal-add-client">
  <div class="modal">
    <div class="modal-header"><div class="modal-title">Add New Client</div><button class="modal-close" onclick="closeModal('modal-add-client')">×</button></div>
    <div class="modal-body">
      <div class="form-grid">
        <div class="form-field"><label>Business Name</label><input type="text" id="nc-name" placeholder="e.g. Jun Bakery"></div>
        <div class="form-field"><label>Contact Person</label><input type="text" id="nc-contact" placeholder="Full name"></div>
        <div class="form-field"><label>Username</label><input type="text" id="nc-username" placeholder="e.g. junbakery2026" autocomplete="off"></div>
        <div class="form-field"><label>Access Code</label><input type="text" id="nc-password" placeholder="Create an access code" autocomplete="off"></div>
        <div class="form-field"><label>Industry Niche</label>
          <select id="nc-niche">
            <option value="cafe">☕ Café / Restaurant / F&B</option>
            <option value="aesthetics">💆 Aesthetics / Beauty Clinic</option>
            <option value="hair">💇 Hair & Beauty Retail</option>
            <option value="education">🎓 Education / School</option>
            <option value="faith">🕌 Church / Faith Organisation</option>
            <option value="events">🎉 Event Catering / Hospitality</option>
            <option value="hospitality">🏠 Hospitality / Airbnb</option>
            <option value="media">🎬 Media / Creative Agency</option>
            <option value="halal">✅ Halal Brand</option>
            <option value="wellness">🏥 Healthcare / Wellness</option>
            <option value="retail">🛍️ Retail / E-Commerce</option>
            <option value="corporate">🏢 Corporate / Professional Services</option>
            <option value="logistics">🚚 Logistics Company</option>
            <option value="tech">💻 Tech Firm</option>
            <option value="homebrand">📦 Home Brand / DTC Delivery</option>
            <option value="delivery">🛵 Delivery / Rider Company</option>
            <option value="event_planner">🎪 Event Planner</option>
            <option value="general">📋 General</option>
          </select>
        </div>
        <div class="form-field"><label>Package</label>
          <select id="nc-package">
            <option>Small Business</option><option>Medium Business</option>
            <option>Large Business</option><option>Enterprise</option>
            <option>CX Support</option><option>Ad-Hoc</option>
          </select>
        </div>
        <div class="form-field"><label>Status</label>
          <select id="nc-status"><option>Active</option><option>Onboarding</option><option>Paused</option></select>
        </div>
      </div>
      <button class="btn-save" onclick="saveClient()" style="margin-top:16px">Add Client</button>
      <div id="client-feedback"></div>
    </div>
  </div>
</div>

<!-- Add / Edit Invoice -->
<div class="modal-overlay" id="modal-add-invoice">
  <div class="modal" style="width:680px">
    <div class="modal-header">
      <div class="modal-title" id="inv-modal-title">Create Invoice</div>
      <button class="modal-close" onclick="closeModal('modal-add-invoice')">×</button>
    </div>
    <div class="modal-body">
      <input type="hidden" id="inv-edit-id" value="">
      <div class="form-grid">
        <div class="form-field"><label>Client</label><select id="inv-client"><option value="">Select client…</option></select></div>
        <div class="form-field"><label>Invoice Number</label><input type="text" id="inv-number" placeholder="e.g. HYL-2026-001"></div>
        <div class="form-field"><label>Currency & Payment Route</label>
          <select id="inv-currency" onchange="setCurrencyDefaults()">
            <option value="NGN">₦ Nigerian Naira — Summit Bank / Moniepoint</option>
            <option value="GBP">£ British Pounds — Clear Bank (UK)</option>
          </select>
        </div>
        <div class="form-field"><label>VAT Rate</label>
          <select id="inv-vat-rate" onchange="calcVAT()">
            <option value="0">0% — VAT Exempt</option>
            <option value="7.5" selected>7.5% — Standard Nigerian VAT</option>
          </select>
        </div>
        <div class="form-field" style="grid-column:1/-1"><label>Description / Scope</label><input type="text" id="inv-desc" placeholder="e.g. Phase 2 — Pre-opening consulting package"></div>
        <div class="form-field"><label>Subtotal — before VAT</label><input type="number" id="inv-amount" placeholder="0" oninput="calcVAT()"></div>
        <div class="form-field"><label>VAT Amount — auto-calculated</label><input type="text" id="inv-vat-amount" readonly style="background:var(--surface2);color:var(--muted)"></div>
        <div class="form-field"><label>Total Inc. VAT</label><input type="text" id="inv-total-inc" readonly style="background:var(--surface2);font-weight:600;color:var(--ink)"></div>
        <div class="form-field"><label>Amount Paid</label><input type="number" id="inv-paid" placeholder="0" value="0"></div>
        <div class="form-field"><label>Invoice Date</label><input type="date" id="inv-date"></div>
        <div class="form-field"><label>Due Date</label><input type="date" id="inv-due"></div>
        <div class="form-field"><label>Status</label>
          <select id="inv-status"><option>Unpaid</option><option>Partial</option><option>Paid</option></select>
        </div>
        <div class="form-field" style="grid-column:1/-1"><label>Payment Instructions</label><textarea id="inv-notes" rows="3" placeholder="Auto-filled based on currency — you can edit this"></textarea></div>
      </div>
      <div style="margin-top:16px;display:flex;gap:8px">
        <button class="btn-save" id="inv-save-btn" onclick="saveInvoice()">Save Invoice</button>
        <button class="btn-cancel" onclick="closeModal('modal-add-invoice')">Cancel</button>
      </div>
      <div id="invoice-feedback"></div>
    </div>
  </div>
</div>

<!-- Invoice PDF Modal -->
<div class="modal-overlay" id="modal-invoice-pdf">
  <div class="modal" style="width:700px;max-height:90vh">
    <div class="modal-header">
      <div class="modal-title">Invoice Preview</div>
      <div style="display:flex;gap:8px">
        <button class="btn-save" onclick="window.print()">Print / Save PDF</button>
        <button class="modal-close" onclick="closeModal('modal-invoice-pdf')">×</button>
      </div>
    </div>
    <div class="modal-body" id="invoice-pdf-body" style="padding:0"></div>
  </div>
</div>

<!-- Add Complaint -->
<div class="modal-overlay" id="modal-add-complaint">
  <div class="modal">
    <div class="modal-header"><div class="modal-title">Log Complaint</div><button class="modal-close" onclick="closeModal('modal-add-complaint')">×</button></div>
    <div class="modal-body">
      <div class="form-grid">
        <div class="form-field"><label>Client</label><select id="comp-client"><option value="">Select client…</option></select></div>
        <div class="form-field"><label>Date Received</label><input type="date" id="comp-date"></div>
        <div class="form-field"><label>Category</label>
          <select id="comp-category">
            <option>Staff Conduct</option><option>Service Quality</option><option>Food / Product Quality</option>
            <option>Hygiene / Safety</option><option>Billing / Invoice</option><option>Communication</option>
            <option>Delay / Late Delivery</option><option>Refund / Compensation</option><option>Facility / Space</option><option>Other</option>
          </select>
        </div>
        <div class="form-field"><label>Raised By</label><input type="text" id="comp-by" placeholder="Customer name or 'Anonymous'"></div>
        <div class="form-field"><label>Severity</label>
          <select id="comp-severity"><option>Low</option><option>Medium</option><option>High</option><option>Critical</option></select>
        </div>
        <div class="form-field"><label>Status</label>
          <select id="comp-status"><option>Open</option><option>In Progress</option><option>Escalated</option><option>Resolved</option></select>
        </div>
        <div class="form-field" style="grid-column:1/-1"><label>Complaint Details</label><textarea id="comp-detail" rows="4" placeholder="Describe the complaint in full…"></textarea></div>
        <div class="form-field" style="grid-column:1/-1"><label>Action Taken</label><textarea id="comp-action" rows="3" placeholder="What has been done so far to address this…"></textarea></div>
        <div class="form-field"><label>Strike Number (if staff-related)</label>
          <select id="comp-strike"><option value="0">N/A</option><option value="1">Strike 1</option><option value="2">Strike 2 — Warning</option><option value="3">Strike 3 — Termination review</option></select>
        </div>
        <div class="form-field"><label>Resolution Date (if resolved)</label><input type="date" id="comp-resolved"></div>
      </div>
      <button class="btn-save" onclick="saveComplaint()" style="margin-top:16px">Log Complaint</button>
      <div id="complaint-feedback"></div>
    </div>
  </div>
</div>

<!-- Add Action -->
<div class="modal-overlay" id="modal-add-action">
  <div class="modal">
    <div class="modal-header"><div class="modal-title">Add Action Item</div><button class="modal-close" onclick="closeModal('modal-add-action')">×</button></div>
    <div class="modal-body">
      <div class="form-grid">
        <div class="form-field"><label>Client</label><select id="act-client"><option value="">Select client…</option></select></div>
        <div class="form-field"><label>Due Date</label><input type="date" id="act-due"></div>
        <div class="form-field" style="grid-column:1/-1"><label>Action Description</label><input type="text" id="act-desc" placeholder="e.g. Deliver staff training programme before opening day"></div>
        <div class="form-field"><label>Owner</label><input type="text" id="act-owner" placeholder="e.g. Haida / Client / Head Baker"></div>
        <div class="form-field"><label>Priority</label>
          <select id="act-priority"><option>High</option><option>Medium</option><option>Low</option></select>
        </div>
        <div class="form-field"><label>Status</label>
          <select id="act-status"><option>Outstanding</option><option>In Progress</option><option>Done</option><option>Blocked</option></select>
        </div>
        <div class="form-field" style="grid-column:1/-1"><label>Notes</label><textarea id="act-notes" rows="2" placeholder="Any additional context…"></textarea></div>
      </div>
      <button class="btn-save" onclick="saveAction()" style="margin-top:16px">Save Action</button>
      <div id="action-feedback"></div>
    </div>
  </div>
</div>

<!-- Add Staff -->
<div class="modal-overlay" id="modal-add-staff">
  <div class="modal">
    <div class="modal-header"><div class="modal-title" id="staff-modal-title">Add Staff Member</div><button class="modal-close" onclick="closeModal('modal-add-staff')">×</button></div>
    <div class="modal-body">
      <input type="hidden" id="stf-edit-id">
      <div class="form-grid">
        <div class="form-field"><label>Client</label><select id="stf-client"><option value="">Select client…</option></select></div>
        <div class="form-field"><label>Full Name</label><input type="text" id="stf-name" placeholder="Staff member's name"></div>
        <div class="form-field"><label>Role</label><input type="text" id="stf-role" placeholder="e.g. Head Baker, Barista"></div>
        <div class="form-field"><label>Start Date</label><input type="date" id="stf-start"></div>
        <div class="form-field"><label>Status</label>
          <select id="stf-status"><option>Active</option><option>Probation</option><option>Flagged</option><option>Terminated</option></select>
        </div>
        <div class="form-field"><label>Last Training Date</label><input type="date" id="stf-trained"></div>
        <div class="form-field" style="grid-column:1/-1"><label>Performance Notes</label><textarea id="stf-notes" rows="3" placeholder="Observations, concerns, strengths…"></textarea></div>
      </div>
      <button class="btn-save" onclick="saveStaff()" style="margin-top:16px">Save Staff Member</button>
      <div id="staff-feedback"></div>
    </div>
  </div>
</div>

<!-- Add Strike Modal (separate from staff add) -->
<div class="modal-overlay" id="modal-add-strike">
  <div class="modal" style="width:520px">
    <div class="modal-header"><div class="modal-title" id="strike-modal-title">Log Strike</div><button class="modal-close" onclick="closeModal('modal-add-strike')">×</button></div>
    <div class="modal-body">
      <input type="hidden" id="strike-staff-id">
      <div class="form-grid">
        <div class="form-field"><label>Strike Number</label>
          <select id="strike-number">
            <option value="1">Strike 1 — First Warning</option>
            <option value="2">Strike 2 — Final Warning</option>
            <option value="3">Strike 3 — Termination Review</option>
          </select>
        </div>
        <div class="form-field"><label>Date of Incident</label><input type="date" id="strike-date"></div>
        <div class="form-field" style="grid-column:1/-1"><label>Reason / Incident Description</label><textarea id="strike-reason" rows="3" placeholder="Describe the incident, rule broken, or performance issue…"></textarea></div>
        <div class="form-field" style="grid-column:1/-1"><label>Action Taken</label><textarea id="strike-action" rows="2" placeholder="Warning issued, meeting held, written notice, suspension…"></textarea></div>
        <div class="form-field"><label>Issued By</label><input type="text" id="strike-by" placeholder="Manager / supervisor name"></div>
        <div class="form-field"><label>Staff Acknowledgement</label>
          <select id="strike-ack"><option value="yes">Yes — acknowledged</option><option value="no">No — refused to sign</option><option value="pending">Pending</option></select>
        </div>
      </div>
      <div style="margin-top:16px;display:flex;gap:8px">
        <button class="btn-save" onclick="saveStrike()">Log Strike</button>
        <button class="btn-cancel" onclick="closeModal('modal-add-strike')">Cancel</button>
      </div>
      <div id="strike-feedback"></div>
    </div>
  </div>
</div>

<!-- Add Contract -->
<div class="modal-overlay" id="modal-add-contract">
  <div class="modal">
    <div class="modal-header"><div class="modal-title">Add Contract</div><button class="modal-close" onclick="closeModal('modal-add-contract')">×</button></div>
    <div class="modal-body">
      <div class="form-grid">
        <div class="form-field"><label>Client</label><select id="con-client"><option value="">Select client…</option></select></div>
        <div class="form-field"><label>Contract Type</label>
          <select id="con-type"><option>Monthly Retainer</option><option>One-Time Project</option><option>Pre-Opening Package</option><option>Ad-Hoc</option></select>
        </div>
        <div class="form-field"><label>Monthly / Total Value (₦)</label><input type="number" id="con-value" placeholder="750000"></div>
        <div class="form-field"><label>Start Date</label><input type="date" id="con-start"></div>
        <div class="form-field"><label>End / Renewal Date</label><input type="date" id="con-end"></div>
        <div class="form-field"><label>Status</label>
          <select id="con-status"><option>Active</option><option>Pending Signature</option><option>Expired</option><option>Terminated</option></select>
        </div>
        <div class="form-field" style="grid-column:1/-1"><label>Scope Summary</label><textarea id="con-scope" rows="3" placeholder="Brief description of what is covered…"></textarea></div>
      </div>
      <button class="btn-save" onclick="saveContract()" style="margin-top:16px">Save Contract</button>
      <div id="contract-feedback"></div>
    </div>
  </div>
</div>

<!-- Add Document -->
<div class="modal-overlay" id="modal-add-doc">
  <div class="modal">
    <div class="modal-header"><div class="modal-title">Add Document</div><button class="modal-close" onclick="closeModal('modal-add-doc')">×</button></div>
    <div class="modal-body">
      <div class="form-grid">
        <div class="form-field"><label>Client</label><select id="doc-client"><option value="">Select client…</option></select></div>
        <div class="form-field"><label>Document Type</label>
          <select id="doc-type">
            <option>SOP</option><option>Staff Handbook</option><option>Training Manual</option>
            <option>Audit Framework</option><option>Brand Guidelines</option><option>Service Agreement</option>
            <option>Onboarding Pack</option><option>Policy Document</option><option>Other</option>
          </select>
        </div>
        <div class="form-field" style="grid-column:1/-1"><label>Document Title</label><input type="text" id="doc-title" placeholder="e.g. Jun Bakery Daily Opening SOP"></div>
        <div class="form-field"><label>Date Added</label><input type="date" id="doc-date"></div>
        <div class="form-field"><label>Version</label><input type="text" id="doc-version" placeholder="e.g. v1.0"></div>
        <div class="form-field" style="grid-column:1/-1"><label>Link (Google Drive, Notion, etc.)</label><input type="text" id="doc-link" placeholder="https://…"></div>
        <div class="form-field" style="grid-column:1/-1"><label>Notes</label><textarea id="doc-notes" rows="2" placeholder="Any notes about this document…"></textarea></div>
      </div>
      <button class="btn-save" onclick="saveDoc()" style="margin-top:16px">Save Document</button>
      <div id="doc-feedback"></div>
    </div>
  </div>
</div>

<!-- View Client -->
<div class="modal-overlay" id="modal-view-client">
  <div class="modal modal-lg">
    <div class="modal-header"><div class="modal-title" id="view-client-title">Client</div><button class="modal-close" onclick="closeModal('modal-view-client')">×</button></div>
    <div class="modal-body" id="view-client-body"></div>
  </div>
</div>

<script>
// ══ SUPABASE ══
const SUPA_URL='https://tdxvioeybgqbbcubngko.supabase.co';
const SUPA_KEY='sb_publishable_VtboyoBjlK5Zp_lOZMr3wA_V45kYBkH';
const db=supabase.createClient(SUPA_URL,SUPA_KEY);

// Task board — separate Supabase project
const TASK_URL='https://vbffwaztmnrrgishlqln.supabase.co';
const TASK_KEY='sb_publishable_L-r2y9EXb2U5vULz7uiuEQ_yfOAC5Zt';
const taskDb=supabase.createClient(TASK_URL,TASK_KEY);

let currentUser=null,allClients=[],reportType='audit';

// ══ NICHES ══
const nicheLabels={cafe:'☕ Café/F&B',aesthetics:'💆 Aesthetics',hair:'💇 Hair & Beauty',education:'🎓 Education',faith:'🕌 Faith Org',events:'🎉 Events',event_planner:'🎪 Event Planner',hospitality:'🏠 Hospitality',media:'🎬 Media',halal:'✅ Halal Brand',wellness:'🏥 Wellness',retail:'🛍️ Retail',corporate:'🏢 Corporate',logistics:'🚚 Logistics',tech:'💻 Tech Firm',homebrand:'📦 Home Brand / DTC',delivery:'🛵 Delivery / Rider Co',general:'📋 General'};


// ══ KPI LIBRARY — all possible KPIs per niche ══
const KPI_LIBRARY = {
  cafe: [
    {id:'avg_ticket_time',label:'Average kitchen ticket time',unit:'mins',lower_is_better:true,description:'Time from order placed to food delivered'},
    {id:'avg_spend',label:'Average spend per cover',unit:'₦',lower_is_better:false,description:'Total revenue divided by covers served'},
    {id:'daily_covers',label:'Daily covers served',unit:'covers',lower_is_better:false,description:'Number of customers served per day'},
    {id:'monthly_revenue',label:'Monthly revenue',unit:'₦',lower_is_better:false,description:'Total café revenue for the month'},
    {id:'food_cost_pct',label:'Food cost percentage',unit:'%',lower_is_better:true,description:'Cost of goods sold as % of revenue — target under 35%'},
    {id:'waste_pct',label:'Food waste percentage',unit:'%',lower_is_better:true,description:'Food discarded as % of total prep'},
    {id:'staff_punctuality',label:'Staff punctuality rate',unit:'%',lower_is_better:false,description:'% of shifts where staff arrived on time'},
    {id:'cx_score',label:'CX mystery visit score',unit:'/10',lower_is_better:false,description:'Score from Haida mystery visit'},
    {id:'greeting_time',label:'Greeting time',unit:'secs',lower_is_better:true,description:'Seconds from customer entry to first greeting'},
    {id:'complimentary_time',label:'Complimentary item arrival',unit:'mins',lower_is_better:true,description:'Minutes from seating to cheese roll delivery'},
    {id:'table_turnover',label:'Table turnover rate',unit:'per day',lower_is_better:false,description:'Average number of seatings per table per day'},
    {id:'google_rating',label:'Google review rating',unit:'stars',lower_is_better:false,description:'Current live Google Maps star rating'},
    {id:'google_reviews',label:'Google review count',unit:'reviews',lower_is_better:false,description:'Total number of Google reviews'},
    {id:'ig_followers',label:'Instagram followers',unit:'followers',lower_is_better:false,description:'Total IG follower count'},
    {id:'ig_posts',label:'Instagram posts this month',unit:'posts',lower_is_better:false,description:'Number of posts published'},
    {id:'ig_engagement',label:'Instagram engagement rate',unit:'%',lower_is_better:false,description:'Avg engagement per post as % of followers'},
    {id:'event_bookings',label:'Event space bookings',unit:'bookings',lower_is_better:false,description:'Number of event space bookings this month'},
    {id:'event_revenue',label:'Event space revenue',unit:'₦',lower_is_better:false,description:'Total revenue from event hire'},
    {id:'delivery_orders',label:'Delivery orders',unit:'orders',lower_is_better:false,description:'Number of delivery orders fulfilled'},
    {id:'menu_compliance',label:'Menu item availability',unit:'%',lower_is_better:false,description:'% of menu items available during visit'},
    {id:'wc_score',label:'WC cleanliness score',unit:'/10',lower_is_better:false,description:'Toilet cleanliness during audit'},
    {id:'opening_compliance',label:'Opening standard compliance',unit:'%',lower_is_better:false,description:'% of days opening checklist was fully completed'},
    {id:'training_pass_rate',label:'Staff training pass rate',unit:'%',lower_is_better:false,description:'% of staff who passed most recent training assessment'},
    {id:'repeat_customers',label:'Estimated repeat customer rate',unit:'%',lower_is_better:false,description:'Estimated % of customers who are returning visitors'},
    {id:'net_profit',label:'Net profit margin',unit:'%',lower_is_better:false,description:'Revenue minus all costs as % of revenue'},
  ],
  aesthetics: [
    {id:'monthly_bookings',label:'Total bookings this month',unit:'bookings',lower_is_better:false},
    {id:'booking_utilisation',label:'Booking utilisation rate',unit:'%',lower_is_better:false,description:'% of available slots that were filled'},
    {id:'client_retention',label:'Client retention rate',unit:'%',lower_is_better:false,description:'% of clients who return within 90 days'},
    {id:'new_clients',label:'New clients acquired',unit:'clients',lower_is_better:false},
    {id:'avg_spend',label:'Average spend per visit',unit:'₦',lower_is_better:false},
    {id:'monthly_revenue',label:'Monthly revenue',unit:'₦',lower_is_better:false},
    {id:'treatment_compliance',label:'Treatment protocol compliance',unit:'/10',lower_is_better:false,description:'Score from Haida audit'},
    {id:'consent_compliance',label:'Consent form completion rate',unit:'%',lower_is_better:false},
    {id:'nafdac_status',label:'NAFDAC compliance status',unit:'score',lower_is_better:false,description:'1=Non-compliant, 2=Partial, 3=Fully compliant'},
    {id:'sterilisation_score',label:'Equipment sterilisation score',unit:'/10',lower_is_better:false},
    {id:'staff_presentation',label:'Staff presentation score',unit:'/10',lower_is_better:false},
    {id:'google_rating',label:'Google review rating',unit:'stars',lower_is_better:false},
    {id:'ig_followers',label:'Instagram followers',unit:'followers',lower_is_better:false},
    {id:'complaint_rate',label:'Complaint rate',unit:'per 100 clients',lower_is_better:true},
    {id:'cancellation_rate',label:'Appointment cancellation rate',unit:'%',lower_is_better:true},
    {id:'product_sell_through',label:'Retail product sell-through rate',unit:'%',lower_is_better:false},
    {id:'training_pass_rate',label:'Staff training pass rate',unit:'%',lower_is_better:false},
    {id:'cx_score',label:'CX audit score',unit:'/10',lower_is_better:false},
  ],
  hair: [
    {id:'monthly_revenue',label:'Monthly revenue',unit:'₦',lower_is_better:false},
    {id:'daily_footfall',label:'Daily customer footfall',unit:'customers',lower_is_better:false},
    {id:'avg_spend',label:'Average spend per customer',unit:'₦',lower_is_better:false},
    {id:'stock_accuracy',label:'Stock accuracy rate',unit:'%',lower_is_better:false,description:'System stock vs physical count'},
    {id:'product_sell_through',label:'Product sell-through rate',unit:'%',lower_is_better:false},
    {id:'halal_compliance',label:'Halal product compliance',unit:'%',lower_is_better:false,description:'% of products with valid halal certification'},
    {id:'upsell_rate',label:'Upsell conversion rate',unit:'%',lower_is_better:false},
    {id:'staff_knowledge',label:'Staff product knowledge score',unit:'/10',lower_is_better:false},
    {id:'display_score',label:'Display standard score',unit:'/10',lower_is_better:false},
    {id:'ig_followers',label:'Instagram followers',unit:'followers',lower_is_better:false},
    {id:'complaint_rate',label:'Complaint rate',unit:'per month',lower_is_better:true},
    {id:'training_pass_rate',label:'Staff training pass rate',unit:'%',lower_is_better:false},
    {id:'repeat_customers',label:'Repeat customer rate',unit:'%',lower_is_better:false},
    {id:'google_rating',label:'Google review rating',unit:'stars',lower_is_better:false},
    {id:'shrinkage_rate',label:'Stock shrinkage rate',unit:'%',lower_is_better:true},
  ],
  education: [
    {id:'fee_collection_rate',label:'Fee collection rate',unit:'%',lower_is_better:false,description:'% of expected fees collected on time'},
    {id:'outstanding_fees',label:'Outstanding fees',unit:'₦',lower_is_better:true},
    {id:'enrolment',label:'Current enrolment',unit:'students',lower_is_better:false},
    {id:'attendance_rate',label:'Student attendance rate',unit:'%',lower_is_better:false},
    {id:'uniform_compliance',label:'Uniform compliance rate',unit:'%',lower_is_better:false},
    {id:'staff_punctuality',label:'Staff punctuality rate',unit:'%',lower_is_better:false},
    {id:'safeguarding_compliance',label:'Safeguarding compliance',unit:'score',lower_is_better:false,description:'1=Non-compliant, 2=Partial, 3=Fully compliant'},
    {id:'extracurricular_delivery',label:'Extracurricular programme delivery',unit:'%',lower_is_better:false,description:'% of scheduled sessions delivered'},
    {id:'parent_satisfaction',label:'Parent satisfaction score',unit:'/10',lower_is_better:false},
    {id:'facility_score',label:'Facility condition score',unit:'/10',lower_is_better:false},
    {id:'classroom_score',label:'Classroom standard score',unit:'/10',lower_is_better:false},
    {id:'complaint_rate',label:'Parent complaints this month',unit:'complaints',lower_is_better:true},
    {id:'staff_training',label:'Staff training completion rate',unit:'%',lower_is_better:false},
    {id:'documentation_score',label:'Admin & documentation score',unit:'/10',lower_is_better:false},
    {id:'portal_usage',label:'Portal/system usage rate',unit:'%',lower_is_better:false},
    {id:'new_enrolments',label:'New enrolments this month',unit:'students',lower_is_better:false},
  ],
  faith: [
    {id:'avg_congregation',label:'Average congregation size',unit:'people',lower_is_better:false},
    {id:'giving_revenue',label:'Monthly giving / tithes',unit:'₦',lower_is_better:false},
    {id:'volunteer_retention',label:'Volunteer retention rate',unit:'%',lower_is_better:false},
    {id:'active_volunteers',label:'Active volunteers',unit:'volunteers',lower_is_better:false},
    {id:'facility_score',label:'Facility standard score',unit:'/10',lower_is_better:false},
    {id:'wudu_score',label:'Wudu / ablution area score',unit:'/10',lower_is_better:false},
    {id:'events_delivered',label:'Events / programmes delivered',unit:'events',lower_is_better:false},
    {id:'event_attendance',label:'Average event attendance',unit:'people',lower_is_better:false},
    {id:'pastoral_reach',label:'Pastoral care contacts made',unit:'contacts',lower_is_better:false},
    {id:'social_media_reach',label:'Social media reach',unit:'accounts reached',lower_is_better:false},
    {id:'new_members',label:'New members this month',unit:'members',lower_is_better:false},
    {id:'compliance_score',label:'Operational compliance score',unit:'/10',lower_is_better:false},
    {id:'staff_training',label:'Staff/volunteer training completion',unit:'%',lower_is_better:false},
    {id:'complaint_rate',label:'Member complaints',unit:'complaints',lower_is_better:true},
  ],
  events: [
    {id:'events_delivered',label:'Events delivered this month',unit:'events',lower_is_better:false},
    {id:'monthly_revenue',label:'Monthly revenue',unit:'₦',lower_is_better:false},
    {id:'avg_event_value',label:'Average event value',unit:'₦',lower_is_better:false},
    {id:'client_satisfaction',label:'Post-event client satisfaction',unit:'/10',lower_is_better:false},
    {id:'setup_punctuality',label:'Setup completed on time rate',unit:'%',lower_is_better:false},
    {id:'allergen_compliance',label:'Allergen form compliance rate',unit:'%',lower_is_better:false},
    {id:'staff_uniform_compliance',label:'Staff uniform compliance',unit:'%',lower_is_better:false},
    {id:'food_temp_compliance',label:'Food temperature compliance',unit:'%',lower_is_better:false},
    {id:'packaging_score',label:'Branded packaging score',unit:'/10',lower_is_better:false},
    {id:'repeat_clients',label:'Repeat client bookings',unit:'bookings',lower_is_better:false},
    {id:'new_clients',label:'New client bookings',unit:'bookings',lower_is_better:false},
    {id:'teardown_score',label:'Teardown / exit standard',unit:'/10',lower_is_better:false},
    {id:'complaint_rate',label:'Post-event complaints',unit:'complaints',lower_is_better:true},
    {id:'ig_followers',label:'Instagram followers',unit:'followers',lower_is_better:false},
    {id:'referral_bookings',label:'Bookings from referrals',unit:'bookings',lower_is_better:false},
    {id:'cancellation_rate',label:'Booking cancellation rate',unit:'%',lower_is_better:true},
    {id:'profit_margin',label:'Net profit margin per event',unit:'%',lower_is_better:false},
  ],
  hospitality: [
    {id:'occupancy_rate',label:'Occupancy rate',unit:'%',lower_is_better:false,description:'% of nights booked vs available'},
    {id:'monthly_revenue',label:'Monthly revenue',unit:'₦',lower_is_better:false},
    {id:'avg_nightly_rate',label:'Average nightly rate',unit:'₦',lower_is_better:false},
    {id:'guest_satisfaction',label:'Guest satisfaction score',unit:'/10',lower_is_better:false},
    {id:'airbnb_rating',label:'Airbnb/booking platform rating',unit:'stars',lower_is_better:false},
    {id:'review_count',label:'Platform review count',unit:'reviews',lower_is_better:false},
    {id:'cleanliness_score',label:'Property cleanliness score',unit:'/10',lower_is_better:false},
    {id:'response_time',label:'Guest communication response time',unit:'hours',lower_is_better:true},
    {id:'maintenance_items',label:'Open maintenance items',unit:'items',lower_is_better:true},
    {id:'amenity_score',label:'Amenity stock score',unit:'/10',lower_is_better:false},
    {id:'repeat_guests',label:'Repeat guest rate',unit:'%',lower_is_better:false},
    {id:'complaint_rate',label:'Guest complaints',unit:'complaints',lower_is_better:true},
    {id:'check_in_score',label:'Check-in experience score',unit:'/10',lower_is_better:false},
    {id:'listing_accuracy',label:'Listing accuracy score',unit:'/10',lower_is_better:false},
    {id:'cancellation_rate',label:'Booking cancellation rate',unit:'%',lower_is_better:true},
  ],
  media: [
    {id:'projects_delivered',label:'Projects delivered on time',unit:'%',lower_is_better:false},
    {id:'monthly_revenue',label:'Monthly revenue',unit:'₦',lower_is_better:false},
    {id:'client_satisfaction',label:'Client satisfaction score',unit:'/10',lower_is_better:false},
    {id:'active_projects',label:'Active projects',unit:'projects',lower_is_better:false},
    {id:'brief_adherence',label:'Brief adherence rate',unit:'%',lower_is_better:false},
    {id:'revision_rounds',label:'Average revision rounds per project',unit:'rounds',lower_is_better:true},
    {id:'new_clients',label:'New clients acquired',unit:'clients',lower_is_better:false},
    {id:'repeat_clients',label:'Repeat client rate',unit:'%',lower_is_better:false},
    {id:'overdue_projects',label:'Overdue projects',unit:'projects',lower_is_better:true},
    {id:'asset_delivery_score',label:'Asset handover quality score',unit:'/10',lower_is_better:false},
    {id:'team_utilisation',label:'Team utilisation rate',unit:'%',lower_is_better:false},
    {id:'complaint_rate',label:'Client complaints',unit:'complaints',lower_is_better:true},
    {id:'ig_followers',label:'Own IG followers',unit:'followers',lower_is_better:false},
    {id:'referral_rate',label:'Referral rate',unit:'%',lower_is_better:false,description:'% of new clients from referrals'},
    {id:'profit_margin',label:'Net profit margin',unit:'%',lower_is_better:false},
  ],
  halal: [
    {id:'cert_status',label:'Halal certification status',unit:'score',lower_is_better:false,description:'1=Not in place, 2=Expired, 3=Current'},
    {id:'supplier_compliance',label:'Supplier halal compliance rate',unit:'%',lower_is_better:false,description:'% of suppliers with verified halal status'},
    {id:'ingredient_verification',label:'Ingredient verification rate',unit:'%',lower_is_better:false},
    {id:'cross_contamination_score',label:'Cross-contamination protocol score',unit:'/10',lower_is_better:false},
    {id:'labelling_compliance',label:'Product labelling compliance',unit:'%',lower_is_better:false},
    {id:'staff_training_halal',label:'Staff halal training completion',unit:'%',lower_is_better:false},
    {id:'audit_score',label:'Overall halal audit score',unit:'/10',lower_is_better:false},
    {id:'monthly_revenue',label:'Monthly revenue',unit:'₦',lower_is_better:false},
    {id:'complaint_rate',label:'Customer complaints',unit:'complaints',lower_is_better:true},
    {id:'new_products',label:'New halal-certified products launched',unit:'products',lower_is_better:false},
    {id:'consumer_trust_score',label:'Consumer trust / brand score',unit:'/10',lower_is_better:false},
    {id:'social_reach',label:'Social media reach',unit:'accounts',lower_is_better:false},
  ],
  wellness: [
    {id:'monthly_appointments',label:'Total appointments this month',unit:'appointments',lower_is_better:false},
    {id:'booking_utilisation',label:'Appointment utilisation rate',unit:'%',lower_is_better:false},
    {id:'client_retention',label:'Client retention rate',unit:'%',lower_is_better:false},
    {id:'new_clients',label:'New clients',unit:'clients',lower_is_better:false},
    {id:'monthly_revenue',label:'Monthly revenue',unit:'₦',lower_is_better:false},
    {id:'avg_spend',label:'Average spend per client',unit:'₦',lower_is_better:false},
    {id:'records_compliance',label:'Patient records compliance',unit:'score',lower_is_better:false,description:'1=Non-compliant, 2=Partial, 3=Fully compliant'},
    {id:'consent_compliance',label:'Consent form compliance rate',unit:'%',lower_is_better:false},
    {id:'equipment_score',label:'Equipment condition score',unit:'/10',lower_is_better:false},
    {id:'clinic_score',label:'Clinic environment score',unit:'/10',lower_is_better:false},
    {id:'complaint_rate',label:'Patient complaints',unit:'complaints',lower_is_better:true},
    {id:'cancellation_rate',label:'Appointment cancellation rate',unit:'%',lower_is_better:true},
    {id:'referral_rate',label:'Referral rate',unit:'%',lower_is_better:false},
    {id:'google_rating',label:'Google review rating',unit:'stars',lower_is_better:false},
    {id:'training_completion',label:'Staff training completion',unit:'%',lower_is_better:false},
    {id:'treatment_outcome_score',label:'Treatment outcome satisfaction',unit:'/10',lower_is_better:false},
  ],
  retail: [
    {id:'monthly_revenue',label:'Monthly revenue',unit:'₦',lower_is_better:false},
    {id:'monthly_orders',label:'Total orders this month',unit:'orders',lower_is_better:false},
    {id:'avg_order_value',label:'Average order value',unit:'₦',lower_is_better:false},
    {id:'stock_accuracy',label:'Stock accuracy rate',unit:'%',lower_is_better:false},
    {id:'fulfilment_time',label:'Average order fulfilment time',unit:'days',lower_is_better:true},
    {id:'return_rate',label:'Return / refund rate',unit:'%',lower_is_better:true},
    {id:'customer_satisfaction',label:'Customer satisfaction score',unit:'/10',lower_is_better:false},
    {id:'new_customers',label:'New customers acquired',unit:'customers',lower_is_better:false},
    {id:'repeat_rate',label:'Repeat purchase rate',unit:'%',lower_is_better:false},
    {id:'display_score',label:'Display / storefront score',unit:'/10',lower_is_better:false},
    {id:'shrinkage',label:'Stock shrinkage rate',unit:'%',lower_is_better:true},
    {id:'complaint_rate',label:'Customer complaints',unit:'complaints',lower_is_better:true},
    {id:'social_followers',label:'Social media followers',unit:'followers',lower_is_better:false},
    {id:'conversion_rate',label:'Online conversion rate',unit:'%',lower_is_better:false},
    {id:'profit_margin',label:'Net profit margin',unit:'%',lower_is_better:false},
    {id:'sell_through_rate',label:'Sell-through rate',unit:'%',lower_is_better:false},
  ],
  corporate: [
    {id:'project_delivery_rate',label:'Project on-time delivery rate',unit:'%',lower_is_better:false},
    {id:'monthly_revenue',label:'Monthly revenue / billing',unit:'₦',lower_is_better:false},
    {id:'client_satisfaction',label:'Client satisfaction score',unit:'/10',lower_is_better:false},
    {id:'process_adherence',label:'Process adherence score',unit:'/10',lower_is_better:false},
    {id:'deadline_compliance',label:'Internal deadline compliance',unit:'%',lower_is_better:false},
    {id:'staff_utilisation',label:'Staff utilisation rate',unit:'%',lower_is_better:false},
    {id:'reporting_accuracy',label:'Internal reporting accuracy',unit:'score',lower_is_better:false,description:'1=Gaps, 2=Mostly accurate, 3=Fully accurate'},
    {id:'complaint_rate',label:'Client complaints',unit:'complaints',lower_is_better:true},
    {id:'training_completion',label:'Staff training completion rate',unit:'%',lower_is_better:false},
    {id:'new_business',label:'New business won',unit:'₦',lower_is_better:false},
    {id:'team_performance',label:'Team performance score',unit:'/10',lower_is_better:false},
    {id:'overdue_items',label:'Overdue action items',unit:'items',lower_is_better:true},
    {id:'profit_margin',label:'Net profit margin',unit:'%',lower_is_better:false},
    {id:'repeat_clients',label:'Client retention rate',unit:'%',lower_is_better:false},
  ],
  logistics: [
    {id:'monthly_revenue',label:'Monthly revenue',unit:'₦',lower_is_better:false},
    {id:'deliveries_completed',label:'Deliveries completed',unit:'deliveries',lower_is_better:false},
    {id:'on_time_rate',label:'On-time delivery rate',unit:'%',lower_is_better:false},
    {id:'failed_deliveries',label:'Failed / returned deliveries',unit:'deliveries',lower_is_better:true},
    {id:'avg_delivery_time',label:'Average delivery time',unit:'hours',lower_is_better:true},
    {id:'fleet_utilisation',label:'Fleet utilisation rate',unit:'%',lower_is_better:false},
    {id:'fuel_cost_pct',label:'Fuel cost as % of revenue',unit:'%',lower_is_better:true},
    {id:'driver_count',label:'Active drivers',unit:'drivers',lower_is_better:false},
    {id:'driver_retention',label:'Driver retention rate',unit:'%',lower_is_better:false},
    {id:'customer_satisfaction',label:'Customer satisfaction score',unit:'/10',lower_is_better:false},
    {id:'complaint_rate',label:'Complaints this month',unit:'complaints',lower_is_better:true},
    {id:'new_clients',label:'New business clients',unit:'clients',lower_is_better:false},
    {id:'vehicle_downtime',label:'Vehicle downtime rate',unit:'%',lower_is_better:true},
    {id:'cost_per_delivery',label:'Cost per delivery',unit:'₦',lower_is_better:true},
    {id:'revenue_per_driver',label:'Revenue per driver',unit:'₦',lower_is_better:false},
    {id:'ops_compliance',label:'Operations compliance score',unit:'/10',lower_is_better:false},
  ],
  tech: [
    {id:'monthly_revenue',label:'Monthly revenue / MRR',unit:'₦',lower_is_better:false},
    {id:'active_users',label:'Active users / clients',unit:'users',lower_is_better:false},
    {id:'churn_rate',label:'Monthly churn rate',unit:'%',lower_is_better:true},
    {id:'new_signups',label:'New sign-ups this month',unit:'users',lower_is_better:false},
    {id:'uptime',label:'Platform uptime',unit:'%',lower_is_better:false},
    {id:'bug_resolution_time',label:'Average bug resolution time',unit:'hours',lower_is_better:true},
    {id:'customer_satisfaction',label:'Customer satisfaction (CSAT)',unit:'/10',lower_is_better:false},
    {id:'support_response_time',label:'Support response time',unit:'hours',lower_is_better:true},
    {id:'ticket_resolution_rate',label:'Support ticket resolution rate',unit:'%',lower_is_better:false},
    {id:'feature_delivery_rate',label:'Feature delivery on schedule',unit:'%',lower_is_better:false},
    {id:'nps',label:'Net Promoter Score (NPS)',unit:'score',lower_is_better:false},
    {id:'complaint_rate',label:'Escalated complaints',unit:'complaints',lower_is_better:true},
    {id:'team_utilisation',label:'Team utilisation rate',unit:'%',lower_is_better:false},
    {id:'runway',label:'Cash runway',unit:'months',lower_is_better:false},
    {id:'burn_rate',label:'Monthly burn rate',unit:'₦',lower_is_better:true},
    {id:'ops_score',label:'Ops & process audit score',unit:'/10',lower_is_better:false},
  ],
  homebrand: [
    {id:'monthly_revenue',label:'Monthly revenue',unit:'₦',lower_is_better:false},
    {id:'orders_fulfilled',label:'Orders fulfilled',unit:'orders',lower_is_better:false},
    {id:'avg_order_value',label:'Average order value',unit:'₦',lower_is_better:false},
    {id:'fulfilment_time',label:'Average fulfilment time',unit:'hours',lower_is_better:true},
    {id:'return_rate',label:'Return / refund rate',unit:'%',lower_is_better:true},
    {id:'delivery_success_rate',label:'Delivery success rate',unit:'%',lower_is_better:false},
    {id:'repeat_customers',label:'Repeat customer rate',unit:'%',lower_is_better:false},
    {id:'new_customers',label:'New customers acquired',unit:'customers',lower_is_better:false},
    {id:'ig_followers',label:'Instagram followers',unit:'followers',lower_is_better:false},
    {id:'ig_engagement',label:'IG engagement rate',unit:'%',lower_is_better:false},
    {id:'whatsapp_broadcast_reach',label:'WhatsApp broadcast reach',unit:'contacts',lower_is_better:false},
    {id:'complaint_rate',label:'Customer complaints',unit:'complaints',lower_is_better:true},
    {id:'stock_accuracy',label:'Stock accuracy rate',unit:'%',lower_is_better:false},
    {id:'packaging_score',label:'Packaging & presentation score',unit:'/10',lower_is_better:false},
    {id:'profit_margin',label:'Net profit margin',unit:'%',lower_is_better:false},
    {id:'referral_rate',label:'Referral rate',unit:'%',lower_is_better:false},
  ],
  delivery: [
    {id:'monthly_revenue',label:'Monthly revenue',unit:'₦',lower_is_better:false},
    {id:'deliveries_completed',label:'Total deliveries completed',unit:'deliveries',lower_is_better:false},
    {id:'on_time_rate',label:'On-time delivery rate',unit:'%',lower_is_better:false},
    {id:'failed_rate',label:'Failed delivery rate',unit:'%',lower_is_better:true},
    {id:'avg_delivery_time',label:'Average delivery time',unit:'mins',lower_is_better:true},
    {id:'active_riders',label:'Active riders',unit:'riders',lower_is_better:false},
    {id:'rider_retention',label:'Rider retention rate',unit:'%',lower_is_better:false},
    {id:'rider_earnings_avg',label:'Average rider earnings',unit:'₦',lower_is_better:false},
    {id:'customer_rating',label:'Average customer rating',unit:'stars',lower_is_better:false},
    {id:'complaint_rate',label:'Delivery complaints',unit:'complaints',lower_is_better:true},
    {id:'cost_per_delivery',label:'Cost per delivery',unit:'₦',lower_is_better:true},
    {id:'revenue_per_rider',label:'Revenue per rider',unit:'₦',lower_is_better:false},
    {id:'vehicle_compliance',label:'Vehicle compliance rate',unit:'%',lower_is_better:false,description:'% of riders with valid insurance, licence, roadworthiness'},
    {id:'ops_score',label:'Ops audit score',unit:'/10',lower_is_better:false},
    {id:'training_completion',label:'Rider training completion rate',unit:'%',lower_is_better:false},
  ],
  event_planner: [
    {id:'events_delivered',label:'Events delivered this month',unit:'events',lower_is_better:false},
    {id:'monthly_revenue',label:'Monthly revenue',unit:'₦',lower_is_better:false},
    {id:'avg_event_value',label:'Average event value',unit:'₦',lower_is_better:false},
    {id:'client_satisfaction',label:'Post-event client satisfaction',unit:'/10',lower_is_better:false},
    {id:'budget_adherence',label:'Events delivered on budget',unit:'%',lower_is_better:false},
    {id:'timeline_adherence',label:'Events delivered on schedule',unit:'%',lower_is_better:false},
    {id:'vendor_reliability',label:'Vendor on-time rate',unit:'%',lower_is_better:false},
    {id:'new_clients',label:'New clients acquired',unit:'clients',lower_is_better:false},
    {id:'repeat_clients',label:'Repeat client bookings',unit:'bookings',lower_is_better:false},
    {id:'complaint_rate',label:'Post-event complaints',unit:'complaints',lower_is_better:true},
    {id:'deposit_collection',label:'Deposits collected on time',unit:'%',lower_is_better:false},
    {id:'supplier_payments',label:'Supplier payments on time',unit:'%',lower_is_better:false},
    {id:'ig_followers',label:'Instagram followers',unit:'followers',lower_is_better:false},
    {id:'referral_bookings',label:'Bookings from referrals',unit:'bookings',lower_is_better:false},
    {id:'profit_margin',label:'Net profit margin',unit:'%',lower_is_better:false},
    {id:'pending_events',label:'Events in pipeline',unit:'events',lower_is_better:false},
  ],
  general: [
    {id:'monthly_revenue',label:'Monthly revenue',unit:'₦',lower_is_better:false},
    {id:'client_satisfaction',label:'Client satisfaction score',unit:'/10',lower_is_better:false},
    {id:'ops_score',label:'Operations audit score',unit:'/10',lower_is_better:false},
    {id:'staff_training',label:'Staff training completion',unit:'%',lower_is_better:false},
    {id:'complaint_rate',label:'Complaints this month',unit:'complaints',lower_is_better:true},
    {id:'action_completion',label:'Action item completion rate',unit:'%',lower_is_better:false},
  ],
};
const nicheFields={
  cafe:[
    {id:'kitchen_speed',label:'Avg ticket time (mins)',type:'text',placeholder:'e.g. 14 mins'},
    {id:'food_temp',label:'Food temperature compliance',type:'select',options:['Pass','Fail','Partial']},
    {id:'display_counter',label:'Display counter standard (0–10)',type:'range'},
    {id:'complimentary_item',label:'Complimentary item arrival time (mins)',type:'text',placeholder:'e.g. 2 mins'},
    {id:'wc_standard',label:'WC cleanliness (0–10)',type:'range'},
    {id:'kids_corner',label:'Kids corner condition',type:'select',options:['Excellent','Good','Needs attention','N/A']},
    {id:'menu_availability',label:'Menu item availability vs listed',type:'textarea',placeholder:'Note unavailable items…'},
    {id:'event_zone',label:'Event zone condition (if applicable)',type:'textarea'},
  ],
  aesthetics:[
    {id:'treatment_protocol',label:'Treatment protocol compliance (0–10)',type:'range'},
    {id:'sterilisation',label:'Equipment sterilisation standard',type:'select',options:['Pass','Fail','Partial']},
    {id:'consent_forms',label:'Consent forms in use',type:'select',options:['Yes — all','Partial','No']},
    {id:'product_storage',label:'Product storage and labelling',type:'textarea'},
    {id:'nafdac',label:'NAFDAC compliance',type:'select',options:['Compliant','Non-compliant','Partial']},
    {id:'booking_system',label:'Booking system accurate',type:'select',options:['Yes','No','Partially']},
  ],
  hair:[
    {id:'product_display',label:'Product display standard (0–10)',type:'range'},
    {id:'halal_cert',label:'Halal certification visible',type:'select',options:['Yes','No','N/A']},
    {id:'product_knowledge',label:'Staff product knowledge (0–10)',type:'range'},
    {id:'stock_notes',label:'Stock levels — flag shortages',type:'textarea'},
    {id:'upsell',label:'Upselling observed',type:'select',options:['Yes','No','Partially']},
  ],
  education:[
    {id:'classroom',label:'Classroom condition (0–10)',type:'range'},
    {id:'attendance',label:'Attendance tracking in use',type:'select',options:['Yes','No','Partially']},
    {id:'fees',label:'Fees collection issues',type:'textarea'},
    {id:'safeguarding',label:'Safeguarding protocols',type:'select',options:['Compliant','Non-compliant','Needs review']},
    {id:'uniform',label:'Uniform compliance (%)',type:'text',placeholder:'e.g. 92%'},
  ],
  faith:[
    {id:'facilities',label:'Facilities standard (0–10)',type:'range'},
    {id:'wudu',label:'Wudu/ablution area condition',type:'select',options:['Excellent','Good','Needs attention','N/A']},
    {id:'prayer_schedule',label:'Prayer schedule displayed',type:'select',options:['Yes','No','N/A']},
    {id:'volunteers',label:'Volunteer management observations',type:'textarea'},
    {id:'event_readiness',label:'Upcoming event readiness',type:'textarea'},
  ],
  events:[
    {id:'setup_time',label:'Setup completed on time',type:'select',options:['Yes','No','Partial']},
    {id:'allergen_form',label:'Allergen form collected and used',type:'select',options:['Yes — all guests','Partial','No']},
    {id:'packaging',label:'Branded packaging standard (0–10)',type:'range'},
    {id:'staff_uniform',label:'Staff uniform compliance',type:'select',options:['Full','Partial','Non-compliant']},
    {id:'food_temp_event',label:'Food temperature on arrival',type:'select',options:['Hot items hot · cold items cold','Issues noted']},
    {id:'teardown',label:'Teardown standard',type:'select',options:['Clean exit','Minor issues','Issues noted']},
    {id:'client_feedback',label:'Client feedback post-event',type:'textarea'},
  ],
  hospitality:[
    {id:'checkin',label:'Check-in process (0–10)',type:'range'},
    {id:'cleanliness',label:'Property cleanliness (0–10)',type:'range'},
    {id:'amenities',label:'Amenity stock levels',type:'select',options:['Fully stocked','Partially','Low stock']},
    {id:'maintenance',label:'Maintenance issues',type:'textarea'},
    {id:'listing',label:'Listing accuracy',type:'select',options:['Accurate','Minor discrepancies','Major issues']},
  ],
  media:[
    {id:'delivery',label:'Project delivery on time (0–10)',type:'range'},
    {id:'brief',label:'Brief adherence',type:'select',options:['Fully met','Partially','Not met']},
    {id:'files',label:'Asset handover / file management',type:'select',options:['Organised','Partially','Disorganised']},
    {id:'client_satisfaction',label:'Client satisfaction (0–10)',type:'range'},
  ],
  halal:[
    {id:'cert_current',label:'Halal certification current',type:'select',options:['Yes','Expired','Not in place']},
    {id:'ingredients',label:'Ingredients halal verified',type:'select',options:['Fully','Partial','Not verified']},
    {id:'cross_contamination',label:'Cross-contamination protocol',type:'select',options:['Strict','Partial','None']},
    {id:'supplier_check',label:'Supplier last checked',type:'text',placeholder:'e.g. March 2026'},
    {id:'labelling',label:'Halal mark on products',type:'select',options:['All products','Some','None']},
    {id:'staff_halal',label:'Staff trained on halal standards',type:'select',options:['All staff','Partial','Not trained']},
  ],
  wellness:[
    {id:'clinic',label:'Clinic environment (0–10)',type:'range'},
    {id:'records',label:'Patient records management',type:'select',options:['Fully compliant','Partial','Non-compliant']},
    {id:'equipment',label:'Equipment condition',type:'select',options:['Good','Needs attention','Non-functional items']},
    {id:'consent',label:'Consent and confidentiality protocols',type:'select',options:['In place','Partial','Absent']},
  ],
  retail:[
    {id:'stock',label:'Stock accuracy (system vs physical)',type:'text',placeholder:'e.g. 97%'},
    {id:'display',label:'Display standard (0–10)',type:'range'},
    {id:'fulfilment',label:'Order fulfilment time (avg days)',type:'text'},
    {id:'returns',label:'Returns/complaints rate (%)',type:'text'},
  ],
  corporate:[
    {id:'process',label:'Process adherence (0–10)',type:'range'},
    {id:'team',label:'Team performance observation',type:'textarea'},
    {id:'deadlines',label:'Deadline compliance (%)',type:'text'},
    {id:'reporting',label:'Internal reporting accuracy',type:'select',options:['Accurate','Some gaps','Significant gaps']},
  ],
  logistics:[
    {id:'on_time_rate',label:'On-time delivery rate (%)',type:'text',placeholder:'e.g. 94%'},
    {id:'failed_deliveries',label:'Failed deliveries this period',type:'text'},
    {id:'fleet_condition',label:'Fleet condition score (0–10)',type:'range'},
    {id:'driver_conduct',label:'Driver conduct observation',type:'textarea'},
    {id:'route_efficiency',label:'Route efficiency notes',type:'textarea'},
    {id:'vehicle_compliance',label:'Vehicle compliance (insurance/docs)',type:'select',options:['Fully compliant','Partial','Non-compliant']},
  ],
  tech:[
    {id:'uptime',label:'Platform uptime (%)',type:'text',placeholder:'e.g. 99.8%'},
    {id:'open_bugs',label:'Open bugs / issues',type:'text'},
    {id:'support_response',label:'Avg support response time (hours)',type:'text'},
    {id:'team_delivery',label:'Team delivery vs sprint plan',type:'select',options:['On track','Slightly delayed','Significantly delayed']},
    {id:'process_adherence',label:'Process adherence score (0–10)',type:'range'},
    {id:'documentation',label:'Documentation / SOPs status',type:'select',options:['Up to date','Needs update','Missing']},
  ],
  homebrand:[
    {id:'fulfilment_time',label:'Avg fulfilment time (hours)',type:'text'},
    {id:'packaging_score',label:'Packaging standard (0–10)',type:'range'},
    {id:'stock_notes',label:'Stock level observations',type:'textarea'},
    {id:'delivery_partner',label:'Delivery partner performance',type:'select',options:['Reliable','Some issues','Consistent problems']},
    {id:'ig_consistency',label:'IG content consistency (0–10)',type:'range'},
    {id:'customer_comms',label:'Customer communication quality (0–10)',type:'range'},
  ],
  delivery:[
    {id:'on_time_rate',label:'On-time delivery rate (%)',type:'text'},
    {id:'rider_uniform',label:'Rider uniform / presentation',type:'select',options:['Full compliance','Partial','Non-compliant']},
    {id:'vehicle_condition',label:'Vehicle condition (0–10)',type:'range'},
    {id:'rider_conduct',label:'Rider conduct observations',type:'textarea'},
    {id:'route_adherence',label:'Route adherence',type:'select',options:['Fully followed','Deviations noted','Significant issues']},
    {id:'customer_rating',label:'Average customer rating (stars)',type:'text'},
  ],
  event_planner:[
    {id:'events_in_period',label:'Events delivered this period',type:'text'},
    {id:'budget_adherence',label:'On-budget delivery rate (%)',type:'text'},
    {id:'vendor_issues',label:'Vendor issues observed',type:'textarea'},
    {id:'client_satisfaction',label:'Client satisfaction (0–10)',type:'range'},
    {id:'documentation_score',label:'Event documentation score (0–10)',type:'range'},
    {id:'pending_pipeline',label:'Events in pipeline / upcoming',type:'text'},
  ],
  general:[],
};

const typeFields={
  audit:[
    {id:'ops_score',label:'Operations Score (0–10)',type:'range'},
    {id:'hygiene_score',label:'Hygiene & Safety (0–10)',type:'range'},
    {id:'compliance_score',label:'Compliance (0–10)',type:'range'},
    {id:'staff_score',label:'Staff & Conduct (0–10)',type:'range'},
    {id:'vendor_notes',label:'Vendor Observations',type:'textarea'},
    {id:'inventory_notes',label:'Inventory & Stock Notes',type:'textarea'},
    {id:'incident_log',label:'Incidents / Flags',type:'textarea'},
  ],
  supervision:[
    {id:'supervisor_name',label:'Supervisor Name',type:'text'},
    {id:'supervision_type',label:'Type',type:'select',options:['Remote','On-site','Spot Check','Scheduled']},
    {id:'staff_present',label:'Staff Present',type:'text'},
    {id:'performance_score',label:'Overall Performance (0–10)',type:'range'},
    {id:'observations',label:'Detailed Observations',type:'textarea'},
    {id:'areas_reviewed',label:'Areas Reviewed',type:'textarea'},
  ],
  site_visit:[
    {id:'visit_number',label:'Visit Number This Month',type:'text',placeholder:'e.g. Visit 2 of 4'},
    {id:'arrival_time',label:'Arrival Time',type:'text',placeholder:'e.g. 10:15am'},
    {id:'departure_time',label:'Departure Time',type:'text'},
    {id:'staff_on_duty',label:'Staff On Duty',type:'textarea'},
    {id:'opening_standard',label:'Opening standard met',type:'select',options:['Yes — full compliance','Partial','No']},
    {id:'floor_observation',label:'Floor Observation',type:'textarea'},
    {id:'staff_conduct',label:'Staff Conduct (0–10)',type:'range'},
    {id:'customer_interaction',label:'Customer Interaction Quality (0–10)',type:'range'},
    {id:'immediate_actions',label:'Actions Taken On-Site',type:'textarea'},
    {id:'follow_up',label:'Follow-Up Required',type:'textarea'},
  ],
  cx_mystery:[
    {id:'visit_time',label:'Time of Visit',type:'text'},
    {id:'greeting_secs',label:'Greeting Time (seconds)',type:'text'},
    {id:'greeting_score',label:'Greeting Quality (0–10)',type:'range'},
    {id:'complimentary_time',label:'Complimentary item arrival (mins)',type:'text'},
    {id:'menu_knowledge',label:'Staff Menu Knowledge (0–10)',type:'range'},
    {id:'allergen_handling',label:'Allergen query handled',type:'select',options:['Yes — correct immediately','Had to check','Incorrect / no response']},
    {id:'food_quality',label:'Food / Product Quality (0–10)',type:'range'},
    {id:'ticket_time',label:'Order to delivery (mins)',type:'text'},
    {id:'check_back',label:'Check-back within 3 mins',type:'select',options:['Yes','No']},
    {id:'billing_exp',label:'Billing Experience (0–10)',type:'range'},
    {id:'exit_standard',label:'Farewell acknowledged',type:'select',options:['Yes','No']},
    {id:'cx_overall',label:'Overall CX Score (0–10)',type:'range'},
    {id:'cx_narrative',label:'Full Narrative',type:'textarea',placeholder:'Walk through the full experience from entry to exit…'},
  ],
  staff_training:[
    {id:'training_type',label:'Training Type',type:'select',options:['Initial Onboarding','Refresher','New Menu / Product','Service Standard','Allergen & Safety','Brand Standards','Disciplinary Follow-up']},
    {id:'staff_trained',label:'Staff Trained (names / roles)',type:'textarea'},
    {id:'trainer',label:'Trainer Name',type:'text'},
    {id:'duration',label:'Duration',type:'text',placeholder:'e.g. 3 hours'},
    {id:'modules',label:'Modules Covered',type:'textarea'},
    {id:'assessment',label:'Assessment Conducted',type:'select',options:['Yes','No']},
    {id:'pass_rate',label:'Pass Rate (%)',type:'text'},
    {id:'weak_areas',label:'Areas Needing Reinforcement',type:'textarea'},
    {id:'retraining',label:'Staff Flagged for Retraining',type:'textarea'},
    {id:'training_score',label:'Training Session Quality (0–10)',type:'range'},
  ],
  brand_review:[
    {id:'period',label:'Review Period',type:'text',placeholder:'e.g. June 2026'},
    {id:'ig_score',label:'Instagram Content Quality (0–10)',type:'range'},
    {id:'ig_posts',label:'Instagram Posts This Period',type:'text'},
    {id:'tt_score',label:'TikTok Content Quality (0–10)',type:'range'},
    {id:'tt_posts',label:'TikTok Posts This Period',type:'text'},
    {id:'caption_standard',label:'Caption Style On-Brand',type:'select',options:['Fully on-brand','Mostly on-brand','Needs correction']},
    {id:'photo_standard',label:'Photography Standard',type:'select',options:['Premium','Acceptable','Below standard']},
    {id:'brand_voice',label:'Brand Voice Consistency (0–10)',type:'range'},
    {id:'google_reviews',label:'Google Reviews (count & rating)',type:'text',placeholder:'e.g. 14 reviews · 4.8 stars'},
    {id:'influencer',label:'Influencer / PR Activity',type:'textarea'},
    {id:'to_fix',label:'Content That Must Be Corrected',type:'textarea'},
    {id:'next_direction',label:'Direction for Next Month',type:'textarea'},
  ],
  performance:[
    {id:'period',label:'Period',type:'text',placeholder:'e.g. May 2026'},
    {id:'ops_score',label:'Operations (0–10)',type:'range'},
    {id:'customer_score',label:'Customer Service (0–10)',type:'range'},
    {id:'staff_score',label:'Staff Performance (0–10)',type:'range'},
    {id:'financial_notes',label:'Financial / Revenue Notes',type:'textarea'},
    {id:'highlights',label:'Highlights & Wins',type:'textarea'},
    {id:'concerns',label:'Areas of Concern',type:'textarea'},
  ],
  kpi:[
    {id:'period',label:'KPI Period',type:'text',placeholder:'e.g. Q2 2026'},
    {id:'revenue_target',label:'Revenue Target (₦)',type:'text'},
    {id:'revenue_actual',label:'Revenue Actual (₦)',type:'text'},
    {id:'kpi_score',label:'Overall KPI Score (0–10)',type:'range'},
    {id:'compliance_rate',label:'Compliance Rate (%)',type:'text'},
    {id:'audit_pass_rate',label:'Audit Pass Rate (%)',type:'text'},
    {id:'kpi_notes',label:'KPI Commentary',type:'textarea'},
    {id:'targets_next',label:'Targets for Next Period',type:'textarea'},
  ],
};

// ══ LOGIN ══
async function handleLogin(){
  const user=document.getElementById('login-user').value.trim();
  const pass=document.getElementById('login-pass').value.trim();
  const err=document.getElementById('login-error');
  err.textContent='';
  if(!user||!pass){err.textContent='Please enter your username and access code.';return;}
  const {data:admins,error:ae}=await db.from('admin_users').select('*').eq('username',user).eq('password_hash',pass).single();
  if(admins&&!ae){currentUser={role:admins.is_owner?'owner':'admin',name:admins.full_name||user,isOwner:!!admins.is_owner};showApp();return;}
  const {data:client,error:ce}=await db.from('clients').select('*').eq('username',user).eq('access_code',pass).single();
  if(client&&!ce){currentUser={role:'client',name:client.business_name,clientId:client.id,package:client.package};showClientPortal(client);return;}
  err.textContent='Incorrect username or access code.';
}

function showApp(){
  document.getElementById('login-screen').style.display='none';
  document.getElementById('app').style.display='flex';
  document.getElementById('admin-nav').style.display='flex';
  document.getElementById('topbar-user').textContent=currentUser.name;
  if(currentUser.isOwner){
    document.getElementById('finance-section').style.cssText='display:block';
    document.getElementById('invoices-tab-btn').style.cssText='display:block';
    document.getElementById('approvals-tab-btn').style.display='';
    document.getElementById('expenditure-tab-btn').style.display='';
    document.getElementById('admin-section').style.display='';
    document.getElementById('onboarding-tab-btn').style.display='';
    loadInvoices();
    loadApprovals();
    loadExpenditure();
    loadNotifications();
    // Show push enable button
    const pushBtn = document.getElementById('push-enable-btn');
    if(pushBtn) pushBtn.style.display = 'inline';
    // Auto-init if already permitted
    if('Notification' in window && Notification.permission === 'granted'){
      setTimeout(initPushForOwner, 1000);
      if(pushBtn) pushBtn.style.display = 'none';
      const ind = document.getElementById('push-active-indicator');
      if(ind) ind.style.display = 'inline';
    } else {
      setTimeout(initPushForOwner, 1500);
    }
  }
  loadClients();loadComplaints();loadActions();loadStaff();loadContracts();loadDocs();loadEvents();
  renderTypeFields();
}

async function showClientPortal(client){
  document.getElementById('login-screen').style.display='none';
  document.getElementById('app').style.display='flex';
  // CRITICAL: clients must never see admin nav
  document.getElementById('admin-nav').style.display='none';
  document.getElementById('topbar-user').textContent=client.business_name;
  showPage('page-portal');
  document.getElementById('portal-welcome').textContent='Welcome, '+client.business_name;
  document.getElementById('portal-package').textContent=client.package+' Package';
  await Promise.all([
    loadPortalReports(client.id),
    loadPortalInvoices(client.id),
    loadPortalComplaints(client.id),
    loadPortalDocs(client.id),
    loadPortalActions(client.id),
    loadPortalRequests(client.id),
    loadPortalStaff(client.id),
  ]);
}

function logout(){currentUser=null;document.getElementById('app').style.display='none';document.getElementById('login-screen').style.display='flex';document.getElementById('login-user').value='';document.getElementById('login-pass').value='';}

// ══ CLIENTS ══
async function loadClients(){
  if(!currentUser||currentUser.role==='client') return; // clients never load full client list
  const {data}=await db.from('clients').select('*,reports(count)').neq('status','Inactive').order('created_at',{ascending:false});
  allClients=data||[];renderClientsTable();populateAllDropdowns();
}

function renderClientsTable(){
  const tbody=document.getElementById('clients-tbody');
  if(!allClients.length){tbody.innerHTML='<tr><td colspan="7" style="text-align:center;padding:24px;color:var(--muted);font-family:Arial">No clients yet.</td></tr>';return;}
  const sb={Active:'badge-green',Onboarding:'badge-gold',Paused:'badge-gray'};
  tbody.innerHTML=allClients.map(c=>`<tr>
    <td><strong style="font-size:14px">${c.business_name}</strong><br><span style="font-family:Arial;font-size:11px;color:var(--muted)">${c.contact_person||''}</span></td>
    <td><span style="font-family:Arial;font-size:12px;color:var(--muted)">${nicheLabels[c.niche]||'—'}</span></td>
    <td style="font-family:Arial;font-size:12px">${c.package}</td>
    <td><span class="badge ${sb[c.status]||'badge-gray'}">${c.status}</span></td>
    <td style="font-family:Arial;font-size:13px;text-align:center">${(c.reports&&c.reports[0]&&c.reports[0].count)||0}</td>
    <td style="font-family:Arial;font-size:12px;color:var(--muted)">—</td>
    <td style="white-space:nowrap">
      <button class="btn-sm" onclick="goAddReport('${c.id}')">+ Report</button>
      <button class="btn-sm" onclick="viewClient('${c.id}')">View</button>
      <button class="btn-danger" onclick="deleteClient('${c.id}','${c.business_name}')">Remove</button>
    </td>
  </tr>`).join('');
}

function populateAllDropdowns(){
  // Only populate for admin/owner — never for clients
  if(!currentUser||currentUser.role==='client') return;
  const opts='<option value="">Select client…</option>'+allClients.map(c=>`<option value="${c.id}">${c.business_name}</option>`).join('');
  ['rf-client','inv-client','comp-client','act-client','stf-client','con-client','doc-client','onb-client-select','exp-client','ev-client'].forEach(id=>{const el=document.getElementById(id);if(el)el.innerHTML=opts.replace('<option value="">Select client…</option>','<option value="">None — internal cost</option>');});
  // Restore proper placeholder for other dropdowns
  ['rf-client','inv-client','comp-client','act-client','stf-client','con-client','doc-client','onb-client-select'].forEach(id=>{const el=document.getElementById(id);if(el)el.innerHTML=opts;});
}

async function saveClient(){
  const name=document.getElementById('nc-name').value.trim();
  const contact=document.getElementById('nc-contact').value.trim();
  const username=document.getElementById('nc-username').value.trim();
  const code=document.getElementById('nc-password').value.trim();
  const fb=document.getElementById('client-feedback');
  if(!name||!username||!code){fb.innerHTML='<div class="error-msg">Fill in all required fields.</div>';return;}
  const {error}=await db.from('clients').insert({business_name:name,contact_person:contact,username,access_code:code,package:document.getElementById('nc-package').value,status:document.getElementById('nc-status').value,niche:document.getElementById('nc-niche').value});
  if(error){fb.innerHTML=`<div class="error-msg">${error.message}</div>`;return;}
  fb.innerHTML='<div class="success-msg">✓ Client added.</div>';
  setTimeout(()=>{closeModal('modal-add-client');loadClients();fb.innerHTML='';['nc-name','nc-contact','nc-username','nc-password'].forEach(id=>document.getElementById(id).value='');},1200);
}

async function deleteClient(id,name){
  if(!confirm(`Remove ${name}? All their data will be deleted.`))return;
  await Promise.all([
    db.from('reports').delete().eq('client_id',id),
    db.from('invoices').delete().eq('client_id',id),
    db.from('complaints').delete().eq('client_id',id),
    db.from('actions').delete().eq('client_id',id),
    db.from('client_staff').delete().eq('client_id',id),
    db.from('contracts').delete().eq('client_id',id),
    db.from('documents').delete().eq('client_id',id),
    db.from('compliance_status').delete().eq('client_id',id),
    db.from('ic_staff_tests').delete().eq('client_id',id),
    db.from('ic_facility_checks').delete().eq('client_id',id),
    db.from('risk_assessments').delete().eq('client_id',id),
    db.from('training_records').delete().eq('client_id',id),
    db.from('checks_balances').delete().eq('client_id',id),
  ]);
  await db.from('clients').delete().eq('id',id);
  loadClients();
}

// viewClient is now defined above in renderReportList section

function goAddReport(clientId){
  showAdminTabByName('reports');
  setTimeout(()=>{
    document.getElementById('rf-client').value=clientId;
    renderTypeFields();
    // After save, return to client view
    _currentViewClientId=clientId;
  },150);
}

// ══ REPORT TYPES ══
function switchReportType(type,btn){reportType=type;document.querySelectorAll('.inner-tab').forEach(b=>b.classList.remove('active'));btn.classList.add('active');renderTypeFields();}

function getClientNiche(){const id=document.getElementById('rf-client').value;const c=allClients.find(x=>x.id===id);return c&&c.niche?c.niche:'general';}

async function generateFullReport(){
  const clientId=document.getElementById('rf-client').value;
  if(!clientId){alert('Please select a client first.');return;}
  const niche=getClientNiche();
  const {data:reports}=await db.from('reports').select('*').eq('client_id',clientId).order('date',{ascending:false});
  if(!reports||!reports.length){alert('No reports found for this client.');return;}
  const clientName=allClients.find(c=>c.id===clientId)?.business_name||'Client';
  const byType={};
  reports.forEach(r=>{if(!byType[r.report_type])byType[r.report_type]=[];byType[r.report_type].push(r);});
  const typeLabel={audit:'Audit Reports',supervision:'Supervision Reports',site_visit:'Site Visit Notes',cx_mystery:'CX Mystery Visits',staff_training:'Staff Training Records',brand_review:'Brand Reviews',performance:'Performance Reviews',kpi:'KPI Reviews'};
  let html=`<div style="font-family:Arial;padding:32px;max-width:700px;margin:0 auto">
    <div style="border-bottom:3px solid #644a3c;padding-bottom:16px;margin-bottom:24px">
      <div style="font-size:11px;letter-spacing:3px;text-transform:uppercase;color:#7e7b67;margin-bottom:4px">Haida Yammar Consulting</div>
      <div style="font-size:24px;font-weight:bold;color:#644a3c">${clientName} — Full Report</div>
      <div style="font-size:12px;color:#7e7b67;margin-top:4px">Niche: ${nicheLabels[niche]||niche} · Generated ${new Date().toLocaleDateString('en-GB',{day:'numeric',month:'long',year:'numeric'})}</div>
    </div>`;
  Object.entries(byType).forEach(([type,reps])=>{
    html+=`<div style="margin-bottom:28px"><div style="font-size:14px;font-weight:bold;color:#644a3c;border-bottom:1px solid #dbc9ba;padding-bottom:6px;margin-bottom:12px;text-transform:uppercase;letter-spacing:1px;font-size:11px">${typeLabel[type]||type} (${reps.length})</div>`;
    reps.slice(0,3).forEach(r=>{
      html+=`<div style="margin-bottom:12px;padding:12px;background:#fff2e6;border-radius:2px">
        <div style="font-weight:bold;font-size:13px;margin-bottom:4px">${r.title} — ${r.date}</div>
        ${r.summary?`<div style="font-size:12px;color:#5e4f46;line-height:1.6">${r.summary}</div>`:''}
        ${r.action_items?'<div style="font-size:11px;color:#7e7b67;margin-top:6px"><strong>Actions:</strong> '+r.action_items.split('\n').join(' · ')+'</div>':''}
      </div>`;
    });
    html+='</div>';
  });
  html+=`<div style="margin-top:32px;padding-top:16px;border-top:1px solid #dbc9ba;font-size:10px;color:#7e7b67;text-align:center">Haida Yammar Consulting · RC No: 9304448 · Confidential</div></div>`;
  document.getElementById('invoice-pdf-body').innerHTML=html;
  openModal('modal-invoice-pdf');
  document.querySelector('#modal-invoice-pdf .modal-title').textContent='Full Client Report';
}

function renderTypeFields(){
  const base=typeFields[reportType]||[];
  const niche=getClientNiche();
  const extra=nicheFields[niche]||[];
  const container=document.getElementById('type-fields');container.innerHTML='';
  const labels={audit:'Audit Scores & Observations',supervision:'Supervision Details',site_visit:'Site Visit Details',cx_mystery:'CX Mystery Visit Scoring',staff_training:'Training Session Details',brand_review:'Brand & Content Review',performance:'Performance Scores',kpi:'KPI Data'};
  document.getElementById('dynamic-sections').textContent=labels[reportType]||'';
  // Update niche banner
  const banner=document.getElementById('niche-report-banner');
  if(banner){
    if(niche&&niche!=='general'){banner.textContent=`${nicheLabels[niche]} — niche-specific fields included for this report type`;banner.style.background='var(--gold-light)';banner.style.color='var(--warn)';}
    else if(niche==='general'){banner.textContent='General client — standard fields only';}
    else{banner.textContent='Select a client to see niche-specific report fields';}
  }
  if(niche!=='general'&&extra.length){const b=document.createElement('div');b.style.cssText='background:var(--gold-light);border-left:3px solid var(--gold);padding:8px 14px;margin-bottom:12px;font-family:Arial;font-size:12px;color:var(--ink2);border-radius:2px;';b.textContent=`${nicheLabels[niche]} — niche fields included`;container.appendChild(b);}
  const all=[...base,...(extra.length?[{id:'_div',label:`── ${nicheLabels[niche]} Specific ──`,type:'divider'}]:[]),...extra];
  const grid=document.createElement('div');grid.className='form-grid';
  all.forEach(f=>{
    if(f.type==='divider'){const d=document.createElement('div');d.style.cssText='grid-column:1/-1;font-family:Arial;font-size:10px;letter-spacing:2px;text-transform:uppercase;color:var(--gold);margin-top:8px;padding-top:8px;border-top:1px solid var(--border);';d.textContent=f.label;grid.appendChild(d);return;}
    const w=document.createElement('div');w.className='form-field';
    if(f.type==='textarea'||f.type==='range')w.style.gridColumn='1/-1';
    w.innerHTML=`<label>${f.label}</label>`;
    if(f.type==='range')w.innerHTML+=`<div class="score-row"><input type="range" min="0" max="10" step="1" value="7" id="trf-${f.id}" oninput="this.nextElementSibling.textContent=this.value"><span class="score-val">7</span></div>`;
    else if(f.type==='textarea')w.innerHTML+=`<textarea id="trf-${f.id}" rows="3" placeholder="${f.placeholder||''}"></textarea>`;
    else if(f.type==='select')w.innerHTML+=`<select id="trf-${f.id}">${f.options.map(o=>`<option>${o}</option>`).join('')}</select>`;
    else w.innerHTML+=`<input type="text" id="trf-${f.id}" placeholder="${f.placeholder||''}">`;
    grid.appendChild(w);
  });
  container.appendChild(grid);
}

async function saveReport(){
  const clientId=document.getElementById('rf-client').value;const date=document.getElementById('rf-date').value;const title=document.getElementById('rf-title').value.trim();const fb=document.getElementById('form-feedback');
  if(!clientId||!date||!title){fb.innerHTML='<div class="error-msg">Please fill in client, date, and title.</div>';return;}
  const typeData={};(typeFields[reportType]||[]).concat(nicheFields[getClientNiche()]||[]).forEach(f=>{const el=document.getElementById('trf-'+f.id);if(el)typeData[f.id]=el.value;});
  const {error}=await db.from('reports').insert({client_id:clientId,date,title,location:document.getElementById('rf-location').value.trim(),report_type:reportType,summary:document.getElementById('rf-summary').value.trim(),action_items:document.getElementById('rf-actions').value.trim(),recommendations:document.getElementById('rf-recs').value.trim(),type_data:typeData});
  if(error){fb.innerHTML=`<div class="error-msg">${error.message}</div>`;return;}
  fb.innerHTML='<div class="success-msg">✓ Report saved.</div>';
  loadClients();
  // If we came from a client view, refresh it
  if(_currentViewClientId){
    setTimeout(()=>{showPage('page-client-view');loadClientViewTab('cv-reports');},800);
  }
  setTimeout(()=>fb.innerHTML='',3000);
}

function clearReportForm(){['rf-client','rf-date','rf-title','rf-location','rf-summary','rf-actions','rf-recs'].forEach(id=>{const el=document.getElementById(id);if(el)el.value='';});document.getElementById('form-feedback').innerHTML='';}

// ══ INVOICES ══
const PAYMENT_DETAILS={
  NGN:{sym:'₦',label:'Nigerian Naira',notes:'Payment to Haida Yammar Limited\nSummit Bank — 0123456789\nMoniepoint — 5400123456\nPlease use your invoice number as reference.'},
  GBP:{sym:'£',label:'British Pounds',notes:'Payment to Wahida Mohammed\nClear Bank\nSort code: 04-08-31\nAccount number: 09217518\nPlease use your invoice number as reference.'},
};
function getCurrSym(){const c=document.getElementById('inv-currency');return c?PAYMENT_DETAILS[c.value]||PAYMENT_DETAILS.NGN:PAYMENT_DETAILS.NGN;}
function setCurrencyDefaults(){
  const curr=getCurrSym();
  document.getElementById('inv-notes').value=curr.notes;
  const vatSel=document.getElementById('inv-vat-rate');
  if(document.getElementById('inv-currency').value==='GBP'){vatSel.value='0';}
  else{vatSel.value='7.5';}
  calcVAT();
}
function calcVAT(){
  const sub=parseFloat(document.getElementById('inv-amount').value)||0;
  const rate=parseFloat(document.getElementById('inv-vat-rate').value)||0;
  const vat=sub*(rate/100);
  const sym=getCurrSym().sym;
  document.getElementById('inv-vat-amount').value=sym+vat.toLocaleString('en',{minimumFractionDigits:2});
  document.getElementById('inv-total-inc').value=sym+(sub+vat).toLocaleString('en',{minimumFractionDigits:2});
}
function openAddInvoice(){
  document.getElementById('inv-edit-id').value='';
  document.getElementById('inv-modal-title').textContent='Create Invoice';
  document.getElementById('inv-save-btn').textContent='Save Invoice';
  ['inv-client','inv-number','inv-desc','inv-amount','inv-paid','inv-due','inv-date'].forEach(id=>{const el=document.getElementById(id);if(el)el.value='';});
  document.getElementById('inv-paid').value='0';
  document.getElementById('inv-currency').value='NGN';
  document.getElementById('inv-vat-rate').value='7.5';
  document.getElementById('inv-vat-amount').value='';
  document.getElementById('inv-total-inc').value='';
  document.getElementById('inv-status').value='Unpaid';
  document.getElementById('inv-notes').value=PAYMENT_DETAILS.NGN.notes;
  openModal('modal-add-invoice');
}
function openEditInvoice(inv){
  document.getElementById('inv-edit-id').value=inv.id;
  document.getElementById('inv-modal-title').textContent='Edit Invoice';
  document.getElementById('inv-save-btn').textContent='Update Invoice';
  document.getElementById('inv-client').value=inv.client_id||'';
  document.getElementById('inv-number').value=inv.invoice_number||'';
  document.getElementById('inv-desc').value=inv.description||'';
  document.getElementById('inv-amount').value=inv.amount||0;
  document.getElementById('inv-paid').value=inv.paid||0;
  document.getElementById('inv-due').value=inv.due_date||'';
  document.getElementById('inv-date').value=inv.invoice_date||'';
  document.getElementById('inv-status').value=inv.status||'Unpaid';
  document.getElementById('inv-notes').value=inv.notes||'';
  document.getElementById('inv-vat-rate').value=inv.vat_rate||7.5;
  document.getElementById('inv-currency').value=inv.currency||'NGN';
  document.getElementById('inv-notes').value=inv.notes||PAYMENT_DETAILS[inv.currency||'NGN'].notes;
  calcVAT();
  openModal('modal-add-invoice');
}
async function loadInvoices(){
  const {data}=await db.from('invoices').select('*,clients(business_name)').order('created_at',{ascending:false});
  renderInvoicesTable(data||[]);renderInvoiceStats(data||[]);
}
let _invMap={};
function renderInvoicesTable(invs){
  const tbody=document.getElementById('invoices-tbody');
  const isOwner=currentUser&&currentUser.isOwner;
  _invMap={};invs.forEach(i=>{_invMap[i.id]=i;});
  if(!invs.length){tbody.innerHTML='<tr><td colspan="9" style="text-align:center;padding:24px;color:var(--muted);font-family:Arial">No invoices yet.</td></tr>';return;}
  tbody.innerHTML=invs.map(inv=>{
    const sub=inv.amount||0;const vatR=inv.vat_rate||0;const vat=sub*(vatR/100);const total=sub+vat;
    const balance=total-(inv.paid||0);
    const sc=inv.status==='Paid'?'inv-status-paid':inv.status==='Partial'?'inv-status-partial':'inv-status-unpaid';
    const amtCells=isOwner
      ?`<td style="font-family:Arial;font-size:13px">₦${Number(sub).toLocaleString()}</td><td style="font-family:Arial;font-size:12px;color:var(--muted)">${vatR}% = ₦${Number(vat).toLocaleString()}</td><td style="font-family:Arial;font-size:13px;font-weight:600">₦${Number(total).toLocaleString()}</td><td style="font-family:Arial;font-size:13px;color:var(--success)">₦${Number(inv.paid||0).toLocaleString()}</td><td style="font-family:Arial;font-size:13px;color:${balance>0?"var(--danger)":"var(--success)"}">₦${Number(balance).toLocaleString()}</td>`
      :`<td colspan="5" style="font-family:Arial;font-size:12px;color:var(--muted);text-align:center;font-style:italic">Amounts visible to owner only</td>`;
    return `<tr>
      <td><strong style="font-family:Arial;font-size:13px">${inv.invoice_number||"—"}</strong><br><span style="font-family:Arial;font-size:11px;color:var(--muted)">${inv.description||""}</span></td>
      <td style="font-family:Arial;font-size:13px">${(inv.clients&&inv.clients.business_name)||"—"}</td>
      ${amtCells}
      <td style="font-family:Arial;font-size:12px">${inv.due_date||"—"}</td>
      <td><span class="${sc}" style="font-family:Arial;font-size:12px">${inv.status}</span></td>
      <td style="white-space:nowrap">
        ${isOwner?'<button class="btn-sm" onclick="openEditInvoice(_invMap[\''+inv.id+'\'])">Edit</button>':""}
        <button class="btn-sm" onclick="printInvoice(_invMap['${inv.id}'])">PDF</button>
        ${isOwner?`<button class="btn-danger" onclick="deleteRow('invoices','${inv.id}',loadInvoices)">Del</button>`:""}
      </td>
    </tr>`;
  }).join('');
}
function renderInvoiceStats(invs){
  const isOwner=currentUser&&currentUser.isOwner;
  if(!isOwner){document.getElementById('invoice-stats').innerHTML='';return;}
  const total=invs.reduce((s,i)=>{const sub=i.amount||0;const vat=sub*((i.vat_rate||0)/100);return s+sub+vat;},0);
  const paid=invs.reduce((s,i)=>s+(i.paid||0),0);
  const unpaid=invs.filter(i=>i.status==='Unpaid').length;
  const partial=invs.filter(i=>i.status==='Partial').length;
  document.getElementById('invoice-stats').innerHTML=`
    <div class="stat-box"><div class="stat-num">₦${(total/1000).toFixed(0)}k</div><div class="stat-label">Total (inc. VAT)</div></div>
    <div class="stat-box"><div class="stat-num">₦${(paid/1000).toFixed(0)}k</div><div class="stat-label">Collected</div></div>
    <div class="stat-box"><div class="stat-num" style="color:var(--danger)">₦${((total-paid)/1000).toFixed(0)}k</div><div class="stat-label">Outstanding</div></div>
    <div class="stat-box"><div class="stat-num" style="color:var(--danger)">${unpaid}</div><div class="stat-label">Unpaid</div></div>
    <div class="stat-box"><div class="stat-num" style="color:var(--warn)">${partial}</div><div class="stat-label">Partial</div></div>`;
}
async function saveInvoice(){
  const editId=document.getElementById('inv-edit-id').value;
  const clientId=document.getElementById('inv-client').value;const fb=document.getElementById('invoice-feedback');
  if(!clientId){fb.innerHTML='<div class="error-msg">Select a client.</div>';return;}
  const sub=parseFloat(document.getElementById('inv-amount').value)||0;
  const vatR=parseFloat(document.getElementById('inv-vat-rate').value)||0;
  const curr=document.getElementById('inv-currency').value||'NGN';
  const payload={client_id:clientId,invoice_number:document.getElementById('inv-number').value.trim(),description:document.getElementById('inv-desc').value.trim(),amount:sub,vat_rate:vatR,currency:curr,paid:parseFloat(document.getElementById('inv-paid').value)||0,due_date:document.getElementById('inv-due').value,invoice_date:document.getElementById('inv-date').value,status:document.getElementById('inv-status').value,notes:document.getElementById('inv-notes').value.trim()};
  let error;
  if(editId){({error}=await db.from('invoices').update(payload).eq('id',editId));}
  else{({error}=await db.from('invoices').insert(payload));}
  if(error){fb.innerHTML=`<div class="error-msg">${error.message}</div>`;return;}
  fb.innerHTML=`<div class="success-msg">Invoice ${editId?'updated':'saved'}.</div>`;loadInvoices();setTimeout(()=>{closeModal('modal-add-invoice');fb.innerHTML='';},1500);
}
function printInvoice(inv){
  const clientName=(inv.clients&&inv.clients.business_name)||allClients.find(c=>c.id===inv.client_id)?.business_name||'Client';
  const sub=inv.amount||0;const vatR=inv.vat_rate||0;const vat=sub*(vatR/100);const total=sub+vat;const balance=total-(inv.paid||0);
  const currKey=inv.currency||'NGN';const currInfo=PAYMENT_DETAILS[currKey]||PAYMENT_DETAILS.NGN;const sym=currInfo.sym;
  document.getElementById('invoice-pdf-body').innerHTML=`
  <div style="font-family:Arial,sans-serif;padding:40px">
    <div style="display:flex;justify-content:space-between;margin-bottom:32px;padding-bottom:20px;border-bottom:3px solid #644a3c">
      <div>
        <div style="font-size:20px;font-weight:bold;color:#644a3c;letter-spacing:2px">HAIDA YAMMAR</div>
        <div style="font-size:11px;color:#7e7b67;letter-spacing:2px;margin-bottom:8px">CONSULTING</div>
        <div style="font-size:11px;color:#7e7b67;line-height:1.8">Haida Yammar Limited<br>RC No: 9304448<br>Abuja, Nigeria<br>+234 908 271 1026</div>
      </div>
      <div style="text-align:right">
        <div style="font-size:26px;font-weight:bold;color:#644a3c">INVOICE</div>
        <div style="font-size:13px;color:#5e4f46;margin:4px 0"><strong>${inv.invoice_number||'—'}</strong></div>
        <div style="font-size:12px;color:#7e7b67">Invoice date: ${inv.invoice_date||'—'}</div>
        <div style="font-size:12px;color:#7e7b67">Due: ${inv.due_date||'—'}</div>
      </div>
    </div>
    <div style="margin-bottom:28px">
      <div style="font-size:10px;letter-spacing:2px;text-transform:uppercase;color:#7e7b67;margin-bottom:5px">Bill To</div>
      <div style="font-size:16px;font-weight:bold;color:#644a3c">${clientName}</div>
    </div>
    <table style="width:100%;border-collapse:collapse;margin-bottom:24px">
      <thead><tr style="background:#644a3c">
        <th style="padding:10px 14px;text-align:left;font-size:11px;letter-spacing:1px;color:white;text-transform:uppercase">Description</th>
        <th style="padding:10px 14px;text-align:right;font-size:11px;letter-spacing:1px;color:white;text-transform:uppercase">Amount</th>
      </tr></thead>
      <tbody>
        <tr style="border-bottom:1px solid #dbc9ba">
          <td style="padding:14px;font-size:14px">${inv.description||'Consulting services'}</td>
          <td style="padding:14px;font-size:14px;text-align:right">${sym}${Number(sub).toLocaleString()}</td>
        </tr>
      </tbody>
    </table>
    <div style="display:flex;justify-content:flex-end;margin-bottom:24px">
      <div style="width:280px">
        <div style="display:flex;justify-content:space-between;padding:8px 0;border-bottom:1px solid #dbc9ba;font-size:13px"><span style="color:#7e7b67">Subtotal</span><span>${sym}${Number(sub).toLocaleString()}</span></div>
        <div style="display:flex;justify-content:space-between;padding:8px 0;border-bottom:1px solid #dbc9ba;font-size:13px"><span style="color:#7e7b67">VAT @ ${vatR}%</span><span>${sym}${Number(vat).toLocaleString()}</span></div>
        <div style="display:flex;justify-content:space-between;padding:10px 0;font-size:15px;font-weight:bold;color:#644a3c"><span>Total (inc. VAT)</span><span>${sym}${Number(total).toLocaleString()}</span></div>
        <div style="display:flex;justify-content:space-between;padding:8px 0;border-top:1px solid #dbc9ba;font-size:13px;color:#1A5C38"><span>Amount Paid</span><span>${sym}${Number(inv.paid||0).toLocaleString()}</span></div>
        <div style="display:flex;justify-content:space-between;padding:10px 0;font-size:15px;font-weight:bold;color:${balance>0?"#8B1A1A":"#1A5C38"}"><span>Balance Due</span><span>${sym}${Number(balance).toLocaleString()}</span></div>
      </div>
    </div>
    ${inv.notes?`<div style="background:#fff2e6;border-left:3px solid #644a3c;padding:12px 16px;font-size:12px;color:#5e4f46;margin-bottom:24px">${inv.notes}</div>`:''}
    <div style="margin-top:32px;padding-top:16px;border-top:1px solid #dbc9ba;font-size:10px;color:#7e7b67;text-align:center">
      Haida Yammar Limited · RC No: 9304448 · Abuja, Nigeria · VAT compliant invoice under Nigeria Tax Act 2025
    </div>
  </div>`;
  openModal('modal-invoice-pdf');
}
// Store invoices globally for portal access
let _portalInvoices=[];
function renderInvoiceList(invs){
  _portalInvoices=invs;
  if(!invs.length)return'<p style="font-family:Arial;font-size:13px;color:var(--muted)">No invoices.</p>';
  return invs.map((inv,idx)=>{
    const sub=inv.amount||0;const vatR=inv.vat_rate||0;const vat=sub*(vatR/100);const total=sub+vat;const balance=total-(inv.paid||0);
    const sc=inv.status==='Paid'?'badge-green':inv.status==='Partial'?'badge-gold':'badge-red';
    return'<div style="padding:12px 0;border-bottom:1px solid var(--border);font-family:Arial;display:flex;justify-content:space-between;align-items:center">'+
      '<div><strong style="font-size:13px">'+(inv.invoice_number||'INV')+'</strong> — <span style="font-size:13px">'+(inv.description||'')+'</span><br>'+
      '<span style="font-size:11px;color:var(--muted)">Due: '+(inv.due_date||'—')+' · Total: ₦'+Number(total).toLocaleString()+' · Balance: ₦'+Number(balance).toLocaleString()+'</span></div>'+
      '<div style="display:flex;gap:8px;align-items:center"><span class="badge '+sc+'">'+inv.status+'</span>'+
      '<button class="btn-sm" onclick="printInvoice(_portalInvoices['+idx+'])">PDF</button></div>'+
    '</div>';
  }).join('');
}

// ══ COMPLAINTS ══
async function loadComplaints(){
  const {data}=await db.from('complaints').select('*,clients(business_name)').order('date',{ascending:false});
  renderComplaintsPage(data||[]);
}

function renderComplaintsPage(comps){
  const stats=document.getElementById('complaint-stats');const list=document.getElementById('complaints-list');
  const open=comps.filter(c=>c.status==='Open'||c.status==='In Progress').length;
  const esc=comps.filter(c=>c.status==='Escalated').length;
  const res=comps.filter(c=>c.status==='Resolved').length;
  stats.innerHTML=`
    <div class="stat-box"><div class="stat-num">${comps.length}</div><div class="stat-label">Total Logged</div></div>
    <div class="stat-box"><div class="stat-num" style="color:var(--danger)">${open}</div><div class="stat-label">Open</div></div>
    <div class="stat-box"><div class="stat-num" style="color:var(--warn)">${esc}</div><div class="stat-label">Escalated</div></div>
    <div class="stat-box"><div class="stat-num" style="color:var(--success)">${res}</div><div class="stat-label">Resolved</div></div>`;
  list.innerHTML=comps.length?comps.map(c=>renderComplaintCard(c)).join(''):'<p style="font-family:Arial;font-size:13px;color:var(--muted)">No complaints logged.</p>';
}

function renderComplaintCard(c,compact=false){
  const isClient = currentUser&&currentUser.role==='client';
  const strikes=parseInt(c.strike_number)||0;
  const strikeDots='<span class="strike-dot"></span>'.repeat(strikes)+'<span class="strike-empty"></span>'.repeat(3-strikes);
  const cls=c.status==='Resolved'?'complaint-resolved':c.status==='Escalated'?'complaint-escalated':'complaint-open';
  const bc=c.status==='Resolved'?'badge-green':c.status==='Escalated'?'badge-gold':c.severity==='Critical'||c.severity==='High'?'badge-red':'badge-blue';
  // Clients only see category, date, status and their own detail — no internal notes, no strikes, no delete
  if(isClient){
    return'<div class="card '+cls+'" style="margin-bottom:12px">'+
      '<div style="display:flex;justify-content:space-between;align-items:flex-start;flex-wrap:wrap;gap:8px">'+
        '<div><div style="font-size:15px;font-weight:normal;color:var(--ink);margin-bottom:4px">'+c.category+'</div>'+
        '<div style="font-family:Arial;font-size:12px;color:var(--muted)">'+(c.date||'')+'</div></div>'+
        '<span class="badge '+bc+'">'+c.status+'</span>'+
      '</div>'+
      (c.action_taken?'<div style="font-family:Arial;font-size:12px;color:var(--muted);margin-top:8px"><strong>Update:</strong> '+c.action_taken+'</div>':'')+
    '</div>';
  }
  return'<div class="card '+cls+'" style="margin-bottom:12px">'+
    '<div style="display:flex;justify-content:space-between;align-items:flex-start;flex-wrap:wrap;gap:8px">'+
      '<div><div style="font-size:15px;font-weight:normal;color:var(--ink);margin-bottom:4px">'+c.category+' — '+((c.clients&&c.clients.business_name)||'')+'</div>'+
      '<div style="font-family:Arial;font-size:12px;color:var(--muted)">'+(c.date||'')+' · Raised by: '+(c.raised_by||'—')+' · Severity: <strong class="priority-'+((c.severity||'low').toLowerCase())+'">'+c.severity+'</strong></div></div>'+
      '<div style="display:flex;gap:8px;align-items:center"><span class="badge '+bc+'">'+c.status+'</span>'+(strikes>0?'<span style="font-family:Arial;font-size:11px;color:var(--muted)">'+strikeDots+'</span>':'')+' </div>'+
    '</div>'+
    (c.detail?'<div style="font-family:Arial;font-size:13px;color:var(--ink2);margin-top:10px;line-height:1.6">'+c.detail+'</div>':'')+
    (c.action_taken?'<div style="font-family:Arial;font-size:12px;color:var(--muted);margin-top:8px"><strong>Action taken:</strong> '+c.action_taken+'</div>':'')+
    (!compact?'<div style="margin-top:10px"><button class="btn-danger" data-cid="'+c.id+'" onclick="deleteRow(&quot;complaints&quot;,this.dataset.cid,loadComplaints)">Delete</button></div>':'')+
  '</div>';
}

function renderComplaintList(comps,compact=false){
  if(!comps.length)return'<p style="font-family:Arial;font-size:13px;color:var(--muted)">No complaints.</p>';
  return comps.map(c=>renderComplaintCard(c,compact)).join('');
}

async function saveComplaint(){
  const clientId=document.getElementById('comp-client').value;const fb=document.getElementById('complaint-feedback');
  if(!clientId){fb.innerHTML='<div class="error-msg">Select a client.</div>';return;}
  const {error}=await db.from('complaints').insert({client_id:clientId,date:document.getElementById('comp-date').value,category:document.getElementById('comp-category').value,raised_by:document.getElementById('comp-by').value.trim(),severity:document.getElementById('comp-severity').value,status:document.getElementById('comp-status').value,detail:document.getElementById('comp-detail').value.trim(),action_taken:document.getElementById('comp-action').value.trim(),strike_number:parseInt(document.getElementById('comp-strike').value)||0,resolution_date:document.getElementById('comp-resolved').value||null});
  if(error){fb.innerHTML=`<div class="error-msg">${error.message}</div>`;return;}
  fb.innerHTML='<div class="success-msg">✓ Complaint logged.</div>';loadComplaints();setTimeout(()=>{closeModal('modal-add-complaint');fb.innerHTML='';},1500);
}

// ══ ACTIONS ══
async function loadActions(){
  const {data}=await db.from('actions').select('*,clients(business_name)').order('due_date',{ascending:true});
  renderActionsTable(data||[]);
}

function renderActionsTable(acts){
  const tbody=document.getElementById('actions-tbody');
  if(!acts.length){tbody.innerHTML='<tr><td colspan="7" style="text-align:center;padding:24px;color:var(--muted);font-family:Arial">No actions outstanding.</td></tr>';return;}
  const sb={Outstanding:'badge-red',Blocked:'badge-red','In Progress':'badge-gold',Done:'badge-green'};
  const today=new Date();
  tbody.innerHTML=acts.map(a=>{
    const due=a.due_date?new Date(a.due_date):null;const overdue=due&&due<today&&a.status!=='Done';
    return`<tr${overdue?' style="background:var(--danger-light)"':''}>
      <td><strong style="font-family:Arial;font-size:13px">${a.description||'—'}</strong>${a.notes?`<br><span style="font-family:Arial;font-size:11px;color:var(--muted)">${a.notes}</span>`:''}${overdue?'<span class="badge badge-red" style="margin-left:8px">OVERDUE</span>':''}</td>
      <td style="font-family:Arial;font-size:13px">${(a.clients&&a.clients.business_name)||'—'}</td>
      <td style="font-family:Arial;font-size:13px">${a.owner||'—'}</td>
      <td style="font-family:Arial;font-size:12px">${a.due_date||'—'}</td>
      <td><span class="priority-${(a.priority||'low').toLowerCase()}" style="font-family:Arial;font-size:12px;font-weight:500">${a.priority||'—'}</span></td>
      <td><span class="badge ${sb[a.status]||'badge-gray'}">${a.status}</span></td>
      <td style="white-space:nowrap">
  <button class="btn-sm" onclick="markActionDone('${a.id}')">Done</button>
  <button class="btn-sm" onclick="editRecord('actions','${a.id}',loadActions)">Edit</button>
  ${currentUser&&currentUser.isOwner?`<button class="btn-danger" onclick="deleteRow('actions','${a.id}',loadActions)">Del</button>`:''}
</td>
    </tr>`;
  }).join('');
}

async function markActionDone(id){await db.from('actions').update({status:'Done'}).eq('id',id);loadActions();}

async function saveAction(){
  const clientId=document.getElementById('act-client').value;const fb=document.getElementById('action-feedback');
  if(!clientId){fb.innerHTML='<div class="error-msg">Select a client.</div>';return;}
  const {error}=await db.from('actions').insert({client_id:clientId,description:document.getElementById('act-desc').value.trim(),owner:document.getElementById('act-owner').value.trim(),due_date:document.getElementById('act-due').value,priority:document.getElementById('act-priority').value,status:document.getElementById('act-status').value,notes:document.getElementById('act-notes').value.trim()});
  if(error){fb.innerHTML=`<div class="error-msg">${error.message}</div>`;return;}
  fb.innerHTML='<div class="success-msg">✓ Action saved.</div>';loadActions();setTimeout(()=>{closeModal('modal-add-action');fb.innerHTML='';},1500);
}

// ══ CLIENT STAFF ══
let _allStaff = [];

async function loadStaff(){
  const {data}=await db.from('client_staff').select('*,clients(business_name)').order('created_at',{ascending:false});
  _allStaff = data||[];
  // Populate client filter dropdown
  const sel = document.getElementById('staff-client-filter');
  if(sel){
    const currentVal = sel.value;
    const clientNames = [...new Set(_allStaff.map(s=>s.clients?.business_name).filter(Boolean))].sort();
    sel.innerHTML = '<option value="">All Clients</option>' + clientNames.map(n=>`<option value="${n}" ${n===currentVal?'selected':''}>${n}</option>`).join('');
  }
  filterStaffByClient();
}

function filterStaffByClient(){
  const clientFilter = document.getElementById('staff-client-filter')?.value || '';
  const statusFilter = document.getElementById('staff-status-filter')?.value || '';
  const filtered = _allStaff.filter(s => {
    const nameMatch = !clientFilter || (s.clients?.business_name === clientFilter);
    const statusMatch = !statusFilter || s.status === statusFilter;
    return nameMatch && statusMatch;
  });
  renderStaffTable(filtered);
}

function renderStaffTable(staff){
  const tbody=document.getElementById('staff-tbody');
  if(!staff.length){tbody.innerHTML='<tr><td colspan="7" style="text-align:center;padding:24px;color:var(--muted);font-family:Arial">No staff records.</td></tr>';return;}
  const sb={Active:'badge-green',Probation:'badge-gold',Flagged:'badge-red',Terminated:'badge-gray'};
  tbody.innerHTML=staff.map(s=>{
    const strikes=parseInt(s.strike_count)||0;
    const dots='<span class="strike-dot"></span>'.repeat(strikes)+'<span class="strike-empty"></span>'.repeat(Math.max(0,3-strikes));
    return`<tr>
      <td><strong style="font-family:Arial;font-size:13px">${s.name}</strong>${s.performance_notes?`<br><span style="font-family:Arial;font-size:11px;color:var(--muted)">${s.performance_notes.substring(0,60)}…</span>`:''}  </td>
      <td style="font-family:Arial;font-size:13px">${(s.clients&&s.clients.business_name)||'—'}</td>
      <td style="font-family:Arial;font-size:13px">${s.role||'—'}</td>
      <td><span class="badge ${sb[s.status]||'badge-gray'}">${s.status}</span></td>
      <td>${dots} <button class="btn-sm" onclick="openStrikeModal('${s.id}','${s.name.replace(/'/g,"\\'")}',${strikes})" style="font-size:10px;margin-left:4px">+ Strike</button></td>
      <td style="font-family:Arial;font-size:12px">${s.last_training_date||'—'}</td>
      <td style="white-space:nowrap">
        <button class="btn-sm" onclick="editRecord('client_staff','${s.id}',loadStaff)">Edit</button>
        ${currentUser&&currentUser.isOwner?`<button class="btn-danger" onclick="deleteRow('client_staff','${s.id}',loadStaff)">Del</button>`:''}
      </td>
    </tr>`;
  }).join('');
}

function openStrikeModal(staffId, staffName, currentStrikes){
  document.getElementById('strike-staff-id').value = staffId;
  document.getElementById('strike-modal-title').textContent = `Log Strike — ${staffName}`;
  document.getElementById('strike-number').value = Math.min(currentStrikes + 1, 3);
  document.getElementById('strike-date').valueAsDate = new Date();
  document.getElementById('strike-reason').value = '';
  document.getElementById('strike-action').value = '';
  document.getElementById('strike-by').value = '';
  document.getElementById('strike-ack').value = 'pending';
  openModal('modal-add-strike');
}

async function saveStrike(){
  const staffId = document.getElementById('strike-staff-id').value;
  const fb = document.getElementById('strike-feedback');
  const strikeNum = parseInt(document.getElementById('strike-number').value);
  const {error: logErr} = await db.from('staff_strikes').insert({
    staff_id: staffId,
    strike_number: strikeNum,
    date: document.getElementById('strike-date').value,
    reason: document.getElementById('strike-reason').value.trim(),
    action_taken: document.getElementById('strike-action').value.trim(),
    issued_by: document.getElementById('strike-by').value.trim(),
    acknowledged: document.getElementById('strike-ack').value,
  });
  if(logErr){fb.innerHTML=`<div class="error-msg">${logErr.message}</div>`;return;}
  // Update strike_count on staff record
  await db.from('client_staff').update({strike_count: strikeNum, status: strikeNum >= 3 ? 'Flagged' : undefined}).eq('id', staffId);
  fb.innerHTML='<div class="success-msg">✓ Strike logged.</div>';
  loadStaff();
  setTimeout(()=>{closeModal('modal-add-strike');fb.innerHTML='';}, 1200);
}

async function saveStaff(){
  const clientId=document.getElementById('stf-client').value;const fb=document.getElementById('staff-feedback');
  if(!clientId){fb.innerHTML='<div class="error-msg">Select a client.</div>';return;}
  const {error}=await db.from('client_staff').insert({client_id:clientId,name:document.getElementById('stf-name').value.trim(),role:document.getElementById('stf-role').value.trim(),start_date:document.getElementById('stf-start').value,status:document.getElementById('stf-status').value,last_training_date:document.getElementById('stf-trained').value||null,performance_notes:document.getElementById('stf-notes').value.trim()});
  if(error){fb.innerHTML=`<div class="error-msg">${error.message}</div>`;return;}
  fb.innerHTML='<div class="success-msg">✓ Staff member saved.</div>';loadStaff();setTimeout(()=>{closeModal('modal-add-staff');fb.innerHTML='';},1500);
}

// ══ CONTRACTS ══
async function loadContracts(){
  const {data}=await db.from('contracts').select('*,clients(business_name)').order('end_date',{ascending:true});
  renderContractsTable(data||[]);
}

function renderContractsTable(cons){
  const tbody=document.getElementById('contracts-tbody');
  const isOwner=currentUser&&currentUser.isOwner;
  if(!cons.length){tbody.innerHTML='<tr><td colspan="8" style="text-align:center;padding:24px;color:var(--muted);font-family:Arial">No contracts.</td></tr>';return;}
  const today=new Date();const sb={Active:'badge-green','Pending Signature':'badge-gold',Expired:'badge-gray',Terminated:'badge-red'};
  tbody.innerHTML=cons.map(c=>{
    const end=c.end_date?new Date(c.end_date):null;
    const daysLeft=end?Math.ceil((end-today)/(1000*60*60*24)):null;
    const dl=daysLeft===null?'—':daysLeft<0?'<span style="color:var(--danger);font-weight:500">Expired</span>':daysLeft<=30?`<span style="color:var(--warn);font-weight:500">${daysLeft}d</span>`:`<span style="color:var(--success)">${daysLeft}d</span>`;
    const valueCell=isOwner
      ?`<td style="font-family:Arial;font-size:13px">₦${Number(c.value||0).toLocaleString()}/mo</td>`
      :`<td style="font-family:Arial;font-size:12px;color:var(--muted);font-style:italic">Owner only</td>`;
    return`<tr>
      <td style="font-family:Arial;font-size:13px">${(c.clients&&c.clients.business_name)||'—'}</td>
      <td style="font-family:Arial;font-size:13px">${c.contract_type||'—'}</td>
      ${valueCell}
      <td style="font-family:Arial;font-size:12px">${c.start_date||'—'}</td>
      <td style="font-family:Arial;font-size:12px">${c.end_date||'—'}</td>
      <td style="font-family:Arial;font-size:12px">${dl}</td>
      <td><span class="badge ${sb[c.status]||'badge-gray'}">${c.status}</span></td>
      <td style="white-space:nowrap">
        <button class="btn-sm" onclick="editRecord('contracts','${c.id}',loadContracts)">Edit</button>
        ${isOwner?`<button class="btn-danger" onclick="deleteRow('contracts','${c.id}',loadContracts)">Del</button>`:''}
      </td>
    </tr>`;
  }).join('');
}

async function saveContract(){
  const clientId=document.getElementById('con-client').value;const fb=document.getElementById('contract-feedback');
  if(!clientId){fb.innerHTML='<div class="error-msg">Select a client.</div>';return;}
  const {error}=await db.from('contracts').insert({client_id:clientId,contract_type:document.getElementById('con-type').value,value:parseFloat(document.getElementById('con-value').value)||0,start_date:document.getElementById('con-start').value,end_date:document.getElementById('con-end').value,status:document.getElementById('con-status').value,scope_summary:document.getElementById('con-scope').value.trim()});
  if(error){fb.innerHTML=`<div class="error-msg">${error.message}</div>`;return;}
  fb.innerHTML='<div class="success-msg">✓ Contract saved.</div>';loadContracts();setTimeout(()=>{closeModal('modal-add-contract');fb.innerHTML='';},1500);
}

// ══ DOCUMENTS ══
async function loadDocs(){
  const {data}=await db.from('documents').select('*,clients(business_name)').order('date_added',{ascending:false});
  renderDocsTable(data||[]);
}

function renderDocsTable(docs){
  const tbody=document.getElementById('docs-tbody');
  if(!docs.length){tbody.innerHTML='<tr><td colspan="6" style="text-align:center;padding:24px;color:var(--muted);font-family:Arial">No documents.</td></tr>';return;}
  tbody.innerHTML=docs.map(d=>`<tr>
    <td><strong style="font-family:Arial;font-size:13px">${d.title||'—'}</strong></td>
    <td style="font-family:Arial;font-size:13px">${(d.clients&&d.clients.business_name)||'—'}</td>
    <td><span class="badge badge-blue" style="font-size:10px">${d.doc_type||'—'}</span></td>
    <td style="font-family:Arial;font-size:12px">${d.date_added||'—'}</td>
    <td style="font-family:Arial;font-size:12px">${d.link?`<a href="${d.link}" target="_blank" style="color:var(--gold)">Open ↗</a>`:d.notes||'—'}</td>
    <td style="white-space:nowrap">
      <button class="btn-sm" onclick="editRecord('documents','${d.id}',loadDocs)">Edit</button>
      ${currentUser&&currentUser.isOwner?`<button class="btn-danger" onclick="deleteRow('documents','${d.id}',loadDocs)">Del</button>`:''}
    </td>
  </tr>`).join('');
}

async function saveDoc(){
  const clientId=document.getElementById('doc-client').value;const fb=document.getElementById('doc-feedback');
  if(!clientId){fb.innerHTML='<div class="error-msg">Select a client.</div>';return;}
  const {error}=await db.from('documents').insert({client_id:clientId,title:document.getElementById('doc-title').value.trim(),doc_type:document.getElementById('doc-type').value,date_added:document.getElementById('doc-date').value,version:document.getElementById('doc-version').value.trim(),link:document.getElementById('doc-link').value.trim(),notes:document.getElementById('doc-notes').value.trim()});
  if(error){fb.innerHTML=`<div class="error-msg">${error.message}</div>`;return;}
  fb.innerHTML='<div class="success-msg">✓ Document saved.</div>';loadDocs();setTimeout(()=>{closeModal('modal-add-doc');fb.innerHTML='';},1500);
}

// ══ CLIENT PORTAL ══
async function loadPortalReports(clientId){
  const {data}=await db.from('reports').select('*').eq('client_id',clientId).order('date',{ascending:false});
  document.getElementById('portal-reports-tab').innerHTML=renderReportList(data||[]);
}
async function loadPortalInvoices(clientId){
  // Clients never see invoices - financial data owner-only
  document.getElementById('portal-invoices-tab').innerHTML='<div style="padding:24px;font-family:Arial;font-size:13px;color:var(--muted);text-align:center">Invoice queries are handled directly by Haida Yammar Consulting. Contact us for billing queries.</div>';
}
async function loadPortalComplaints(clientId){
  // Clients don't see this tab — complaints are internal. Tab is hidden.
  document.getElementById('portal-complaints-tab').innerHTML='';
}
async function loadPortalDocs(clientId){
  const {data}=await db.from('documents').select('*').eq('client_id',clientId).order('date_added',{ascending:false});
  const isClient=currentUser&&currentUser.role==='client';
  const addForm=isClient?`
    <div class="card" style="margin-bottom:16px">
      <div class="card-title">Add a Document</div>
      <div class="form-grid">
        <div class="form-field" style="grid-column:1/-1">
          <label>Document Title</label>
          <input type="text" id="pdoc-title" placeholder="e.g. June staff rota, New product brief">
        </div>
        <div class="form-field">
          <label>Type</label>
          <select id="pdoc-type">
            <option>Internal Note</option>
            <option>Staff Document</option>
            <option>Reference Material</option>
            <option>Media / Asset</option>
            <option>SOP</option>
            <option>Other</option>
          </select>
        </div>
        <div class="form-field">
          <label>Link (Google Drive, Dropbox etc.)</label>
          <input type="text" id="pdoc-link" placeholder="https://…">
        </div>
        <div class="form-field" style="grid-column:1/-1">
          <label>Notes</label>
          <input type="text" id="pdoc-notes" placeholder="Any context for Haida…">
        </div>
      </div>
      <button class="btn-save" data-cid="'+clientId+'" onclick="clientAddDoc(this.dataset.cid)" style="margin-top:12px">Upload Document</button>
      <div id="pdoc-feedback" style="margin-top:8px"></div>
    </div>` : '';
  const docs=data||[];
  const docsHtml=docs.length?docs.map(d=>{
    const canDel=isClient&&d.added_by==='client';
    return '<div style="border:1px solid var(--border);border-radius:2px;padding:14px 18px;margin-bottom:10px;display:flex;justify-content:space-between;align-items:center">'+
      '<div><strong style="font-size:14px">'+d.title+'</strong><br>'+
      '<span style="font-family:Arial;font-size:12px;color:var(--muted)">'+d.doc_type+(d.date_added?' · '+d.date_added:'')+(d.version?' · v'+d.version:'')+(d.added_by==='client'?' · Added by you':'')+'</span>'+
      (d.notes?'<br><span style="font-family:Arial;font-size:11px;color:var(--muted)">'+d.notes+'</span>':'')+
      '</div>'+
      '<div style="display:flex;gap:8px;align-items:center">'+
        (d.link?'<a href="'+d.link+'" target="_blank" style="color:var(--gold);font-family:Arial;font-size:13px">Open ↗</a>':'')+
        (canDel?'<button class="btn-danger" style="font-size:11px" data-did="'+d.id+'" data-cid="'+clientId+'" onclick="clientDeleteDoc(this.dataset.did,this.dataset.cid)">Remove</button>':'')+
      '</div>'+
    '</div>';
  }).join(''):'<p style="font-family:Arial;font-size:13px;color:var(--muted)">No documents yet. Add one above.</p>';
  document.getElementById('portal-docs-tab').innerHTML=addForm+docsHtml;
}

async function clientAddDoc(clientId){
  const title=document.getElementById('pdoc-title').value.trim();
  const fb=document.getElementById('pdoc-feedback');
  if(!title){fb.innerHTML='<div class="error-msg">Please enter a document title.</div>';return;}
  const {error}=await db.from('documents').insert({
    client_id:clientId,
    title:title,
    doc_type:document.getElementById('pdoc-type').value,
    link:document.getElementById('pdoc-link').value.trim(),
    notes:document.getElementById('pdoc-notes').value.trim(),
    date_added:new Date().toISOString().substring(0,10),
    version:'1.0',
    added_by:'client'
  });
  if(error){fb.innerHTML='<div class="error-msg">'+error.message+'</div>';return;}
  fb.innerHTML='<div class="success-msg">✓ Document added.</div>';
  ['pdoc-title','pdoc-link','pdoc-notes'].forEach(id=>document.getElementById(id).value='');
  setTimeout(()=>loadPortalDocs(clientId),800);
}

async function clientDeleteDoc(docId,clientId){
  if(!confirm('Remove this document?'))return;
  await db.from('documents').delete().eq('id',docId);
  loadPortalDocs(clientId);
}
async function loadPortalActions(clientId){
  const {data}=await db.from('actions').select('*').eq('client_id',clientId).neq('status','Done').order('due_date',{ascending:true});
  const isClient=currentUser&&currentUser.role==='client';
  const addForm=isClient?`
    <div class="card" style="margin-bottom:16px">
      <div class="card-title">Add an Action Item</div>
      <div class="form-grid">
        <div class="form-field" style="grid-column:1/-1">
          <label>Action Description</label>
          <input type="text" id="pact-desc" placeholder="e.g. Update opening hours on Google, Send updated staff contract">
        </div>
        <div class="form-field">
          <label>Who should do this</label>
          <input type="text" id="pact-owner" placeholder="e.g. Manager, Haida, Me">
        </div>
        <div class="form-field">
          <label>Due Date</label>
          <input type="date" id="pact-due">
        </div>
        <div class="form-field">
          <label>Priority</label>
          <select id="pact-priority"><option>High</option><option selected>Medium</option><option>Low</option></select>
        </div>
        <div class="form-field" style="grid-column:1/-1">
          <label>Notes</label>
          <input type="text" id="pact-notes" placeholder="Any extra context…">
        </div>
      </div>
      <button class="btn-save" data-cid="'+clientId+'" onclick="clientAddAction(this.dataset.cid)" style="margin-top:12px">Add Action</button>
      <div id="pact-feedback" style="margin-top:8px"></div>
    </div>` : '';
  const acts=data||[];
  const priColor={High:'var(--danger)',Medium:'var(--warn)',Low:'var(--success)'};
  const actsHtml=acts.length?acts.map(a=>{
    const canDone=isClient&&a.added_by==='client';
    return '<div style="border:1px solid var(--border);border-left:3px solid '+(priColor[a.priority]||'var(--border)')+';border-radius:2px;padding:14px 18px;margin-bottom:10px">'+
      '<div style="display:flex;justify-content:space-between;align-items:flex-start;flex-wrap:wrap;gap:8px">'+
        '<div style="font-family:Arial;font-size:13px;font-weight:500">'+a.description+'</div>'+
        (canDone?'<button class="btn-sm" style="font-size:11px" data-aid="'+a.id+'" data-cid="'+clientId+'" onclick="clientMarkActionDone(this.dataset.aid,this.dataset.cid)">Mark Done</button>':'')+
      '</div>'+
      '<div style="font-family:Arial;font-size:12px;color:var(--muted);margin-top:4px">'+
        'Owner: '+(a.owner||'—')+' · Due: '+(a.due_date||'—')+' · Priority: <strong class="priority-'+((a.priority||'low').toLowerCase())+'">'+a.priority+'</strong>'+
      '</div>'+
      (a.notes?'<div style="font-family:Arial;font-size:12px;color:var(--muted);margin-top:4px">'+a.notes+'</div>':'')+
    '</div>';
  }).join(''):'<p style="font-family:Arial;font-size:13px;color:var(--muted)">No outstanding actions.</p>';
  document.getElementById('portal-actions-tab').innerHTML=addForm+actsHtml;
}

async function clientAddAction(clientId){
  const desc=document.getElementById('pact-desc').value.trim();
  const fb=document.getElementById('pact-feedback');
  if(!desc){fb.innerHTML='<div class="error-msg">Please describe the action.</div>';return;}
  const {error}=await db.from('actions').insert({
    client_id:clientId,
    description:desc,
    owner:document.getElementById('pact-owner').value.trim()||'Client',
    due_date:document.getElementById('pact-due').value||null,
    priority:document.getElementById('pact-priority').value,
    notes:document.getElementById('pact-notes').value.trim(),
    status:'Outstanding',
    added_by:'client'
  });
  if(error){fb.innerHTML='<div class="error-msg">'+error.message+'</div>';return;}
  fb.innerHTML='<div class="success-msg">✓ Action added. Haida can see this.</div>';
  ['pact-desc','pact-owner','pact-notes','pact-due'].forEach(id=>document.getElementById(id).value='');
  setTimeout(()=>loadPortalActions(clientId),800);
}

async function clientMarkActionDone(actionId,clientId){
  await db.from('actions').update({status:'Done'}).eq('id',actionId);
  loadPortalActions(clientId);
}

// ══ CLIENT REQUESTS ══
async function submitClientRequest(){
  const title=document.getElementById('req-title').value.trim();
  const desc=document.getElementById('req-desc').value.trim();
  const urgency=document.getElementById('req-urgency').value;
  const service=document.getElementById('req-service').value;
  const deadline=document.getElementById('req-deadline').value;
  const notes=document.getElementById('req-notes').value.trim();
  const fb=document.getElementById('req-feedback');
  if(!title){fb.textContent='Please enter a task title.';fb.style.color='var(--danger)';return;}
  const clientName=currentUser.name||'Client';
  const fullDesc=`[${service}] ${desc}${notes?'\nNotes: '+notes:''}${deadline?'\nDeadline: '+deadline:''}`;

  // Save to portal DB for client's own view
  await db.from('client_requests').insert({
    client_id:currentUser.clientId,
    title,description:desc,urgency,service,deadline:deadline||null,notes,status:'Submitted'
  });

  // Push to task board Supabase
  const {error}=await taskDb.from('tasks').insert({
    title:title,
    notes:fullDesc,
    client:clientName,
    logged_by:'Client Portal',
    priority:urgency,
    status:'Inbox',
    deadline:deadline||null
  });

  if(error){
    fb.textContent='Saved to your portal. Task board sync pending.';
    fb.style.color='var(--warn)';
  } else {
    fb.textContent='✓ Request submitted. Haida has been notified.';
    fb.style.color='var(--success)';
  }
  // Save notification to DB for badge
  await db.from('notifications').insert({
    type:'client_request',
    message:'New request from '+clientName+': '+title,
    client_id:currentUser.clientId,
    is_read:false
  }).then(()=>{}).catch(()=>{});

  // Email Haida via Supabase Edge Function
  fetch('https://tdxvioeybgqbbcubngko.supabase.co/functions/v1/notifyhaida', {
    method:'POST',
    headers:{
      'Content-Type':'application/json',
      'Authorization':'Bearer sb_publishable_VtboyoBjlK5Zp_lOZMr3wA_V45kYBkH'
    },
    body:JSON.stringify({
      client_name:clientName,
      task_title:title,
      urgency:urgency,
      service:service
    })
  }).catch(()=>{}); // silent fail — doesn't block the client

  // Clear form
  ['req-title','req-desc','req-notes','req-deadline'].forEach(id=>document.getElementById(id).value='');
  document.getElementById('req-urgency').value='Medium';

  // Reload requests list
  await loadPortalRequests(currentUser.clientId);
  setTimeout(()=>fb.textContent='',4000);
}

async function loadPortalRequests(clientId){
  const {data}=await db.from('client_requests').select('*').eq('client_id',clientId).order('created_at',{ascending:false});
  const list=document.getElementById('portal-requests-list');
  if(!list)return;
  const reqs=data||[];
  if(!reqs.length){list.innerHTML='<p style="font-family:Arial;font-size:13px;color:var(--muted)">No requests submitted yet.</p>';return;}
  const urgColor={High:'var(--danger)',Medium:'var(--warn)',Low:'var(--success)'};
  list.innerHTML=reqs.map(r=>`
    <div style="border:1px solid var(--border);border-left:3px solid ${urgColor[r.urgency]||'var(--border)'};border-radius:2px;padding:14px 18px;margin-bottom:10px;background:white">
      <div style="display:flex;justify-content:space-between;align-items:flex-start;flex-wrap:wrap;gap:6px;margin-bottom:4px">
        <strong style="font-size:14px;color:var(--ink)">${r.title}</strong>
        <div style="display:flex;gap:6px">
          <span class="badge" style="background:${urgColor[r.urgency]||'var(--border)'};color:white">${r.urgency}</span>
          <span class="badge badge-blue">${r.service||'General'}</span>
          <span class="badge ${r.status==='Done'?'badge-green':r.status==='In Progress'?'badge-gold':'badge-gray'}">${r.status}</span>
        </div>
      </div>
      <div style="font-family:Arial;font-size:12px;color:var(--muted)">${r.description||''}${r.deadline?' · Deadline: '+r.deadline:''}</div>
      <div style="font-family:Arial;font-size:11px;color:var(--muted);margin-top:4px">${new Date(r.created_at).toLocaleDateString('en-GB',{day:'numeric',month:'short',year:'numeric'})}</div>
    </div>`).join('');
}

async function loadPortalStaff(clientId){
  const {data}=await db.from('client_staff').select('*').eq('client_id',clientId).order('name',{ascending:true});
  const tab=document.getElementById('portal-staff-tab');
  if(!tab)return;
  const staff=data||[];
  if(!staff.length){tab.innerHTML='<p style="font-family:Arial;font-size:13px;color:var(--muted)">No staff records on file yet.</p>';return;}
  const sb={Active:'badge-green',Probation:'badge-gold',Flagged:'badge-red',Terminated:'badge-gray'};
  tab.innerHTML=`<div style="font-family:Arial;font-size:13px;color:var(--muted);margin-bottom:16px">${staff.length} staff member${staff.length!==1?'s':''} on record</div>`+staff.map(s=>`
    <div style="border:1px solid var(--border);border-radius:2px;padding:14px 18px;margin-bottom:10px;background:white">
      <div style="display:flex;justify-content:space-between;align-items:flex-start;flex-wrap:wrap;gap:8px;margin-bottom:${(s.performance_notes||s.last_training_date)?'10px':'0'}">
        <div>
          <div style="font-size:14px;font-weight:500;color:var(--ink);margin-bottom:3px">${s.name}</div>
          <div style="font-size:12px;color:var(--muted)">${s.role||'—'}</div>
        </div>
        <span class="badge ${sb[s.status]||'badge-gray'}">${s.status}</span>
      </div>
      ${s.last_training_date?`<div style="font-size:11px;color:var(--muted);margin-bottom:6px">🎓 Last trained: ${s.last_training_date}</div>`:''}
      ${s.performance_notes?`<div style="font-size:12px;color:var(--ink2);background:var(--surface2);padding:8px 12px;border-radius:2px;border-left:3px solid var(--gold)">${s.performance_notes}</div>`:''}
    </div>`).join('');
}

async function loadPortalEvents(clientId){
  const tab = document.getElementById('portal-events-tab');
  if(!tab) return;
  tab.innerHTML = '<div style="font-family:Arial;font-size:13px;color:var(--muted);padding:16px 0">Loading…</div>';

  const {data} = await db.from('events').select('*').eq('client_id', clientId).order('event_date',{ascending:true});
  const events = data || [];

  if(!events.length){
    tab.innerHTML = '<div style="text-align:center;padding:48px;font-family:Arial;font-size:13px;color:var(--muted)">No events on file yet. Your consultant will add event details when planning begins.</div>';
    return;
  }

  const today = new Date();
  tab.innerHTML = events.map(ev => {
    const evDate = ev.event_date ? new Date(ev.event_date) : null;
    const isPast = evDate && evDate < today;
    const daysLeft = evDate ? Math.ceil((evDate-today)/(1000*60*60*24)) : null;
    const statusColor = ev.status === 'Confirmed' ? 'var(--success)' : ev.status === 'In Progress' ? 'var(--warn)' : 'var(--muted)';

    // Parse updates log
    let updates = [];
    try { updates = JSON.parse(ev.updates_log||'[]'); } catch(e){}

    // Parse secured/purchased items
    let secured = [];
    try { secured = JSON.parse(ev.secured_items||'[]'); } catch(e){}

    // Parse deliverables and tasks
    let deliverables = [];
    try { deliverables = JSON.parse(ev.deliverables||'[]'); } catch(e){}
    let tasks = [];
    try { tasks = JSON.parse(ev.tasks||'[]'); } catch(e){}

    return `<div style="border:1px solid var(--border);border-radius:2px;margin-bottom:16px;background:white;overflow:hidden">
      <div style="background:var(--ink);color:white;padding:14px 18px;display:flex;justify-content:space-between;align-items:center;flex-wrap:wrap;gap:8px">
        <div>
          <div style="font-size:15px;font-weight:500">${ev.event_name||'Event'}</div>
          <div style="font-size:11px;opacity:.7;margin-top:2px">${ev.event_type||''} ${ev.venue?'· '+ev.venue:''}</div>
        </div>
        <div style="text-align:right">
          <div style="font-size:13px">${evDate?evDate.toLocaleDateString('en-GB',{day:'numeric',month:'long',year:'numeric'}):'Date TBC'}</div>
          ${daysLeft!==null&&!isPast?`<div style="font-size:11px;opacity:.7">${daysLeft} days to go</div>`:''}
          ${isPast?`<div style="font-size:11px;opacity:.7">Completed</div>`:''}
        </div>
      </div>
      <div style="padding:14px 18px">
        <div style="display:flex;gap:16px;margin-bottom:14px;flex-wrap:wrap">
          <div><span style="font-family:Arial;font-size:10px;text-transform:uppercase;letter-spacing:1px;color:var(--muted)">Status</span><br><span style="font-size:13px;font-weight:600;color:${statusColor}">${ev.status||'Planning'}</span></div>
          ${ev.budget?`<div><span style="font-family:Arial;font-size:10px;text-transform:uppercase;letter-spacing:1px;color:var(--muted)">Budget</span><br><span style="font-size:13px;font-weight:600">₦${parseInt(ev.budget).toLocaleString()}</span></div>`:''}
          ${ev.guest_count?`<div><span style="font-family:Arial;font-size:10px;text-transform:uppercase;letter-spacing:1px;color:var(--muted)">Guests</span><br><span style="font-size:13px;font-weight:600">${ev.guest_count}</span></div>`:''}
        </div>
        ${updates.length?`
          <div style="margin-bottom:12px">
            <div style="font-family:Arial;font-size:10px;text-transform:uppercase;letter-spacing:1px;color:var(--gold);margin-bottom:8px">Recent Updates</div>
            ${updates.slice(-5).reverse().map(u=>`
              <div style="padding:8px 12px;border-left:2px solid var(--gold-mid);margin-bottom:6px;font-family:Arial;font-size:12px">
                <span style="color:var(--muted);font-size:10px">${u.date||''}</span><br>
                ${u.text||u}
              </div>`).join('')}
          </div>` : ''}
        ${deliverables.length?`
          <div style="margin-bottom:12px">
            <div style="font-family:Arial;font-size:10px;text-transform:uppercase;letter-spacing:1px;color:var(--gold);margin-bottom:8px">What's Been Decided / Confirmed</div>
            ${deliverables.map(d=>`
              <div style="display:flex;align-items:center;gap:8px;padding:5px 0;border-bottom:1px solid var(--border);font-family:Arial;font-size:12px">
                <span style="width:16px;height:16px;border-radius:50%;background:${d.confirmed?'var(--success)':'var(--border)'};display:inline-flex;align-items:center;justify-content:center;flex-shrink:0;font-size:9px;color:white">${d.confirmed?'✓':''}</span>
                <span style="${d.confirmed?'color:var(--muted);text-decoration:line-through':''}">${d.text}</span>
              </div>`).join('')}
          </div>` : ''}
        ${tasks.length?`
          <div style="margin-bottom:12px">
            <div style="font-family:Arial;font-size:10px;text-transform:uppercase;letter-spacing:1px;color:var(--gold);margin-bottom:8px">Task Checklist</div>
            ${tasks.map(t=>`
              <div style="display:flex;align-items:center;gap:8px;padding:5px 0;border-bottom:1px solid var(--border);font-family:Arial;font-size:12px">
                <span style="width:16px;height:16px;border-radius:50%;background:${t.done?'var(--success)':'var(--border)'};display:inline-flex;align-items:center;justify-content:center;flex-shrink:0;font-size:9px;color:white">${t.done?'✓':''}</span>
                <span style="flex:1;${t.done?'color:var(--muted);text-decoration:line-through':''}">${t.text}</span>
                ${t.due?`<span style="font-family:Arial;font-size:10px;color:var(--warn)">by ${t.due}</span>`:''}
              </div>`).join('')}
          </div>` : ''}
        ${secured.length?`
          <div>
            <div style="font-family:Arial;font-size:10px;text-transform:uppercase;letter-spacing:1px;color:var(--gold);margin-bottom:8px">Secured / Purchased</div>
            <div style="display:flex;flex-wrap:wrap;gap:6px">
              ${secured.map(item=>`<span style="background:var(--gold-light);border:1px solid var(--gold-mid);padding:3px 10px;border-radius:2px;font-family:Arial;font-size:12px">✓ ${item}</span>`).join('')}
            </div>
          </div>` : ''}
      </div>
    </div>`;
  }).join('');
}

function switchPortalTab(tabId,btn){
  document.querySelectorAll('.ptab').forEach(b=>b.classList.remove('active'));btn.classList.add('active');
  document.querySelectorAll('.portal-tab-content').forEach(t=>t.style.display='none');
  document.getElementById(tabId).style.display='block';
}

// ══ REPORTS RENDER ══

// Build a flat label lookup from all field definitions
function buildFieldLabelMap(){
  const map={};
  const allDefs=[...Object.values(typeFields),...Object.values(nicheFields)].flat();
  allDefs.forEach(f=>{if(f&&f.id&&f.label)map[f.id]=f.label;});
  // Add common fields
  map.ops_score='Operations Score';map.hygiene_score='Hygiene & Safety';
  map.compliance_score='Compliance';map.staff_score='Staff & Conduct';
  return map;
}

function renderReportList(reports){
  if(!reports.length)return'<div style="text-align:center;padding:32px;font-family:Arial;font-size:13px;color:var(--muted)">No reports yet.</div>';
  const typeLabel={audit:'Audit Report',supervision:'Supervision Report',site_visit:'Site Visit Note',cx_mystery:'CX Mystery Visit',staff_training:'Staff Training Record',brand_review:'Brand Review',performance:'Performance Review',kpi:'KPI Review','Compliance Summary':'Compliance Summary','Infection Control':'Infection Control Report'};
  const typeBadge={audit:'badge-gold',supervision:'badge-blue',site_visit:'badge-blue',cx_mystery:'badge-green',staff_training:'badge-green',brand_review:'badge-blue',performance:'badge-green',kpi:'badge-gray','Compliance Summary':'badge-gold','Infection Control':'badge-green'};
  const labelMap=buildFieldLabelMap();
  const isAdmin=currentUser&&(currentUser.isOwner||currentUser.role==='admin');
  let html='';
  reports.forEach(function(r,i){
    var td=r.type_data||{};
    if(typeof td==='string'){try{td=JSON.parse(td);}catch(e){td={};}}
    if(typeof td!=='object'||Array.isArray(td)){td={};}
    if(r.report_type==='Compliance Summary')td={};
    var scores=Object.entries(td).filter(function(e){return e[0].includes('score')&&!isNaN(Number(e[1]))&&e[1]!=='';});
    var kpiHtml='';
    if(scores.length){
      kpiHtml='<div class="kpi-grid">';
      scores.forEach(function(e){kpiHtml+='<div class="kpi-box"><div class="kpi-score">'+e[1]+'</div><div class="kpi-label">'+(labelMap[e[0]]||e[0].replace(/_/g,' '))+'</div></div>';});
      kpiHtml+='</div>';
    }
    var otherFields=Object.entries(td).filter(function(e){return !e[0].includes('score')&&e[1]&&e[1]!==''&&!e[0].startsWith('_');});
    var fieldsHtml='';
    otherFields.forEach(function(e){
      var lbl=labelMap[e[0]]||e[0].replace(/_/g,' ').split(' ').map(function(w){return w.charAt(0).toUpperCase()+w.slice(1);}).join(' ');
      fieldsHtml+='<div class="detail-section"><h4>'+lbl+'</h4><div class="detail-text">'+e[1]+'</div></div>';
    });
    var summaryHtml=r.summary?'<div class="detail-section"><h4>Summary</h4><div class="detail-text">'+r.summary+'</div></div>':'';
    // Handle compliance summary and infection control reports that use the content field
    if(r.content && (r.report_type==='Compliance Summary'||r.report_type==='Infection Control')){
      summaryHtml='<div class="detail-section"><div class="detail-text" style="white-space:pre-wrap;font-size:12px;line-height:1.8">'+r.content+'</div></div>';
    }
    var actionHtml='';
    if(r.action_items){
      var items=r.action_items.split(String.fromCharCode(10)).filter(Boolean);
      actionHtml='<div class="detail-section"><h4>Action Items</h4><ul class="action-items">'+items.map(function(a){return'<li>'+a+'</li>';}).join('')+'</ul></div>';
    }
    var recoHtml=r.recommendations?'<div class="detail-section"><h4>Recommendations</h4><div class="detail-text">'+r.recommendations+'</div></div>':'';
    var delBtn='';
    var ownerNoteHtml='';
    if(isAdmin){
      delBtn='<div style="margin-top:8px"><button class="btn-danger" style="font-size:11px" data-rid="'+r.id+'" onclick="deleteReportById(this.dataset.rid)">Delete Report</button></div>';
    }
    // Owner note — visible to everyone, editable only by owner
    if(r.owner_note){
      ownerNoteHtml='<div style="margin-top:12px;padding:12px 14px;background:#fff9f0;border-left:3px solid var(--gold);border-radius:2px">'
        +'<div style="font-family:Arial;font-size:9px;text-transform:uppercase;letter-spacing:1.5px;color:var(--gold);margin-bottom:6px">Note from Haida</div>'
        +'<div style="font-family:Arial;font-size:13px;color:var(--ink2);line-height:1.6;white-space:pre-wrap">'+r.owner_note+'</div>'
        +(currentUser&&currentUser.isOwner?'<button class="btn-sm" style="font-size:10px;margin-top:8px" data-rid="'+r.id+'" data-note="'+r.owner_note.replace(/"/g,'&quot;')+'" onclick="editOwnerNote(this.dataset.rid,this.dataset.note)">Edit Note</button>':'')
      +'</div>';
    } else if(currentUser&&currentUser.isOwner){
      ownerNoteHtml='<div style="margin-top:12px"><button class="btn-sm" style="font-size:11px" data-rid="'+r.id+'" onclick="addOwnerNote(this.dataset.rid)">+ Add Owner Note</button></div>';
    }
    var bid='rr'+i;
    html+='<div class="report-card">';
    html+='<div class="report-header" data-bid="'+bid+'" onclick="toggleReport(this.dataset.bid,this)">';
    html+='<div><div class="report-title">'+r.title+'</div><div class="report-meta">'+(r.date||new Date(r.created_at).toLocaleDateString('en-GB')||'')+(r.location?' · '+r.location:'')+'</div></div>';
    html+='<div style="display:flex;align-items:center;gap:10px"><span class="badge '+(typeBadge[r.report_type]||'badge-gray')+'">'+(typeLabel[r.report_type]||r.report_type)+'</span>';
    html+='<span class="expand-icon" id="icon-'+bid+'">›</span></div>';
    html+='</div>';
    html+='<div class="report-body" id="'+bid+'">';
    html+=kpiHtml+summaryHtml+fieldsHtml+actionHtml+recoHtml+ownerNoteHtml+delBtn;
    html+='</div></div>';
  });
  return html;
}

function toggleReport(id,header){const body=document.getElementById(id);const icon=document.getElementById('icon-'+id);body.classList.toggle('open');icon.classList.toggle('open');}

async function addOwnerNote(reportId){
  const note = prompt('Add your note to this report (visible to client, original report unchanged):');
  if(!note||!note.trim()) return;
  const {error} = await db.from('reports').update({owner_note: note.trim()}).eq('id', reportId);
  if(error){ alert('Could not save note: '+error.message); return; }
  // Reload current view
  const cvList = document.getElementById('cv-reports-list');
  if(cvList && _currentViewClientId) loadClientViewReports(_currentViewClientId);
  else loadReports();
}

async function editOwnerNote(reportId, currentNote){
  const note = prompt('Edit your note:', currentNote);
  if(note===null) return; // cancelled
  const {error} = await db.from('reports').update({owner_note: note.trim()||null}).eq('id', reportId);
  if(error){ alert('Could not save note: '+error.message); return; }
  const cvList = document.getElementById('cv-reports-list');
  if(cvList && _currentViewClientId) loadClientViewReports(_currentViewClientId);
  else loadReports();
}

async function deleteReportById(id){
  if(!confirm('Delete this report? This cannot be undone.'))return;
  const {error}=await db.from('reports').delete().eq('id',id);
  if(error){alert('Could not delete: '+error.message);return;}
  // Reload whichever view is active
  const cvList=document.getElementById('cv-reports-list');
  if(cvList&&_currentViewClientId) loadClientViewReports(_currentViewClientId);
  else loadReports();
}

// ── FIX 2: View Client → opens dedicated report page for that client ──
let _currentViewClientId=null;

async function viewClient(clientId){
  _currentViewClientId=clientId;
  const client=allClients.find(c=>c.id===clientId);if(!client)return;

  // Switch to a full client view — hide all other pages, show client-view page
  showPage('page-client-view');

  document.getElementById('client-view-name').textContent=client.business_name;
  document.getElementById('client-view-niche').textContent=nicheLabels[client.niche]||client.niche||'—';
  document.getElementById('client-view-package').textContent=client.package;
  document.getElementById('client-view-status').textContent=client.status;

  // Load all reports for this client — persisted in DB, always up to date
  loadClientViewReports(clientId);
  loadClientViewTab('cv-reports',clientId);
}

async function loadClientViewReports(clientId){
  const {data}=await db.from('reports').select('*').eq('client_id',clientId).order('date',{ascending:false});
  document.getElementById('cv-reports-list').innerHTML=renderReportList(data||[]);
}

async function loadClientViewTab(tab,clientId){
  const cid=clientId||_currentViewClientId;
  document.querySelectorAll('.cv-tab').forEach(b=>b.classList.remove('active'));
  const btn=document.getElementById('cvtab-'+tab);if(btn)btn.classList.add('active');
  document.querySelectorAll('.cv-tab-content').forEach(t=>t.style.display='none');
  const panel=document.getElementById(tab+'-panel');if(panel)panel.style.display='block';

  if(tab==='cv-reports'){
    const {data}=await db.from('reports').select('*').eq('client_id',cid).order('date',{ascending:false});
    document.getElementById('cv-reports-list').innerHTML=renderReportList(data||[]);
  } else if(tab==='cv-complaints'){
    const {data}=await db.from('complaints').select('*').eq('client_id',cid).order('date',{ascending:false});
    document.getElementById('cv-complaints-list').innerHTML=data&&data.length?renderComplaintList(data,true):'<p style="font-family:Arial;font-size:13px;color:var(--muted)">No complaints.</p>';
  } else if(tab==='cv-actions'){
    const {data}=await db.from('actions').select('*').eq('client_id',cid).neq('status','Done').order('due_date',{ascending:true});
    document.getElementById('cv-actions-list').innerHTML=data&&data.length?data.map(a=>'<div style="padding:10px 0;border-bottom:1px solid var(--border);font-family:Arial;font-size:13px"><strong>'+a.description+'</strong><br><span style="color:var(--muted);font-size:11px">Due: '+a.due_date+' · Owner: '+a.owner+' · Priority: '+a.priority+'</span></div>').join(''):'<p style="font-family:Arial;font-size:13px;color:var(--muted)">No outstanding actions.</p>';
  } else if(tab==='cv-staff'){
    const {data}=await db.from('client_staff').select('*').eq('client_id',cid).order('name',{ascending:true});
    const sb={Active:'badge-green',Probation:'badge-gold',Flagged:'badge-red',Terminated:'badge-gray'};
    document.getElementById('cv-staff-list').innerHTML=data&&data.length?data.map(s=>'<div style="padding:10px 0;border-bottom:1px solid var(--border);display:flex;justify-content:space-between;align-items:center"><div><strong style="font-family:Arial;font-size:13px">'+s.name+'</strong> — <span style="font-family:Arial;font-size:12px;color:var(--muted)">'+s.role+'</span><br><span style="font-family:Arial;font-size:11px;color:var(--muted)">Last trained: '+(s.last_training_date||'Not recorded')+'</span></div><span class="badge '+(sb[s.status]||'badge-gray')+'">'+s.status+'</span></div>').join(''):'<p style="font-family:Arial;font-size:13px;color:var(--muted)">No staff records.</p>';
  } else if(tab==='cv-docs'){
    const {data}=await db.from('documents').select('*').eq('client_id',cid).order('date_added',{ascending:false});
    document.getElementById('cv-docs-list').innerHTML=data&&data.length?data.map(d=>'<div style="padding:10px 0;border-bottom:1px solid var(--border);display:flex;justify-content:space-between;align-items:center"><div><strong style="font-family:Arial;font-size:13px">'+d.title+'</strong><br><span style="font-family:Arial;font-size:11px;color:var(--muted)">'+d.doc_type+' · v'+d.version+'</span></div>'+(d.link?'<a href="'+d.link+'" target="_blank" style="color:var(--gold);font-family:Arial;font-size:12px">Open ↗</a>':'')+'</div>').join(''):'<p style="font-family:Arial;font-size:13px;color:var(--muted)">No documents.</p>';
  } else if(tab==='cv-kpis'){
    await loadClientViewTab_KPI(cid);
  } else if(tab==='cv-compliance'){
    await loadCVCompliance(cid);
  }
}

// ── CLIENT COMPLIANCE (ADMIN SIDE ONLY) ─────────────────────

async function loadCVCompliance(clientId) {
  const el = document.getElementById('cv-compliance-list');
  if (!el) return;
  el.innerHTML = '<div style="font-family:Arial;font-size:13px;color:var(--muted);padding:16px 0">Loading…</div>';

  const [bodiesRes, statusRes] = await Promise.all([
    db.from('compliance_bodies').select('*').order('country').order('tier').order('name'),
    db.from('compliance_status').select('*').eq('client_id', clientId)
  ]);

  const allBodies = bodiesRes.data || [];
  const statuses = statusRes.data || [];
  const statusMap = {};
  statuses.forEach(s => { statusMap[s.body_id] = s; });
  const assigned = allBodies.filter(b => statusMap[b.id]);
  const today = new Date();

  const done = statuses.filter(s=>s.status==='Done').length;
  const overdue = statuses.filter(s=>s.next_due&&new Date(s.next_due)<today&&s.status!=='Done'&&s.status!=='N/A').length;

  const statsHtml = assigned.length ? `<div class="stats-row" style="margin-bottom:16px">
    ${[['Assigned',assigned.length,''],['Done',done,'color:var(--success)'],['Overdue',overdue,'color:var(--danger)'],['Not Started',statuses.filter(s=>s.status==='Not Started').length,'color:var(--danger)']].map(([l,v,s])=>`<div class="stat-box"><div class="stat-num" style="${s}">${v}</div><div class="stat-label">${l}</div></div>`).join('')}
  </div>` : '';

  const toolbarHtml = `<div style="display:flex;gap:8px;flex-wrap:wrap;align-items:center;margin-bottom:16px">
    <button class="btn-add" onclick="openCVAssignModal('${clientId}')">+ Assign / Edit Bodies</button>
    <button onclick="printCVComplianceReport('${clientId}')" style="padding:7px 14px;background:var(--ink);color:white;border:none;border-radius:2px;font-family:Arial;font-size:12px;font-weight:700;cursor:pointer;letter-spacing:.5px">📄 Print Report</button>
    <button onclick="summariseCompliance('${clientId}')" style="padding:7px 14px;background:var(--gold);color:var(--ink);border:none;border-radius:2px;font-family:Arial;font-size:12px;font-weight:700;cursor:pointer;letter-spacing:.5px">✨ Summarise & Save as Report</button>
    <select onchange="filterCVCompliance(this.value)" style="padding:7px 10px;border:1px solid var(--border);border-radius:2px;font-family:Arial;font-size:12px;color:var(--ink)">
      <option value="all">All Statuses</option>
      <option value="Not Started">Not Started</option>
      <option value="In Progress">In Progress</option>
      <option value="Done">Done</option>
      <option value="N/A">N/A</option>
    </select>
  </div>`;

  if (!assigned.length) {
    el.innerHTML = toolbarHtml + '<div style="text-align:center;padding:48px;font-family:Arial;font-size:13px;color:var(--muted)">No bodies assigned yet. Click + Assign / Edit Bodies to start.</div>';
    return;
  }

  const grouped = {};
  assigned.forEach(b => {
    const key = (b.country||'Other') + ' — ' + (b.tier||'General');
    if (!grouped[key]) grouped[key] = [];
    grouped[key].push(b);
  });

  const tableHtml = Object.entries(grouped).map(([key, group]) => {
    const rows = group.map(b => {
      const s = statusMap[b.id] || {};
      const status = s.status || 'Not Started';
      const scls = {'Done':'sbc-done','In Progress':'sbc-in-progress','N/A':'sbc-na'}[status] || 'sbc-not-started';
      const isOverdue = s.next_due && new Date(s.next_due) < today && status !== 'Done' && status !== 'N/A';
      const dueSoon = s.next_due && !isOverdue && ((new Date(s.next_due)-today)/(86400000)) <= 30;
      return `<tr class="cv-comp-row" data-status="${status}" style="${isOverdue?'background:var(--danger-light)':''}">
        <td><strong style="font-family:Arial;font-size:13px">${b.name}</strong><br><span style="font-size:10px;color:var(--muted)">${b.abbr||''} · ${b.frequency||''}</span></td>
        <td><span class="status-badge-comp ${scls}">${status}</span></td>
        <td style="font-family:Arial;font-size:12px">${s.date_done||'—'}</td>
        <td style="font-family:Arial;font-size:12px">${s.next_due||'—'}${isOverdue?' <span class="badge badge-red" style="font-size:9px">Overdue</span>':dueSoon?' <span class="pending-badge" style="font-size:9px">Due Soon</span>':''}</td>
        <td style="font-family:Arial;font-size:12px;color:var(--muted)">${s.reference_number||'—'}</td>
        <td style="font-family:Arial;font-size:12px;color:var(--muted);max-width:160px">${s.notes||'—'}</td>
        <td><button class="btn-sm" onclick="openCVStatusModal('${b.id}','${clientId}','${b.name.replace(/'/g,"\\'")}','${b.frequency||''}')" style="font-size:11px">Update</button></td>
      </tr>`;
    }).join('');
    return `<div class="comp-tier-header">🌍 ${key}</div>
    <div class="card" style="padding:0;overflow:hidden;margin-bottom:12px">
      <table class="data-table">
        <thead><tr><th>Body</th><th>Status</th><th>Date Done</th><th>Next Due</th><th>Reference</th><th>Notes</th><th></th></tr></thead>
        <tbody>${rows}</tbody>
      </table>
    </div>`;
  }).join('');

  el.innerHTML = statsHtml + toolbarHtml + `<div id="cv-comp-table">${tableHtml}</div>`;
}

function filterCVCompliance(filter) {
  document.querySelectorAll('.cv-comp-row').forEach(row => {
    row.style.display = filter === 'all' || row.dataset.status === filter ? '' : 'none';
  });
}

function openCVAssignModal(clientId) {
  const clientName = allClients.find(c=>c.id===clientId)?.business_name || 'Client';
  document.getElementById('cv-assign-modal-title').textContent = `Assign Bodies — ${clientName}`;
  document.getElementById('cv-assign-client-id').value = clientId;

  // Get already assigned
  const assignedIds = new Set(
    Object.keys(_compStatuses).filter(k=>k.endsWith('_'+clientId)).map(k=>k.replace('_'+clientId,''))
  );

  const grouped = {};
  _compBodies.forEach(b => {
    const key = (b.country||'Other') + ' — ' + (b.tier||'General');
    if (!grouped[key]) grouped[key] = [];
    grouped[key].push(b);
  });

  let html = '';
  Object.entries(grouped).forEach(([key, group]) => {
    html += `<div style="font-family:Arial;font-size:10px;letter-spacing:1.5px;text-transform:uppercase;color:var(--gold);margin:14px 0 6px;padding-bottom:4px;border-bottom:1px solid var(--border)">${key}</div>`;
    group.forEach(b => {
      html += `<label class="cv-assign-row" data-name="${b.name.toLowerCase()}" data-country="${b.country||''}" style="display:flex;align-items:center;gap:10px;padding:6px 4px;cursor:pointer;font-family:Arial;font-size:13px;color:var(--ink)">
        <input type="checkbox" class="cv-assign-cb" value="${b.id}" ${assignedIds.has(b.id)?'checked':''} style="width:15px;height:15px;flex-shrink:0">
        <span style="flex:1">${b.name}</span>
        <span style="font-size:10px;color:var(--muted)">${b.abbr||''}</span>
      </label>`;
    });
  });

  document.getElementById('cv-assign-body-list').innerHTML = html;
  document.getElementById('cv-assign-count').textContent = `${assignedIds.size} assigned`;
  openModal('modal-cv-assign');
}

async function saveCVAssignedBodies() {
  const clientId = document.getElementById('cv-assign-client-id').value;
  const fb = document.getElementById('cv-assign-feedback');
  fb.innerHTML = '<span style="font-family:Arial;font-size:12px;color:var(--muted)">Saving…</span>';

  const checked = [...document.querySelectorAll('.cv-assign-cb:checked')].map(cb=>cb.value);
  const {data: existingRows} = await db.from('compliance_status').select('body_id').eq('client_id', clientId);
  const existing = new Set((existingRows||[]).map(r=>r.body_id));
  const toAdd = checked.filter(id=>!existing.has(id));
  const toRemove = [...existing].filter(id=>!checked.includes(id));

  if (toRemove.length) await db.from('compliance_status').delete().eq('client_id',clientId).in('body_id',toRemove);
  if (toAdd.length) {
    const {error} = await db.from('compliance_status').insert(toAdd.map(bodyId=>({body_id:bodyId,client_id:clientId,status:'Not Started',updated_at:new Date().toISOString()})));
    if (error) { fb.innerHTML = `<span style="color:var(--danger);font-size:12px">${error.message}</span>`; return; }
  }

  // Refresh local cache for this client
  const {data} = await db.from('compliance_status').select('*').eq('client_id',clientId);
  (data||[]).forEach(s => { _compStatuses[s.body_id+'_'+s.client_id] = s; });

  fb.innerHTML = '<span style="color:var(--success);font-size:12px">✓ Saved</span>';
  await loadCVCompliance(clientId);
  setTimeout(()=>{ closeModal('modal-cv-assign'); fb.innerHTML=''; }, 800);
}

function openCVStatusModal(bodyId, clientId, bodyName, frequency) {
  document.getElementById('cv-status-body-id').value = bodyId;
  document.getElementById('cv-status-client-id').value = clientId;
  document.getElementById('cv-status-freq').value = frequency;
  document.getElementById('cv-status-modal-title').textContent = bodyName;
  const s = _compStatuses[bodyId+'_'+clientId] || {};
  document.getElementById('cv-status-val').value = s.status || 'Not Started';
  document.getElementById('cv-status-done-date').value = s.date_done || '';
  document.getElementById('cv-status-next-due').value = s.next_due || '';
  document.getElementById('cv-status-ref').value = s.reference_number || '';
  document.getElementById('cv-status-contact').value = s.contact_name || '';
  document.getElementById('cv-status-notes').value = s.notes || '';
  openModal('modal-cv-status');
}

async function saveCVStatus() {
  const bodyId = document.getElementById('cv-status-body-id').value;
  const clientId = document.getElementById('cv-status-client-id').value;
  const fb = document.getElementById('cv-status-feedback');
  const dateDone = document.getElementById('cv-status-done-date').value || null;
  const nextDue = document.getElementById('cv-status-next-due').value || null;
  const payload = {
    body_id: bodyId, client_id: clientId,
    status: document.getElementById('cv-status-val').value,
    date_done: dateDone, next_due: nextDue,
    reference_number: document.getElementById('cv-status-ref').value.trim(),
    contact_name: document.getElementById('cv-status-contact').value.trim(),
    notes: document.getElementById('cv-status-notes').value.trim(),
    updated_at: new Date().toISOString(),
  };
  const {error} = await db.from('compliance_status').upsert(payload, {onConflict:'body_id,client_id'});
  if (error) { fb.innerHTML=`<div class="error-msg">${error.message}</div>`; return; }
  _compStatuses[bodyId+'_'+clientId] = payload;
  fb.innerHTML='<div class="success-msg">✓ Saved.</div>';
  await loadCVCompliance(clientId);
  setTimeout(()=>{ closeModal('modal-cv-status'); fb.innerHTML=''; }, 800);
}

async function printCVComplianceReport(clientId) {
  const client = allClients.find(c=>c.id===clientId);
  const clientName = client ? client.business_name : 'Client';
  const today = new Date().toLocaleDateString('en-GB',{day:'numeric',month:'long',year:'numeric'});

  // Fetch directly from Supabase
  const [bodiesRes, statusRes] = await Promise.all([
    db.from('compliance_bodies').select('*'),
    db.from('compliance_status').select('*').eq('client_id', clientId)
  ]);
  const allBodies = bodiesRes.data || [];
  const statuses = statusRes.data || [];
  const statusMap = {};
  statuses.forEach(s => { statusMap[s.body_id] = s; });
  const assigned = allBodies.filter(b => statusMap[b.id]);

  if(!assigned.length){ alert('No compliance bodies assigned yet.'); return; }

  const done = statuses.filter(s=>s.status==='Done').length;
  const pct = Math.round((done/assigned.length)*100);

  const grouped = {};
  assigned.forEach(b=>{
    const key=(b.country||'Other')+' — '+(b.tier||'General');
    if(!grouped[key])grouped[key]=[];
    grouped[key].push(b);
  });

  let tableRows = '';
  Object.entries(grouped).forEach(([key,group])=>{
    tableRows += '<tr style="background:#f5f0eb"><td colspan="5" style="padding:8px 12px;font-size:10px;letter-spacing:1.5px;text-transform:uppercase;color:#7A4F00"><strong>'+key+'</strong></td></tr>';
    group.forEach(b=>{
      const s=statusMap[b.id]||{};
      const col=s.status==='Done'?'#1A5C38':s.status==='In Progress'?'#7A4F00':s.status==='N/A'?'#7e7b67':'#8B1A1A';
      tableRows += '<tr><td style="padding:8px 12px;font-size:12px"><strong>'+b.name+'</strong><br><span style="color:#7e7b67;font-size:10px">'+( b.abbr||'')+'</span></td>';
      tableRows += '<td style="padding:8px 12px;font-size:12px;color:'+col+';font-weight:600">'+(s.status||'Not Started')+'</td>';
      tableRows += '<td style="padding:8px 12px;font-size:12px">'+(s.date_done||'—')+'</td>';
      tableRows += '<td style="padding:8px 12px;font-size:12px">'+(s.next_due||'—')+'</td>';
      tableRows += '<td style="padding:8px 12px;font-size:12px;color:#7e7b67">'+(s.reference_number||'—')+'</td></tr>';
    });
  });

  const html = '<!DOCTYPE html><html><head><title>Compliance Report — '+clientName+'</title>'
    +'<style>body{font-family:Arial,sans-serif;margin:0;padding:32px;color:#1e1e1e}'
    +'table{width:100%;border-collapse:collapse}td,th{border-bottom:1px solid #dbc9ba;text-align:left}'
    +'th{font-size:10px;letter-spacing:1px;text-transform:uppercase;color:#7e7b67;padding:8px 12px}'
    +'@media print{body{padding:0}}</style></head><body>'
    +'<div style="background:#1e1e1e;color:white;padding:20px 24px;margin-bottom:24px">'
    +'<div style="font-size:20px;font-weight:bold;margin-bottom:4px">Compliance Report — '+clientName+'</div>'
    +'<div style="font-size:12px;opacity:.7">Generated: '+today+' · Haida Yammar Consulting</div></div>'
    +'<div style="display:flex;gap:24px;margin-bottom:24px">'
    +'<div style="text-align:center"><div style="font-size:28px;font-weight:bold">'+assigned.length+'</div><div style="font-size:10px;color:#7e7b67;text-transform:uppercase;letter-spacing:1px">Assigned</div></div>'
    +'<div style="text-align:center"><div style="font-size:28px;font-weight:bold;color:#1A5C38">'+done+'</div><div style="font-size:10px;color:#7e7b67;text-transform:uppercase;letter-spacing:1px">Done</div></div>'
    +'<div style="text-align:center"><div style="font-size:28px;font-weight:bold;color:#1A5C38">'+pct+'%</div><div style="font-size:10px;color:#7e7b67;text-transform:uppercase;letter-spacing:1px">Complete</div></div>'
    +'</div>'
    +'<table><thead><tr><th>Body</th><th>Status</th><th>Date Done</th><th>Next Due</th><th>Reference</th></tr></thead>'
    +'<tbody>'+tableRows+'</tbody></table>'
    +'<div style="margin-top:32px;font-size:10px;color:#7e7b67;border-top:1px solid #dbc9ba;padding-top:12px">Haida Yammar Consulting · haidayammar01@gmail.com</div>'
    +'</body></html>';

  const win = window.open('','_blank');
  win.document.write(html);
  win.document.close();
  setTimeout(()=>win.print(),400);
}

async function summariseCompliance(clientId) {
  const client = allClients.find(c=>c.id===clientId);
  const clientName = client?.business_name || 'Client';

  const btn = event.target;
  const originalText = btn.textContent;
  btn.textContent = '⏳ Generating…';
  btn.disabled = true;

  const [bodiesRes, statusRes] = await Promise.all([
    db.from('compliance_bodies').select('*'),
    db.from('compliance_status').select('*').eq('client_id', clientId)
  ]);

  const allBodies = bodiesRes.data || [];
  const statuses = statusRes.data || [];
  const statusMap = {};
  statuses.forEach(s => { statusMap[s.body_id] = s; });
  const assigned = allBodies.filter(b => statusMap[b.id]);

  if(!assigned.length){
    btn.textContent = originalText;
    btn.disabled = false;
    alert('No compliance bodies assigned to this client yet.');
    return;
  }

  const today = new Date();
  const todayStr = today.toLocaleDateString('en-GB',{day:'numeric',month:'long',year:'numeric'});

  const done = statuses.filter(s=>s.status==='Done');
  const inProgress = statuses.filter(s=>s.status==='In Progress');
  const notStarted = statuses.filter(s=>s.status==='Not Started');
  const na = statuses.filter(s=>s.status==='N/A');
  const pct = Math.round((done.length/assigned.length)*100);

  // Overdue and due soon
  const overdue = assigned.filter(b=>{
    const s = statusMap[b.id];
    return s?.next_due && new Date(s.next_due) < today && s.status !== 'Done' && s.status !== 'N/A';
  });
  const dueSoon = assigned.filter(b=>{
    const s = statusMap[b.id];
    if(!s?.next_due || s.status === 'Done' || s.status === 'N/A') return false;
    const days = (new Date(s.next_due) - today) / (1000*60*60*24);
    return days >= 0 && days <= 30;
  });

  // Group countries
  const countries = [...new Set(assigned.map(b=>b.country).filter(Boolean))];

  // Build summary text
  let summary = `COMPLIANCE SUMMARY REPORT\n`;
  summary += `Client: ${clientName}\n`;
  summary += `Date: ${todayStr}\n`;
  summary += `Prepared by: Haida Yammar Consulting\n\n`;

  summary += `OVERVIEW\n`;
  summary += `As of ${todayStr}, ${clientName} has ${assigned.length} regulatory compliance bod${assigned.length===1?'y':'ies'} assigned`;
  if(countries.length) summary += ` across ${countries.join(', ')}`;
  summary += `.\n\n`;

  summary += `COMPLIANCE STATUS\n`;
  summary += `• Completed: ${done.length} of ${assigned.length} (${pct}%)\n`;
  if(inProgress.length) summary += `• In Progress: ${inProgress.length}\n`;
  if(notStarted.length) summary += `• Not Yet Started: ${notStarted.length}\n`;
  if(na.length) summary += `• Not Applicable: ${na.length}\n\n`;

  if(overdue.length){
    summary += `IMMEDIATE ATTENTION REQUIRED\n`;
    summary += `The following ${overdue.length} item${overdue.length===1?'':'s'} ${overdue.length===1?'is':'are'} overdue:\n`;
    overdue.forEach(b=>{
      const s = statusMap[b.id];
      summary += `• ${b.name} — due ${s.next_due}${s.notes?'. Note: '+s.notes:''}\n`;
    });
    summary += '\n';
  }

  if(dueSoon.length){
    summary += `DUE WITHIN 30 DAYS\n`;
    dueSoon.forEach(b=>{
      const s = statusMap[b.id];
      summary += `• ${b.name} — due ${s.next_due}\n`;
    });
    summary += '\n';
  }

  if(done.length){
    summary += `COMPLETED\n`;
    done.forEach(s=>{
      const b = allBodies.find(x=>x.id===s.body_id);
      if(!b) return;
      summary += `• ${b.name}`;
      if(s.date_done) summary += ` — completed ${s.date_done}`;
      if(s.reference_number) summary += ` (Ref: ${s.reference_number})`;
      if(s.next_due) summary += ` · Next renewal: ${s.next_due}`;
      summary += '\n';
    });
    summary += '\n';
  }

  if(notStarted.length){
    summary += `OUTSTANDING — ACTION NEEDED\n`;
    notStarted.forEach(s=>{
      const b = allBodies.find(x=>x.id===s.body_id);
      if(!b) return;
      summary += `• ${b.name}`;
      if(b.deadline) summary += ` — ${b.deadline}`;
      if(s.notes) summary += `. ${s.notes}`;
      summary += '\n';
    });
    summary += '\n';
  }

  // Notes from all entries
  const withNotes = assigned.filter(b=>statusMap[b.id]?.notes);
  if(withNotes.length){
    summary += `NOTES & OBSERVATIONS\n`;
    withNotes.forEach(b=>{
      summary += `• ${b.name}: ${statusMap[b.id].notes}\n`;
    });
    summary += '\n';
  }

  summary += `---\nThis report was prepared by Haida Yammar Consulting. For queries contact haidayammar01@gmail.com`;

  // Save as report
  const {error} = await db.from('reports').insert({
    client_id: clientId,
    report_type: 'Compliance Summary',
    title: `Compliance Summary Report — ${todayStr}`,
    content: summary,
    date: new Date().toISOString().split('T')[0],
    created_by: currentUser?.name || 'Haida',
    created_at: new Date().toISOString(),
  });

  btn.textContent = originalText;
  btn.disabled = false;

  if(error){ alert('Generated but could not save: ' + error.message); return; }

  // Show popup
  const div = document.createElement('div');
  div.className = 'comp-summary-modal';
  div.innerHTML = `<div style="position:fixed;inset:0;background:rgba(0,0,0,.5);z-index:1000;display:flex;align-items:center;justify-content:center">
    <div style="background:white;border-radius:4px;padding:28px;max-width:640px;width:90%;max-height:80vh;overflow-y:auto;font-family:Arial">
      <div style="display:flex;justify-content:space-between;align-items:center;margin-bottom:16px">
        <h3 style="font-size:16px;color:#644a3c;margin:0">Compliance Summary — ${clientName}</h3>
        <button onclick="this.closest('.comp-summary-modal').remove()" style="background:none;border:none;font-size:20px;cursor:pointer;color:#999">×</button>
      </div>
      <div id="summary-text-content" style="font-size:12px;line-height:1.8;color:#333;white-space:pre-wrap;background:#f9f6f3;padding:16px;border-radius:2px">${summary}</div>
      <div style="margin-top:16px;padding-top:16px;border-top:1px solid #eee;display:flex;gap:8px;align-items:center">
        <button onclick="navigator.clipboard.writeText(document.getElementById('summary-text-content').textContent)" style="padding:7px 14px;background:#644a3c;color:white;border:none;border-radius:2px;font-family:Arial;font-size:12px;cursor:pointer">Copy</button>
        <button onclick="this.closest('.comp-summary-modal').remove()" style="padding:7px 14px;background:#f5f0eb;color:#644a3c;border:1px solid #dbc9ba;border-radius:2px;font-family:Arial;font-size:12px;cursor:pointer">Close</button>
        <span style="font-size:11px;color:#999;margin-left:4px">✓ Saved to ${clientName}'s Reports tab</span>
      </div>
    </div>
  </div>`;
  document.body.appendChild(div);
}
// Haida services with pricing
// ══ HAIDA SERVICES PRICING MATRIX ══
// Prices are BASE per month per tier. Duration multipliers applied in UI.
// Max discount: 10% hard cap, no stacking.

const HAIDA_PACKAGES = {
  services: [
    {
      id: 'ops_management',
      label: 'Operations Management',
      desc: 'Monthly ops oversight, systems management, process improvement, reporting',
      icon: '⚙️',
      prices: { small: 350000, medium: 700000, large: 1400000, enterprise: 2800000 },
      duration_type: 'retainer', // monthly × duration
    },
    {
      id: 'audit',
      label: 'Audit',
      desc: 'Operations audit, site visits, findings report, recommendations — one audit per month',
      icon: '🔍',
      prices: { small: 250000, medium: 500000, large: 1000000, enterprise: 2000000 },
      duration_type: 'retainer',
    },
    {
      id: 'staff',
      label: 'Staff Recruitment, Testing & Training',
      desc: 'Recruitment support, psychometric testing, full training programme delivery',
      icon: '👥',
      prices: { small: 500000, medium: 1000000, large: 2000000, enterprise: 4000000 },
      duration_type: 'retainer',
    },
    {
      id: 'cx',
      label: 'CX / Customer Experience Support',
      desc: 'Outsourced CX team managed by Haida. Includes ₦500,000 one-time setup fee.',
      icon: '💬',
      prices: { small: 800000, medium: 1300000, large: 2300000, enterprise: 4300000 },
      setup_fee: 500000,
      duration_type: 'retainer',
    },
    {
      id: 'sop',
      label: 'SOP Development',
      desc: 'Custom SOPs, checklists, procedure manuals, staff handbooks',
      icon: '📋',
      prices: { small: 350000, medium: 700000, large: 1400000, enterprise: 2800000 },
      duration_type: 'onetime', // one-off or with support months
    },
    {
      id: 'compliance',
      label: 'Compliance Check & Management',
      desc: 'Regulatory compliance audit, tracker management, body liaison, renewals',
      icon: '✅',
      prices: { small: 300000, medium: 600000, large: 1200000, enterprise: 2400000 },
      duration_type: 'retainer',
    },
    {
      id: 'brand',
      label: 'Brand Strategy',
      desc: 'Brand identity, positioning, content strategy, 12-week content calendar',
      icon: '🎨',
      prices: { small: 500000, medium: 1000000, large: 2000000, enterprise: 4000000 },
      duration_type: 'onetime',
    },
    {
      id: 'event',
      label: 'Corporate Event Planning',
      desc: 'Full corporate event management — vendor coordination, logistics, production, day-of management. Strictly corporate events. Vendor costs billed separately.',
      icon: '🎯',
      prices: { small: 1500000, medium: 3500000, large: 7000000, enterprise: 15000000 },
      duration_type: 'perevent',
    },
    {
      id: 'full',
      label: 'Full Package',
      desc: 'Everything: Ops + Audit + Staff + CX + SOPs + Compliance + Brand. Excludes Event Planning. ~10% bundle savings already applied.',
      icon: '🚀',
      prices: { small: 2200000, medium: 4400000, large: 8800000, enterprise: 17600000 },
      duration_type: 'retainer',
      is_bundle: true,
    },
  ],
  durations: [
    { id: '3', label: '3 Months', multiplier: 3 },
    { id: '6', label: '6 Months', multiplier: 6 },
    { id: '12', label: '12 Months', multiplier: 12 },
  ],
  tiers: [
    { id: 'small', label: 'Small Business', desc: '1–10 staff' },
    { id: 'medium', label: 'Medium Business', desc: '11–30 staff' },
    { id: 'large', label: 'Large Business', desc: '31–100 staff' },
    { id: 'enterprise', label: 'Enterprise', desc: '100+ staff or complex' },
  ],
  discount_max: 0.10, // 10% hard cap — no exceptions
};

// Niche service templates — recommended service combos per business type
const NICHE_TEMPLATES = [
  {
    id: 'food_cafe',
    label: '🍽️ Food & Beverage / Café',
    services: ['ops_management','audit','staff','compliance','sop'],
    duration: '3',
    tier: 'small',
    notes: 'Priority: Ops Management to establish daily routines, Audit monthly, Compliance for NAFDAC/AMAC/NTDC, SOPs for kitchen and service standards, Staff training on food hygiene.',
  },
  {
    id: 'aesthetics',
    label: '💆 Aesthetics & Wellness',
    services: ['ops_management','compliance','sop','staff'],
    duration: '3',
    tier: 'small',
    notes: 'Priority: Compliance first (NAFDAC, MOH facility licence), SOPs for clinical protocols and infection control, Staff training and recruitment, Ops for smooth daily operations.',
  },
  {
    id: 'retail',
    label: '🛍️ Retail',
    services: ['ops_management','audit','sop','cx'],
    duration: '3',
    tier: 'small',
    notes: 'Priority: Ops Management for daily processes, CX support for customer-facing team, Audit to catch stock and service gaps, SOPs for sales floor and back office.',
  },
  {
    id: 'education',
    label: '🎓 Education / School',
    services: ['ops_management','compliance','staff','sop'],
    duration: '6',
    tier: 'small',
    notes: 'Priority: Compliance (Ministry of Education registration, TRCN for teachers), Staff recruitment and training, SOPs for academic and administrative processes, Ops management.',
  },
  {
    id: 'hospitality',
    label: '🏨 Hospitality / Hotel',
    services: ['ops_management','audit','cx','staff','compliance'],
    duration: '6',
    tier: 'medium',
    notes: 'Priority: Full Ops management, monthly audits across departments, CX outsourcing for guest-facing team, Compliance for tourism and health regulations, Staff training.',
  },
  {
    id: 'corporate',
    label: '🏢 Corporate / SME',
    services: ['ops_management','audit','sop','brand'],
    duration: '6',
    tier: 'medium',
    notes: 'Priority: Operations management, quarterly audits, SOPs for internal processes, Brand strategy for positioning and growth.',
  },
  {
    id: 'startup',
    label: '🚀 Startup / Pre-Launch',
    services: ['sop','compliance','brand','ops_management'],
    duration: '3',
    tier: 'small',
    notes: 'Priority: SOPs before opening, Compliance for all regulatory requirements, Brand strategy for launch identity, Ops management to set up operating rhythm from day one.',
  },
  {
    id: 'cx_only',
    label: '📞 CX Support Only',
    services: ['cx'],
    duration: '3',
    tier: 'small',
    notes: 'Standalone CX outsourcing. ₦500,000 setup fee applies. Includes dedicated agents, team lead, and Haida supervision.',
  },
  {
    id: 'audit_only',
    label: '🔍 Audit Only',
    services: ['audit'],
    duration: '3',
    tier: 'small',
    notes: 'Standalone audit service. One audit per month with full written findings report and recommendations. No ongoing ops management.',
  },
  {
    id: 'compliance_only',
    label: '✅ Compliance Only',
    services: ['compliance'],
    duration: '6',
    tier: 'small',
    notes: 'Standalone compliance management. Track, manage, and renew all regulatory registrations. Best taken for 6 months minimum to cover the full registration cycle.',
  },
  {
    id: 'event_corp',
    label: '🎯 Corporate Event',
    services: ['event'],
    duration: '3',
    tier: 'medium',
    notes: 'One-off corporate event planning. Full production management, vendor coordination, logistics, day-of supervision. Vendor costs billed separately.',
  },
  {
    id: 'full_package',
    label: '🌟 Full Package Client',
    services: ['full'],
    duration: '6',
    tier: 'medium',
    notes: 'Full service engagement. Everything except event planning. Best value — bundle pricing applied. Recommended for new clients wanting end-to-end transformation.',
  },
];

// ── ONBOARDING QUOTE RENDERER ──

let _onbSelectedServices = new Set();
let _onbTier = 'small';
let _onbDuration = '3';
let _onbDiscount = 0;
let _onbManualOverride = null;

function renderServiceSelector(clientId) {
  const container = document.getElementById('onb-service-selector');
  if (!container) return;

  // Build per-service price state if not exists
  if (!window._onbPrices) window._onbPrices = {};
  HAIDA_PACKAGES.services.forEach(svc => {
    if (!window._onbPrices[svc.id]) {
      window._onbPrices[svc.id] = svc.prices[_onbTier] || 350000;
    }
  });

  // Recalculate totals from selected services using custom prices
  let subtotal = 0, setupFees = 0;
  _onbSelectedServices.forEach(id => {
    const svc = HAIDA_PACKAGES.services.find(s=>s.id===id);
    if (!svc) return;
    const price = window._onbPrices[id] || svc.prices[_onbTier] || 350000;
    const durMult = parseInt(_onbDuration)||3;
    subtotal += svc.duration_type === 'retainer' ? price * durMult : price;
    if (svc.setup_fee) setupFees += svc.setup_fee;
  });

  const discountAmt = _onbManualOverride !== null ? 0 : Math.round(subtotal * Math.min(_onbDiscount/100, 0.10));
  const total = _onbManualOverride !== null ? _onbManualOverride : (subtotal + setupFees - discountAmt);
  const durMult = parseInt(_onbDuration)||3;

  // Slider helpers — log scale ₦350k to ₦100m
  function sliderToPrice(val) {
    // val 0-100 maps to log scale 350000–100000000
    const min = Math.log(350000), max = Math.log(100000000);
    return Math.round(Math.exp(min + (val/100)*(max-min)) / 1000) * 1000;
  }
  function priceToSlider(price) {
    const min = Math.log(350000), max = Math.log(100000000);
    return Math.round(((Math.log(Math.max(price,350000))-min)/(max-min))*100);
  }

  container.innerHTML = `
    <!-- TIER + DURATION -->
    <div style="display:grid;grid-template-columns:1fr 1fr;gap:12px;margin-bottom:16px">
      <div class="form-field">
        <label>Client Tier (for reference pricing)</label>
        <select onchange="_onbTier=this.value;window._onbPrices={};renderServiceSelector('${clientId}')" style="font-family:Arial;font-size:13px">
          ${HAIDA_PACKAGES.tiers.map(t=>`<option value="${t.id}" ${_onbTier===t.id?'selected':''}>${t.label} — ${t.desc}</option>`).join('')}
        </select>
      </div>
      <div class="form-field">
        <label>Duration</label>
        <select onchange="_onbDuration=this.value;renderServiceSelector('${clientId}')" style="font-family:Arial;font-size:13px">
          ${HAIDA_PACKAGES.durations.map(d=>`<option value="${d.id}" ${_onbDuration===d.id?'selected':''}>${d.label}</option>`).join('')}
        </select>
      </div>
    </div>

    <!-- NICHE TEMPLATES -->
    <div style="margin-bottom:16px">
      <div style="font-family:Arial;font-size:11px;letter-spacing:1.5px;text-transform:uppercase;color:var(--gold);margin-bottom:8px">Quick Templates</div>
      <div style="display:flex;flex-wrap:wrap;gap:6px">
        ${NICHE_TEMPLATES.map(t=>`<button onclick="applyNicheTemplate('${t.id}','${clientId}')" style="padding:5px 12px;background:var(--surface2);border:1px solid var(--border);border-radius:2px;font-family:Arial;font-size:11px;cursor:pointer;color:var(--ink)">${t.label}</button>`).join('')}
        <button onclick="_onbSelectedServices.clear();window._onbPrices={};renderServiceSelector('${clientId}')" style="padding:5px 12px;background:var(--danger-light);border:1px solid var(--danger);border-radius:2px;font-family:Arial;font-size:11px;cursor:pointer;color:var(--danger)">Clear All</button>
      </div>
    </div>

    <!-- SERVICE TOGGLES WITH PRICE SLIDERS -->
    <div style="margin-bottom:16px">
      <div style="font-family:Arial;font-size:11px;letter-spacing:1.5px;text-transform:uppercase;color:var(--gold);margin-bottom:8px">Toggle Services & Set Price</div>
      <div style="display:flex;flex-direction:column;gap:8px">
        ${HAIDA_PACKAGES.services.map(svc => {
          const isSelected = _onbSelectedServices.has(svc.id);
          const refPrice = svc.prices[_onbTier] || 350000;
          const currentPrice = window._onbPrices[svc.id] || refPrice;
          const sliderVal = priceToSlider(currentPrice);
          const lineTotal = svc.duration_type==='retainer' ? currentPrice*durMult : currentPrice;
          return `<div style="border:2px solid ${isSelected?'var(--gold)':'var(--border)'};border-radius:2px;padding:12px 14px;background:${isSelected?'var(--gold-light)':'white'}">
            <div style="display:flex;justify-content:space-between;align-items:center;margin-bottom:${isSelected?'10px':'0'}">
              <label style="display:flex;align-items:center;gap:10px;cursor:pointer;flex:1">
                <input type="checkbox" ${isSelected?'checked':''} onchange="
                  if(this.checked){_onbSelectedServices.add('${svc.id}');}else{_onbSelectedServices.delete('${svc.id}');}
                  _onbManualOverride=null;renderServiceSelector('${clientId}')"
                  style="width:16px;height:16px;cursor:pointer;flex-shrink:0">
                <span style="font-size:13px;font-weight:${isSelected?'600':'normal'};color:var(--ink)">${svc.icon} ${svc.label}</span>
              </label>
              <div style="text-align:right;flex-shrink:0;margin-left:12px">
                <div style="font-family:Arial;font-size:13px;font-weight:600;color:${isSelected?'var(--ink)':'var(--muted)'}">₦${currentPrice.toLocaleString()}${svc.duration_type==='retainer'?'/mo':svc.duration_type==='perevent'?'/event':' one-off'}</div>
                ${isSelected&&svc.duration_type==='retainer'?`<div style="font-family:Arial;font-size:10px;color:var(--muted)">Total: ₦${lineTotal.toLocaleString()}</div>`:''}
              </div>
            </div>
            <div style="font-family:Arial;font-size:11px;color:var(--muted);margin-bottom:${isSelected?'8px':'0'};${isSelected?'':'display:none'}">${svc.desc}${svc.setup_fee?` · +₦${svc.setup_fee.toLocaleString()} setup fee`:''}</div>
            ${isSelected?`
            <div>
              <div style="display:flex;justify-content:space-between;font-family:Arial;font-size:10px;color:var(--muted);margin-bottom:3px">
                <span>₦350,000</span>
                <span style="color:var(--gold);font-size:10px">Reference: ₦${refPrice.toLocaleString()}/mo</span>
                <span>₦100,000,000</span>
              </div>
              <input type="range" min="0" max="100" value="${sliderVal}"
                oninput="
                  const min=Math.log(350000),max=Math.log(100000000);
                  const price=Math.round(Math.exp(min+(this.value/100)*(max-min))/1000)*1000;
                  window._onbPrices['${svc.id}']=price;
                  _onbManualOverride=null;
                  renderServiceSelector('${clientId}')"
                style="width:100%;accent-color:var(--gold)">
            </div>` : ''}
          </div>`;
        }).join('')}
      </div>
    </div>

    <!-- LIVE QUOTE -->
    <div style="background:var(--ink);color:white;border-radius:2px;padding:16px 20px;margin-bottom:12px">
      <div style="font-family:Arial;font-size:11px;letter-spacing:1.5px;text-transform:uppercase;opacity:.7;margin-bottom:12px">Live Quote</div>
      ${_onbSelectedServices.size === 0 ?
        '<div style="font-family:Arial;font-size:13px;opacity:.5">Toggle services above to build a quote</div>' :
        `<div style="margin-bottom:10px">
          ${Array.from(_onbSelectedServices).map(id => {
            const svc = HAIDA_PACKAGES.services.find(s=>s.id===id);
            if (!svc) return '';
            const price = window._onbPrices[id] || svc.prices[_onbTier]||350000;
            const lineTotal = svc.duration_type==='retainer' ? price*durMult : price;
            return `<div style="display:flex;justify-content:space-between;font-family:Arial;font-size:12px;padding:4px 0;border-bottom:1px solid rgba(255,255,255,.1)">
              <span>${svc.icon} ${svc.label}${svc.duration_type==='retainer'?' (×'+_onbDuration+'mo)':''}</span>
              <span>₦${lineTotal.toLocaleString()}</span>
            </div>`;
          }).join('')}
          ${setupFees>0?`<div style="display:flex;justify-content:space-between;font-family:Arial;font-size:12px;padding:4px 0;border-bottom:1px solid rgba(255,255,255,.1)"><span>Setup Fee</span><span>₦${setupFees.toLocaleString()}</span></div>`:''}
          ${discountAmt>0?`<div style="display:flex;justify-content:space-between;font-family:Arial;font-size:12px;padding:4px 0;border-bottom:1px solid rgba(255,255,255,.1);color:#aaffaa"><span>Discount (${_onbDiscount}%)</span><span>-₦${discountAmt.toLocaleString()}</span></div>`:''}
        </div>
        <div style="display:flex;justify-content:space-between;align-items:center;margin-top:8px">
          <div style="font-size:24px;font-weight:bold">₦${total.toLocaleString()}</div>
          <div style="font-family:Arial;font-size:11px;opacity:.7;text-align:right">${HAIDA_PACKAGES.tiers.find(t=>t.id===_onbTier)?.label}<br>${_onbDuration} months</div>
        </div>`
      }
    </div>

    ${_onbSelectedServices.size > 0 ? `
    <!-- DISCOUNT -->
    <div style="margin-bottom:12px">
      <div style="display:flex;justify-content:space-between;margin-bottom:6px">
        <label style="font-family:Arial;font-size:12px;color:var(--ink)">Discount</label>
        <span style="font-family:Arial;font-size:13px;font-weight:600;color:var(--ink)">${_onbDiscount}% ${_onbDiscount>=10?'<span style="color:var(--danger);font-size:10px">HARD CAP REACHED</span>':''}</span>
      </div>
      <input type="range" min="0" max="10" step="1" value="${_onbDiscount}"
        oninput="_onbDiscount=parseInt(this.value);_onbManualOverride=null;renderServiceSelector('${clientId}')"
        style="width:100%;accent-color:var(--gold)">
      <div style="display:flex;justify-content:space-between;font-family:Arial;font-size:10px;color:var(--muted)"><span>0%</span><span style="color:var(--danger);font-weight:600">10% MAX — NO EXCEPTIONS</span></div>
    </div>

    <!-- MANUAL OVERRIDE -->
    <div style="margin-bottom:16px">
      <label style="font-family:Arial;font-size:12px;color:var(--ink);display:block;margin-bottom:6px">Final Agreed Price Override <span style="color:var(--muted)">(optional — use if you've negotiated a specific number)</span></label>
      <div style="display:flex;gap:8px">
        <input type="number" id="onb-manual-price" placeholder="e.g. 5000000"
          value="${_onbManualOverride!==null?_onbManualOverride:''}"
          oninput="_onbManualOverride=this.value?parseInt(this.value):null;renderServiceSelector('${clientId}')"
          style="flex:1;padding:8px 12px;border:1px solid var(--border);border-radius:2px;font-family:Arial;font-size:13px;color:var(--ink)">
        <button onclick="_onbManualOverride=null;document.getElementById('onb-manual-price').value='';renderServiceSelector('${clientId}')"
          style="padding:8px 12px;background:var(--surface2);border:1px solid var(--border);border-radius:2px;font-family:Arial;font-size:12px;cursor:pointer">Reset</button>
      </div>
      ${_onbManualOverride!==null?'<div style="font-family:Arial;font-size:11px;color:var(--warn);margin-top:4px">⚠ Override active — slider prices ignored for total</div>':''}
    </div>

    <button onclick="saveOnbQuote('${clientId}')" class="btn-save" style="width:100%;padding:12px;font-size:14px">✓ Save Quote to Onboarding</button>
    ` : ''}

    <div id="onb-template-notes"></div>
  `;
}


function applyNicheTemplate(templateId, clientId) {
  const t = NICHE_TEMPLATES.find(x=>x.id===templateId);
  if (!t) return;
  _onbSelectedServices = new Set(t.services);
  _onbTier = t.tier;
  _onbDuration = t.duration;
  _onbDiscount = 0;
  _onbManualOverride = null;
  renderServiceSelector(clientId);
  // Show template notes
  const notes = document.getElementById('onb-template-notes');
  if (notes) notes.innerHTML = `<div style="margin-top:12px;padding:12px 14px;background:var(--gold-light);border-left:3px solid var(--gold);border-radius:2px;font-family:Arial;font-size:12px;color:var(--ink2);line-height:1.6"><strong>${t.label}</strong><br>${t.notes}</div>`;
}

async function saveOnbQuote(clientId) {
  if (!clientId) return;
  const tier = HAIDA_PACKAGES.tiers.find(t=>t.id===_onbTier)?.label || _onbTier;
  const durMult = parseInt(_onbDuration)||3;
  let subtotal = 0, setupFees = 0;
  const lines = [];
  HAIDA_PACKAGES.services.forEach(svc => {
    if (_onbSelectedServices.has(svc.id)) {
      const bp = svc.prices[_onbTier]||0;
      const lineTotal = svc.duration_type==='retainer' ? bp*durMult : bp;
      subtotal += lineTotal;
      if (svc.setup_fee) setupFees += svc.setup_fee;
      lines.push(`${svc.label}: ₦${lineTotal.toLocaleString()}`);
    }
  });
  const discountAmt = _onbManualOverride!==null ? 0 : Math.round(subtotal*Math.min(_onbDiscount/100,0.10));
  const total = _onbManualOverride!==null ? _onbManualOverride : (subtotal+setupFees-discountAmt);

  const quoteText = `Services: ${lines.join(' | ')} | Tier: ${tier} | Duration: ${_onbDuration} months | Total: ₦${total.toLocaleString()}${_onbManualOverride!==null?' (manual override)':''}`;

  // Save to onboarding data
  const existing = onbData['services'] || {};
  existing.quote_services = Array.from(_onbSelectedServices).join(',');
  existing.quote_tier = _onbTier;
  existing.quote_duration = _onbDuration;
  existing.quote_discount = _onbDiscount.toString();
  existing.quote_total = total.toString();
  existing.quote_summary = quoteText;
  existing._done = true;

  await db.from('onboarding').upsert({client_id:clientId, step_id:'services', data:JSON.stringify(existing)},{onConflict:'client_id,step_id'});
  onbData['services'] = existing;
  alert('✓ Quote saved to onboarding. Go to Proposal step to send it.');
}

const ONBOARDING_STEPS=[
  {id:'discovery',label:'Discovery Call',fields:[
    {id:'call_date',label:'Call Date',type:'date'},
    {id:'call_notes',label:'What was discussed',type:'textarea',placeholder:'Key points, pain points, what they need…'},
    {id:'decision_maker',label:'Decision maker name & role',type:'text'},
    {id:'referral_source',label:'How they heard about Haida Consulting',type:'text'},
    {id:'currency_pref',label:'Invoice Currency',type:'select',options:['NGN — Nigerian Naira (₦)','GBP — British Pounds (£)']},
  ]},
  {id:'services',label:'Services & Quote Builder',fields:[
    {id:'quote_summary',label:'Quote Summary (auto-filled)',type:'text',placeholder:'Will be filled when you save the quote above'},
    {id:'services_notes',label:'Additional scope notes',type:'textarea',placeholder:'Any bespoke requirements, adjustments, or context…'},
  ]},
  {id:'proposal',label:'Proposal Sent',fields:[
    {id:'proposal_date',label:'Date Sent',type:'date'},
    {id:'proposal_value',label:'Proposed Value',type:'text',placeholder:'e.g. ₦750,000 one-time'},
    {id:'proposal_scope',label:'Scope Proposed',type:'textarea'},
    {id:'proposal_feedback',label:'Client Feedback / Response',type:'textarea'},
  ]},
  {id:'contract',label:'Contract Signed',fields:[
    {id:'contract_date',label:'Date Signed',type:'date'},
    {id:'contract_type',label:'Contract Type',type:'text',placeholder:'e.g. Pre-opening package + 3-month retainer'},
    {id:'contract_value',label:'Agreed Value',type:'text'},
    {id:'contract_notes',label:'Any special terms noted',type:'textarea'},
  ]},
  {id:'payment',label:'First Payment Received',fields:[
    {id:'payment_date',label:'Date Received',type:'date'},
    {id:'payment_amount',label:'Amount Received',type:'text'},
    {id:'payment_method',label:'Payment Method',type:'text',placeholder:'e.g. Bank transfer — Summit Bank'},
    {id:'payment_notes',label:'Notes',type:'text'},
  ]},
  {id:'portal',label:'Portal Access Set Up',fields:[
    {id:'portal_date',label:'Date Set Up',type:'date'},
    {id:'portal_username',label:'Username Given',type:'text'},
    {id:'portal_sent',label:'Access code sent to client',type:'select',options:['Yes','No','Pending']},
    {id:'portal_notes',label:'Notes',type:'text'},
  ]},
  {id:'kickoff',label:'Kick-off / First Visit',fields:[
    {id:'kickoff_date',label:'Date',type:'date'},
    {id:'kickoff_type',label:'Type',type:'select',options:['On-site','Remote call','Hybrid']},
    {id:'kickoff_attendees',label:'Who was present',type:'text'},
    {id:'kickoff_notes',label:'Key outcomes and next steps',type:'textarea'},
  ]},
  {id:'first_report',label:'First Report Submitted',fields:[
    {id:'first_report_date',label:'Date of First Report',type:'date'},
    {id:'first_report_type',label:'Report Type',type:'text',placeholder:'e.g. Site Visit Note'},
    {id:'first_report_notes',label:'Notes',type:'text'},
  ]},
  {id:'rhythm',label:'Monthly Rhythm Established',fields:[
    {id:'rhythm_day',label:'Fixed contact / report day',type:'text',placeholder:'e.g. Wednesdays'},
    {id:'rhythm_next',label:'Next scheduled touchpoint',type:'date'},
    {id:'rhythm_notes',label:'Notes on communication preferences',type:'textarea'},
  ]},
];

let onbData={};
function toggleSvcCard(svcId, clientId){
  const card = document.getElementById('svc-card-'+svcId);
  const chk = document.getElementById('svcchk-'+svcId);
  if(chk.checked){ card.style.background='var(--gold-light)'; }
  else { card.style.background='white'; }
  // Recalculate total live
  const isGBP = (onbData['discovery']&&onbData['discovery']['currency_pref']&&onbData['discovery']['currency_pref'].includes('GBP'));
  let total = 0; let count = 0;
  HAIDA_SERVICES.forEach(function(svc){
    const c2 = document.getElementById('svcchk-'+svc.id);
    if(c2&&c2.checked){ total += isGBP ? svc.price_gbp : svc.price_ngn; count++; }
  });
  const sym = isGBP ? '£' : '₦';
  const tot = document.getElementById('svc-total');
  const cnt = document.getElementById('svc-count');
  if(tot) tot.textContent = sym+total.toLocaleString();
  if(cnt) cnt.textContent = count+' service'+(count!==1?'s':'')+' selected';
}

async function saveServiceSelection(clientId){
  const selectedIds = [];
  HAIDA_SERVICES.forEach(function(svc){
    const chk = document.getElementById('svcchk-'+svc.id);
    if(chk&&chk.checked) selectedIds.push(svc.id);
  });
  if(!onbData['services']) onbData['services'] = {};
  onbData['services']['_selected'] = JSON.stringify(selectedIds);
  // Save notes too
  const notesEl = document.getElementById('onb-services-services_notes');
  if(notesEl) onbData['services']['services_notes'] = notesEl.value;
  await saveOnboarding(clientId);
  alert('Services saved. '+ selectedIds.length + ' services selected.');
  loadOnboarding();
}

async function loadOnboarding(){
  const clientId=document.getElementById('onb-client-select').value;
  const body=document.getElementById('onboarding-body');
  if(!clientId){body.innerHTML='';return;}
  const {data}=await db.from('onboarding').select('*').eq('client_id',clientId).single();
  onbData=data?JSON.parse(data.data||'{}'):{};
  renderOnboarding(clientId);
}

function renderOnboarding(clientId){
  const body=document.getElementById('onboarding-body');
  const completed=ONBOARDING_STEPS.filter(s=>onbData[s.id]&&onbData[s.id]._done).length;
  body.innerHTML=`
    <div class="stats-row" style="margin-bottom:20px">
      <div class="stat-box"><div class="stat-num">${completed}/${ONBOARDING_STEPS.length}</div><div class="stat-label">Steps Complete</div></div>
      <div class="stat-box"><div class="stat-num" style="font-size:20px">${Math.round(completed/ONBOARDING_STEPS.length*100)}%</div><div class="stat-label">Onboarding Progress</div></div>
    </div>
    ${ONBOARDING_STEPS.map((step,si)=>{
      const sd=onbData[step.id]||{};const done=!!sd._done;
      return`<div class="card" style="margin-bottom:12px;border-left:4px solid ${done?'var(--success)':'var(--border)'}">
        <div style="display:flex;justify-content:space-between;align-items:center;margin-bottom:${done?'0':'16px'}">
          <div style="display:flex;align-items:center;gap:12px">
            <div style="width:28px;height:28px;border-radius:50%;background:${done?'var(--success)':'var(--border)'};display:flex;align-items:center;justify-content:center;color:white;font-family:Arial;font-size:12px;font-weight:bold;flex-shrink:0">${done?'✓':si+1}</div>
            <div style="font-size:15px;color:var(--ink)">${step.label}</div>
          </div>
          <button class="btn-sm" onclick="toggleOnbStep('${step.id}',${si})">${done?'Reopen':'Mark as done & collapse'}</button>
        </div>
        ${!done?`<div class="form-grid" style="margin-top:4px">
          ${step.id==='services'?`<div style="grid-column:1/-1"><div id="onb-service-selector"></div></div>`:''}
          ${step.fields.map(f=>{
            const val=sd[f.id]||'';
            const fld=f.type==='textarea'?`<textarea id="onb-${step.id}-${f.id}" rows="2" placeholder="${f.placeholder||''}" style="width:100%">${val}</textarea>`:
              f.type==='select'?`<select id="onb-${step.id}-${f.id}">${f.options.map(o=>`<option${o===val?' selected':''}>${o}</option>`).join('')}</select>`:
              `<input type="${f.type}" id="onb-${step.id}-${f.id}" value="${val}" placeholder="${f.placeholder||''}">`;
            return`<div class="form-field"${f.type==='textarea'?' style="grid-column:1/-1"':''}><label>${f.label}</label>${fld}</div>`;
          }).join('')}
        </div>
        <div style="margin-top:12px;display:flex;gap:8px">
          ${step.id!=='services'?`<button class="btn-save" onclick="saveOnbStep('${step.id}','${clientId}')">Save</button>`:''}
        </div>`:''}
      </div>`;
    }).join('')}`;
  // Render the service selector after DOM is set
  const clientId2 = document.getElementById('onb-client-select')?.value;
  if (clientId2) setTimeout(()=>renderServiceSelector(clientId2), 0);
}

async function saveOnbStep(stepId,clientId){
  const step=ONBOARDING_STEPS.find(s=>s.id===stepId);if(!step)return;
  const sd={};
  step.fields.forEach(f=>{const el=document.getElementById(`onb-${stepId}-${f.id}`);if(el)sd[f.id]=el.value;});
  onbData[stepId]=sd;
  await saveOnboarding(clientId);
  loadOnboarding();
}

async function toggleOnbStep(stepId,si){
  const clientId=document.getElementById('onb-client-select').value;if(!clientId)return;
  if(!onbData[stepId])onbData[stepId]={};
  onbData[stepId]._done=!onbData[stepId]._done;
  await saveOnboarding(clientId);
  loadOnboarding();
}

async function saveOnboarding(clientId){
  const {data:existing}=await db.from('onboarding').select('id').eq('client_id',clientId).single();
  const payload={client_id:clientId,data:JSON.stringify(onbData)};
  if(existing){await db.from('onboarding').update(payload).eq('client_id',clientId);}
  else{await db.from('onboarding').insert(payload);}
}

// ══ APPROVALS ══
const APPROVAL_LABELS={reports:'Report',complaints:'Complaint',actions:'Action',client_staff:'Staff Record',contracts:'Contract',documents:'Document',clients:'Client'};

async function loadApprovals(){
  if(!currentUser||!currentUser.isOwner)return;
  const {data}=await db.from('pending_approvals').select('*,clients(business_name)').eq('status','pending').order('created_at',{ascending:false});
  const pending=data||[];
  const badge=document.getElementById('approvals-badge');
  const btn=document.getElementById('approvals-tab-btn');
  if(pending.length>0){badge.textContent=pending.length;badge.style.display='inline';btn.style.color='var(--warn)';}
  else{badge.style.display='none';btn.style.color='';}
  renderApprovals(pending);
}

function renderApprovals(pending){
  const el=document.getElementById('approvals-list');
  if(!pending.length){el.innerHTML='<div style="padding:32px;text-align:center;font-family:Arial;font-size:13px;color:var(--muted)">No pending approvals. All clear.</div>';return;}
  el.innerHTML=pending.map(p=>{
    const changes=JSON.parse(p.changes||'{}');
    const original=JSON.parse(p.original||'{}');
    const changedKeys=Object.keys(changes).filter(k=>k!=='id'&&k!=='client_id'&&k!=='created_at'&&changes[k]!==original[k]);
    return`<div class="approval-card">
      <div style="display:flex;justify-content:space-between;align-items:flex-start;flex-wrap:wrap;gap:8px;margin-bottom:12px">
        <div>
          <span class="pending-badge">Pending Approval</span>
          <span style="font-family:Arial;font-size:13px;color:var(--muted);margin-left:10px">${APPROVAL_LABELS[p.table_name]||p.table_name} · ${(p.clients&&p.clients.business_name)||'—'}</span>
        </div>
        <div style="font-family:Arial;font-size:11px;color:var(--muted)">${p.admin_name||'Admin'} · ${p.created_at?new Date(p.created_at).toLocaleDateString():''}</div>
      </div>
      <div style="margin-bottom:12px">
        ${changedKeys.length?changedKeys.map(k=>`
          <div style="font-family:Arial;font-size:12px;margin-bottom:6px;padding:6px 10px;background:var(--surface2);border-radius:2px">
            <span style="color:var(--muted);text-transform:uppercase;letter-spacing:1px;font-size:10px">${k.replace(/_/g,' ')}</span><br>
            <span style="color:var(--danger);text-decoration:line-through">${original[k]||'—'}</span>
            <span style="color:var(--muted);margin:0 6px">→</span>
            <span style="color:var(--success);font-weight:500">${changes[k]||'—'}</span>
          </div>`).join(''):'<p style="font-family:Arial;font-size:12px;color:var(--muted)">New record submitted for approval.</p>'}
      </div>
      <div style="display:flex;gap:8px">
        <button class="btn-save" onclick="approveEdit('${p.id}','${p.table_name}','${p.record_id}')">✓ Approve</button>
        <button class="btn-danger" onclick="rejectEdit('${p.id}')">✗ Reject</button>
      </div>
    </div>`;
  }).join('');
}

async function approveEdit(approvalId,tableName,recordId){
  const {data:ap}=await db.from('pending_approvals').select('*').eq('id',approvalId).single();
  if(!ap)return;
  const changes=JSON.parse(ap.changes||'{}');
  delete changes.id;delete changes.client_id;delete changes.created_at;
  if(recordId){await db.from(tableName).update(changes).eq('id',recordId);}
  else{await db.from(tableName).insert(JSON.parse(ap.changes||'{}'));}
  await db.from('pending_approvals').update({status:'approved'}).eq('id',approvalId);
  loadApprovals();
  // Reload the relevant table
  const loaders={reports:()=>loadClients(),complaints:loadComplaints,actions:loadActions,client_staff:loadStaff,contracts:loadContracts,documents:loadDocs,clients:loadClients};
  if(loaders[tableName])loaders[tableName]();
}

async function rejectEdit(approvalId){
  if(!confirm('Reject this edit? It will be discarded.'))return;
  await db.from('pending_approvals').update({status:'rejected'}).eq('id',approvalId);
  loadApprovals();
}

// Admin submits an edit for approval instead of saving directly
async function submitForApproval(tableName,recordId,changes,original){
  const clientId=changes.client_id||original.client_id||null;
  const {error}=await db.from('pending_approvals').insert({
    table_name:tableName,record_id:recordId||null,client_id:clientId,
    admin_name:currentUser.name,changes:JSON.stringify(changes),
    original:JSON.stringify(original),status:'pending'
  });
  return error;
}

// ══ EXPENDITURE ══
const EXP_CATS=['Staff Salary','Freelancer / Contractor','Software & Subscriptions','Fuel & Transport','Client Expenses','Office & Stationery','Marketing & Advertising','Equipment & Tools','Professional Development','Bank Charges & Fees','Utilities','Other'];
const EXP_CAT_COLORS={'Staff Salary':'#644a3c','Software & Subscriptions':'#1A3A6B','Fuel & Transport':'#7A4F00','Client Expenses':'#1A5C38','Other':'#7e7b67'};

function openAddExpenditure(){
  document.getElementById('exp-edit-id').value='';
  document.getElementById('exp-modal-title').textContent='Log Expense';
  document.getElementById('exp-save-btn').textContent='Log Expense';
  ['exp-desc','exp-amount','exp-vendor','exp-receipt','exp-notes'].forEach(id=>{document.getElementById(id).value='';});
  document.getElementById('exp-date').valueAsDate=new Date();
  document.getElementById('exp-currency').value='NGN';
  document.getElementById('exp-client').value='';
  document.getElementById('exp-cat').value='Staff Salary';
  openModal('modal-add-expenditure');
}

async function loadExpenditure(){
  const month=document.getElementById('exp-month-filter')?document.getElementById('exp-month-filter').value:'';
  let q=db.from('expenditure').select('*,clients(business_name)').order('date',{ascending:false});
  if(month)q=q.gte('date',month+'-01').lte('date',month+'-31');
  const {data}=await q;
  renderExpTable(data||[]);
  renderExpStats(data||[]);
  populateExpMonths(data||[]);
}

function populateExpMonths(exps){
  const sel=document.getElementById('exp-month-filter');
  if(!sel)return;
  const months=[...new Set(exps.map(e=>e.date?e.date.substring(0,7):null).filter(Boolean))].sort().reverse();
  const cur=sel.value;
  sel.innerHTML='<option value="">All months</option>'+months.map(m=>`<option value="${m}" ${m===cur?'selected':''}>${m}</option>`).join('');
}

function renderExpTable(exps){
  const tbody=document.getElementById('exp-tbody');
  if(!exps.length){tbody.innerHTML='<tr><td colspan="8" style="text-align:center;padding:24px;color:var(--muted);font-family:Arial">No expenses logged yet.</td></tr>';return;}
  tbody.innerHTML=exps.map(e=>`<tr>
    <td><strong style="font-family:Arial;font-size:13px">${e.description||'—'}</strong></td>
    <td><span style="font-family:Arial;font-size:11px;background:var(--surface2);padding:2px 8px;border-radius:2px">${e.category||'—'}</span></td>
    <td style="font-family:Arial;font-size:12px;color:var(--muted)">${e.vendor||'—'}</td>
    <td style="font-family:Arial;font-size:12px">${e.date||'—'}</td>
    <td style="font-family:Arial;font-size:14px;font-weight:600;color:var(--ink)">${e.currency==='GBP'?'£':'₦'}${Number(e.amount||0).toLocaleString()}</td>
    <td style="font-family:Arial;font-size:12px">${(e.clients&&e.clients.business_name)||'—'}</td>
    <td style="font-family:Arial;font-size:12px;color:var(--muted)">${e.receipt_link?`<a href="${e.receipt_link}" target="_blank" style="color:var(--gold)">Receipt ↗</a>`:e.notes||'—'}</td>
    <td style="white-space:nowrap">
      <button class="btn-sm" onclick="editExpenditure('${e.id}')">Edit</button>
      <button class="btn-danger" onclick="deleteRow('expenditure','${e.id}',loadExpenditure)">Del</button>
    </td>
  </tr>`).join('');
}

function renderExpStats(exps){
  const ngn=exps.filter(e=>e.currency!=='GBP').reduce((s,e)=>s+(e.amount||0),0);
  const gbp=exps.filter(e=>e.currency==='GBP').reduce((s,e)=>s+(e.amount||0),0);
  const byCat={};exps.forEach(e=>{const k=e.category||'Other';byCat[k]=(byCat[k]||0)+(e.amount||0);});
  const topCat=Object.entries(byCat).sort((a,b)=>b[1]-a[1])[0];
  const clientExp=exps.filter(e=>e.client_id).reduce((s,e)=>s+(e.amount||0),0);
  document.getElementById('exp-stats').innerHTML=`
    <div class="stat-box"><div class="stat-num">₦${(ngn/1000).toFixed(0)}k</div><div class="stat-label">Total (₦)</div></div>
    ${gbp>0?`<div class="stat-box"><div class="stat-num">£${(gbp/1000).toFixed(1)}k</div><div class="stat-label">Total (£)</div></div>`:''}
    <div class="stat-box"><div class="stat-num">${exps.length}</div><div class="stat-label">Entries</div></div>
    <div class="stat-box"><div class="stat-num" style="font-size:14px">${topCat?topCat[0]:'—'}</div><div class="stat-label">Top Category</div></div>
    <div class="stat-box"><div class="stat-num">₦${(clientExp/1000).toFixed(0)}k</div><div class="stat-label">Client Expenses</div></div>`;
}

async function saveExpenditure(){
  const editId=document.getElementById('exp-edit-id').value;
  const fb=document.getElementById('exp-feedback');
  const payload={
    description:document.getElementById('exp-desc').value.trim(),
    category:document.getElementById('exp-cat').value,
    amount:parseFloat(document.getElementById('exp-amount').value)||0,
    currency:document.getElementById('exp-currency').value,
    date:document.getElementById('exp-date').value,
    vendor:document.getElementById('exp-vendor').value.trim(),
    client_id:document.getElementById('exp-client').value||null,
    receipt_link:document.getElementById('exp-receipt').value.trim(),
    notes:document.getElementById('exp-notes').value.trim(),
  };
  if(!payload.description){fb.innerHTML='<div class="error-msg">Please enter a description.</div>';return;}
  let error;
  if(editId){({error}=await db.from('expenditure').update(payload).eq('id',editId));}
  else{({error}=await db.from('expenditure').insert(payload));}
  if(error){fb.innerHTML=`<div class="error-msg">${error.message}</div>`;return;}
  fb.innerHTML=`<div class="success-msg">✓ Expense ${editId?'updated':'logged'}.</div>`;
  loadExpenditure();
  setTimeout(()=>{closeModal('modal-add-expenditure');fb.innerHTML='';},1500);
}

async function editExpenditure(id){
  const {data:e}=await db.from('expenditure').select('*').eq('id',id).single();
  if(!e)return;
  document.getElementById('exp-edit-id').value=e.id;
  document.getElementById('exp-modal-title').textContent='Edit Expense';
  document.getElementById('exp-save-btn').textContent='Update Expense';
  document.getElementById('exp-desc').value=e.description||'';
  document.getElementById('exp-cat').value=e.category||'Other';
  document.getElementById('exp-amount').value=e.amount||0;
  document.getElementById('exp-currency').value=e.currency||'NGN';
  document.getElementById('exp-date').value=e.date||'';
  document.getElementById('exp-vendor').value=e.vendor||'';
  document.getElementById('exp-client').value=e.client_id||'';
  document.getElementById('exp-receipt').value=e.receipt_link||'';
  document.getElementById('exp-notes').value=e.notes||'';
  openModal('modal-add-expenditure');
}

// ══ NOTIFICATIONS ══
let _realtimeChannel = null;

async function loadNotifications(){
  if(!currentUser||!currentUser.isOwner)return;
  const {data}=await db.from('notifications').select('*').eq('is_read',false).order('created_at',{ascending:false});
  const count=(data||[]).length;
  const badge=document.getElementById('notif-badge');
  const cnt=document.getElementById('notif-count');
  if(count>0&&badge&&cnt){badge.style.display='inline';cnt.textContent=count;}
  else if(badge){badge.style.display='none';}
  // Subscribe to realtime notifications
  if(!_realtimeChannel){
    _realtimeChannel = db.channel('portal-notifs')
      .on('postgres_changes',{event:'INSERT',schema:'public',table:'notifications'},
        function(payload){
          const msg = payload.new;
          // Update badge
          loadNotifications();
          // Fire browser notification
          const title = '🔔 Haida Portal — New Request';
          const body = msg.message||'A client has submitted a new request.';
          if(typeof sendPushToSelf === 'function'){
            sendPushToSelf(title, body);
          } else if('Notification' in window && Notification.permission==='granted'){
            new Notification(title,{body,tag:'haida-request',renotify:true});
          }
        }
      )
      .subscribe();
  }
}

async function markNotificationsRead(){
  await db.from('notifications').update({is_read:true}).eq('is_read',false);
  loadNotifications();
}

async function markNotificationsRead(){
  await db.from('notifications').update({is_read:true}).eq('is_read',false);
  loadNotifications();
}

// ══ CLIENT ACCESS RESET ══
async function resetClientAccess(clientId,username){
  const newCode=prompt(`Reset access code for ${username}:\nEnter new access code:`);
  if(!newCode||newCode.trim()==='')return;
  const {error}=await db.from('clients').update({access_code:newCode.trim()}).eq('id',clientId);
  if(error){alert('Error: '+error.message);return;}
  alert(`✓ Access code updated.\nUsername: ${username}\nNew code: ${newCode.trim()}\n\nShare this with the client.`);
}

// ══ EVENT PLANNER ══
let _evInventory = {}; // {eventId: [{name,category,qty,cost,status}]}
let _currentEventId = null;

async function loadEvents(){
  const {data} = await db.from('events').select('*,clients(business_name)').order('event_date',{ascending:true});
  renderEventsList(data||[]);
}

function renderEventsList(events){
  const el = document.getElementById('events-list');
  if(!events.length){ el.innerHTML='<p style="font-family:Arial;font-size:13px;color:var(--muted);padding:24px 0">No events yet. Click + New Event to start.</p>'; return; }
  const statusColor={Enquiry:'var(--muted)',Confirmed:'var(--gold)','In Planning':'var(--ink2)',Production:'var(--warn)','Day Of':'var(--danger)','Completed':'var(--success)',Cancelled:'var(--muted)'};
  const today = new Date();
  el.innerHTML = events.map(function(ev){
    const evDate = ev.event_date ? new Date(ev.event_date) : null;
    const daysLeft = evDate ? Math.ceil((evDate-today)/(1000*60*60*24)) : null;
    const dl = daysLeft===null?'—':daysLeft<0?'Past':daysLeft===0?'TODAY':''+daysLeft+'d away';

    // Parse JSON fields
    let vendors=[],deliverables=[],tasks=[],updates=[];
    try{vendors=JSON.parse(ev.vendors||'[]');}catch(e){}
    try{deliverables=JSON.parse(ev.deliverables||'[]');}catch(e){}
    try{tasks=JSON.parse(ev.tasks||'[]');}catch(e){}
    try{updates=JSON.parse(ev.updates_log||'[]');}catch(e){}

    // Budget calc
    const totalBudget = parseFloat(ev.budget)||0;
    const totalPaid = vendors.reduce(function(a,v){return a+(parseFloat(v.paid)||0);},0);
    const totalOutstanding = vendors.reduce(function(a,v){return a+(parseFloat(v.outstanding)||0);},0);
    const doneTasksCount = tasks.filter(function(t){return t.done;}).length;
    const doneDelivCount = deliverables.filter(function(d){return d.confirmed;}).length;

    const eid = ev.id;

    return '<div class="card" style="margin-bottom:16px;padding:0;overflow:hidden" id="event-card-'+eid+'">'
      // Header
      +'<div style="background:var(--ink);color:white;padding:16px 20px;display:flex;justify-content:space-between;align-items:flex-start;flex-wrap:wrap;gap:8px">'
        +'<div>'
          +'<div style="font-size:17px;margin-bottom:4px">'+ev.event_name+'</div>'
          +'<div style="font-family:Arial;font-size:12px;opacity:.7">'+(ev.clients?ev.clients.business_name:'No client')+' · '+ev.event_type+' · '+(ev.venue||'Venue TBC')+'</div>'
        +'</div>'
        +'<div style="text-align:right;flex-shrink:0">'
          +'<div style="font-size:13px">'+(ev.event_date||'Date TBC')+'</div>'
          +'<div style="font-family:Arial;font-size:11px;opacity:.7">'+dl+'</div>'
          +'<span style="display:inline-block;margin-top:4px;font-family:Arial;font-size:10px;padding:2px 8px;border:1px solid rgba(255,255,255,.3);border-radius:10px">'+ev.status+'</span>'
        +'</div>'
      +'</div>'

      // Stats bar
      +'<div style="display:grid;grid-template-columns:repeat(5,1fr);border-bottom:1px solid var(--border)">'
        +'<div style="padding:10px 14px;text-align:center;border-right:1px solid var(--border)"><div style="font-size:16px;font-weight:600;color:var(--ink)">₦'+(totalBudget?totalBudget.toLocaleString():'—')+'</div><div style="font-family:Arial;font-size:9px;text-transform:uppercase;letter-spacing:.5px;color:var(--muted)">Budget</div></div>'
        +'<div style="padding:10px 14px;text-align:center;border-right:1px solid var(--border)"><div style="font-size:16px;font-weight:600;color:var(--success)">₦'+totalPaid.toLocaleString()+'</div><div style="font-family:Arial;font-size:9px;text-transform:uppercase;letter-spacing:.5px;color:var(--muted)">Paid</div></div>'
        +'<div style="padding:10px 14px;text-align:center;border-right:1px solid var(--border)"><div style="font-size:16px;font-weight:600;color:var(--danger)">₦'+totalOutstanding.toLocaleString()+'</div><div style="font-family:Arial;font-size:9px;text-transform:uppercase;letter-spacing:.5px;color:var(--muted)">Outstanding</div></div>'
        +'<div style="padding:10px 14px;text-align:center;border-right:1px solid var(--border)"><div style="font-size:16px;font-weight:600;color:var(--ink)">'+doneDelivCount+'/'+deliverables.length+'</div><div style="font-family:Arial;font-size:9px;text-transform:uppercase;letter-spacing:.5px;color:var(--muted)">Deliverables</div></div>'
        +'<div style="padding:10px 14px;text-align:center"><div style="font-size:16px;font-weight:600;color:var(--ink)">'+doneTasksCount+'/'+tasks.length+'</div><div style="font-family:Arial;font-size:9px;text-transform:uppercase;letter-spacing:.5px;color:var(--muted)">Tasks Done</div></div>'
      +'</div>'

      // Body with 4 sections
      +'<div style="display:grid;grid-template-columns:1fr 1fr;gap:0">'

        // VENDORS
        +'<div style="padding:16px;border-right:1px solid var(--border);border-bottom:1px solid var(--border)">'
          +'<div style="display:flex;justify-content:space-between;align-items:center;margin-bottom:10px">'
            +'<div style="font-family:Arial;font-size:10px;text-transform:uppercase;letter-spacing:1.5px;color:var(--gold)">Vendors</div>'
            +'<button class="btn-sm" data-eid="'+eid+'" onclick="openAddVendor(this.dataset.eid)" style="font-size:10px">+ Add</button>'
          +'</div>'
          +(vendors.length?vendors.map(function(v){
            return '<div style="padding:8px 0;border-bottom:1px solid var(--border);font-family:Arial;font-size:12px">'
              +'<div style="display:flex;justify-content:space-between;align-items:flex-start">'
                +'<div><strong>'+v.name+'</strong> <span style="color:var(--muted);font-size:10px">'+v.category+'</span></div>'
                +'<button class="btn-danger" data-eid="'+eid+'" data-vid="'+v.id+'" onclick="deleteVendor(this.dataset.eid,this.dataset.vid)" style="font-size:9px;padding:1px 6px">×</button>'
              +'</div>'
              +'<div style="color:var(--muted);margin-top:2px">'
                +(v.contact?'📞 '+v.contact+' · ':'')
                +(v.total?'Total: ₦'+parseFloat(v.total).toLocaleString()+' · ':'')
                +'<span style="color:var(--success)">Paid: ₦'+parseFloat(v.paid||0).toLocaleString()+'</span>'
                +(v.outstanding?' · <span style="color:var(--danger)">Due: ₦'+parseFloat(v.outstanding).toLocaleString()+'</span>':'')
              +'</div>'
              +(v.notes?'<div style="color:var(--muted);font-size:11px;margin-top:2px">'+v.notes+'</div>':'')
            +'</div>';
          }).join(''):'<div style="font-family:Arial;font-size:12px;color:var(--muted)">No vendors yet.</div>')
        +'</div>'

        // DELIVERABLES
        +'<div style="padding:16px;border-bottom:1px solid var(--border)">'
          +'<div style="display:flex;justify-content:space-between;align-items:center;margin-bottom:10px">'
            +'<div style="font-family:Arial;font-size:10px;text-transform:uppercase;letter-spacing:1.5px;color:var(--gold)">What\'s Been Decided / Confirmed</div>'
            +'<button class="btn-sm" data-eid="'+eid+'" onclick="openAddDeliverable(this.dataset.eid)" style="font-size:10px">+ Add</button>'
          +'</div>'
          +(deliverables.length?deliverables.map(function(d){
            return '<div style="display:flex;align-items:flex-start;gap:8px;padding:5px 0;border-bottom:1px solid var(--border);font-family:Arial;font-size:12px">'
              +'<input type="checkbox" '+(d.confirmed?'checked':'')+' data-eid="'+eid+'" data-did="'+d.id+'" onchange="toggleDeliverable(this.dataset.eid,this.dataset.did,this.checked)" style="margin-top:2px;flex-shrink:0">'
              +'<span style="flex:1;'+(d.confirmed?'color:var(--muted);text-decoration:line-through':'')+'">'+d.text+'</span>'
              +'<button class="btn-danger" data-eid="'+eid+'" data-did="'+d.id+'" onclick="deleteDeliverable(this.dataset.eid,this.dataset.did)" style="font-size:9px;padding:1px 6px">×</button>'
            +'</div>';
          }).join(''):'<div style="font-family:Arial;font-size:12px;color:var(--muted)">No deliverables logged yet.</div>')
        +'</div>'

        // TASKS
        +'<div style="padding:16px;border-right:1px solid var(--border)">'
          +'<div style="display:flex;justify-content:space-between;align-items:center;margin-bottom:10px">'
            +'<div style="font-family:Arial;font-size:10px;text-transform:uppercase;letter-spacing:1.5px;color:var(--gold)">Task Checklist</div>'
            +'<button class="btn-sm" data-eid="'+eid+'" onclick="openAddTask(this.dataset.eid)" style="font-size:10px">+ Add</button>'
          +'</div>'
          +(tasks.length?tasks.map(function(t){
            return '<div style="display:flex;align-items:flex-start;gap:8px;padding:5px 0;border-bottom:1px solid var(--border);font-family:Arial;font-size:12px">'
              +'<input type="checkbox" '+(t.done?'checked':'')+' data-eid="'+eid+'" data-tid="'+t.id+'" onchange="toggleTask(this.dataset.eid,this.dataset.tid,this.checked)" style="margin-top:2px;flex-shrink:0">'
              +'<span style="flex:1;'+(t.done?'color:var(--muted);text-decoration:line-through':'')+'">'+t.text+(t.due?' <span style="color:var(--warn);font-size:10px">by '+t.due+'</span>':'')+'</span>'
              +'<button class="btn-danger" data-eid="'+eid+'" data-tid="'+t.id+'" onclick="deleteTask(this.dataset.eid,this.dataset.tid)" style="font-size:9px;padding:1px 6px">×</button>'
            +'</div>';
          }).join(''):'<div style="font-family:Arial;font-size:12px;color:var(--muted)">No tasks yet.</div>')
        +'</div>'

        // UPDATES LOG
        +'<div style="padding:16px">'
          +'<div style="display:flex;justify-content:space-between;align-items:center;margin-bottom:10px">'
            +'<div style="font-family:Arial;font-size:10px;text-transform:uppercase;letter-spacing:1.5px;color:var(--gold)">Updates Log</div>'
            +'<button class="btn-sm" data-eid="'+eid+'" onclick="openAddUpdate(this.dataset.eid)" style="font-size:10px">+ Log Update</button>'
          +'</div>'
          +(updates.length?updates.slice().reverse().slice(0,5).map(function(u){
            return '<div style="padding:6px 0;border-bottom:1px solid var(--border);font-family:Arial;font-size:12px">'
              +'<div style="color:var(--muted);font-size:10px">'+u.date+'</div>'
              +'<div>'+u.text+'</div>'
            +'</div>';
          }).join(''):'<div style="font-family:Arial;font-size:12px;color:var(--muted)">No updates yet.</div>')
        +'</div>'

      +'</div>'

      // Actions footer
      +'<div style="padding:12px 16px;border-top:1px solid var(--border);display:flex;gap:8px;background:var(--surface)">'
        +'<button class="btn-sm" data-eid="'+eid+'" onclick="editEvent(this.dataset.eid)">Edit Event</button>'
        +'<button class="btn-danger" data-eid="'+eid+'" onclick="deleteRow(\'events\',this.dataset.eid,loadEvents)">Delete Event</button>'
      +'</div>'

    +'</div>';
  }).join('');
}

async function saveEvent(){
  const editId = document.getElementById('ev-edit-id').value;
  const fb = document.getElementById('event-feedback');
  const name = document.getElementById('ev-name').value.trim();
  if(!name){ fb.innerHTML='<div class="error-msg">Event name is required.</div>'; return; }
  const payload = {
    event_name: name,
    client_id: document.getElementById('ev-client').value||null,
    event_type: document.getElementById('ev-type').value,
    event_date: document.getElementById('ev-date').value||null,
    venue: document.getElementById('ev-venue').value.trim(),
    guest_count: parseInt(document.getElementById('ev-guests').value)||null,
    budget: parseFloat(document.getElementById('ev-budget').value)||null,
    status: document.getElementById('ev-status').value,
    description: document.getElementById('ev-desc').value.trim(),
    haida_role: document.getElementById('ev-role').value,
  };
  let error;
  if(editId){ ({error}=await db.from('events').update(payload).eq('id',editId)); }
  else { ({error}=await db.from('events').insert(payload)); }
  if(error){ fb.innerHTML='<div class="error-msg">'+error.message+'</div>'; return; }
  fb.innerHTML='<div class="success-msg">✓ Event saved.</div>';
  loadEvents();
  setTimeout(function(){ closeModal('modal-add-event'); fb.innerHTML=''; ['ev-edit-id','ev-name','ev-venue','ev-guests','ev-budget','ev-desc'].forEach(function(id){var el=document.getElementById(id);if(el)el.value='';});},1200);
}

// ── EVENT JSON FIELD HELPERS ──────────────────────

async function getEventField(eventId, field){
  const {data} = await db.from('events').select(field).eq('id',eventId).single();
  try{ return JSON.parse(data?.[field]||'[]'); } catch(e){ return []; }
}

async function setEventField(eventId, field, arr){
  const update = {};
  update[field] = JSON.stringify(arr);
  await db.from('events').update(update).eq('id',eventId);
  loadEvents();
}

// ── VENDORS ──
function openAddVendor(eventId){
  const name = prompt('Vendor name (e.g. Catering by Amara):');
  if(!name) return;
  const category = prompt('Category (e.g. Caterer, Venue, Decorator, MC, Photographer, AV):') || 'General';
  const contact = prompt('Contact number (optional):') || '';
  const total = prompt('Total cost (numbers only, e.g. 150000):') || '0';
  const paid = prompt('Amount already paid:') || '0';
  const outstanding = (parseFloat(total)||0) - (parseFloat(paid)||0);
  const notes = prompt('Notes (optional):') || '';
  getEventField(eventId,'vendors').then(function(vendors){
    vendors.push({id:Date.now().toString(),name,category,contact,total,paid,outstanding:outstanding.toString(),notes});
    setEventField(eventId,'vendors',vendors);
  });
}

function deleteVendor(eventId, vendorId){
  if(!confirm('Remove this vendor?')) return;
  getEventField(eventId,'vendors').then(function(vendors){
    setEventField(eventId,'vendors',vendors.filter(function(v){return v.id!==vendorId;}));
  });
}

// ── DELIVERABLES ──
function openAddDeliverable(eventId){
  const text = prompt('What has been decided/confirmed? (e.g. Venue confirmed — Intercontinental Hotel):');
  if(!text) return;
  getEventField(eventId,'deliverables').then(function(items){
    items.push({id:Date.now().toString(),text,confirmed:false});
    setEventField(eventId,'deliverables',items);
  });
}

function toggleDeliverable(eventId, itemId, checked){
  getEventField(eventId,'deliverables').then(function(items){
    items = items.map(function(d){ return d.id===itemId ? Object.assign({},d,{confirmed:checked}) : d; });
    setEventField(eventId,'deliverables',items);
  });
}

function deleteDeliverable(eventId, itemId){
  getEventField(eventId,'deliverables').then(function(items){
    setEventField(eventId,'deliverables',items.filter(function(d){return d.id!==itemId;}));
  });
}

// ── TASKS ──
function openAddTask(eventId){
  const text = prompt('Task to complete (e.g. Confirm menu with caterer):');
  if(!text) return;
  const due = prompt('Due date (optional, e.g. 20 Jun):') || '';
  getEventField(eventId,'tasks').then(function(tasks){
    tasks.push({id:Date.now().toString(),text,due,done:false});
    setEventField(eventId,'tasks',tasks);
  });
}

function toggleTask(eventId, taskId, checked){
  getEventField(eventId,'tasks').then(function(tasks){
    tasks = tasks.map(function(t){ return t.id===taskId ? Object.assign({},t,{done:checked}) : t; });
    setEventField(eventId,'tasks',tasks);
  });
}

function deleteTask(eventId, taskId){
  getEventField(eventId,'tasks').then(function(tasks){
    setEventField(eventId,'tasks',tasks.filter(function(t){return t.id!==taskId;}));
  });
}

// ── UPDATES LOG ──
function openAddUpdate(eventId){
  const text = prompt('What happened / what was done?');
  if(!text) return;
  const date = new Date().toLocaleDateString('en-GB',{day:'numeric',month:'short',year:'numeric'});
  getEventField(eventId,'updates_log').then(function(updates){
    updates.push({id:Date.now().toString(),date,text});
    setEventField(eventId,'updates_log',updates);
  });
}

async function editEvent(id){
  const {data:ev} = await db.from('events').select('*').eq('id',id).single();
  if(!ev) return;
  document.getElementById('ev-edit-id').value = ev.id;
  document.getElementById('ev-modal-title').textContent = 'Edit Event';
  document.getElementById('ev-name').value = ev.event_name||'';
  document.getElementById('ev-client').value = ev.client_id||'';
  document.getElementById('ev-type').value = ev.event_type||'Birthday / Celebration';
  document.getElementById('ev-date').value = ev.event_date||'';
  document.getElementById('ev-venue').value = ev.venue||'';
  document.getElementById('ev-guests').value = ev.guest_count||'';
  document.getElementById('ev-budget').value = ev.budget||'';
  document.getElementById('ev-status').value = ev.status||'Enquiry';
  document.getElementById('ev-desc').value = ev.description||'';
  document.getElementById('ev-role').value = ev.haida_role||'Full Event Planner — end to end';
  openModal('modal-add-event');
}

async function openEventInventory(eventId, eventName){
  _currentEventId = eventId;
  document.getElementById('inv-event-title').textContent = eventName + ' — Inventory';
  await loadInventoryItems(eventId);
  openModal('modal-event-inventory');
}

async function loadInventoryItems(eventId){
  const {data} = await db.from('event_inventory').select('*').eq('event_id',eventId).order('category',{ascending:true});
  _evInventory[eventId] = data||[];
  renderInventoryList(data||[]);
}

function renderInventoryList(items){
  const el = document.getElementById('inv-items-list');
  const totals = document.getElementById('inv-totals');
  if(!items.length){ el.innerHTML='<p style="font-family:Arial;font-size:13px;color:var(--muted)">No items yet. Add items above.</p>'; totals.innerHTML=''; return; }
  const statusBadge={'To Source':'badge-red','Ordered':'badge-gold','Confirmed':'badge-gold','Delivered':'badge-green','Returned':'badge-gray'};
  const total = items.reduce(function(s,i){ return s+((i.qty||0)*(i.unit_cost||0)); },0);
  const confirmed = items.filter(function(i){ return i.status==='Delivered'||i.status==='Confirmed'; }).reduce(function(s,i){ return s+((i.qty||0)*(i.unit_cost||0)); },0);
  el.innerHTML = '<table class="data-table"><thead><tr><th>Item</th><th>Category</th><th>Qty</th><th>Unit Cost</th><th>Total</th><th>Status</th><th></th></tr></thead><tbody>'+
    items.map(function(item){
      return '<tr>'+
        '<td style="font-family:Arial;font-size:13px">'+item.item_name+'</td>'+
        '<td style="font-family:Arial;font-size:12px;color:var(--muted)">'+item.category+'</td>'+
        '<td style="font-family:Arial;font-size:13px;text-align:center">'+item.qty+'</td>'+
        '<td style="font-family:Arial;font-size:13px">₦'+Number(item.unit_cost||0).toLocaleString()+'</td>'+
        '<td style="font-family:Arial;font-size:13px;font-weight:600">₦'+Number((item.qty||0)*(item.unit_cost||0)).toLocaleString()+'</td>'+
        '<td><span class="badge '+(statusBadge[item.status]||'badge-gray')+'">'+item.status+'</span></td>'+
        '<td><button class="btn-danger" data-iid="'+item.id+'" onclick="deleteInventoryItem(this.dataset.iid)">×</button></td>'+
      '</tr>';
    }).join('')+
  '</tbody></table>';
  totals.innerHTML = '<div style="display:flex;gap:24px;flex-wrap:wrap">'+
    '<div><span style="color:var(--muted)">Total inventory cost: </span><strong>₦'+Number(total).toLocaleString()+'</strong></div>'+
    '<div><span style="color:var(--muted)">Confirmed / delivered: </span><strong style="color:var(--success)">₦'+Number(confirmed).toLocaleString()+'</strong></div>'+
    '<div><span style="color:var(--muted)">Outstanding: </span><strong style="color:var(--danger)">₦'+Number(total-confirmed).toLocaleString()+'</strong></div>'+
  '</div>';
}

async function addInventoryItem(){
  const name = document.getElementById('inv-item-name').value.trim();
  if(!name){ alert('Item name required'); return; }
  const {error} = await db.from('event_inventory').insert({
    event_id: _currentEventId,
    item_name: name,
    category: document.getElementById('inv-item-category').value.trim()||'General',
    qty: parseInt(document.getElementById('inv-item-qty').value)||1,
    unit_cost: parseFloat(document.getElementById('inv-item-cost').value)||0,
    status: document.getElementById('inv-item-status').value,
  });
  if(error){ alert('Error: '+error.message); return; }
  ['inv-item-name','inv-item-category','inv-item-qty','inv-item-cost'].forEach(function(id){ document.getElementById(id).value=''; });
  loadInventoryItems(_currentEventId);
}

async function deleteInventoryItem(id){
  await db.from('event_inventory').delete().eq('id',id);
  loadInventoryItems(_currentEventId);
}

// Load events when tab is shown
const _origShowAdminTab = showAdminTab;
// Hook into loadClients to also load events
const _origLoadApp = loadClients;

// ══ UTILS ══
async function deleteRow(table,id,reloadFn){
  if(!currentUser||!currentUser.isOwner){
    // Non-owner: submit delete request for approval
    if(!confirm('Request deletion of this record? It will be submitted for owner approval.'))return;
    const {data:rec}=await db.from(table).select('*').eq('id',id).single();
    await db.from('pending_approvals').insert({
      table_name:table,record_id:id,client_id:rec?rec.client_id:null,
      admin_name:currentUser.name,changes:JSON.stringify({_action:'DELETE'}),
      original:JSON.stringify(rec||{}),status:'pending'
    });
    alert('Delete request submitted for owner approval.');
    loadApprovals();
    return;
  }
  if(!confirm('Permanently delete this record?'))return;
  await db.from(table).delete().eq('id',id);reloadFn();
}

// Generic edit — owner saves directly, admin submits for approval
async function editRecord(tableName,recordId,reloadFn){
  const {data:record}=await db.from(tableName).select('*').eq('id',recordId).single();
  if(!record)return;
  // Build a simple edit modal dynamically
  const fields=Object.entries(record).filter(([k])=>!['id','client_id','created_at','updated_at'].includes(k));
  const formHtml=fields.map(([k,v])=>{
    const lbl=k.replace(/_/g,' ').split(' ').map(w=>w.charAt(0).toUpperCase()+w.slice(1)).join(' ');
    const isLong=typeof v==='string'&&v.length>80;
    const safeVal=String(v||'').replace(/&/g,'&amp;').replace(/"/g,'&quot;').replace(/</g,'&lt;');
    const field=isLong?('<textarea id="er-'+k+'" rows="3">'+(v||'')+'</textarea>'):('<input type="text" id="er-'+k+'" value="'+safeVal+'">');
    return '<div class="form-field"'+(isLong?' style="grid-column:1/-1"':'')+'><label>'+lbl+'</label>'+field+'</div>';
  }).join('');
  document.getElementById('generic-edit-title').textContent=`Edit ${APPROVAL_LABELS[tableName]||tableName}`;
  document.getElementById('generic-edit-form').innerHTML=`<div class="form-grid">${formHtml}</div>`;
  document.getElementById('generic-edit-save').onclick=async()=>{
    const changes={};
    fields.forEach(([k])=>{const el=document.getElementById('er-'+k);if(el)changes[k]=el.value;});
    if(currentUser&&currentUser.isOwner){
      // Owner: save directly
      const {error}=await db.from(tableName).update(changes).eq('id',recordId);
      if(error){alert('Error: '+error.message);return;}
      alert('Record updated.');
      reloadFn();
    } else {
      // Admin or client: submit for approval only
      const error=await submitForApproval(tableName,recordId,changes,record);
      if(error){alert('Error submitting request: '+error.message);return;}
      alert('Your edit request has been submitted. Changes will only take effect once approved by Wahida.');
    }
    closeModal('modal-generic-edit');
  };
  const note=document.getElementById('generic-edit-note');
  if(!currentUser.isOwner){note.textContent='Your edit will be submitted for owner approval before going live.';}
  else{note.textContent='';}
  openModal('modal-generic-edit');
}

// Gate Add Client for admin
function gateAddClient(){
  if(!currentUser||!currentUser.isOwner){alert('Only the owner can add new clients.');return;}
  openModal('modal-add-client');
}

function openModal(id){document.getElementById(id).classList.add('open');}
function closeModal(id){document.getElementById(id).classList.remove('open');}

function showAdminTab(tab,btn){
  if(tab==='invoices'&&(!currentUser||!currentUser.isOwner)){return;}
  document.querySelectorAll('.tab-btn').forEach(b=>b.classList.remove('active'));
  btn.classList.add('active');
  showPage('page-'+tab);
  if(tab==='compliance') loadCompliancePage();
}
function showAdminTabByName(tab){const btn=document.querySelector(`.tab-btn[onclick*="'${tab}'"]`);if(btn)showAdminTab(tab,btn);}
function showPage(id){document.querySelectorAll('.page').forEach(p=>p.classList.remove('active'));document.getElementById(id).classList.add('active');}

// ══════════════════════════════════════════════════
// COMPLIANCE MODULE
// ══════════════════════════════════════════════════

let _compBodies = [];
let _compStatuses = {};

const COMP_NICHES = {
  'all-biz':{label:'All Businesses',cls:'cn-all'},
  'food':{label:'Food & Bev',cls:'cn-food'},
  'hospitality':{label:'Hospitality',cls:'cn-hospitality'},
  'employer':{label:'Employer',cls:'cn-employer'},
  'data':{label:'Data / Digital',cls:'cn-data'},
  'fct':{label:'FCT / Abuja',cls:'cn-fct'},
  'fintech':{label:'Fintech',cls:'cn-fintech'},
  'aesthetics':{label:'Aesthetics',cls:'cn-hospitality'},
  'retail':{label:'Retail',cls:'cn-all'},
  'education':{label:'Education',cls:'cn-data'},
};

const STATUS_CLS = {
  'Not Started':'sbc-not-started',
  'In Progress':'sbc-in-progress',
  'Done':'sbc-done',
  'N/A':'sbc-na',
};

// Frequency → days for next-due calculation
const FREQ_DAYS = {
  'Monthly':30, 'Monthly returns':30, 'Monthly contribution':30,
  'Quarterly':90, 'Annual':365, 'Annual renewal':365,
  'Every 2 years':730, 'One-time':null, 'Ongoing':null,
};

function nextDueFromFrequency(freq, lastDoneDate) {
  if (!lastDoneDate || !freq) return null;
  const days = Object.entries(FREQ_DAYS).find(([k]) => freq.toLowerCase().includes(k.toLowerCase()));
  if (!days || !days[1]) return null;
  const d = new Date(lastDoneDate);
  d.setDate(d.getDate() + days[1]);
  return d.toISOString().split('T')[0];
}

async function loadCompliancePage() {
  await refreshComplianceData();
}

async function refreshComplianceData() {
  const [bodiesRes, statusRes] = await Promise.all([
    db.from('compliance_bodies').select('*').order('country').order('tier').order('name'),
  ]);
  _compBodies = bodiesRes.data || [];
  _compStatuses = {};
  (statusRes.data || []).forEach(s => {
    _compStatuses[s.body_id + '_' + s.client_id] = s;
  });
  renderComplianceBodies();
}

// ── MASTER LIBRARY VIEW ──────────────────────────
// Shows all bodies — reference only, no status tracking

function renderComplianceBodies() {
  const search = (document.getElementById('comp-search')?.value || '').toLowerCase();
  const country = document.getElementById('comp-country')?.value || 'all';
  const niche = document.getElementById('comp-niche')?.value || 'all';

  const listEl = document.getElementById('comp-body-list');
  if (!listEl) return;

  if (!_compBodies.length) {
    listEl.innerHTML = '<div style="text-align:center;padding:48px;color:var(--muted);font-family:Arial;font-size:13px">Loading bodies…</div>';
    return;
  }

  let bodies = _compBodies.filter(b => {
    if (country !== 'all' && b.country !== country) return false;
    if (niche !== 'all') {
      const niches = (b.niches || '').split(',').map(n => n.trim());
      if (!niches.includes(niche)) return false;
    }
    if (search) {
      const hay = (b.name + b.abbr + (b.what||'') + b.tier + (b.niches||'')).toLowerCase();
      if (!hay.includes(search)) return false;
    }
    return true;
  });

  const statsEl = document.getElementById('comp-stats-row');
  if (statsEl) statsEl.textContent = `${bodies.length} bodies`;

  const grouped = {};
  bodies.forEach(b => {
    const key = (b.country || 'Other') + ' || ' + (b.tier || 'General');
    if (!grouped[key]) grouped[key] = [];
    grouped[key].push(b);
  });

  if (!bodies.length) {
    listEl.innerHTML = '<div style="text-align:center;padding:48px;color:var(--muted);font-family:Arial;font-size:13px">No bodies match your filters.</div>';
    return;
  }

  listEl.innerHTML = Object.entries(grouped).map(([key, group]) => {
    const [countryLabel, tierLabel] = key.split(' || ');
    return `<div class="comp-tier-header">🌍 ${countryLabel} — ${tierLabel} (${group.length})</div>` +
      group.map(b => renderCompBodyCard(b)).join('');
  }).join('');

  listEl.querySelectorAll('.comp-card-header').forEach(h => {
    h.addEventListener('click', () => h.closest('.comp-card').classList.toggle('open'));
  });
}

function renderCompBodyCard(b) {
  const niches = (b.niches || '').split(',').map(n => n.trim()).filter(Boolean);
  const nichePills = niches.map(n => {
    const info = COMP_NICHES[n] || {label:n, cls:'cn-all'};
    return `<span class="comp-niche-pill ${info.cls}">${info.label}</span>`;
  }).join('');
  const docs = (b.docs || '').split('\n').filter(Boolean);

  return `<div class="comp-card" id="comp-card-${b.id}">
    <div class="comp-card-header">
      <span class="comp-abbr-tag">${b.abbr||''}</span>
      <div class="comp-card-names">
        <div class="comp-card-name">${b.name}</div>
        <div class="comp-niche-pills">${nichePills}</div>
      </div>
      <div class="comp-card-right">
        <span style="font-family:Arial;font-size:10px;color:var(--muted)">${b.country||''}</span>
        <span class="comp-expand">▼</span>
      </div>
    </div>
    <div class="comp-body">
      <div class="comp-info-grid">
        <div class="comp-info-box"><label>What it does</label><div class="cval">${b.what||'—'}</div></div>
        <div class="comp-info-box"><label>Cost</label><div class="cval" style="color:var(--warn);font-weight:600">${b.cost||'—'}</div></div>
        <div class="comp-info-box"><label>Frequency</label><div class="cval">${b.frequency||'—'}</div></div>
        <div class="comp-info-box"><label>Deadline</label><div class="cval">${b.deadline||'—'}</div></div>
      </div>
      <div class="comp-penalty">⚠ <strong>Penalty:</strong> ${b.penalty||'—'}</div>
      <div class="comp-docs-section" style="margin-bottom:10px">
        <h5>Documents Required</h5>
        <ul class="comp-doc-list">${docs.map(d=>`<li>${d}</li>`).join('')||'<li>—</li>'}</ul>
      </div>
      <div class="comp-docs-section" style="margin-bottom:10px">
        <h5>How to Renew</h5>
        <div style="font-family:Arial;font-size:12px;color:var(--ink2);line-height:1.6">${b.renewal||'—'}</div>
      </div>
      <div class="comp-contact">📞 ${b.contact||'—'}</div>
      ${currentUser&&currentUser.isOwner?`<div style="margin-top:10px;padding-top:10px;border-top:1px solid var(--border);display:flex;gap:8px">
        <button class="btn-sm" onclick="editCompBody('${b.id}')" style="font-size:11px">Edit</button>
        <button class="btn-danger" onclick="deleteRow('compliance_bodies','${b.id}',refreshComplianceData)" style="font-size:11px">Delete</button>
      </div>`:''}
    </div>
  </div>`;
}

// ── ASSIGN BODIES MODAL ──────────────────────────











// ══════════════════════════════════════════════════
// INFECTION CONTROL MODULE
// Tables needed:
//   ic_staff_tests   (id, client_id, staff_name, role, test_type, date_tested, next_due, result, lab, notes, client_visible, created_at)
//   ic_facility_checks (id, client_id, check_type, category, date_checked, checked_by, result, score, findings, action_required, next_due, client_visible, created_at)
// ══════════════════════════════════════════════════

let _icCurrentSection = 'staff';
let _icStaffTests = [];
let _icFacilityChecks = [];

function switchCompTab(tab, btn) {
  document.querySelectorAll('#page-compliance .inner-tab').forEach(b => b.classList.remove('active'));
  btn.classList.add('active');
  document.getElementById('ctab-bodies').style.display = tab === 'bodies' ? '' : 'none';
  document.getElementById('ctab-infection').style.display = tab === 'infection' ? '' : 'none';
  document.getElementById('ctab-risktraining').style.display = tab === 'risktraining' ? '' : 'none';
  if (tab === 'bodies') { if (!_compBodies.length) loadCompliancePage(); else renderComplianceBodies(); }
  if (tab === 'infection') loadICData();
  if (tab === 'risktraining') loadRTData();
}

function switchICSection(sec, btn) {
  _icCurrentSection = sec;
  document.querySelectorAll('.ic-section-btn').forEach(b => {
    b.style.background = 'var(--white)';
    b.style.color = 'var(--ink)';
  });
  btn.style.background = 'var(--ink)';
  btn.style.color = 'white';
  ['staff','facility','food','reports'].forEach(s => {
    const el = document.getElementById('ic-sec-' + s);
    if (el) el.style.display = s === sec ? '' : 'none';
  });
  const addBtn = document.getElementById('ic-add-btn');
  if (sec === 'staff') { addBtn.textContent = '+ Log Test'; addBtn.style.display = ''; }
  else if (sec === 'facility' || sec === 'food') { addBtn.textContent = '+ Log Check'; addBtn.style.display = ''; }
  else { addBtn.style.display = 'none'; }
  renderICSection();
}

function icAddAction() {
  const clientId = document.getElementById('ic-client-sel').value;
  if (_icCurrentSection === 'staff') {
    document.getElementById('ic-staff-edit-id').value = '';
    document.getElementById('ic-s-client').value = clientId;
    document.getElementById('ic-s-date').valueAsDate = new Date();
    openModal('modal-ic-staff');
  } else {
    document.getElementById('ic-fac-edit-id').value = '';
    document.getElementById('ic-f-client').value = clientId;
    document.getElementById('ic-f-date').valueAsDate = new Date();
    document.getElementById('ic-facility-modal-title').textContent =
      _icCurrentSection === 'food' ? 'Log Food Safety Check' : 'Log Facility Hygiene Check';
    openModal('modal-ic-facility');
  }
}

function populateICClientDropdowns() {
  const opts = '<option value="">Select client / location…</option>' +
    (allClients || []).map(c => `<option value="${c.id}">${c.business_name}</option>`).join('');
  ['ic-client-sel','ic-s-client','ic-f-client'].forEach(id => {
    const el = document.getElementById(id);
    if (el) { const v = el.value; el.innerHTML = opts; el.value = v; }
  });
}

async function loadICData() {
  populateICClientDropdowns();
  const clientId = document.getElementById('ic-client-sel')?.value || '';
  const q1 = clientId
    ? db.from('ic_staff_tests').select('*').eq('client_id', clientId).order('date_tested', {ascending: false})
    : db.from('ic_staff_tests').select('*').order('date_tested', {ascending: false});
  const q2 = clientId
    ? db.from('ic_facility_checks').select('*').eq('client_id', clientId).order('date_checked', {ascending: false})
    : db.from('ic_facility_checks').select('*').order('date_checked', {ascending: false});
  const [r1, r2] = await Promise.all([q1, q2]);
  _icStaffTests = r1.data || [];
  _icFacilityChecks = r2.data || [];
  renderICStats();
  renderICSection();
}

function renderICStats() {
  const today = new Date();
  const staffFlagged = _icStaffTests.filter(t => t.result === 'Flagged').length;
  const staffOverdue = _icStaffTests.filter(t => t.next_due && new Date(t.next_due) < today && t.result !== 'Exempt').length;
  const facFail = _icFacilityChecks.filter(c => c.result?.startsWith('Fail')).length;
  const facDue = _icFacilityChecks.filter(c => c.next_due && new Date(c.next_due) < today).length;
  const el = document.getElementById('ic-stats-row');
  if (!el) return;
  el.innerHTML = [
    ['Staff Tested', _icStaffTests.length, ''],
    ['Flagged', staffFlagged, staffFlagged > 0 ? 'color:var(--danger)' : ''],
    ['Tests Overdue', staffOverdue, staffOverdue > 0 ? 'color:var(--danger)' : ''],
    ['Checks Logged', _icFacilityChecks.length, ''],
    ['Checks Failed', facFail, facFail > 0 ? 'color:var(--danger)' : ''],
    ['Checks Due', facDue, facDue > 0 ? 'color:var(--warn)' : ''],
  ].map(([l,v,s]) => `<div class="stat-box"><div class="stat-num" style="${s}">${v}</div><div class="stat-label">${l}</div></div>`).join('');
}

function renderICSection() {
  if (_icCurrentSection === 'staff') renderICStaffTable();
  else if (_icCurrentSection === 'facility') renderICFacilityTable('facility');
  else if (_icCurrentSection === 'food') renderICFacilityTable('food');
  else renderICReports();
}

function renderICStaffTable() {
  const filter = document.getElementById('ic-staff-filter')?.value || 'all';
  const today = new Date();
  let tests = _icStaffTests;
  if (filter === 'Overdue') tests = tests.filter(t => t.next_due && new Date(t.next_due) < today && t.result !== 'Exempt');
  else if (filter === 'Due Soon') tests = tests.filter(t => {
    if (!t.next_due) return false;
    const d = (new Date(t.next_due) - today) / (1000*60*60*24);
    return d >= 0 && d <= 30;
  });
  else if (filter === 'Clear') tests = tests.filter(t => t.result === 'Clear');
  else if (filter === 'Flagged') tests = tests.filter(t => t.result === 'Flagged');

  const el = document.getElementById('ic-staff-table');
  if (!el) return;
  if (!tests.length) { el.innerHTML = '<div style="font-family:Arial;font-size:13px;color:var(--muted);padding:24px 0">No test records yet. Click + Log Test to add the first one.</div>'; return; }

  const rClass = {Clear:'ic-result-clear', Flagged:'ic-result-flagged', Pending:'ic-result-pending', Exempt:'ic-result-pending'};
  const rowClass = r => r.result === 'Flagged' ? 'ic-staff-row-flagged' : (r.next_due && new Date(r.next_due) < today && r.result !== 'Exempt' ? 'ic-staff-row-overdue' : '');

  el.innerHTML = `<div class="card" style="padding:0;overflow:hidden">
    <table class="data-table">
      <thead><tr><th>Staff Member</th><th>Role</th><th>Test</th><th>Date Tested</th><th>Next Due</th><th>Result</th><th>Lab / By</th><th>Notes</th><th>Visible</th>${currentUser?.isOwner ? '<th></th>' : ''}</tr></thead>
      <tbody>${tests.map(t => {
        const overdue = t.next_due && new Date(t.next_due) < today && t.result !== 'Exempt';
        const dueSoon = t.next_due && !overdue && ((new Date(t.next_due)-today)/(1000*60*60*24)) <= 30;
        const dueFlag = overdue ? ' <span class="badge badge-red" style="font-size:9px">Overdue</span>' : dueSoon ? ' <span class="pending-badge" style="font-size:9px">Due Soon</span>' : '';
        return `<tr class="${rowClass(t)}">
          <td style="font-weight:600">${t.staff_name}</td>
          <td style="font-family:Arial;font-size:12px;color:var(--muted)">${t.role||'—'}</td>
          <td style="font-family:Arial;font-size:12px">${t.test_type}</td>
          <td style="font-family:Arial;font-size:12px">${t.date_tested||'—'}</td>
          <td style="font-family:Arial;font-size:12px">${t.next_due||'—'}${dueFlag}</td>
          <td><span class="${rClass[t.result]||''}">${t.result}</span></td>
          <td style="font-family:Arial;font-size:12px;color:var(--muted)">${t.lab||'—'}</td>
          <td style="font-family:Arial;font-size:12px;color:var(--muted);max-width:140px">${t.notes||'—'}</td>
          <td><span class="badge ${t.client_visible==='yes'?'badge-green':'badge-gray'}">${t.client_visible==='yes'?'Client':'Internal'}</span></td>
          ${currentUser?.isOwner ? `<td><button class="btn-danger" data-id="${t.id}" onclick="deleteRow('ic_staff_tests',this.dataset.id,loadICData)" style="font-size:10px">×</button></td>` : ''}
        </tr>`;
      }).join('')}</tbody>
    </table></div>`;
}

const FOOD_CHECKS = new Set(['Temp Log — Cold Storage','Temp Log — Hot Holding','Delivery Acceptance Check','Date Labelling Audit','Allergen Record','Cooking Temp Probe Log','Plate & Cutlery Sanitisation','Hand Hygiene Compliance']);

function renderICFacilityTable(section) {
  const isFood = section === 'food';
  const checks = _icFacilityChecks.filter(c => isFood ? FOOD_CHECKS.has(c.check_type) : !FOOD_CHECKS.has(c.check_type));
  const today = new Date();
  const el = document.getElementById('ic-' + section + '-table');
  if (!el) return;
  if (!checks.length) { el.innerHTML = '<div style="font-family:Arial;font-size:13px;color:var(--muted);padding:24px 0">No check records yet. Click + Log Check to start building your compliance log.</div>'; return; }

  const rClass = {'Pass':'ic-result-pass','Pass with Notes':'ic-result-pending','Fail — Action Required':'ic-result-fail','Pending':'ic-result-pending'};
  el.innerHTML = `<div class="card" style="padding:0;overflow:hidden">
    <table class="data-table">
      <thead><tr><th>Check</th><th>Date</th><th>Checked By</th><th>Result</th><th>Score</th><th>Findings</th><th>Action</th><th>Next Due</th><th>Visible</th>${currentUser?.isOwner ? '<th></th>' : ''}</tr></thead>
      <tbody>${checks.map(c => {
        const overdue = c.next_due && new Date(c.next_due) < today;
        return `<tr ${c.result?.startsWith('Fail') ? 'style="background:var(--danger-light)"' : overdue ? 'class="pending-row"' : ''}>
          <td style="font-weight:600;font-family:Arial;font-size:12px">${c.check_type}</td>
          <td style="font-family:Arial;font-size:12px">${c.date_checked||'—'}</td>
          <td style="font-family:Arial;font-size:12px;color:var(--muted)">${c.checked_by||'—'}</td>
          <td><span class="${rClass[c.result]||''}" style="font-family:Arial;font-size:12px">${c.result}</span></td>
          <td style="font-family:Arial;font-size:13px;font-weight:600;text-align:center">${c.score ? c.score+'/10' : '—'}</td>
          <td style="font-family:Arial;font-size:12px;color:var(--ink2);max-width:160px">${c.findings||'—'}</td>
          <td style="font-family:Arial;font-size:12px;color:${c.action_required?'var(--danger)':'var(--muted)'}">${c.action_required||'—'}</td>
          <td style="font-family:Arial;font-size:12px">${c.next_due||'—'}${overdue?' <span class="badge badge-red" style="font-size:9px">Overdue</span>':''}</td>
          <td><span class="badge ${c.client_visible==='yes'?'badge-green':'badge-gray'}">${c.client_visible==='yes'?'Client':'Internal'}</span></td>
          ${currentUser?.isOwner ? `<td><button class="btn-danger" data-id="${c.id}" onclick="deleteRow('ic_facility_checks',this.dataset.id,loadICData)" style="font-size:10px">×</button></td>` : ''}
        </tr>`;
      }).join('')}</tbody>
    </table></div>`;
}

function renderICReports() {
  const el = document.getElementById('ic-reports-list');
  if (!el) return;
  const clientSel = document.getElementById('ic-client-sel');
  const clientName = clientSel?.options[clientSel?.selectedIndex]?.text || 'All Clients';
  const today = new Date();
  const todayStr = today.toLocaleDateString('en-GB', {day:'numeric', month:'long', year:'numeric'});

  // Build report
  const staffVisible = _icStaffTests.filter(t => t.client_visible === 'yes');
  const checksVisible = _icFacilityChecks.filter(c => c.client_visible === 'yes');
  const staffFlagged = staffVisible.filter(t => t.result === 'Flagged');
  const staffClear = staffVisible.filter(t => t.result === 'Clear');
  const checksPassed = checksVisible.filter(c => c.result === 'Pass');
  const checksFailed = checksVisible.filter(c => c.result?.startsWith('Fail'));
  const avgScore = checksVisible.filter(c=>c.score).reduce((a,b,_,arr)=>a+b.score/arr.length,0);

  el.innerHTML = `
    <div style="display:flex;justify-content:flex-end;margin-bottom:12px">
      <button class="btn-sm" onclick="window.print()">🖨 Print Report</button>
    </div>
    <div class="ic-report-card">
      <div class="ic-report-header">
        <h3>Infection Control & Hygiene Report</h3>
        <p>${clientName} · Generated: ${todayStr} · Haida Yammar Consulting</p>
      </div>
      <div class="ic-report-section">
        <h4>Summary Scores</h4>
        <div class="ic-score-grid">
          <div class="ic-score-box"><div class="ic-score-num">${staffVisible.length}</div><div class="ic-score-lbl">Staff Tested</div></div>
          <div class="ic-score-box"><div class="ic-score-num" style="${staffFlagged.length>0?'color:var(--danger)':''}">${staffFlagged.length}</div><div class="ic-score-lbl">Flagged</div></div>
          <div class="ic-score-box"><div class="ic-score-num" style="color:var(--success)">${staffClear.length}</div><div class="ic-score-lbl">Clear</div></div>
          <div class="ic-score-box"><div class="ic-score-num">${checksVisible.length}</div><div class="ic-score-lbl">Hygiene Checks</div></div>
          <div class="ic-score-box"><div class="ic-score-num" style="${checksFailed.length>0?'color:var(--danger)':'color:var(--success)'}">
            ${checksVisible.length ? Math.round((checksPassed.length/checksVisible.length)*100) : 0}%</div><div class="ic-score-lbl">Pass Rate</div></div>
          <div class="ic-score-box"><div class="ic-score-num">${avgScore ? avgScore.toFixed(1)+'/10' : '—'}</div><div class="ic-score-lbl">Avg Score</div></div>
        </div>
      </div>
      ${staffVisible.length ? `
      <div class="ic-report-section">
        <h4>Staff Health Testing</h4>
        <table class="data-table" style="font-size:12px">
          <thead><tr><th>Staff Member</th><th>Role</th><th>Test</th><th>Date</th><th>Next Due</th><th>Result</th></tr></thead>
          <tbody>${staffVisible.map(t=>`<tr>
            <td>${t.staff_name}</td><td style="color:var(--muted)">${t.role||'—'}</td>
            <td>${t.test_type}</td><td>${t.date_tested||'—'}</td><td>${t.next_due||'—'}</td>
            <td class="${t.result==='Clear'?'ic-result-clear':t.result==='Flagged'?'ic-result-flagged':'ic-result-pending'}">${t.result}</td>
          </tr>`).join('')}</tbody>
        </table>
      </div>` : ''}
      ${checksVisible.length ? `
      <div class="ic-report-section">
        <h4>Facility & Food Safety Checks</h4>
        <table class="data-table" style="font-size:12px">
          <thead><tr><th>Check</th><th>Date</th><th>Checked By</th><th>Result</th><th>Score</th><th>Action Required</th></tr></thead>
          <tbody>${checksVisible.map(c=>`<tr ${c.result?.startsWith('Fail')?'style="background:var(--danger-light)"':''}>
            <td>${c.check_type}</td><td>${c.date_checked||'—'}</td><td style="color:var(--muted)">${c.checked_by||'—'}</td>
            <td class="${c.result==='Pass'?'ic-result-pass':c.result?.startsWith('Fail')?'ic-result-fail':'ic-result-pending'}">${c.result}</td>
            <td style="text-align:center">${c.score?c.score+'/10':'—'}</td>
            <td style="color:${c.action_required?'var(--danger)':'var(--muted)'}">${c.action_required||'None'}</td>
          </tr>`).join('')}</tbody>
        </table>
      </div>` : ''}
      <div style="padding:16px 20px;font-family:Arial;font-size:11px;color:var(--muted);border-top:1px solid var(--border)">
        This report was prepared by Haida Yammar Consulting. Only records marked as Client-Visible are included. Contact your consultant for the full internal log.
      </div>
    </div>`;
}

async function saveICStaffTest() {
  const editId = document.getElementById('ic-staff-edit-id').value;
  const fb = document.getElementById('ic-staff-feedback');
  const name = document.getElementById('ic-s-name').value.trim();
  if (!name) { fb.innerHTML = '<div class="error-msg">Staff name required.</div>'; return; }
  const payload = {
    client_id: document.getElementById('ic-s-client').value || null,
    staff_name: name,
    role: document.getElementById('ic-s-role').value.trim(),
    test_type: document.getElementById('ic-s-test').value,
    date_tested: document.getElementById('ic-s-date').value || null,
    next_due: document.getElementById('ic-s-next').value || null,
    result: document.getElementById('ic-s-result').value,
    lab: document.getElementById('ic-s-lab').value.trim(),
    notes: document.getElementById('ic-s-notes').value.trim(),
    client_visible: document.getElementById('ic-s-visible').value,
  };
  let error;
  if (editId) { ({error} = await db.from('ic_staff_tests').update(payload).eq('id', editId)); }
  else { ({error} = await db.from('ic_staff_tests').insert(payload)); }
  if (error) { fb.innerHTML = `<div class="error-msg">${error.message}</div>`; return; }
  fb.innerHTML = '<div class="success-msg">✓ Test record saved.</div>';
  await loadICData();
  setTimeout(() => { closeModal('modal-ic-staff'); fb.innerHTML = ''; }, 1000);
}

async function saveICFacilityCheck() {
  const editId = document.getElementById('ic-fac-edit-id').value;
  const fb = document.getElementById('ic-facility-feedback');
  const checkType = document.getElementById('ic-f-check').value;
  const payload = {
    client_id: document.getElementById('ic-f-client').value || null,
    check_type: checkType,
    date_checked: document.getElementById('ic-f-date').value || null,
    checked_by: document.getElementById('ic-f-by').value.trim(),
    result: document.getElementById('ic-f-result').value,
    score: parseInt(document.getElementById('ic-f-score').value) || null,
    findings: document.getElementById('ic-f-findings').value.trim(),
    action_required: document.getElementById('ic-f-action').value.trim(),
    next_due: document.getElementById('ic-f-next').value || null,
    client_visible: document.getElementById('ic-f-visible').value,
  };
  let error;
  if (editId) { ({error} = await db.from('ic_facility_checks').update(payload).eq('id', editId)); }
  else { ({error} = await db.from('ic_facility_checks').insert(payload)); }
  if (error) { fb.innerHTML = `<div class="error-msg">${error.message}</div>`; return; }
  fb.innerHTML = '<div class="success-msg">✓ Check saved.</div>';
  await loadICData();
  setTimeout(() => { closeModal('modal-ic-facility'); fb.innerHTML = ''; }, 1000);
}

// Wire client selector for IC
document.getElementById('ic-client-sel')?.addEventListener('change', () => { loadICData(); });

async function generateICStaffReport() {
  const clientSel = document.getElementById('ic-client-sel');
  const clientId = clientSel?.value || '';
  const clientName = clientId ? (allClients.find(c=>c.id===clientId)?.business_name||'Client') : 'All Clients';

  if (!_icStaffTests.length) { alert('No staff test records to report on. Log some tests first.'); return; }

  const today = new Date();
  const todayStr = today.toLocaleDateString('en-GB',{day:'numeric',month:'long',year:'numeric'});

  const clear = _icStaffTests.filter(t=>t.result==='Clear');
  const flagged = _icStaffTests.filter(t=>t.result==='Flagged');
  const pending = _icStaffTests.filter(t=>t.result==='Pending');
  const overdue = _icStaffTests.filter(t=>t.next_due&&new Date(t.next_due)<today&&t.result!=='Exempt');

  let content = `INFECTION CONTROL — STAFF HEALTH TESTING REPORT\n`;
  content += `Client: ${clientName}\nDate: ${todayStr}\nPrepared by: Haida Yammar Consulting\n\n`;
  content += `SUMMARY\nTotal staff tested: ${_icStaffTests.length}\n`;
  content += `Clear: ${clear.length} · Flagged: ${flagged.length} · Pending: ${pending.length} · Overdue: ${overdue.length}\n\n`;

  if (flagged.length) {
    content += `⚠ FLAGGED — IMMEDIATE ATTENTION REQUIRED\n`;
    flagged.forEach(t => { content += `• ${t.staff_name} (${t.role||'—'}) — ${t.test_type}${t.notes?'. '+t.notes:''}\n`; });
    content += '\n';
  }
  if (overdue.length) {
    content += `OVERDUE TESTS\n`;
    overdue.forEach(t => { content += `• ${t.staff_name} — ${t.test_type} (due ${t.next_due})\n`; });
    content += '\n';
  }
  content += `FULL RESULTS\n`;
  _icStaffTests.forEach(t => {
    content += `• ${t.staff_name} (${t.role||'—'}) — ${t.test_type}: ${t.result}`;
    if (t.date_tested) content += ` · Tested: ${t.date_tested}`;
    if (t.next_due) content += ` · Next due: ${t.next_due}`;
    if (t.lab) content += ` · ${t.lab}`;
    content += '\n';
  });
  content += `\n---\nThis report was prepared by Haida Yammar Consulting. For queries contact haidayammar01@gmail.com`;

  const {error} = await db.from('reports').insert({
    client_id: clientId||null,
    report_type: 'Infection Control',
    title: `Staff Health Testing Report — ${todayStr}`,
    content: content,
    date: today.toISOString().split('T')[0],
    created_by: currentUser?.name||'Haida',
    created_at: new Date().toISOString(),
  });
  if (error) { alert('Could not save report: '+error.message); return; }
  alert('✓ Report saved to '+clientName+'\'s Reports tab.');
}

// ══════════════════════════════════════════════════
// RISK & TRAINING MODULE
// Tables needed:
//   risk_assessments (id, client_id, category, description, likelihood, severity, controls, action_required, assessed_by, date_assessed, review_date, status, client_visible, created_at)
//   training_records  (id, client_id, staff_name, role, training_type, date_completed, next_due, provider, cert_issued, notes, client_visible, created_at)
//   checks_balances   (id, client_id, check_type, date, conducted_by, result, score, findings, actions_required, next_due, client_visible, created_at)
//   staff_strikes     (id, staff_id, strike_number, date, reason, action_taken, issued_by, acknowledged, created_at)
//   expenditure       (id, client_id, date, description, category, amount, currency, paid_by, receipt, notes, created_at)
// ══════════════════════════════════════════════════

let _rtSection = 'risk';
let _rtRisks = [], _rtTraining = [], _rtChecks = [];

function switchRTSection(sec, btn) {
  _rtSection = sec;
  document.querySelectorAll('.rt-section-btn').forEach(b => { b.style.background = 'var(--white)'; b.style.color = 'var(--ink)'; });
  btn.style.background = 'var(--ink)'; btn.style.color = 'white';
  ['risk','training','checks'].forEach(s => {
    const el = document.getElementById('rt-sec-' + s);
    if (el) el.style.display = s === sec ? '' : 'none';
  });
  const addBtn = document.getElementById('rt-add-btn');
  const labels = {risk:'+ Add Risk Assessment', training:'+ Log Training Record', checks:'+ Log Check'};
  addBtn.textContent = labels[sec] || '+ Add';
  renderRTSection();
}

function rtAddAction() {
  const clientId = document.getElementById('rt-client-sel').value;
  if (_rtSection === 'risk') {
    document.getElementById('rt-risk-edit-id').value = '';
    document.getElementById('rt-r-client').value = clientId;
    document.getElementById('rt-r-date').valueAsDate = new Date();
    openModal('modal-rt-risk');
  } else if (_rtSection === 'training') {
    document.getElementById('rt-tr-edit-id').value = '';
    document.getElementById('rt-t-client').value = clientId;
    document.getElementById('rt-t-date').valueAsDate = new Date();
    openModal('modal-rt-training');
  } else {
    document.getElementById('rt-ch-edit-id').value = '';
    document.getElementById('rt-c-client').value = clientId;
    document.getElementById('rt-c-date').valueAsDate = new Date();
    openModal('modal-rt-check');
  }
}

function populateRTClientDropdowns() {
  const opts = '<option value="">Select…</option>' + (allClients||[]).map(c=>`<option value="${c.id}">${c.business_name}</option>`).join('');
  ['rt-client-sel','rt-r-client','rt-t-client','rt-c-client'].forEach(id => {
    const el = document.getElementById(id);
    if (el) { const v = el.value; el.innerHTML = opts; el.value = v; }
  });
}

async function loadRTData() {
  populateRTClientDropdowns();
  const clientId = document.getElementById('rt-client-sel')?.value || '';
  const filter = q => clientId ? q.eq('client_id', clientId) : q;
  const [r1, r2, r3] = await Promise.all([
    filter(db.from('risk_assessments').select('*').order('date_assessed', {ascending:false})),
    filter(db.from('training_records').select('*').order('date_completed', {ascending:false})),
    filter(db.from('checks_balances').select('*').order('date', {ascending:false})),
  ]);
  _rtRisks = r1.data || [];
  _rtTraining = r2.data || [];
  _rtChecks = r3.data || [];
  renderRTStats();
  renderRTSection();
}

function renderRTStats() {
  const today = new Date();
  const openRisks = _rtRisks.filter(r => r.status === 'Open' || r.status === 'Escalated').length;
  const highRisks = _rtRisks.filter(r => (r.likelihood||1)*(r.severity||1) >= 12).length;
  const trainingDue = _rtTraining.filter(t => t.next_due && new Date(t.next_due) < today).length;
  const checksDue = _rtChecks.filter(c => c.next_due && new Date(c.next_due) < today).length;
  const el = document.getElementById('rt-stats-row');
  if (!el) return;
  el.innerHTML = [
    ['Risk Assessments', _rtRisks.length, ''],
    ['Open Risks', openRisks, openRisks > 0 ? 'color:var(--danger)' : ''],
    ['High Risk (12+)', highRisks, highRisks > 0 ? 'color:var(--danger)' : ''],
    ['Training Records', _rtTraining.length, ''],
    ['Training Overdue', trainingDue, trainingDue > 0 ? 'color:var(--danger)' : ''],
    ['Checks Overdue', checksDue, checksDue > 0 ? 'color:var(--warn)' : ''],
  ].map(([l,v,s]) => `<div class="stat-box"><div class="stat-num" style="${s}">${v}</div><div class="stat-label">${l}</div></div>`).join('');
}

function renderRTSection() {
  if (_rtSection === 'risk') renderRiskTable();
  else if (_rtSection === 'training') renderTrainingTable();
  else renderChecksTable();
}

function renderRiskTable() {
  const el = document.getElementById('rt-risk-table');
  if (!el) return;
  if (!_rtRisks.length) { el.innerHTML = '<div style="font-family:Arial;font-size:13px;color:var(--muted);padding:24px 0">No risk assessments yet. Click + Add Risk Assessment to start.</div>'; return; }
  const today = new Date();
  const riskScore = r => (parseInt(r.likelihood)||1) * (parseInt(r.severity)||1);
  const riskLevel = s => s >= 15 ? ['CRITICAL','var(--danger)'] : s >= 10 ? ['HIGH','#E06B00'] : s >= 5 ? ['MEDIUM','var(--warn)'] : ['LOW','var(--success)'];
  const sCls = {Open:'badge-red', 'Controls Applied':'badge-gold', 'Closed / Accepted':'badge-green', Escalated:'badge-red'};
  el.innerHTML = `<div class="card" style="padding:0;overflow:hidden"><table class="data-table">
    <thead><tr><th>Category</th><th>Risk</th><th>Score</th><th>Level</th><th>Controls</th><th>Action</th><th>Assessed By</th><th>Date</th><th>Review</th><th>Status</th><th>Visible</th>${currentUser?.isOwner?'<th></th>':''}</tr></thead>
    <tbody>${_rtRisks.map(r => {
      const score = riskScore(r);
      const [level, color] = riskLevel(score);
      const reviewDue = r.review_date && new Date(r.review_date) < today;
      return `<tr ${r.status === 'Escalated' ? 'style="background:var(--danger-light)"' : ''}>
        <td style="font-family:Arial;font-size:12px;font-weight:600">${r.category}</td>
        <td style="font-family:Arial;font-size:12px;max-width:160px">${r.description||'—'}</td>
        <td style="text-align:center;font-weight:700;font-size:14px;color:${color}">${score}</td>
        <td><span style="font-family:Arial;font-size:10px;font-weight:700;color:${color};text-transform:uppercase;letter-spacing:.5px">${level}</span></td>
        <td style="font-family:Arial;font-size:11px;color:var(--ink2);max-width:140px">${r.controls||'—'}</td>
        <td style="font-family:Arial;font-size:11px;color:${r.action_required?'var(--danger)':'var(--muted)'};max-width:140px">${r.action_required||'None'}</td>
        <td style="font-family:Arial;font-size:12px;color:var(--muted)">${r.assessed_by||'—'}</td>
        <td style="font-family:Arial;font-size:12px">${r.date_assessed||'—'}</td>
        <td style="font-family:Arial;font-size:12px">${r.review_date||'—'}${reviewDue?' <span class="badge badge-red" style="font-size:9px">Due</span>':''}</td>
        <td><span class="badge ${sCls[r.status]||'badge-gray'}">${r.status}</span></td>
        <td><span class="badge ${r.client_visible==='yes'?'badge-green':'badge-gray'}">${r.client_visible==='yes'?'Client':'Internal'}</span></td>
        ${currentUser?.isOwner ? `<td><button class="btn-danger" onclick="deleteRow('risk_assessments','${r.id}',loadRTData)" style="font-size:10px">×</button></td>` : ''}
      </tr>`;
    }).join('')}</tbody></table></div>`;
}

function renderTrainingTable() {
  const el = document.getElementById('rt-training-table');
  if (!el) return;
  if (!_rtTraining.length) { el.innerHTML = '<div style="font-family:Arial;font-size:13px;color:var(--muted);padding:24px 0">No training records yet. Click + Log Training Record to start.</div>'; return; }
  const today = new Date();
  el.innerHTML = `<div class="card" style="padding:0;overflow:hidden"><table class="data-table">
    <thead><tr><th>Staff Member</th><th>Role</th><th>Training</th><th>Completed</th><th>Next Due</th><th>Provider</th><th>Certificate</th><th>Notes</th><th>Visible</th>${currentUser?.isOwner?'<th></th>':''}</tr></thead>
    <tbody>${_rtTraining.map(t => {
      const overdue = t.next_due && new Date(t.next_due) < today;
      const dueSoon = t.next_due && !overdue && ((new Date(t.next_due)-today)/(1000*60*60*24)) <= 30;
      return `<tr ${overdue ? 'class="ic-staff-row-overdue"' : ''}>
        <td style="font-weight:600;font-family:Arial;font-size:13px">${t.staff_name}</td>
        <td style="font-family:Arial;font-size:12px;color:var(--muted)">${t.role||'—'}</td>
        <td style="font-family:Arial;font-size:12px">${t.training_type}</td>
        <td style="font-family:Arial;font-size:12px">${t.date_completed||'—'}</td>
        <td style="font-family:Arial;font-size:12px">${t.next_due||'—'}${overdue?' <span class="badge badge-red" style="font-size:9px">Overdue</span>':dueSoon?' <span class="pending-badge" style="font-size:9px">Due Soon</span>':''}</td>
        <td style="font-family:Arial;font-size:12px;color:var(--muted)">${t.provider||'—'}</td>
        <td><span class="badge ${t.cert_issued==='yes'?'badge-green':t.cert_issued==='pending'?'badge-gold':'badge-gray'}">${t.cert_issued||'—'}</span></td>
        <td style="font-family:Arial;font-size:11px;color:var(--muted);max-width:140px">${t.notes||'—'}</td>
        <td><span class="badge ${t.client_visible==='yes'?'badge-green':'badge-gray'}">${t.client_visible==='yes'?'Client':'Internal'}</span></td>
        ${currentUser?.isOwner ? `<td><button class="btn-danger" onclick="deleteRow('training_records','${t.id}',loadRTData)" style="font-size:10px">×</button></td>` : ''}
      </tr>`;
    }).join('')}</tbody></table></div>`;
}

function renderChecksTable() {
  const el = document.getElementById('rt-checks-table');
  if (!el) return;
  if (!_rtChecks.length) { el.innerHTML = '<div style="font-family:Arial;font-size:13px;color:var(--muted);padding:24px 0">No checks logged yet. Click + Log Check to start.</div>'; return; }
  const today = new Date();
  const rCls = {'Pass':'ic-result-pass','Pass with Issues':'ic-result-pending','Fail — Action Required':'ic-result-fail'};
  el.innerHTML = `<div class="card" style="padding:0;overflow:hidden"><table class="data-table">
    <thead><tr><th>Check Type</th><th>Date</th><th>By</th><th>Result</th><th>Score</th><th>Findings</th><th>Actions</th><th>Next Due</th><th>Visible</th>${currentUser?.isOwner?'<th></th>':''}</tr></thead>
    <tbody>${_rtChecks.map(c => {
      const overdue = c.next_due && new Date(c.next_due) < today;
      return `<tr ${c.result?.startsWith('Fail') ? 'style="background:var(--danger-light)"' : overdue ? 'class="ic-staff-row-overdue"' : ''}>
        <td style="font-weight:600;font-family:Arial;font-size:12px">${c.check_type}</td>
        <td style="font-family:Arial;font-size:12px">${c.date||'—'}</td>
        <td style="font-family:Arial;font-size:12px;color:var(--muted)">${c.conducted_by||'—'}</td>
        <td><span class="${rCls[c.result]||''}" style="font-family:Arial;font-size:12px">${c.result||'—'}</span></td>
        <td style="text-align:center;font-weight:600">${c.score ? c.score+'/10' : '—'}</td>
        <td style="font-family:Arial;font-size:11px;color:var(--ink2);max-width:150px">${c.findings||'—'}</td>
        <td style="font-family:Arial;font-size:11px;color:${c.actions_required?'var(--danger)':'var(--muted)'};max-width:140px">${c.actions_required||'None'}</td>
        <td style="font-family:Arial;font-size:12px">${c.next_due||'—'}${overdue?' <span class="badge badge-red" style="font-size:9px">Overdue</span>':''}</td>
        <td><span class="badge ${c.client_visible==='yes'?'badge-green':'badge-gray'}">${c.client_visible==='yes'?'Client':'Internal'}</span></td>
        ${currentUser?.isOwner ? `<td><button class="btn-danger" onclick="deleteRow('checks_balances','${c.id}',loadRTData)" style="font-size:10px">×</button></td>` : ''}
      </tr>`;
    }).join('')}</tbody></table></div>`;
}

async function saveRiskAssessment() {
  const fb = document.getElementById('rt-risk-feedback');
  const editId = document.getElementById('rt-risk-edit-id').value;
  const payload = {
    client_id: document.getElementById('rt-r-client').value || null,
    category: document.getElementById('rt-r-category').value,
    description: document.getElementById('rt-r-desc').value.trim(),
    likelihood: parseInt(document.getElementById('rt-r-likelihood').value) || 1,
    severity: parseInt(document.getElementById('rt-r-severity').value) || 1,
    controls: document.getElementById('rt-r-controls').value.trim(),
    action_required: document.getElementById('rt-r-action').value.trim(),
    assessed_by: document.getElementById('rt-r-by').value.trim(),
    date_assessed: document.getElementById('rt-r-date').value || null,
    review_date: document.getElementById('rt-r-review').value || null,
    status: document.getElementById('rt-r-status').value,
    client_visible: document.getElementById('rt-r-visible').value,
  };
  let error;
  if (editId) { ({error} = await db.from('risk_assessments').update(payload).eq('id', editId)); }
  else { ({error} = await db.from('risk_assessments').insert(payload)); }
  if (error) { fb.innerHTML = `<div class="error-msg">${error.message}</div>`; return; }
  fb.innerHTML = '<div class="success-msg">✓ Saved.</div>';
  await loadRTData();
  setTimeout(() => { closeModal('modal-rt-risk'); fb.innerHTML = ''; }, 1000);
}

async function saveTrainingRecord() {
  const fb = document.getElementById('rt-training-feedback');
  const payload = {
    client_id: document.getElementById('rt-t-client').value || null,
    staff_name: document.getElementById('rt-t-staff').value.trim(),
    role: document.getElementById('rt-t-role').value.trim(),
    training_type: document.getElementById('rt-t-type').value,
    date_completed: document.getElementById('rt-t-date').value || null,
    next_due: document.getElementById('rt-t-next').value || null,
    provider: document.getElementById('rt-t-provider').value.trim(),
    cert_issued: document.getElementById('rt-t-cert').value,
    notes: document.getElementById('rt-t-notes').value.trim(),
    client_visible: document.getElementById('rt-t-visible').value,
  };
  const {error} = await db.from('training_records').insert(payload);
  if (error) { fb.innerHTML = `<div class="error-msg">${error.message}</div>`; return; }
  fb.innerHTML = '<div class="success-msg">✓ Training record saved.</div>';
  await loadRTData();
  setTimeout(() => { closeModal('modal-rt-training'); fb.innerHTML = ''; }, 1000);
}

async function saveCheckRecord() {
  const fb = document.getElementById('rt-check-feedback');
  const payload = {
    client_id: document.getElementById('rt-c-client').value || null,
    check_type: document.getElementById('rt-c-type').value,
    date: document.getElementById('rt-c-date').value || null,
    conducted_by: document.getElementById('rt-c-by').value.trim(),
    result: document.getElementById('rt-c-result').value,
    score: parseInt(document.getElementById('rt-c-score').value) || null,
    findings: document.getElementById('rt-c-findings').value.trim(),
    actions_required: document.getElementById('rt-c-actions').value.trim(),
    next_due: document.getElementById('rt-c-next').value || null,
    client_visible: document.getElementById('rt-c-visible').value,
  };
  const {error} = await db.from('checks_balances').insert(payload);
  if (error) { fb.innerHTML = `<div class="error-msg">${error.message}</div>`; return; }
  fb.innerHTML = '<div class="success-msg">✓ Check logged.</div>';
  await loadRTData();
  setTimeout(() => { closeModal('modal-rt-check'); fb.innerHTML = ''; }, 1000);
}

// ══ INIT ══
document.getElementById('login-pass').addEventListener('keydown',e=>{if(e.key==='Enter')handleLogin();});
document.getElementById('login-user').addEventListener('keydown',e=>{if(e.key==='Enter')document.getElementById('login-pass').focus();});
document.getElementById('rf-date').valueAsDate=new Date();
document.getElementById('rf-client').addEventListener('change',()=>renderTypeFields());
</script>
<!-- Expenditure Modal -->
<div class="modal-overlay" id="modal-add-expenditure">
  <div class="modal" style="width:680px">
    <div class="modal-header">
      <div class="modal-title" id="exp-modal-title">Log Expense</div>
      <button class="modal-close" onclick="closeModal('modal-add-expenditure')">×</button>
    </div>
    <div class="modal-body">
      <input type="hidden" id="exp-edit-id" value="">
      <div class="form-grid">
        <div class="form-field" style="grid-column:1/-1"><label>Description</label><input type="text" id="exp-desc" placeholder="e.g. Monthly salary — Head Baker Jun Bakery"></div>
        <div class="form-field"><label>Category</label>
          <select id="exp-cat">
            <option>Staff Salary</option>
            <option>Freelancer / Contractor</option>
            <option>Software & Subscriptions</option>
            <option>Fuel & Transport</option>
            <option>Client Expenses</option>
            <option>Office & Stationery</option>
            <option>Marketing & Advertising</option>
            <option>Equipment & Tools</option>
            <option>Professional Development</option>
            <option>Bank Charges & Fees</option>
            <option>Utilities</option>
            <option>Other</option>
          </select>
        </div>
        <div class="form-field"><label>Amount (₦ or £)</label><input type="number" id="exp-amount" placeholder="0"></div>
        <div class="form-field"><label>Currency</label>
          <select id="exp-currency"><option value="NGN">₦ Nigerian Naira</option><option value="GBP">£ British Pounds</option></select>
        </div>
        <div class="form-field"><label>Date</label><input type="date" id="exp-date"></div>
        <div class="form-field"><label>Vendor / Payee</label><input type="text" id="exp-vendor" placeholder="e.g. MTN, AWS, Shell Station"></div>
        <div class="form-field"><label>Client (if client-related)</label><select id="exp-client"><option value="">None — internal cost</option></select></div>
        <div class="form-field"><label>Receipt / Reference Link</label><input type="text" id="exp-receipt" placeholder="Google Drive link or reference number"></div>
        <div class="form-field" style="grid-column:1/-1"><label>Notes</label><textarea id="exp-notes" rows="2" placeholder="Any additional context…"></textarea></div>
      </div>
      <div style="margin-top:16px;display:flex;gap:8px">
        <button class="btn-save" id="exp-save-btn" onclick="saveExpenditure()">Log Expense</button>
        <button class="btn-cancel" onclick="closeModal('modal-add-expenditure')">Cancel</button>
      </div>
      <div id="exp-feedback"></div>
    </div>
  </div>
</div>

<!-- KPI Scoring Modal -->
<div class="modal-overlay" id="modal-kpi-scoring">
  <div class="modal" style="width:700px">
    <div class="modal-header">
      <div class="modal-title">Monthly KPI Scoring</div>
      <button class="modal-close" onclick="closeModal('modal-kpi-scoring')">×</button>
    </div>
    <div class="modal-body">
      <input type="hidden" id="kpi-score-client-id">
      <div class="form-grid" style="margin-bottom:16px">
        <div class="form-field">
          <label>Scoring Period</label>
          <input type="month" id="kpi-score-period" onchange="loadKpiScoringForm(document.getElementById('kpi-score-client-id').value)">
        </div>
      </div>
      <div id="kpi-score-fields"></div>
      <div style="margin-top:16px;display:flex;gap:8px">
        <button class="btn-save" onclick="saveKpiScores()">Save Scores</button>
        <button class="btn-cancel" onclick="closeModal('modal-kpi-scoring')">Cancel</button>
      </div>
    </div>
  </div>
</div>

<!-- Add Event Modal -->
<div class="modal-overlay" id="modal-add-event">
  <div class="modal" style="width:720px">
    <div class="modal-header">
      <div class="modal-title" id="event-modal-title">New Event</div>
      <button class="modal-close" onclick="closeModal('modal-add-event')">×</button>
    </div>
    <div class="modal-body">
      <input type="hidden" id="ev-edit-id">
      <div class="form-grid">
        <div class="form-field" style="grid-column:1/-1"><label>Event Name</label><input type="text" id="ev-name" placeholder="e.g. Ummukhulthum Birthday Dinner"></div>
        <div class="form-field"><label>Client</label><select id="ev-client"><option value="">Select client…</option></select></div>
        <div class="form-field"><label>Event Type</label>
          <select id="ev-type">
            <option>Birthday / Celebration</option>
            <option>Corporate Event</option>
            <option>Wedding</option>
            <option>Baby Shower / Naming</option>
            <option>Brand Activation</option>
            <option>Conference / Seminar</option>
            <option>Product Launch</option>
            <option>Private Dinner</option>
            <option>Other</option>
          </select>
        </div>
        <div class="form-field"><label>Event Date</label><input type="date" id="ev-date"></div>
        <div class="form-field"><label>Venue</label><input type="text" id="ev-venue" placeholder="e.g. Transcorp Hilton, Abuja"></div>
        <div class="form-field"><label>Guest Count</label><input type="number" id="ev-guests" placeholder="e.g. 80"></div>
        <div class="form-field"><label>Budget (₦)</label><input type="number" id="ev-budget" placeholder="e.g. 2500000"></div>
        <div class="form-field"><label>Status</label>
          <select id="ev-status"><option>Enquiry</option><option>Confirmed</option><option>In Planning</option><option>Production</option><option>Day Of</option><option>Completed</option><option>Cancelled</option></select>
        </div>
        <div class="form-field" style="grid-column:1/-1"><label>Description / Brief</label><textarea id="ev-desc" rows="3" placeholder="What does the client want? Key requirements, theme, vibe…"></textarea></div>
        <div class="form-field" style="grid-column:1/-1"><label>Haida Role</label>
          <select id="ev-role">
            <option>Full Event Planner — end to end</option>
            <option>Coordinator — client has their own planner</option>
            <option>Consultant — advisory only</option>
            <option>Catering Coordination</option>
            <option>Décor & Styling</option>
            <option>Vendor Management</option>
          </select>
        </div>
      </div>
      <div style="margin-top:16px;display:flex;gap:8px">
        <button class="btn-save" onclick="saveEvent()">Save Event</button>
        <button class="btn-cancel" onclick="closeModal('modal-add-event')">Cancel</button>
      </div>
      <div id="event-feedback"></div>
    </div>
  </div>
</div>

<!-- Event Inventory Modal -->
<div class="modal-overlay" id="modal-event-inventory">
  <div class="modal" style="width:760px;max-height:90vh">
    <div class="modal-header">
      <div class="modal-title" id="inv-event-title">Event Inventory</div>
      <button class="modal-close" onclick="closeModal('modal-event-inventory')">×</button>
    </div>
    <div class="modal-body">
      <div style="display:flex;gap:8px;margin-bottom:16px;flex-wrap:wrap">
        <input type="text" id="inv-item-name" placeholder="Item name" style="flex:1;min-width:160px;padding:8px 12px;border:1px solid var(--border);border-radius:2px;font-family:Arial;font-size:13px">
        <input type="text" id="inv-item-category" placeholder="Category (e.g. Décor, Catering)" style="flex:1;min-width:140px;padding:8px 12px;border:1px solid var(--border);border-radius:2px;font-family:Arial;font-size:13px">
        <input type="number" id="inv-item-qty" placeholder="Qty" style="width:80px;padding:8px 12px;border:1px solid var(--border);border-radius:2px;font-family:Arial;font-size:13px">
        <input type="number" id="inv-item-cost" placeholder="Unit cost (₦)" style="width:120px;padding:8px 12px;border:1px solid var(--border);border-radius:2px;font-family:Arial;font-size:13px">
        <select id="inv-item-status" style="padding:8px 12px;border:1px solid var(--border);border-radius:2px;font-family:Arial;font-size:13px">
          <option>To Source</option><option>Ordered</option><option>Confirmed</option><option>Delivered</option><option>Returned</option>
        </select>
        <button class="btn-add" onclick="addInventoryItem()">+ Add</button>
      </div>
      <div id="inv-items-list"></div>
      <div id="inv-totals" style="margin-top:12px;padding:12px;background:var(--surface2);border-radius:2px;font-family:Arial;font-size:13px"></div>
    </div>
  </div>
</div>

<!-- Generic Edit Modal -->
<div class="modal-overlay" id="modal-generic-edit">
  <div class="modal" style="width:680px">
    <div class="modal-header">
      <div class="modal-title" id="generic-edit-title">Edit Record</div>
      <button class="modal-close" onclick="closeModal('modal-generic-edit')">×</button>
    </div>
    <div class="modal-body">
      <div id="generic-edit-form"></div>
      <div style="margin-top:16px;display:flex;gap:8px;align-items:center">
        <button class="btn-save" id="generic-edit-save">Save</button>
        <button class="btn-cancel" onclick="closeModal('modal-generic-edit')">Cancel</button>
        <span id="generic-edit-note" style="font-family:Arial;font-size:12px;color:var(--warn);margin-left:8px"></span>
      </div>
    </div>
  </div>
</div>
</body>
</html>
